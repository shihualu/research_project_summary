diff --git a/.gitignore b/.gitignore
index 20374e4a..86937958 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,3 +1,5 @@
+gradle.properties
+
 .gradle
 *.iml
 .idea/
diff --git a/DaoGenerator/build.gradle b/DaoGenerator/build.gradle
index 843f8141..a5b24969 100644
--- a/DaoGenerator/build.gradle
+++ b/DaoGenerator/build.gradle
@@ -4,9 +4,14 @@ apply plugin: 'signing'
 
 group = 'de.greenrobot'
 archivesBaseName = 'greendao-generator'
-version = '2.1.1-SNAPSHOT'
+version = '3.1.1-SNAPSHOT'
 sourceCompatibility = 1.7
 
+ext {
+    sonatypeUsername = "admin"
+    sonatypePassword = "password"
+}
+
 def isSnapshot = version.endsWith('-SNAPSHOT')
 def sonatypeRepositoryUrl
 if(isSnapshot) {
@@ -88,13 +93,15 @@ signing {
         sign configurations.archives
     } else {
         println "Signing information missing/incomplete for ${project.name}"
+        sign configurations.archives
     }
 }
 
+
 uploadArchives {
     repositories {
         mavenDeployer {
-            if(project.hasProperty('preferedRepo') && project.hasProperty('preferedUsername') 
+            if(project.hasProperty('preferedRepo') && project.hasProperty('preferedUsername')
                 && project.hasProperty('preferedPassword')) {
                 configuration = configurations.deployerJars
                 repository(url: preferedRepo) {
@@ -135,12 +142,12 @@ uploadArchives {
                        name 'greenrobot'
                    }
                }
-               
+
                issueManagement {
                    system 'GitHub Issues'
                    url 'https://github.com/greenrobot/greenDAO/issues'
                }
-               
+
                organization {
                    name 'greenrobot'
                    url 'http://greenrobot.de'
@@ -148,4 +155,4 @@ uploadArchives {
            }
         }
     }
-}
\ No newline at end of file
+}
diff --git a/settings.gradle b/settings.gradle
index ef5f41d2..6e5ca2ef 100644
--- a/settings.gradle
+++ b/settings.gradle
@@ -1,5 +1 @@
-include 'DaoCore', 'DaoGenerator', 'DaoExampleGenerator'
-include 'DaoTest' // Travis needs some Android setup for this
-include 'DaoTestGenerator'
-include ':DaoExample'
-
+include 'DaoGenerator', 'DaoExampleGenerator'
