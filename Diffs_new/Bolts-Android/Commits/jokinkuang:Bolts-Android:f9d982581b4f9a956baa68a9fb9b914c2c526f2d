diff --git a/bolts-android/build.gradle b/bolts-android/build.gradle
index bc0b35a..0db91ad 100644
--- a/bolts-android/build.gradle
+++ b/bolts-android/build.gradle
@@ -1,4 +1,4 @@
-import com.android.builder.core.BuilderConstants
+//import com.android.builder.core.BuilderConstants
 
 apply plugin: 'com.android.library'
 apply plugin: 'com.github.kt3k.coveralls'
@@ -49,9 +49,9 @@ android.libraryVariants.all { variant ->
         exclude '**/BuildConfig.class'
     }
 
-    if (name.equals(BuilderConstants.RELEASE)) {
-        artifacts.add('archives', task);
-    }
+//    if (name.equals(BuilderConstants.RELEASE)) {
+//        artifacts.add('archives', task);
+//    }
 }
 
 //region Maven
@@ -65,7 +65,7 @@ def ossrhPassword = hasProperty('NEXUS_PASSWORD') ? NEXUS_PASSWORD : System.gete
 
 uploadArchives {
     repositories.mavenDeployer {
-        beforeDeployment { MavenDeployment deployment -> signing.signPom(deployment) }
+//        beforeDeployment { MavenDeployment deployment -> signing.signPom(deployment) }
 
         repository(url: "https://oss.sonatype.org/service/local/staging/deploy/maven2/") {
             authentication(userName: ossrhUsername, password: ossrhPassword)
diff --git a/bolts-applinks/build.gradle b/bolts-applinks/build.gradle
index ead606f..c18d665 100644
--- a/bolts-applinks/build.gradle
+++ b/bolts-applinks/build.gradle
@@ -1,4 +1,4 @@
-import com.android.builder.core.BuilderConstants
+//import com.android.builder.core.BuilderConstants
 
 apply plugin: 'com.android.library'
 
@@ -51,9 +51,9 @@ android.libraryVariants.all { variant ->
         exclude '**/BuildConfig.class'
     }
 
-    if (name.equals(BuilderConstants.RELEASE)) {
-        artifacts.add('archives', task);
-    }
+//    if (name.equals(BuilderConstants.RELEASE)) {
+//        artifacts.add('archives', task);
+//    }
 }
 
 //region Maven
@@ -67,7 +67,7 @@ def ossrhPassword = hasProperty('NEXUS_PASSWORD') ? NEXUS_PASSWORD : System.gete
 
 uploadArchives {
     repositories.mavenDeployer {
-        beforeDeployment { MavenDeployment deployment -> signing.signPom(deployment) }
+//        beforeDeployment { MavenDeployment deployment -> signing.signPom(deployment) }
 
         repository(url: "https://oss.sonatype.org/service/local/staging/deploy/maven2/") {
             authentication(userName: ossrhUsername, password: ossrhPassword)
diff --git a/bolts-tasks/build.gradle b/bolts-tasks/build.gradle
index 5731294..06a3745 100644
--- a/bolts-tasks/build.gradle
+++ b/bolts-tasks/build.gradle
@@ -30,7 +30,7 @@ def ossrhPassword = hasProperty('NEXUS_PASSWORD') ? NEXUS_PASSWORD : System.gete
 
 uploadArchives {
     repositories.mavenDeployer {
-        beforeDeployment { MavenDeployment deployment -> signing.signPom(deployment) }
+//        beforeDeployment { MavenDeployment deployment -> signing.signPom(deployment) }
 
         repository(url: "https://oss.sonatype.org/service/local/staging/deploy/maven2/") {
             authentication(userName: ossrhUsername, password: ossrhPassword)
diff --git a/build.gradle b/build.gradle
index 6c9a99c..bd1cc29 100644
--- a/build.gradle
+++ b/build.gradle
@@ -2,9 +2,13 @@
 buildscript {
     repositories {
         mavenCentral()
+        maven {
+            url 'https://maven.google.com/'
+            name 'Google'
+        }
     }
     dependencies {
-        classpath 'com.android.tools.build:gradle:1.3.1'
+        classpath 'com.android.tools.build:gradle:2.1.3'
         classpath 'org.kt3k.gradle.plugin:coveralls-gradle-plugin:2.0.1x'
     }
 }
@@ -12,6 +16,10 @@ buildscript {
 allprojects {
     repositories {
         mavenCentral()
+        maven {
+            url 'https://maven.google.com/'
+            name 'Google'
+        }
     }
 }
 
diff --git a/gradle/wrapper/gradle-wrapper.properties b/gradle/wrapper/gradle-wrapper.properties
index 12e13c1..39666f9 100644
--- a/gradle/wrapper/gradle-wrapper.properties
+++ b/gradle/wrapper/gradle-wrapper.properties
@@ -1,6 +1,6 @@
-#Thu Apr 07 12:11:59 PDT 2016
+#Fri Mar 23 17:08:55 CST 2018
 distributionBase=GRADLE_USER_HOME
 distributionPath=wrapper/dists
 zipStoreBase=GRADLE_USER_HOME
 zipStorePath=wrapper/dists
-distributionUrl=https\://services.gradle.org/distributions/gradle-2.2-all.zip
+distributionUrl=https\://services.gradle.org/distributions/gradle-2.14.1-all.zip
