diff --git a/library/src/main/java/com/androidsocialnetworks/lib/impl/GooglePlusSocialNetwork.java b/library/src/main/java/com/androidsocialnetworks/lib/impl/GooglePlusSocialNetwork.java
index 36badbd..255334f 100644
--- a/library/src/main/java/com/androidsocialnetworks/lib/impl/GooglePlusSocialNetwork.java
+++ b/library/src/main/java/com/androidsocialnetworks/lib/impl/GooglePlusSocialNetwork.java
@@ -2,6 +2,7 @@
 
 import android.app.Activity;
 import android.content.Intent;
+import android.content.IntentSender;
 import android.os.AsyncTask;
 import android.os.Bundle;
 import android.os.Handler;
@@ -70,17 +71,17 @@ public void requestLogin(OnLoginCompleteListener onLoginCompleteListener) {
         super.requestLogin(onLoginCompleteListener);
         mConnectRequested = true;
 
-        if(mConnectionResult == null && !mPlusClient.isConnecting()) {
-            mPlusClient.connect();
-        } else {
+        if(mConnectionResult != null) {
             try {
                 mConnectionResult.startResolutionForResult(mSocialNetworkManager.getActivity(), REQUEST_AUTH);
             } catch (Exception e) {
-                Log.e(TAG, "ERROR", e);
+                Log.i(TAG, "ERROR", e);
                 if (!mPlusClient.isConnecting()) {
                     mPlusClient.connect();
                 }
             }
+        } else if(!mPlusClient.isConnecting()) {
+            mPlusClient.connect();
         }
     }
 
@@ -276,7 +277,16 @@ public void onConnected(Bundle bundle) {
             }
 
             if (mLocalListeners.get(REQUEST_LOGIN) != null) {
-                mLocalListeners.get(REQUEST_LOGIN).onError(getID(), REQUEST_LOGIN, "get person == null", null);
+                Log.i("onConnected", "Error: get person == null");
+                if(mConnectionResult != null) {
+                    try {
+                        mConnectionResult.startResolutionForResult(mSocialNetworkManager.getActivity(), REQUEST_AUTH);
+                    } catch (IntentSender.SendIntentException e) {
+                        e.printStackTrace();
+                    }
+                } else {
+                    mLocalListeners.get(REQUEST_LOGIN).onError(getID(), REQUEST_LOGIN, "get person == null", null);
+                }
             }
         }
 
@@ -292,7 +302,19 @@ public void onDisconnected() {
     public void onConnectionFailed(ConnectionResult connectionResult) {
         mConnectionResult = connectionResult;
         if (mConnectRequested && mLocalListeners.get(REQUEST_LOGIN) != null) {
-            mLocalListeners.get(REQUEST_LOGIN).onError(getID(), REQUEST_LOGIN, "error: " + connectionResult.getErrorCode(), null);
+            int errorCode = mConnectionResult.getErrorCode();
+
+            if(errorCode == ConnectionResult.SIGN_IN_REQUIRED || errorCode == ConnectionResult.RESOLUTION_REQUIRED) {
+                Log.i("onConnectionFailed", "Error code: " + errorCode);
+                try {
+                    mConnectionResult.startResolutionForResult(mSocialNetworkManager.getActivity(), REQUEST_AUTH);
+                    return;
+                } catch (IntentSender.SendIntentException e) {
+                    e.printStackTrace();
+                }
+            } else {
+                mLocalListeners.get(REQUEST_LOGIN).onError(getID(), REQUEST_LOGIN, "error: " + connectionResult.getErrorCode(), null);
+            }
         }
 
         mConnectRequested = false;
