diff --git a/TMessagesProj/src/main/java/org/telegram/ui/UltraHighlightWordsActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/UltraHighlightWordsActivity.java
index 93c3ca565..d632f5f61 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/UltraHighlightWordsActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/UltraHighlightWordsActivity.java
@@ -33,6 +33,7 @@
 import org.telegram.ui.ActionBar.ActionBarMenu;
 import org.telegram.ui.ActionBar.BaseFragment;
 import org.telegram.ui.Adapters.BaseFragmentAdapter;
+import org.telegram.ui.Cells.DividerCell;
 import org.telegram.ui.Cells.TextColorCell;
 import org.telegram.ui.Cells.TextFieldCell;
 import org.telegram.ui.Cells.TextInfoPrivacyCell;
@@ -44,6 +45,7 @@
     private int colorValue;
 
     private int highlightWordRow;
+    private int dividerRow;
     private int colorRow;
     private int helpRow;
     private int rowCount = 0;
@@ -53,6 +55,7 @@
     @Override
     public boolean onFragmentCreate() {
         highlightWordRow = rowCount++;
+        dividerRow = rowCount++;
         colorRow = rowCount++;
         helpRow = rowCount++;
 
@@ -85,10 +88,8 @@ public void onItemClick(int id) {
             highlightWordField.setFieldText(ApplicationLoader.WORDS_HIGHLIGHT);
             highlightWordField.setBackgroundColor(0xffffffff);
 
-
             fragmentView = new FrameLayout(getParentActivity());
             FrameLayout frameLayout = (FrameLayout) fragmentView;
-         //   frameLayout.setBackgroundColor(0xfff0f0f0);
 
             listView = new ListView(getParentActivity());
             listView.setDivider(null);
@@ -155,7 +156,7 @@ public boolean areAllItemsEnabled() {
 
         @Override
         public boolean isEnabled(int i) {
-            return i == colorRow;
+            return (i == colorRow);
         }
 
         @Override
@@ -202,6 +203,10 @@ public View getView(int i, View view, ViewGroup viewGroup) {
                 }
                 TextColorCell textcolorCell = (TextColorCell) view;
                 textcolorCell.setTextAndColor(LocaleController.getString("HighlightColor", R.string.HighlightColor), colorValue, true);
+            } else if (viewType == 3) {
+                if (view == null) {
+                    view = new DividerCell(mContext);
+                }
             }
             return view;
         }
@@ -214,13 +219,15 @@ public int getItemViewType(int i) {
                 return 1;
             } else if (i == colorRow) {
                 return 2;
+            } else if (i == dividerRow) {
+                return 3;
             }
-            return 0;
+            return 3;
         }
 
         @Override
         public int getViewTypeCount() {
-            return 3;
+            return 4;
         }
 
         @Override
@@ -253,7 +260,6 @@ private void saveHighlightWords() {
         if (!ApplicationLoader.WORDS_HIGHLIGHT.equals(newWords)) {
             // Change the global variable to the new value
             ApplicationLoader.WORDS_HIGHLIGHT = newWords;
-//            ApplicationLoader.WORDS_HIGHLIGHT_COLOR = colorCell.get;
 
             // Save the new value
             SharedPreferences.Editor editor = ApplicationLoader.applicationContext.getSharedPreferences("Ultra", Activity.MODE_PRIVATE).edit();
diff --git a/TMessagesProj/src/main/res/values-ar/strings.xml b/TMessagesProj/src/main/res/values-ar/strings.xml
index bcc9ee616..348e415a1 100644
--- a/TMessagesProj/src/main/res/values-ar/strings.xml
+++ b/TMessagesProj/src/main/res/values-ar/strings.xml
@@ -591,7 +591,7 @@
     <string name="PhotoQualityInfo">القيمة هنا تستخدم مع الصور الجديدة فقط، القيم العالية تعطي جودة اعلى لكن حجم ملف اكبر.\n\n* نسبة 80% عادة تقدم توازن بين الجودة والحجم.</string>
     <string name="disableTabletMode">عطل عرض اللوحي</string>
     <string name="disableTabletModeInfo">اعد تشغيل البرنامج عند تغيير الخيارِ</string>
-    <string name="HighlightWords">تمييز كلمات</string>
+    <string name="HighlightWords">تمييز الكلمات</string>
     <string name="WordsToHighlight">الكلمات المراد تمييزها</string>
     <string name="HighlightWordsEmpty">لا يوجد</string>
     <string name="HighlightWordsHelp">"بإمكانك اختيار كلمة أو عدة كلمات مفصولة بفواصل منقوطة ليتم تمييزها بلون في المحادثات. بالإمكان استخدام اسمك أو أية كلمة أخرى.&lt;br/&gt;&lt;br/&gt;هذا مثال لجملة بدون تمييز:&lt;br/&gt;&lt;font color=&#39;#000000&#39;&gt;ذهبت مع %1$s إلى المطعم&lt;/font&gt;&lt;br/&gt;&lt;br/&gt;مع التمييز:&lt;br/&gt;&lt;font color=&#39;#000000&#39;&gt;ذهبت مع &lt;/font&gt;&lt;font color=&#39;%2$s&#39;&gt;%1$s&lt;/font&gt;&lt;font color=&#39;#000000&#39;&gt; إلى المطعم&lt;/font&gt;&lt;br/&gt;&lt;br/&gt;هذه الخاصية مفيدة مع المجموعات النشطة إذ تساعدك على التصفح السريع وقراءة الرسائل التي تحوي الكلمات المختارة."</string>
