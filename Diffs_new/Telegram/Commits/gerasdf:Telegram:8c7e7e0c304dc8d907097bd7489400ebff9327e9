diff --git a/TMessagesProj/src/main/java/org/telegram/android/MessagesController.java b/TMessagesProj/src/main/java/org/telegram/android/MessagesController.java
index 6d2b29972..2eed4f9b3 100644
--- a/TMessagesProj/src/main/java/org/telegram/android/MessagesController.java
+++ b/TMessagesProj/src/main/java/org/telegram/android/MessagesController.java
@@ -39,6 +39,7 @@
 import java.util.concurrent.Semaphore;
 
 public class MessagesController implements NotificationCenter.NotificationCenterDelegate {
+    static private boolean PrivacyPlus = true;
 
     private ConcurrentHashMap<Integer, TLRPC.Chat> chats = new ConcurrentHashMap<>(100, 1.0f, 2);
     private ConcurrentHashMap<Integer, TLRPC.EncryptedChat> encryptedChats = new ConcurrentHashMap<>(10, 1.0f, 2);
@@ -1332,6 +1333,8 @@ public void cancelTyping(long dialog_id) {
     }
 
     public void sendTyping(final long dialog_id, int classGuid) {
+        if (PrivacyPlus) return;
+
         if (dialog_id == 0) {
             return;
         }
@@ -1833,6 +1836,8 @@ public void markDialogAsRead(final long dialog_id, final int max_id, final int m
         int lower_part = (int)dialog_id;
         int high_id = (int)(dialog_id >> 32);
 
+        if (PrivacyPlus) return;
+
         if (lower_part != 0) {
             if (max_positive_id == 0 && offset == 0 || high_id == 1) {
                 return;
