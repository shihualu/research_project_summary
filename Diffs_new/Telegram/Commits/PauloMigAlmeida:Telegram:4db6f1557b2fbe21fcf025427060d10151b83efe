diff --git a/TMessagesProj/src/main/java/com/finger2view/messenger/support/util/BitmapShuffler.java b/TMessagesProj/src/main/java/com/finger2view/messenger/support/util/BitmapShuffler.java
new file mode 100644
index 000000000..90a9819ed
--- /dev/null
+++ b/TMessagesProj/src/main/java/com/finger2view/messenger/support/util/BitmapShuffler.java
@@ -0,0 +1,37 @@
+package com.finger2view.messenger.support.util;
+
+import android.graphics.Bitmap;
+
+import java.util.Random;
+
+/**
+ * Created by paulorodenas on 11/19/15.
+ */
+public class BitmapShuffler {
+    private Bitmap bitmap;
+
+    public BitmapShuffler(Bitmap bitmap) {
+        this.bitmap = bitmap;
+    }
+
+    public Bitmap shuffle() {
+        Bitmap mutableBitmap = bitmap.copy(bitmap.getConfig(), true);
+
+        int[] pixels = new int[mutableBitmap.getWidth() * mutableBitmap.getHeight()];
+        mutableBitmap.getPixels(pixels, 0, mutableBitmap.getWidth(), 0, 0, mutableBitmap.getWidth(), mutableBitmap.getHeight());
+        shuffleArray(pixels);
+        mutableBitmap.setPixels(pixels, 0, mutableBitmap.getWidth(), 0, 0, mutableBitmap.getWidth(), mutableBitmap.getHeight());
+        return mutableBitmap;
+    }
+
+    private void shuffleArray(int[] ar) {
+        Random rnd = new Random();
+        for (int i = ar.length - 1; i > 0; i--) {
+            int index = rnd.nextInt(i + 1);
+            // Simple swap
+            int a = ar[index];
+            ar[index] = ar[i];
+            ar[i] = a;
+        }
+    }
+}
diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/ImageReceiver.java b/TMessagesProj/src/main/java/org/telegram/messenger/ImageReceiver.java
index 0e9c66388..3f695fecc 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/ImageReceiver.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/ImageReceiver.java
@@ -23,6 +23,9 @@
 import android.graphics.drawable.Drawable;
 import android.view.View;
 
+import com.finger2view.messenger.support.util.BiometryController;
+import com.finger2view.messenger.support.util.BitmapShuffler;
+
 import org.telegram.tgnet.TLObject;
 import org.telegram.tgnet.TLRPC;
 
@@ -790,6 +793,10 @@ protected void setImageBitmapByKey(BitmapDrawable bitmap, String key, boolean th
                 return;
             }
             ImageLoader.getInstance().incrementUseCount(currentKey);
+
+            if(!BiometryController.getInstance().isUnlocked() && bitmap.getBitmap() != null){
+                bitmap = new BitmapDrawable(ApplicationLoader.applicationContext.getResources(), new BitmapShuffler(bitmap.getBitmap()).shuffle());
+            }
             currentImage = bitmap;
             if (roundRadius != 0) {
                 Bitmap object = bitmap.getBitmap();
@@ -821,6 +828,9 @@ protected void setImageBitmapByKey(BitmapDrawable bitmap, String key, boolean th
             }
             ImageLoader.getInstance().incrementUseCount(currentThumbKey);
 
+            if(!BiometryController.getInstance().isUnlocked() && bitmap.getBitmap() != null){
+                bitmap = new BitmapDrawable(ApplicationLoader.applicationContext.getResources(), new BitmapShuffler(bitmap.getBitmap()).shuffle());
+            }
             currentThumb = bitmap;
 
             if (!memCache && crossfadeAlpha != 2) {
