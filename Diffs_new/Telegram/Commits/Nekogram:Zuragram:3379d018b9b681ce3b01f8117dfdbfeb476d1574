diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java b/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java
index da3b52ade..4c1427529 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Cells/ChatMessageCell.java
@@ -85,6 +85,7 @@
 import org.telegram.ui.Components.URLSpanNoUnderline;
 import org.telegram.ui.PhotoViewer;
 import org.telegram.ui.SecretMediaViewer;
+import org.zuragram.android.ZuraConfig;
 
 import java.io.File;
 import java.util.ArrayList;
@@ -521,6 +522,11 @@ public void run() {
         }
     };
 
+    private boolean isBlockedUserMessage() {
+        return MessagesController.getInstance(currentAccount).blockedUsers.indexOfKey(
+                currentMessageObject.getFromId()) >= 0 && ZuraConfig.ignoreBlocked;
+    }
+
     public ChatMessageCell(Context context) {
         super(context);
 
@@ -4276,6 +4282,9 @@ private void setMessageContent(MessageObject messageObject, MessageObject.Groupe
                 radialProgress.setImageOverlay(null, null);
             }
         }
+        if (isBlockedUserMessage()) {
+            totalHeight = 0;
+        }
         updateWaveform();
         updateButtonState(false, dataChanged && !messageObject.cancelEditing, true);
 
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
index fa7be1599..4e5061aea 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
@@ -13361,8 +13361,10 @@ public void onAnimationEnd(Animator animation) {
                             }
                         });
                     }
-                    if(fromUserBlocked){
+                    if (fromUserBlocked){
                         messageCell.setVisibility(View.GONE);
+                    } else {
+                        messageCell.setVisibility(View.VISIBLE);
                     }
                 } else if (view instanceof ChatActionCell) {
                     ChatActionCell actionCell = (ChatActionCell) view;
