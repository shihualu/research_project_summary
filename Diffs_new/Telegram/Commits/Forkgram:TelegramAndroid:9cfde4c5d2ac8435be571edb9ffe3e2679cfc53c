diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/SharedConfig.java b/TMessagesProj/src/main/java/org/telegram/messenger/SharedConfig.java
index 13fcfd585..c3e86868a 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/SharedConfig.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/SharedConfig.java
@@ -78,6 +78,8 @@
     public static boolean useSystemEmoji;
     public static int fontSize = AndroidUtilities.dp(16);
 
+    public static boolean hideTitleDialog = false;
+
     static {
         loadConfig();
     }
@@ -224,6 +226,8 @@ public static void loadConfig() {
             suggestStickers = preferences.getInt("suggestStickers", 0);
             sortContactsByName = preferences.getBoolean("sortContactsByName", false);
 
+            hideTitleDialog = preferences.getBoolean("hideTitle", false);
+
             configLoaded = true;
         }
     }
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
index df166fcfb..c60d88aea 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
@@ -223,6 +223,7 @@
     private StickersAdapter stickersAdapter;
     private FrameLayout stickersPanel;
     private TextView muteItem;
+    private TextView hideTitleItem;
     private FrameLayout pagedownButton;
     private ImageView pagedownButtonImage;
     private boolean pagedownButtonShowedByScroll;
@@ -560,6 +561,7 @@ public void sendButtonPressed(int index, VideoEditedInfo videoEditedInfo) {
     private final static int bot_help = 30;
     private final static int bot_settings = 31;
     private final static int call = 32;
+    private final static int hideTitle = 33;
 
     private final static int attach_photo = 0;
     private final static int attach_gallery = 1;
@@ -1250,6 +1252,20 @@ public void onItemClick(final int id) {
                         chatActivityEnterView.getEditField().setSelectionOverride(editTextStart, editTextEnd);
                         chatActivityEnterView.getEditField().makeSelectedRegular();
                     }
+                } else if (id == hideTitle) {
+                    SharedConfig.hideTitleDialog = !SharedConfig.hideTitleDialog;
+                    updateTitle();
+                    checkAndUpdateAvatar();
+                    if (hideTitleItem != null) {
+                        if (SharedConfig.hideTitleDialog) {
+                            hideTitleItem.setText("Show title");
+                        } else {
+                            hideTitleItem.setText("Hide title");
+                        }
+                    }
+
+                    SharedPreferences preferences = MessagesController.getGlobalMainSettings();
+                    preferences.edit().putBoolean("hideTitle", SharedConfig.hideTitleDialog).commit();
                 }
             }
         });
@@ -1438,6 +1454,9 @@ public boolean forceShowClear() {
             updateBotButtons();
         }
 
+        String hideTitleString = SharedConfig.hideTitleDialog ? "Show title" : "Hide title";
+        hideTitleItem = headerItem.addSubItem(hideTitle, hideTitleString);
+
         if (currentUser != null) {
             headerItem.addSubItem(call, LocaleController.getString("Call", R.string.Call));
             TLRPC.TL_userFull userFull = MessagesController.getInstance(currentAccount).getUserFull(currentUser.id);
@@ -6760,6 +6779,12 @@ private void updateTitle() {
         if (avatarContainer == null) {
             return;
         }
+
+        if (SharedConfig.hideTitleDialog) {
+            avatarContainer.setTitle("");
+            return;
+        }
+
         if (currentChat != null) {
             avatarContainer.setTitle(currentChat.title);
         } else if (currentUser != null) {
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java
index 8c5af51f4..3553c2a03 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/ChatAvatarContainer.java
@@ -386,6 +386,13 @@ public void checkAndUpdateAvatar() {
             avatarDrawable.setInfo(chat);
             parentObject = chat;
         }
+
+        if (org.telegram.messenger.SharedConfig.hideTitleDialog) {
+            avatarImageView.setVisibility(android.view.View.GONE);
+        } else {
+            avatarImageView.setVisibility(android.view.View.VISIBLE);
+        }
+
         if (avatarImageView != null) {
             avatarImageView.setImage(newPhoto, "50_50", avatarDrawable, parentObject);
         }
