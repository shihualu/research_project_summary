diff --git a/TMessagesProj/src/main/java/org/telegram/ui/DialogsActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/DialogsActivity.java
index 80221974b..39657eca0 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/DialogsActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/DialogsActivity.java
@@ -68,6 +68,7 @@
     private ActionBarMenuItem proxyItem;
     private ProxyDrawable proxyDrawable;
     private ImageView floatingButton;
+    private TabsView newTabsView;
 
     private ImageView unreadFloatingButton;
     private FrameLayout unreadFloatingButtonContainer;
@@ -233,6 +234,7 @@ public View createView(final Context context) {
         final ActionBarMenuItem item = menu.addItem(0, R.drawable.ic_ab_search).setIsSearchField(true).setActionBarMenuItemSearchListener(new ActionBarMenuItem.ActionBarMenuItemSearchListener() {
             @Override
             public void onSearchExpand() {
+                newTabsView.hide(true);
                 searching = true;
                 if (switchItem != null) {
                     switchItem.setVisibility(View.GONE);
@@ -270,6 +272,7 @@ public boolean canCollapseSearch() {
 
             @Override
             public void onSearchCollapse() {
+                newTabsView.hide(false);
                 searching = false;
                 searchWas = false;
                 if (listView != null) {
@@ -562,7 +565,7 @@ public void smoothScrollToPosition(RecyclerView recyclerView, RecyclerView.State
         listView.setLayoutManager(layoutManager);
         listView.setVerticalScrollbarPosition(LocaleController.isRTL ? RecyclerListView.SCROLLBAR_POSITION_LEFT : RecyclerListView.SCROLLBAR_POSITION_RIGHT);
 
-        TabsView newTabsView = new TabsView(context)
+        newTabsView = new TabsView(context)
                 .refreshAction((type, scroll) -> {
                     //todo: check for the same type?
                     int firstVisibleItem = layoutManager.findFirstVisibleItemPosition();
