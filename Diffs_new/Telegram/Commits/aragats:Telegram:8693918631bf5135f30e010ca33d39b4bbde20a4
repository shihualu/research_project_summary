diff --git a/TMessagesProj/src/main/java/org/telegram/android/LocaleController.java b/TMessagesProj/src/main/java/org/telegram/android/LocaleController.java
index 4783272e3..02d9b7052 100644
--- a/TMessagesProj/src/main/java/org/telegram/android/LocaleController.java
+++ b/TMessagesProj/src/main/java/org/telegram/android/LocaleController.java
@@ -19,6 +19,7 @@
 import android.util.Xml;
 
 import org.telegram.android.time.FastDateFormat;
+
 import ru.aragats.aracle.ApplicationLoader;
 import ru.aragats.aracle.R;
 
@@ -117,6 +118,7 @@ public static LocaleInfo createWithString(String string) {
     private ArrayList<LocaleInfo> otherLanguages = new ArrayList<>();
 
     private static volatile LocaleController Instance = null;
+
     public static LocaleController getInstance() {
         LocaleController localInstance = Instance;
         if (localInstance == null) {
@@ -457,13 +459,13 @@ private void loadOtherLanguages() {
             String value = null;
             String attrName = null;
             while (eventType != XmlPullParser.END_DOCUMENT) {
-                if(eventType == XmlPullParser.START_TAG) {
+                if (eventType == XmlPullParser.START_TAG) {
                     name = parser.getName();
                     int c = parser.getAttributeCount();
                     if (c > 0) {
                         attrName = parser.getAttributeValue(0);
                     }
-                } else if(eventType == XmlPullParser.TEXT) {
+                } else if (eventType == XmlPullParser.TEXT) {
                     if (attrName != null) {
                         value = parser.getText();
                         if (value != null) {
@@ -773,18 +775,21 @@ public static String stringForMessageListDate(long date) {
             int dateDay = rightNow.get(Calendar.DAY_OF_YEAR);
             int dateYear = rightNow.get(Calendar.YEAR);
 
+            String result;
+            String time = formatterDay.format(new Date(date * 1000));
             if (year != dateYear) {
-                return formatterYear.format(new Date(date * 1000));
+                result = formatterYear.format(new Date(date * 1000));
             } else {
                 int dayDiff = dateDay - day;
-                if(dayDiff == 0 || dayDiff == -1 && (int)(System.currentTimeMillis() / 1000) - date < 60 * 60 * 8) {
-                    return formatterDay.format(new Date(date * 1000));
-                } else if(dayDiff > -7 && dayDiff <= -1) {
-                    return formatterWeek.format(new Date(date * 1000));
+                if (dayDiff == 0 || dayDiff == -1 && (int) (System.currentTimeMillis() / 1000) - date < 60 * 60 * 8) {
+                    return time;
+                } else if (dayDiff > -7 && dayDiff <= -1) {
+                    result = formatterWeek.format(new Date(date * 1000));
                 } else {
-                    return formatterMonth.format(new Date(date * 1000));
+                    result = formatterMonth.format(new Date(date * 1000));
                 }
             }
+            return result + " " + time;
         } catch (Exception e) {
             FileLog.e("tmessages", e);
         }
@@ -792,7 +797,6 @@ public static String stringForMessageListDate(long date) {
     }
 
 
-
     public String getTranslitString(String src) {
         if (translitChars == null) {
             translitChars = new HashMap<>(520);
