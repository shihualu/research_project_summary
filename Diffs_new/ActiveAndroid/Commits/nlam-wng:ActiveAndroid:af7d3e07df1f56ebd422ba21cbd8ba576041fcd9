diff --git a/src/com/activeandroid/query/From.java b/src/com/activeandroid/query/From.java
index 4da87d92..5ecaa695 100644
--- a/src/com/activeandroid/query/From.java
+++ b/src/com/activeandroid/query/From.java
@@ -37,6 +37,7 @@
 	private String mOrderBy;
 	private String mLimit;
 	private String mOffset;
+	private String mOrderByOrder;
 
 	private List<Object> mArguments;
 
@@ -47,10 +48,12 @@ public From(Class<? extends Model> table, Sqlable queryBase) {
 
 		mJoins = new ArrayList<Join>();
 		mArguments = new ArrayList<Object>();
+		mOrderByOrder = "ASC";
 	}
 
 	public From as(String alias) {
 		mAlias = alias;
+        mOrderByOrder = "ASC";
 		return this;
 	}
 
@@ -113,6 +116,11 @@ public From orderBy(String orderBy) {
 		mOrderBy = orderBy;
 		return this;
 	}
+    public From orderBy(String orderBy, String order) {
+        mOrderBy = orderBy;
+        mOrderByOrder = order;
+        return this;
+    }
 
 	public From limit(int limit) {
 		return limit(String.valueOf(limit));
@@ -164,7 +172,7 @@ public String toSql() {
 		}
 
 		if (mOrderBy != null) {
-			sql += "ORDER BY " + mOrderBy + " ";
+			sql += "ORDER BY " + mOrderBy + " " + mOrderByOrder + " ";
 		}
 
 		if (mLimit != null) {
