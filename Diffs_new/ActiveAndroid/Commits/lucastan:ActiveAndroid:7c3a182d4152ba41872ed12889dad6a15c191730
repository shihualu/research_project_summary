diff --git a/src/com/activeandroid/Model.java b/src/com/activeandroid/Model.java
index a73c0254..96483d97 100644
--- a/src/com/activeandroid/Model.java
+++ b/src/com/activeandroid/Model.java
@@ -181,12 +181,17 @@ public final boolean upsert(long id, String updateWhere, String[] updateWhereArg
             // Don't update the ID so as to avoid triggering foreign
             // keys ON UPDATE.
             values.remove(idName);
+
+            String whereClause;
             if (updateWhere != null)
                 updateWhere = updateWhere.trim();
-            if (!TextUtils.isEmpty(updateWhere))
-                updateWhere = " and (" + updateWhere + ")";
-            int rows = db.update(mTableInfo.getTableName(), values, idName
-                    + "=" + id + updateWhere, updateWhereArgs);
+            if (TextUtils.isEmpty(updateWhere))
+                whereClause = idName + "=" + id;
+            else
+                whereClause = idName + "=" + id + " and (" + updateWhere + ")";
+
+            int rows = db.update(mTableInfo.getTableName(), values,
+                    whereClause, updateWhereArgs);
             if (rows == 0)
             {
                 return false;
