diff --git a/src/com/activeandroid/util/SQLiteUtils.java b/src/com/activeandroid/util/SQLiteUtils.java
index df746b45..792d2286 100644
--- a/src/com/activeandroid/util/SQLiteUtils.java
+++ b/src/com/activeandroid/util/SQLiteUtils.java
@@ -121,7 +121,7 @@ public static void put(ContentValues values, Class<?> fieldType, String fieldNam
             if (ReflectionUtils.isModel(fieldType)) {
                 values.put(fieldName, ((Model) value).getId());
             }
-            else if (ReflectionUtils.isSubclassOf(fieldType, Enum.class)) {
+            else if (Enum.class.isAssignableFrom(fieldType)){
                 values.put(fieldName, ((Enum<?>) value).name());
             }
             return;
@@ -174,7 +174,7 @@ public static Object get(Cursor cursor, Class<?> fieldType, int columnIndex) {
                 return entity;
             }
             
-            if (ReflectionUtils.isSubclassOf(fieldType, Enum.class)) {
+            if (Enum.class.isAssignableFrom(fieldType)){
                 @SuppressWarnings({"rawtypes", "unchecked"})
                 final Class<? extends Enum> enumType = (Class<? extends Enum>) fieldType;
                 @SuppressWarnings("unchecked")
@@ -390,7 +390,7 @@ else if (ReflectionUtils.isModel(type)) {
 			definition.append(" ");
 			definition.append(SQLiteType.INTEGER.toString());
 		}
-		else if (ReflectionUtils.isSubclassOf(type, Enum.class)) {
+		else if (Enum.class.isAssignableFrom(type)){
 			definition.append(name);
 			definition.append(" ");
 			definition.append(SQLiteType.TEXT.toString());
