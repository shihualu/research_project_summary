diff --git a/src/com/activeandroid/query/From.java b/src/com/activeandroid/query/From.java
index ab3837a9..a701e497 100644
--- a/src/com/activeandroid/query/From.java
+++ b/src/com/activeandroid/query/From.java
@@ -37,7 +37,8 @@
 	private final StringBuilder mWhere = new StringBuilder();
 	private String mGroupBy;
 	private String mHaving;
-	private String mOrderBy;
+    private String mOrderBy;
+    private OrderType mOrderType;
 	private String mLimit;
 	private String mOffset;
 
@@ -133,9 +134,14 @@ public From having(String having) {
 	}
 
 	public From orderBy(String orderBy) {
-		mOrderBy = orderBy;
-		return this;
-	}
+		return orderBy(orderBy, OrderType.ASC);
+    }
+    
+    public From orderBy(String orderBy, OrderType type) {
+        mOrderBy = orderBy;
+        mOrderType = type;
+        return this;
+    }
 
 	public From limit(int limit) {
 		return limit(String.valueOf(limit));
@@ -209,6 +215,7 @@ private void addOrderBy(final StringBuilder sql) {
         if (mOrderBy != null) {
             sql.append("ORDER BY ");
             sql.append(mOrderBy);
+            sql.append(mOrderType.name());
             sql.append(" ");
         }
     }
@@ -342,5 +349,9 @@ public int count() {
 		}
 
 		return args;
-	}
+    }
+    
+    public enum OrderType {
+        ASC, DESC
+    }
 }
