diff --git a/src/com/activeandroid/DatabaseHelper.java b/src/com/activeandroid/DatabaseHelper.java
index dd49379b..c249702e 100644
--- a/src/com/activeandroid/DatabaseHelper.java
+++ b/src/com/activeandroid/DatabaseHelper.java
@@ -42,6 +42,12 @@
 
 	public final static String MIGRATION_PATH = "migrations";
 
+    //////////////////////////////////////////////////////////////////////////////////////
+    // PRIVATE VARIABLES
+    //////////////////////////////////////////////////////////////////////////////////////
+
+    private int mDatabaseVersion;
+
 	//////////////////////////////////////////////////////////////////////////////////////
 	// CONSTRUCTORS
 	//////////////////////////////////////////////////////////////////////////////////////
@@ -49,6 +55,7 @@
 	public DatabaseHelper(Configuration configuration) {
 		super(configuration.getContext(), configuration.getDatabaseName(), null, configuration.getDatabaseVersion());
 		copyAttachedDatabase(configuration.getContext(), configuration.getDatabaseName());
+        mDatabaseVersion = configuration.getDatabaseVersion();
 	}
 
 	//////////////////////////////////////////////////////////////////////////////////////
@@ -64,7 +71,7 @@ public void onOpen(SQLiteDatabase db) {
 	public void onCreate(SQLiteDatabase db) {
 		executePragmas(db);
 		executeCreate(db);
-		executeMigrations(db, -1, db.getVersion());
+		executeMigrations(db, -1, mDatabaseVersion);
 	}
 
 	@Override
