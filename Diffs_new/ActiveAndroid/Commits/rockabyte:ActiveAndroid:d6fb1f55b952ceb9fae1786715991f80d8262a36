diff --git a/src/com/activeandroid/Model.java b/src/com/activeandroid/Model.java
index 3b434d4a..bf4303ec 100644
--- a/src/com/activeandroid/Model.java
+++ b/src/com/activeandroid/Model.java
@@ -226,9 +226,9 @@ public final void loadFromCursor(Cursor cursor) {
 			Class<?> fieldType = field.getType();
             final int columnIndex = columnsOrdered.indexOf(fieldName);
 
-			if (columnIndex < 0) {
-				continue;
-			}
+//			if (columnIndex < 0) {
+//				continue;
+//			}
 
 			field.setAccessible(true);
 
@@ -244,7 +244,7 @@ public final void loadFromCursor(Cursor cursor) {
 				// TODO: Find a smarter way to do this? This if block is necessary because we
 				// can't know the type until runtime.
 				if (columnIsNull) {
-					field = null;
+					value = null;
 				}
 				else if (fieldType.equals(Byte.class) || fieldType.equals(byte.class)) {
 					value = cursor.getInt(columnIndex);
@@ -299,9 +299,7 @@ else if (ReflectionUtils.isSubclassOf(fieldType, Enum.class)) {
 				}
 
 				// Set the field value
-				if (value != null) {
-					field.set(this, value);
-				}
+				field.set(this, value);
 			}
 			catch (IllegalArgumentException e) {
 				Log.e(e.getClass().getName(), e);
