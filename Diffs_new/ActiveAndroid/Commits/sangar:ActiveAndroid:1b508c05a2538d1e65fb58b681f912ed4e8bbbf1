diff --git a/src/com/activeandroid/Model.java b/src/com/activeandroid/Model.java
index d0ce5d65..a33610da 100644
--- a/src/com/activeandroid/Model.java
+++ b/src/com/activeandroid/Model.java
@@ -62,6 +62,10 @@ public final void setId(Long id) {
         this.mId = id;
     }
 
+    protected boolean valid() {
+        return true;
+    };
+
 	public final void delete() {
 		Cache.openDatabase().delete(mTableInfo.getTableName(), "Id=?", new String[] { getId().toString() });
 		Cache.removeEntity(this);
@@ -73,6 +77,10 @@ public final void delete() {
 	public boolean save() {
         boolean modelSaved = false;
 
+        if (!valid()) {
+            return modelSaved;
+        }
+
 		final SQLiteDatabase db = Cache.openDatabase();
 		final ContentValues values = new ContentValues();
 
