diff --git a/src/com/activeandroid/DatabaseHelper.java b/src/com/activeandroid/DatabaseHelper.java
index e63486d4..2290ac76 100644
--- a/src/com/activeandroid/DatabaseHelper.java
+++ b/src/com/activeandroid/DatabaseHelper.java
@@ -229,7 +229,7 @@ private void executeDelimitedSqlScript(SQLiteDatabase db, InputStream stream) th
 	    for(String command : commands) {
 	        try {
 				db.execSQL(command);
-			} catch (IOException e) {
+			} catch (Exception e) {
 				Log.e("Failed to execute " + file, e);
 			}
 	    }
@@ -250,7 +250,7 @@ private void executeLegacySqlScript(SQLiteDatabase db, InputStream stream) throw
                 if (!TextUtils.isEmpty(line)) {
                    try {
 						db.execSQL(line);
-					} catch (IOException e) {
+					} catch (Exception e) {
 						Log.e("Failed to execute " + file, e);
 					}
                 }
