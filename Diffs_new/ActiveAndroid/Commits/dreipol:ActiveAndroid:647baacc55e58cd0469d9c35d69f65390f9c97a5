diff --git a/src/com/activeandroid/Model.java b/src/com/activeandroid/Model.java
index 3f44d9e7..cb196dcd 100644
--- a/src/com/activeandroid/Model.java
+++ b/src/com/activeandroid/Model.java
@@ -334,7 +334,7 @@ public int hashCode() {
         T entity;
         try {
             Field f = getUniqueField(objectClass, uniqueIdentifier);
-            String columnName = info.getColumnName(f);
+            String columnName = info.getColumn(f);
             f.setAccessible(true);
             Object value = f.get(object);
             entity = new Select().from(objectClass).where(columnName + "=?", value).executeSingle();
diff --git a/src/com/activeandroid/TableInfo.java b/src/com/activeandroid/TableInfo.java
index 67466085..64dd935f 100644
--- a/src/com/activeandroid/TableInfo.java
+++ b/src/com/activeandroid/TableInfo.java
@@ -152,6 +152,14 @@ public String getDatabaseName(Field field) {
         return name;
     }
 
+    /**
+     * @param field The model class field
+     * @return the full column name (table.column)
+     */
+    public String getColumn(Field field){
+        return mTableName + "." + getDatabaseName(field);
+    }
+
     public boolean hasComputedFields() {
         return mComputedNames.size() > 0;
     }
