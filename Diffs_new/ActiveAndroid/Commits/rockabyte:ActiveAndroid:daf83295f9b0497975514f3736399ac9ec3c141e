diff --git a/src/com/activeandroid/Cache.java b/src/com/activeandroid/Cache.java
index ab1c3e02..11a1f4a4 100644
--- a/src/com/activeandroid/Cache.java
+++ b/src/com/activeandroid/Cache.java
@@ -149,7 +149,18 @@ public static synchronized TableInfo getTableInfo(Class<? extends Model> type) {
 	}
 
 	public static synchronized TypeSerializer getParserForType(Class<?> type) {
-		return sModelInfo.getTypeSerializer(type);
+        TypeSerializer typeSerializer = null;
+
+        while(type != null) {
+            typeSerializer = sModelInfo.getTypeSerializer(type);
+
+            if (typeSerializer == null) {
+                type = type.getSuperclass();
+                continue;
+            }
+            break;
+        }
+		return typeSerializer;
 	}
 
 	public static synchronized String getTableName(Class<? extends Model> type) {
