diff --git a/src/com/activeandroid/util/SQLiteUtils.java b/src/com/activeandroid/util/SQLiteUtils.java
index 9f64a7f1..73de11ef 100644
--- a/src/com/activeandroid/util/SQLiteUtils.java
+++ b/src/com/activeandroid/util/SQLiteUtils.java
@@ -161,7 +161,7 @@ public static void createUniqueColumnDefinition(TableInfo tableInfo, Field field
 			String group = groups[i];
 			ConflictAction conflictAction = conflictActions[i];
 
-			if (group.isEmpty())
+			if (TextUtils.isEmpty(group))
 				continue;
 
 			List<String> list = sUniqueGroupMap.get(group);
@@ -212,7 +212,7 @@ public static void createIndexColumnDefinition(TableInfo tableInfo, Field field)
 
 		String[] groups = column.indexGroups();
 		for (String group : groups) {
-			if (group.isEmpty())
+			if (TextUtils.isEmpty(group))
 				continue;
 
 			List<String> list = sIndexGroupMap.get(group);
