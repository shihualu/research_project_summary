diff --git a/annotations/src/main/java/com/activeandroid/sebbia/annotation/Table.java b/annotations/src/main/java/com/activeandroid/sebbia/annotation/Table.java
index ec60e2c1..245e4fdf 100644
--- a/annotations/src/main/java/com/activeandroid/sebbia/annotation/Table.java
+++ b/annotations/src/main/java/com/activeandroid/sebbia/annotation/Table.java
@@ -28,7 +28,9 @@
 
     public static final String DEFAULT_ID_NAME = "Id";
 
-    public String name();
+    String name();
 
-    public String id() default DEFAULT_ID_NAME;
+    String db() default "";
+
+    String id() default DEFAULT_ID_NAME;
 }
diff --git a/library/build.gradle b/library/build.gradle
index b4fe7f9f..d0c93e45 100644
--- a/library/build.gradle
+++ b/library/build.gradle
@@ -7,6 +7,8 @@ android {
     buildToolsVersion autoBuildToolsVersion('23.0.1')
 
     defaultConfig {
+        testApplicationId "com.activeandroid.test"
+
         minSdkVersion 16
         targetSdkVersion autoCompileSdkVersion(22)
 
diff --git a/library/src/androidTest/AndroidManifest.xml b/library/src/androidTest/AndroidManifest.xml
index 2a8c871b..f613e5c8 100644
--- a/library/src/androidTest/AndroidManifest.xml
+++ b/library/src/androidTest/AndroidManifest.xml
@@ -4,16 +4,16 @@
           android:versionCode="1"
           android:versionName="1.0">
 
-    <uses-sdk android:minSdkVersion="4"/>
+    <!--<uses-sdk android:minSdkVersion="4"/>-->
 
-    <instrumentation
-        android:name="android.test.InstrumentationTestRunner"
-        android:targetPackage="com.activeandroid.test"/>
+    <!--<instrumentation-->
+        <!--android:name="android.test.InstrumentationTestRunner"-->
+        <!--android:targetPackage="com.activeandroid.test"/>-->
 
-    <application
-        android:icon="@drawable/ic_launcher"
-        android:label="@string/app_name">
-        <uses-library android:name="android.test.runner"/>
-    </application>
+    <!--<application-->
+        <!--android:icon="@drawable/ic_launcher"-->
+        <!--android:label="@string/app_name">-->
+        <!--<uses-library android:name="android.test.runner"/>-->
+    <!--</application>-->
 
 </manifest>
\ No newline at end of file
diff --git a/library/src/main/java/com/activeandroid/sebbia/Cache.java b/library/src/main/java/com/activeandroid/sebbia/Cache.java
index 7828392b..346b815d 100644
--- a/library/src/main/java/com/activeandroid/sebbia/Cache.java
+++ b/library/src/main/java/com/activeandroid/sebbia/Cache.java
@@ -215,6 +215,11 @@ public static synchronized String getTableName(Class<? extends Model> type)
         return sModelInfo.getTableInfo(type).getTableName();
     }
 
+    public static synchronized String getDatabaseName(Class<? extends Model> type)
+    {
+        return sModelInfo.getTableInfo(type).getDatabaseName();
+    }
+
     static ModelFiller getFiller(Class<? extends Model> type)
     {
         return sFillers.get(type);
diff --git a/library/src/main/java/com/activeandroid/sebbia/DatabaseHelper.java b/library/src/main/java/com/activeandroid/sebbia/DatabaseHelper.java
index 42d767be..0a506fac 100644
--- a/library/src/main/java/com/activeandroid/sebbia/DatabaseHelper.java
+++ b/library/src/main/java/com/activeandroid/sebbia/DatabaseHelper.java
@@ -17,6 +17,7 @@
  */
 
 import android.content.Context;
+import android.database.SQLException;
 import android.database.sqlite.SQLiteDatabase;
 import android.database.sqlite.SQLiteOpenHelper;
 import android.text.TextUtils;
@@ -38,6 +39,7 @@
 import java.util.Arrays;
 import java.util.Collections;
 import java.util.List;
+import java.util.regex.Pattern;
 
 public final class DatabaseHelper extends SQLiteOpenHelper
 {
@@ -53,6 +55,8 @@
 
     private final String mSqlParser;
 
+    private final String mName;
+
     //////////////////////////////////////////////////////////////////////////////////////
     // CONSTRUCTORS
     //////////////////////////////////////////////////////////////////////////////////////
@@ -60,6 +64,8 @@
     public DatabaseHelper(Context context, String name, int version, String sqlParser)
     {
         super(context, name, null, version);
+        mName = name;
+
         copyAttachedDatabase(context, name);
         mSqlParser = sqlParser;
     }
@@ -158,6 +164,18 @@ private void executeCreateIndex(SQLiteDatabase db)
         {
             for (TableInfo tableInfo : Cache.getTableInfos())
             {
+                boolean createTable;
+                if (TextUtils.isEmpty(tableInfo.getDatabaseName()))
+                {
+                    createTable = true;
+                }
+                else
+                {
+                    createTable = Pattern.compile(tableInfo.getDatabaseName()).matcher(mName).matches();
+                }
+
+                if (!createTable) continue;
+
                 String[] definitions = SQLiteUtils.createIndexDefinition(tableInfo);
 
                 for (String definition : definitions)
@@ -180,7 +198,27 @@ private void executeCreate(SQLiteDatabase db)
         {
             for (TableInfo tableInfo : Cache.getTableInfos())
             {
-                db.execSQL(SQLiteUtils.createTableDefinition(tableInfo));
+                boolean createTable;
+                if (TextUtils.isEmpty(tableInfo.getDatabaseName()))
+                {
+                    createTable = true;
+                }
+                else
+                {
+                    createTable = Pattern.compile(tableInfo.getDatabaseName()).matcher(mName).matches();
+                }
+
+                try
+                {
+                    if (createTable)
+                    {
+                        db.execSQL(SQLiteUtils.createTableDefinition(tableInfo));
+                    }
+                }
+                catch (SQLException e)
+                {
+                    throw e;
+                }
             }
             db.setTransactionSuccessful();
         }
diff --git a/library/src/main/java/com/activeandroid/sebbia/TableInfo.java b/library/src/main/java/com/activeandroid/sebbia/TableInfo.java
index a3d68147..d900beb8 100644
--- a/library/src/main/java/com/activeandroid/sebbia/TableInfo.java
+++ b/library/src/main/java/com/activeandroid/sebbia/TableInfo.java
@@ -41,8 +41,9 @@
     // PRIVATE MEMBERS
     //////////////////////////////////////////////////////////////////////////////////////
 
-    private Class<? extends Model> mType;
-    private String                 mTableName;
+    private       Class<? extends Model> mType;
+    private       String                 mTableName;
+    private final String                 mDatabaseName;
     private String mIdName = Table.DEFAULT_ID_NAME;
 
     private Map<Field, String> mColumnNames = new LinkedHashMap<Field, String>();
@@ -66,11 +67,13 @@ public TableInfo(Class<? extends Model> type)
         if (tableAnnotation != null)
         {
             mTableName = tableAnnotation.name();
+            mDatabaseName = tableAnnotation.db();
             mIdName = tableAnnotation.id();
         }
         else
         {
             mTableName = type.getSimpleName();
+            mDatabaseName = null;
         }
 
         // Manually add the id column since it is not declared like the other columns.
@@ -117,6 +120,11 @@ public String getTableName()
         return mTableName;
     }
 
+    public String getDatabaseName()
+    {
+        return mDatabaseName;
+    }
+
     public String getIdName()
     {
         return mIdName;
diff --git a/library/src/main/java/com/activeandroid/sebbia/query/Select.java b/library/src/main/java/com/activeandroid/sebbia/query/Select.java
index e47d0b6b..5b366e22 100644
--- a/library/src/main/java/com/activeandroid/sebbia/query/Select.java
+++ b/library/src/main/java/com/activeandroid/sebbia/query/Select.java
@@ -96,6 +96,7 @@ else if (mAll)
 
         if (mColumns != null && mColumns.length > 0)
         {
+            sql.append("Id, ");
             sql.append(TextUtils.join(", ", mColumns) + " ");
         }
         else
