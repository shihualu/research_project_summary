diff --git a/src/com/activeandroid/Model.java b/src/com/activeandroid/Model.java
index 96483d97..25c5d1d2 100644
--- a/src/com/activeandroid/Model.java
+++ b/src/com/activeandroid/Model.java
@@ -224,16 +224,17 @@ public static void delete(Class<? extends Model> type, long id) {
      * @param model Model instance to copy. Must not be null.
      * @return The new model instance or null if an error occurs.
      */
-    public static <T extends Model> T clone(T model) {
-        final Class<T> cls = (Class<T>) model.getClass();
+    public static <T extends Model> T clone(T m) {
+        final Class<T> cls = (Class<T>) m.getClass();
         T newInstance = null;
+        Model model = (Model) m;
 
         try {
             newInstance = cls.newInstance();
 
             // copy fields from model to newInstance.
             for (Field field : model.mTableInfo.getFields()) {
-                Object val = field.get(model);
+                Object val = field.get(m);
 
                 if (val instanceof Model)
                     val = clone((Model)val);
@@ -241,7 +242,7 @@ public static void delete(Class<? extends Model> type, long id) {
                 field.set(newInstance, val);
             }
 
-            newInstance.mId = model.mId;
+            ((Model) newInstance).mId = model.mId;
 
             return newInstance;
         }
