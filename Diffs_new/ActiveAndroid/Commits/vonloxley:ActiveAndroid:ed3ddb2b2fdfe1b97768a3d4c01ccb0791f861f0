diff --git a/src/com/activeandroid/Model.java b/src/com/activeandroid/Model.java
index e06169f8..5bcec118 100644
--- a/src/com/activeandroid/Model.java
+++ b/src/com/activeandroid/Model.java
@@ -131,6 +131,9 @@ else if (fieldType.equals(Byte[].class) || fieldType.equals(byte[].class)) {
 				else if (ReflectionUtils.isModel(fieldType)) {
 					values.put(fieldName, ((Model) value).getId());
 				}
+				else if (ReflectionUtils.isSubclassOf(fieldType, Enum.class)){
+					values.put(fieldName, ((Enum<?>) value).name());
+				}
 			}
 			catch (IllegalArgumentException e) {
 				Log.e(e.getClass().getName(), e);
@@ -227,6 +230,11 @@ else if (ReflectionUtils.isModel(fieldType)) {
 
 					value = entity;
 				}
+				else if (ReflectionUtils.isSubclassOf(fieldType, Enum.class)){
+					@SuppressWarnings("rawtypes")
+					final Class<? extends Enum> enumType =  (Class<? extends Enum>) fieldType;
+					value=Enum.valueOf(enumType, cursor.getString(columnIndex));
+				}
 
 				// Use a deserializer if one is available
 				if (typeSerializer != null && !columnIsNull) {
diff --git a/src/com/activeandroid/util/SQLiteUtils.java b/src/com/activeandroid/util/SQLiteUtils.java
index 7351d5d6..8ab6348a 100644
--- a/src/com/activeandroid/util/SQLiteUtils.java
+++ b/src/com/activeandroid/util/SQLiteUtils.java
@@ -140,6 +140,9 @@ public static String createColumnDefinition(TableInfo tableInfo, Field field) {
 		else if (ReflectionUtils.isModel(type)) {
 			definition = name + " " + SQLiteType.INTEGER.toString();
 		}
+		else if (ReflectionUtils.isSubclassOf(type, Enum.class)) {
+			definition = name + " " + SQLiteType.TEXT.toString();
+		}
 
 		if (definition != null) {
 			if (column.length() > -1) {
