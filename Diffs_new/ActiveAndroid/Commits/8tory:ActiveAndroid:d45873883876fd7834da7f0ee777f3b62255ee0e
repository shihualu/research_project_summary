diff --git a/src/com/activeandroid/Model.java b/src/com/activeandroid/Model.java
index ca39bb79..9546759f 100644
--- a/src/com/activeandroid/Model.java
+++ b/src/com/activeandroid/Model.java
@@ -347,7 +347,7 @@ public static void delete(Class<? extends Model> type, long id) {
 
 	// Model population
 
-	public final void loadFromCursor(Cursor cursor) {
+	public final int loadFromCursor(Cursor cursor) {
 		int sizeOfColumnNotFound = 0;
 
 		for (Field field : mTableInfo.getFields()) {
@@ -444,9 +444,7 @@ else if (ReflectionUtils.isSubclassOf(fieldType, Enum.class)) {
 			}
 		}
 
-		if (mId != null && sizeOfColumnNotFound == 0) {
-			Cache.addEntity(this);
-		}
+		return sizeOfColumnNotFound;
 	}
 
 	public static void registerContentObserver(Class<? extends Model> type, boolean notifyForDescendents, ContentObserver observer) {
diff --git a/src/com/activeandroid/util/ModelCursorMarshaller.java b/src/com/activeandroid/util/ModelCursorMarshaller.java
index 5409d030..129e7a26 100644
--- a/src/com/activeandroid/util/ModelCursorMarshaller.java
+++ b/src/com/activeandroid/util/ModelCursorMarshaller.java
@@ -28,8 +28,11 @@ public T marshall(Cursor cursor) {
             if (entity == null) {
                 entity = type.newInstance();
                 //android.util.Log.d("ModelCursorMarshaller", "" + type);
-                entity.loadFromCursor(cursor);
+                int columnSize = entity.loadFromCursor(cursor);
                 //android.util.Log.d("ModelCursorMarshaller", "" + entity);
+                if (entity.getId() != null && columnSize == 0) {
+                    Cache.addEntity(entity);
+                }
             }
         }
         /*
