diff --git a/maven_push.gradle b/maven_push.gradle
index 2b35c7127..ba3dd327f 100644
--- a/maven_push.gradle
+++ b/maven_push.gradle
@@ -8,6 +8,11 @@ if (isReleaseBuild()) {
     maveRepo = mavenRepoSnapshot
 }
 
+signing {
+    required { has("release") && gradle.taskGraph.hasTask("uploadArchives") }
+    sign configurations.archives
+}
+
 // figure out why this is in afterEvaluate
 afterEvaluate { project ->
     uploadArchives {
