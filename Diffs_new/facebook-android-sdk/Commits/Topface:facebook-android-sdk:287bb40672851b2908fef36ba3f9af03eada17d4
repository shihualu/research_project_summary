diff --git a/facebook/src/com/facebook/topface/FbDialog.java b/facebook/src/com/facebook/topface/FbDialog.java
index 7d42beea8..f83b30940 100644
--- a/facebook/src/com/facebook/topface/FbDialog.java
+++ b/facebook/src/com/facebook/topface/FbDialog.java
@@ -99,7 +99,7 @@ private void createCrossImage() {
             @Override
             public void onClick(View v) {
                 mListener.onCancel();
-                FbDialog.this.dismiss();
+                dismissDialog(FbDialog.this);
             }
         });
         Drawable crossDrawable = getContext().getResources().getDrawable(R
@@ -150,11 +150,11 @@ public boolean shouldOverrideUrlLoading(WebView view, String url) {
                     mListener.onFacebookError(new FacebookError(error));
                 }
 
-                FbDialog.this.dismiss();
+                dismissDialog(FbDialog.this);
                 return true;
             } else if (url.startsWith(Facebook.CANCEL_URI)) {
                 mListener.onCancel();
-                FbDialog.this.dismiss();
+                dismissDialog(FbDialog.this);
                 return true;
             } else if (url.contains(DISPLAY_STRING)) {
                 return false;
@@ -171,34 +171,20 @@ public void onReceivedError(WebView view, int errorCode,
             super.onReceivedError(view, errorCode, description, failingUrl);
             mListener.onError(
                     new DialogError(description, errorCode, failingUrl));
-            FbDialog.this.dismiss();
+            dismissDialog(FbDialog.this);
         }
 
         @Override
         public void onPageStarted(WebView view, String url, Bitmap favicon) {
             Util.logd("Facebook-WebView", "Webview loading URL: " + url);
             super.onPageStarted(view, url, favicon);
-            try {
-                mSpinner.show();
-            }
-            catch (Exception e) {
-                Log.e("Facebook", e.toString());
-                e.printStackTrace();
-            }
+            showDialog(mSpinner);
         }
 
         @Override
         public void onPageFinished(WebView view, String url) {
             super.onPageFinished(view, url);
-            try {
-                if (mSpinner != null && mSpinner.isShowing()) {
-                    mSpinner.dismiss();
-                }
-            }
-            catch (Exception e) {
-                Log.e("Facebook", e.toString());
-                e.printStackTrace();
-            }
+            dismissDialog(mSpinner);
             /*
              * Once webview is fully loaded, set the mContent background to be transparent
              * and make visible the 'x' image. 
@@ -207,5 +193,28 @@ public void onPageFinished(WebView view, String url) {
             mWebView.setVisibility(View.VISIBLE);
             mCrossImage.setVisibility(View.VISIBLE);
         }
+
+    }
+
+    public void dismissDialog(Dialog dialog) {
+        try {
+            if (dialog != null && dialog.isShowing()) {
+                dialog.dismiss();
+            }
+        } catch (Exception e) {
+            Log.e("Facebook", e.toString());
+            e.printStackTrace();
+        }
+    }
+
+    public void showDialog(Dialog dialog) {
+        try {
+            if (dialog != null ) {
+                dialog.show();
+            }
+        } catch (Exception e) {
+            Log.e("Facebook", e.toString());
+            e.printStackTrace();
+        }
     }
 }
