diff --git a/facebook/src/com/facebook/Utility.java b/facebook/src/com/facebook/Utility.java
index c5583ae45..d6466d678 100644
--- a/facebook/src/com/facebook/Utility.java
+++ b/facebook/src/com/facebook/Utility.java
@@ -366,8 +366,12 @@ static boolean stringsEqualOrEmpty(String a, String b) {
     private static void clearCookiesForDomain(Context context, String domain) {
         // This is to work around a bug where CookieManager may fail to instantiate if CookieSyncManager
         // has never been created.
-        CookieSyncManager syncManager = CookieSyncManager.createInstance(context);
-        syncManager.sync();
+        try {
+            CookieSyncManager syncManager = CookieSyncManager.createInstance(context);
+            syncManager.sync();
+        } catch (Exception e) {
+            // CookieSyncManager.createInstance(context) can throw SqliteDiskIOException
+        }
 
         CookieManager cookieManager = CookieManager.getInstance();
 
