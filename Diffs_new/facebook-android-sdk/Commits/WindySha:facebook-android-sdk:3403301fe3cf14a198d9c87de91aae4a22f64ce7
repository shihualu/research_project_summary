diff --git a/facebook-core/src/main/java/com/facebook/FacebookSdk.java b/facebook-core/src/main/java/com/facebook/FacebookSdk.java
index df41b25fc..c09cd5865 100644
--- a/facebook-core/src/main/java/com/facebook/FacebookSdk.java
+++ b/facebook-core/src/main/java/com/facebook/FacebookSdk.java
@@ -715,7 +715,7 @@ static void loadDefaultsFromMetadata(Context context) {
                 } else {
                     applicationId = appIdString;
                 }
-            } else if (appId instanceof Integer) {
+            } else if (appId instanceof Integer || appId instanceof Float) {
                 throw new FacebookException(
                         "App Ids cannot be directly placed in the manifest." +
                         "They must be prefixed by 'fb' or be placed in the string resource file.");
