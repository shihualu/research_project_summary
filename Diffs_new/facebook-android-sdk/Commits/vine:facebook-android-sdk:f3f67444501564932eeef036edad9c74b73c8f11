diff --git a/facebook/src/com/facebook/AuthorizationClient.java b/facebook/src/com/facebook/AuthorizationClient.java
index 1c62ccd7f..adb35108a 100644
--- a/facebook/src/com/facebook/AuthorizationClient.java
+++ b/facebook/src/com/facebook/AuthorizationClient.java
@@ -188,7 +188,7 @@ private void completeWithFailure() {
     }
 
     boolean tryCurrentHandler() {
-        return !(currentHandler.needsInternetPermission()) && currentHandler.tryAuthorize(pendingRequest);
+        return currentHandler.tryAuthorize(pendingRequest);
     }
 
     void completeAndValidate(Result outcome) {
@@ -386,10 +386,6 @@ boolean needsRestart() {
             return false;
         }
 
-        boolean needsInternetPermission() {
-            return false;
-        }
-
         void cancel() {
         }
     }
@@ -405,11 +401,6 @@ boolean needsRestart() {
             return true;
         }
 
-        @Override
-        boolean needsInternetPermission() {
-            return true;
-        }
-
         @Override
         void cancel() {
             if (loginDialog != null) {
