diff --git a/facebook/res/drawable/back.png b/facebook/res/drawable/back.png
deleted file mode 100755
index 15bd39076..000000000
Binary files a/facebook/res/drawable/back.png and /dev/null differ
diff --git a/facebook/res/drawable/top_icon_leaderboard.png b/facebook/res/drawable/top_icon_leaderboard.png
deleted file mode 100755
index 32d74ace7..000000000
Binary files a/facebook/res/drawable/top_icon_leaderboard.png and /dev/null differ
diff --git a/facebook/res/drawable/top_icon_tournament.png b/facebook/res/drawable/top_icon_tournament.png
deleted file mode 100755
index 0ca988fb6..000000000
Binary files a/facebook/res/drawable/top_icon_tournament.png and /dev/null differ
diff --git a/facebook/res/drawable/tournament_back.png b/facebook/res/drawable/tournament_back.png
new file mode 100644
index 000000000..3c1e312c2
Binary files /dev/null and b/facebook/res/drawable/tournament_back.png differ
diff --git a/facebook/res/drawable/tournament_back_high.png b/facebook/res/drawable/tournament_back_high.png
new file mode 100644
index 000000000..c32a2d22e
Binary files /dev/null and b/facebook/res/drawable/tournament_back_high.png differ
diff --git a/facebook/res/drawable/tournament_carbon.png b/facebook/res/drawable/tournament_carbon.png
new file mode 100644
index 000000000..8785a6687
Binary files /dev/null and b/facebook/res/drawable/tournament_carbon.png differ
diff --git a/facebook/res/drawable/tournament_info.png b/facebook/res/drawable/tournament_info.png
new file mode 100644
index 000000000..2b0243ecf
Binary files /dev/null and b/facebook/res/drawable/tournament_info.png differ
diff --git a/facebook/res/drawable/tournament_info_high.png b/facebook/res/drawable/tournament_info_high.png
new file mode 100644
index 000000000..ab2b58619
Binary files /dev/null and b/facebook/res/drawable/tournament_info_high.png differ
diff --git a/facebook/res/drawable/tournament_leaderboard.png b/facebook/res/drawable/tournament_leaderboard.png
new file mode 100644
index 000000000..deeee0130
Binary files /dev/null and b/facebook/res/drawable/tournament_leaderboard.png differ
diff --git a/facebook/res/drawable/tournament_leaderboard_high.png b/facebook/res/drawable/tournament_leaderboard_high.png
new file mode 100644
index 000000000..b0422de8f
Binary files /dev/null and b/facebook/res/drawable/tournament_leaderboard_high.png differ
diff --git a/facebook/src/com/explodingbarrel/FacebookIAPMainActivity.java b/facebook/src/com/explodingbarrel/FacebookIAPMainActivity.java
index 54c357962..1c16214c3 100644
--- a/facebook/src/com/explodingbarrel/FacebookIAPMainActivity.java
+++ b/facebook/src/com/explodingbarrel/FacebookIAPMainActivity.java
@@ -12,6 +12,9 @@
 import android.content.pm.*;
 import android.webkit.WebView;
 import android.view.View;
+import android.view.Window;
+import android.view.WindowManager;
+import android.view.WindowManager.LayoutParams;
 
 import com.unity3d.player.*;
 import com.facebook.*;
@@ -71,9 +74,9 @@ protected void onResume()
 		}
     }
     
-    boolean WebViewShowFullscreen( String url, String tabs )
+    boolean WebViewShowFullscreen( String url, String tabs, int targetWidth )
     {
-    	Log.d(TAG, "WebViewShowFullscreen : baseurl = " + url + " tabs = " + tabs );
+    	Log.d(TAG, "WebViewShowFullscreen : url = " + url + " tabs = " + tabs + " targetWidth = " + targetWidth );
     	
     	boolean valid = false;
     	
@@ -83,6 +86,7 @@ boolean WebViewShowFullscreen( String url, String tabs )
     		Log.d(TAG, "WebViewShowFullscreen : Pending - Starting full screen webview activity" );
     		webViewIntent.putExtra("url", url);
     		webViewIntent.putExtra("tabs", tabs);
+    		webViewIntent.putExtra("targetWidth", targetWidth);
     		startActivity( webViewIntent );
     		valid = true;
     	}
@@ -94,31 +98,55 @@ boolean WebViewShowFullscreen( String url, String tabs )
     	return valid;
     }
     
-    boolean WebViewPopup( String baseurl, String stoken, int popup, final float normalizedX, final float normalizedY, final float normalizedWidth, final float normalizedHeight )
+    private WebViewDialog ActiveWebViewPopup = null; 
+    
+    boolean WebViewPopup( final String url, final int targetWidth, final float normalizedX, final float normalizedY, final float normalizedWidth, final float normalizedHeight )
     {	
+    	Log.d(TAG, "WebViewPopup : url = " + url + " normalizedX = " + normalizedX + " normalizedY = " + normalizedY + " normalizedWidth = " + normalizedWidth + " normalizedHeight = " + normalizedHeight );
+    	
     	this.runOnUiThread( new Runnable()
     	{
     		 public void run()
              {				
     			 DisplayMetrics metrics = new DisplayMetrics();
     			 getWindowManager().getDefaultDisplay().getMetrics(metrics);
+    			 
+    			 Log.d(TAG, "WebViewPopup : Screen Width = " + metrics.widthPixels + " Screen Height = " + metrics.heightPixels );
 				
     			 float x = normalizedX * metrics.widthPixels;
     			 float y = normalizedY * metrics.heightPixels;
     			 float width = normalizedWidth * metrics.widthPixels;
-    			 float height = normalizedWidth * metrics.heightPixels; 
+    			 float height = normalizedHeight * metrics.heightPixels;
+    			 
+    			 Log.d(TAG, "WebViewPopup : url = " + url + " x = " + x + " y = " + y + " width = " + width + " height = " + height );
     			 
-    			 WebViewDialog dialog = new WebViewDialog( FacebookIAPMainActivity.this, "http://www.google.com", (int)x, (int)y, (int)width, (int)height );
-    			 dialog.show();
+    			 FacebookIAPMainActivity.this.ActiveWebViewPopup = new WebViewDialog( FacebookIAPMainActivity.this, url, targetWidth, (int)x, (int)y, (int)width, (int)height );
+    			 Window window = FacebookIAPMainActivity.this.ActiveWebViewPopup.getWindow();
+    			 window.setFlags( WindowManager.LayoutParams.FLAG_NOT_TOUCH_MODAL, WindowManager.LayoutParams.FLAG_NOT_TOUCH_MODAL );
+    			 window.clearFlags( WindowManager.LayoutParams.FLAG_DIM_BEHIND );
+    			 FacebookIAPMainActivity.this.ActiveWebViewPopup.show();
              }
     	} );
     	
     	return true;
     }
     
-    boolean WebViewClose()
+    boolean WebViewPopupClose()
     {
+    	Log.d(TAG, "WebViewPopupClose" );
     	boolean valid = false;
+    	if( this.ActiveWebViewPopup != null )
+    	{
+    		valid = true;
+    		this.runOnUiThread( new Runnable()
+    		{
+    			public void run()
+    			{	
+    				FacebookIAPMainActivity.this.ActiveWebViewPopup.dismiss();
+		    	}
+    		});
+    	}
+    	
     	return valid;
     }
     
diff --git a/facebook/src/com/explodingbarrel/webview/WebViewDialog.java b/facebook/src/com/explodingbarrel/webview/WebViewDialog.java
index 6f8072981..317e94de7 100644
--- a/facebook/src/com/explodingbarrel/webview/WebViewDialog.java
+++ b/facebook/src/com/explodingbarrel/webview/WebViewDialog.java
@@ -13,6 +13,7 @@
 {
 	private Activity Parent = null;
 	private String Url = null;
+	private int TargetWidth = 1024;
 	private int X = 0;
 	private int Y = 0;
 	private int Width = 0;
@@ -32,11 +33,12 @@ public void onScaleChanged( WebView view, float oldScale, float newScale )
 	    }
 	}
 	
-	public WebViewDialog( Activity a, String url, int x, int y, int width, int height )
+	public WebViewDialog( Activity a, String url, int targetWidth, int x, int y, int width, int height )
 	{
 		super(a);
 		this.Parent = a;
 		this.Url = url;
+		this.TargetWidth = targetWidth;
 		this.X = x;
 		this.Y = y;
 		this.Width = width;
@@ -61,6 +63,9 @@ protected void onCreate(Bundle savedInstanceState)
 		webSettings.setJavaScriptEnabled( true );
 		webSettings.setLoadWithOverviewMode( true );
 		webSettings.setUseWideViewPort(true);
+		
+		float scale = ((float)this.TargetWidth) / this.Width;
+		webView.setInitialScale( (int)( scale * 100.0f ) );
 	    
 		webView.loadUrl( this.Url );
 
diff --git a/facebook/src/com/explodingbarrel/webview/WebViewFullScreenActivity.java b/facebook/src/com/explodingbarrel/webview/WebViewFullScreenActivity.java
index 522d27b1a..3c95d558c 100644
--- a/facebook/src/com/explodingbarrel/webview/WebViewFullScreenActivity.java
+++ b/facebook/src/com/explodingbarrel/webview/WebViewFullScreenActivity.java
@@ -41,6 +41,7 @@ public void onCreate(Bundle savedInstanceState)
 		
 		String url = getIntent().getExtras().getString("url");
 		String tabsJson = getIntent().getExtras().getString("tabs");
+		int targetWidth = getIntent().getExtras().getInt("targetWidth");
 		
 		Log.d( TAG, "FullScreenWebViewActivity onCreate : url = " + url + " tabs = " + tabsJson );
 		
@@ -58,7 +59,8 @@ public void onCreate(Bundle savedInstanceState)
         this.CloseButton = new ImageButton(this);
     	if( this.CloseButton != null )
     	{
-    		this.CloseButton.setImageResource( Helpers.getIdResource(this, "drawable/back") );
+    		this.CloseButton.setImageResource( Helpers.getIdResource(this, "drawable/tournament_back") );
+    		this.CloseButton.setBackgroundColor(0xFF000000);
     		this.CloseButton.setLayoutParams(new LinearLayout.LayoutParams(LayoutParams.WRAP_CONTENT, LayoutParams.WRAP_CONTENT));
     		this.CloseButton.setOnClickListener( new OnClickListener()
     												{
@@ -87,6 +89,8 @@ public void onClick(View arg0)
     				final String tabImage = tab.getString( "Image" );
 
     				Button tabButton = new Button(this);
+    				tabButton.setBackgroundColor(0xFF000000);
+    				tabButton.setTextColor(0xFFFFFFFF);
     				tabButton.setText( tabText );
     				if( tabImage.length() > 0 )
     				{
@@ -121,7 +125,7 @@ public void onClick(View arg0)
 		//Support for diff screens
 		DisplayMetrics metrics = new DisplayMetrics();
 	    getWindowManager().getDefaultDisplay().getMetrics(metrics);
-	    float scale = 1024.0f / metrics.widthPixels;
+	    float scale = ((float)targetWidth) / metrics.widthPixels;
 	    this.FullScreenWebView.setInitialScale( (int)( scale * 100.0f ) );
 	    
 		//String url = generateURL( page );
