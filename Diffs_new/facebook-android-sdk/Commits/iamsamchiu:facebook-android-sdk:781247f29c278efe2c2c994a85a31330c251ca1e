diff --git a/.travis.yml b/.travis.yml
index bb3076d3f..76acb805d 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -14,6 +14,8 @@ android:
     - extra
 
 script:
+  - set -e
+  - set -x
   - rsync -avhP ./android-licenses/ "$ANDROID_HOME/licenses/"
 
 # Override the install and test so that we don't run integration tests
