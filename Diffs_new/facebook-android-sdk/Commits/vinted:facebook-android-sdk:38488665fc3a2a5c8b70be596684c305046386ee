diff --git a/facebook/build.gradle b/facebook/build.gradle
index 2ebc03e9a..dbbda85b5 100644
--- a/facebook/build.gradle
+++ b/facebook/build.gradle
@@ -1,17 +1,17 @@
 apply plugin: 'android-library'
 
 dependencies {
-    compile 'com.android.support:support-v4:13.0.+'
-    compile files('libs/bolts.jar')
+    compile 'com.android.support:support-v13:19.0.+'
+    compile files('libs/bolts.jar', 'libs/AudienceNetwork.jar')
 }
 
 android {
-    compileSdkVersion Integer.parseInt(project.ANDROID_BUILD_SDK_VERSION)
-    buildToolsVersion project.ANDROID_BUILD_TOOLS_VERSION
+    compileSdkVersion 19
+    buildToolsVersion "19.0.3"
 
     defaultConfig {
-        minSdkVersion Integer.parseInt(project.ANDROID_BUILD_MIN_SDK_VERSION)
-        targetSdkVersion Integer.parseInt(project.ANDROID_BUILD_TARGET_SDK_VERSION)
+        minSdkVersion 14
+        targetSdkVersion 19
     }
 
     lintOptions {
diff --git a/libs/AudienceNetwork.jar b/libs/AudienceNetwork.jar
new file mode 100644
index 000000000..eb4f7d03f
Binary files /dev/null and b/libs/AudienceNetwork.jar differ
