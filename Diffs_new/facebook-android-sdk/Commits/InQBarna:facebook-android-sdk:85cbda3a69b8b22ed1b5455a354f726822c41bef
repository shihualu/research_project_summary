diff --git a/facebook/src/com/facebook/LoginButton.java b/facebook/src/com/facebook/LoginButton.java
index 0b9b85430..bc551b744 100644
--- a/facebook/src/com/facebook/LoginButton.java
+++ b/facebook/src/com/facebook/LoginButton.java
@@ -324,6 +324,9 @@ public void onFinishInflate() {
     }
 
     private void finishInit() {
+    	if (isInEditMode())
+    		return;
+    	
         sessionTracker = new SessionTracker(getContext(), new LoginButtonCallback(), null, false);
         setOnClickListener(new LoginClickListener());
         setButtonText();
@@ -345,11 +348,14 @@ public void setFragment(Fragment fragment) {
     @Override
     protected void onAttachedToWindow() {
         super.onAttachedToWindow();
-        if (!sessionTracker.isTracking()) {
-            sessionTracker.startTracking();
-            fetchUserInfo();
-            setButtonText();
-        }
+
+		if (!isInEditMode()) {
+			if (!sessionTracker.isTracking()) {
+				sessionTracker.startTracking();
+				fetchUserInfo();
+				setButtonText();
+			}
+		}
     }
 
     @Override
@@ -384,6 +390,10 @@ private void setButtonText() {
     }
 
     private boolean initializeActiveSessionWithCachedToken(Context context) {
+    	
+    	if (isInEditMode())
+    		return true;
+    	
         if (context == null) {
             return false;
         }
