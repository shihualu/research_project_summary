diff --git a/build.gradle b/build.gradle
index d803c3c..b65f6a5 100644
--- a/build.gradle
+++ b/build.gradle
@@ -6,7 +6,6 @@ buildscript {
     dependencies {
         classpath 'com.android.tools.build:gradle:3.3.2'
         classpath 'com.github.ben-manes:gradle-versions-plugin:0.17.0'
-        classpath 'com.github.dcendents:android-maven-gradle-plugin:2.1'
     }
 }
 
diff --git a/library/build.gradle b/library/build.gradle
index 0b1f9fc..d4a34dc 100644
--- a/library/build.gradle
+++ b/library/build.gradle
@@ -1,10 +1,17 @@
+buildscript {
+    repositories {
+        jcenter()
+    }
+    dependencies {
+        // from aputils
+        classpath 'digital.wup:android-maven-publish:3.0.0'
+    }
+}
+
 apply plugin: 'com.android.library'
 apply plugin: 'com.github.ben-manes.versions'
 apply from: '../build-config/gradle-quality.gradle'
 
-apply plugin: 'com.github.dcendents.android-maven' // for jitpack
-group='com.github.AdventureProjects' // for jitpack
-
 archivesBaseName = 'android-job'
 
 dependencies {
@@ -75,9 +82,18 @@ android {
     }
 }
 
-task sourcesJar(type: Jar) {
-    classifier = 'sources'
-    from android.sourceSets.main.java.srcDirs
+// publish to local, also works in jitpack.io
+apply plugin: 'digital.wup.android-maven-publish'
+publishing {
+    publications {
+        aar(MavenPublication) {
+            groupId = 'com.github.AdventureProjects'
+            artifactId = 'android-job'
+            version = project.VERSION_NAME
+            artifact bundleReleaseAar
+        }
+    }
 }
+tasks.publishToMavenLocal.dependsOn(build)
 
 apply from: '../build-config/gradle-push.gradle'
