diff --git a/.circleci/config.yml b/.circleci/config.yml
index 475f2e6476..9b3c4e53cc 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -21,10 +21,13 @@ jobs:
       - run:
           name: Create avd
           command: echo no | avdmanager create avd -n testEmulator -k "system-images;android-24;default;armeabi-v7a"
-                   which emulator
           environment:
             TERM: dumb
 
+      - run:
+          name: Create avd
+          command: which emulator
+
       - run:
           name: Start emulator
           command: emulator -avd testEmulator -no-audio -no-window
