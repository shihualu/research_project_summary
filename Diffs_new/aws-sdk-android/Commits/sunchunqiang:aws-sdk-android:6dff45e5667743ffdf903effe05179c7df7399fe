diff --git a/.circleci/config.yml b/.circleci/config.yml
index 3ce7f41083..322d7d98e8 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -104,6 +104,11 @@ commands:
       - run:
           name: checkout repository for bump version
           command: |
+            if [ -z "$target_branch" ]
+            then 
+              target_branch="master"
+            fi
+
             git config --local user.name "${GITHUB_BUMPVERSION_USER}"
             git clone "https://github.com/${GITHUB_BUMPVERSION_USER}/${bumpversion_repo_name}.git"
             cd ${bumpversion_repo_name}
@@ -119,7 +124,7 @@ commands:
             git remote add upstream https://github.com/${bumpversion_repo_user}/${bumpversion_repo_name}
             git remote -v
             git fetch upstream
-            git reset --hard upstream/master  
+            git reset --hard upstream/$target_branch  
             echo "push update the branch"
             git push  --force   -q https://${GITHUB_BUMPVERSION_TOKEN}@github.com/${GITHUB_BUMPVERSION_USER}/${bumpversion_repo_name}.git
 
@@ -143,6 +148,10 @@ commands:
       - run:
           name: check in changes
           command: |
+            if [ -z "$target_branch" ]
+            then 
+              target_branch="master"
+            fi          
             cd ${bumpversion_repo_name}
             git status
             git config --local user.name "${GITHUB_BUMPVERSION_USER}"
@@ -152,7 +161,7 @@ commands:
             content="${bump_version_message}" 
             echo "title:$title"
             echo "content:$content"
-            python3 ../CircleciScripts/create_pullrequest.py  "${GITHUB_BUMPVERSION_USER}" "${GITHUB_BUMPVERSION_TOKEN}" "$title" "$content" "master" "${GITHUB_BUMPVERSION_USER}:bump_version" ${bumpversion_repo_user} ${bumpversion_repo_name}
+            python3 ../CircleciScripts/create_pullrequest.py  "${GITHUB_BUMPVERSION_USER}" "${GITHUB_BUMPVERSION_TOKEN}" "$title" "$content" "$target_branch" "${GITHUB_BUMPVERSION_USER}:bump_version" ${bumpversion_repo_user} ${bumpversion_repo_name}
   setup_android_platform23:
     description: >-
       setup android platform 23
@@ -165,10 +174,10 @@ commands:
             sudo yes | sdkmanager "platforms;android-23"
   skip_job_if_triggered_by_circleci:
     description: >-
-      skip the job is release_sdk is triggered by circleci
+      skip the job if release process is triggered by circleci
     steps:
       - run:
-          name: download android platform 23
+          name: check commit messange and halt job
           command: |
             currentcommit="${CIRCLE_SHA1}"
             commitmessage=$(git log --pretty=oneline ${currentcommit} | grep ${currentcommit})
@@ -199,9 +208,10 @@ commands:
           command: |
               echo ""
               # we can add script here to halt a job for circleci script test
-              declare -a jobs=("prepare_release_sdk", "merge_to_master", "bump_sdk_version")
+              declare -a jobs=("prepare_release_sdk" "merge_to_master" "bump_sdk_version")
               for i in "${jobs[@]}"
               do
+                echo "$i":"${CIRCLE_JOB}"
                 if [ "$i" == "${CIRCLE_JOB}" ]
                 then 
                   echo "not skip job $i"
@@ -500,7 +510,6 @@ jobs:
       - run:
           name: reset OVERRIDE_TEST environment variabe 
           command: |
-            setvarialbe_endpoint="https://circleci.com/api/v1.1/project/github/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}/envvar?circle-token=${CIRCLE_API_USER_TOKEN}"
             parameters='{"name":"OVERRIDE_TEST","value":"FALSE"}'
             curl -X POST \
               -H "Content-type: application/json" \
@@ -576,7 +585,40 @@ jobs:
       - store_artifacts:
           path: "~/test_results"
       - store_artifacts:
-          path: logcat.log          
+          path: logcat.log     
+  build_api10:
+    working_directory: ~/code
+    docker:
+      - image: circleci/android:api-27-alpha
+    environment:
+      JVM_OPTS: -Xmx3200m
+    steps:
+      - skip_job_if_required      
+      - checkout
+      - generate_gradle_wrapper
+      - run:
+          name: download android-10
+          command:
+            sudo yes | sdkmanager "platforms;android-10"
+      - run:
+          name: install python3-pip
+          command: |
+            sudo apt-get update
+            sudo apt-get -y install python3-pip            
+      - run:
+          name: install json parser
+          command: sudo pip3 install demjson           
+      - run:
+          name: update code for api10
+          command: |
+            python3 CircleciScripts/replace_android10.py "$(pwd)"
+            rm aws-android-sdk-core/src/main/java/com/amazonaws/internal/keyvaluestore/KeyProvider18.java
+            rm aws-android-sdk-core/src/main/java/com/amazonaws/internal/keyvaluestore/KeyProvider23.java
+            cat aws-android-sdk-core/src/main/java/com/amazonaws/internal/keyvaluestore/AWSKeyValueStore.java
+      - run:
+          name: build the whole project
+          command: |
+            bash gradlew :aws-android-sdk-core:build  -x test               
   uitest:
     working_directory: ~/code
     docker:
@@ -697,8 +739,42 @@ jobs:
       JVM_OPTS: -Xmx1024m
     steps:
       - skip_job_if_required
-      - checkout
       - set_enviroment_variables
+      - checkout  
+
+      - run:
+          name: set_bumpversion_enviroment
+          command: |
+            git fetch
+            git checkout master CircleciScripts/ 
+            bumpversion_repo_user=aws-amplify
+            bumpversion_repo_name=aws-sdk-android
+            release_version=$(cat version)
+            release_version=$(echo "$release_version" | xargs echo -n)
+            echo "release_version=[$release_version]"
+            bump_version_message="bump android sdk version to ${release_version}"
+            bump_version_pr_title="bump android sdk version to ${release_version}"
+            target_branch=develop
+            echo "export bumpversion_repo_user=$bumpversion_repo_user" >> $BASH_ENV
+            echo "export bumpversion_repo_name=$bumpversion_repo_name" >> $BASH_ENV
+            echo "export release_version=$release_version" >> $BASH_ENV
+            echo "export bump_version_message='${bump_version_message}'" >> $BASH_ENV
+            echo "export bump_version_pr_title='${bump_version_pr_title}'" >> $BASH_ENV
+            echo "export target_branch='${target_branch}'" >> $BASH_ENV
+            echo "bump_version_pr_title:$bump_version_pr_title"
+
+      - bump_version_pre
+      - run:
+          name: bump version
+          command: |
+            python3 CircleciScripts/bump_sdk_version.py "$(pwd)/${bumpversion_repo_name}" $release_version 1.0.0
+      - bump_version_post
+
+
+
+
+
+
       - run:
           name: checkout_developbranch
           command: |
@@ -751,12 +827,7 @@ jobs:
           command: |
             git config --local user.name "${GITHUB_USER}"
             git checkout master
-            echo "git reset"
-            git fetch
-            git reset --hard
-            echo "git pull"
             git pull --rebase
-            echo "get git log" 
             commitlog=$(git log)
             currentcommit="${CIRCLE_SHA1}"
             echo "currentcommit=$currentcommit"
@@ -818,6 +889,10 @@ workflows:
           filters:
             branches:
               ignore: bump_sdk_version     
+      - build_api10:
+          filters:
+            branches:
+              ignore: bump_sdk_version                  
       - unittest:
            filters:
             branches:
@@ -1119,6 +1194,12 @@ workflows:
               ignore: /.*/
             tags:
               only: /^(release|beta)_v[0-9]+.[0-9]+.[0-9]+$/
+      - build_api10:
+          filters:
+            branches:
+              ignore: /.*/
+            tags:
+              only: /^(release|beta)_v[0-9]+.[0-9]+.[0-9]+$/              
       - unittest:
           filters:
             branches:
@@ -1397,6 +1478,7 @@ workflows:
           requires:
             - unittest
             - post_integrationtest
+            - build_api10
           filters:
             branches:
               ignore: /.*/
diff --git a/version b/version
index c305492dc7..e2bdf6e45a 100644
--- a/version
+++ b/version
@@ -1,2 +1 @@
-2.7.2
-sdkversion
+2.7.3
\ No newline at end of file
