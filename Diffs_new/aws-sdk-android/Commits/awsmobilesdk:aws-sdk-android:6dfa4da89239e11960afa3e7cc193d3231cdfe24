diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/auth/policy/actions/SNSActions.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/auth/policy/actions/SNSActions.java
index 8fa3909e3d..614e61b6a7 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/auth/policy/actions/SNSActions.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/auth/policy/actions/SNSActions.java
@@ -82,6 +82,9 @@
     /** Action for the ListSubscriptionsByTopic operation. */
     ListSubscriptionsByTopic("sns:ListSubscriptionsByTopic"),
 
+    /** Action for the ListTagsForResource operation. */
+    ListTagsForResource("sns:ListTagsForResource"),
+
     /** Action for the ListTopics operation. */
     ListTopics("sns:ListTopics"),
 
@@ -112,8 +115,14 @@
     /** Action for the Subscribe operation. */
     Subscribe("sns:Subscribe"),
 
+    /** Action for the TagResource operation. */
+    TagResource("sns:TagResource"),
+
     /** Action for the Unsubscribe operation. */
-    Unsubscribe("sns:Unsubscribe");
+    Unsubscribe("sns:Unsubscribe"),
+
+    /** Action for the UntagResource operation. */
+    UntagResource("sns:UntagResource");
 
     private final String action;
 
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNS.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNS.java
index 3d536f9e80..a770144874 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNS.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNS.java
@@ -28,10 +28,10 @@
  * Amazon SNS to easily push real-time notification messages to interested
  * subscribers over multiple delivery protocols. For more information about this
  * product see <a
- * href="http://aws.amazon.com/sns/">http://aws.amazon.com/sns</a>. For detailed
- * information about Amazon SNS features and their associated API calls, see the
- * <a href="http://docs.aws.amazon.com/sns/latest/dg/">Amazon SNS Developer
- * Guide</a>.
+ * href="http://aws.amazon.com/sns/">https://aws.amazon.com/sns</a>. For
+ * detailed information about Amazon SNS features and their associated API
+ * calls, see the <a href="https://docs.aws.amazon.com/sns/latest/dg/">Amazon
+ * SNS Developer Guide</a>.
  * </p>
  * <p>
  * We also provide SDKs that enable you to access Amazon SNS from your preferred
@@ -144,6 +144,7 @@ void addPermission(AddPermissionRequest addPermissionRequest) throws AmazonClien
      *         Amazon Simple Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -177,6 +178,7 @@ CheckIfPhoneNumberIsOptedOutResult checkIfPhoneNumberIsOptedOut(
      * @throws NotFoundException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws FilterPolicyLimitExceededException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -192,7 +194,7 @@ ConfirmSubscriptionResult confirmSubscription(
     /**
      * <p>
      * Creates a platform application object for one of the supported push
-     * notification services, such as APNS and GCM, to which devices and mobile
+     * notification services, such as APNS and FCM, to which devices and mobile
      * apps may register. You must specify PlatformPrincipal and
      * PlatformCredential attributes when using the
      * <code>CreatePlatformApplication</code> action. The PlatformPrincipal is
@@ -212,21 +214,21 @@ ConfirmSubscriptionResult confirmSubscription(
      * "secret key". The PlatformApplicationArn that is returned when using
      * <code>CreatePlatformApplication</code> is then used as an attribute for
      * the <code>CreatePlatformEndpoint</code> action. For more information, see
-     * <a href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">
+     * <a href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">
      * Using Amazon SNS Mobile Push Notifications</a>. For more information
      * about obtaining the PlatformPrincipal and PlatformCredential for each of
      * the supported push notification services, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-apns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-apns.html"
      * >Getting Started with Apple Push Notification Service</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-adm.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-adm.html"
      * >Getting Started with Amazon Device Messaging</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-baidu.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-baidu.html"
      * >Getting Started with Baidu Cloud Push</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-gcm.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-gcm.html"
      * >Getting Started with Google Cloud Messaging for Android</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-mpns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-mpns.html"
      * >Getting Started with MPNS</a>, or <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-wns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-wns.html"
      * >Getting Started with WNS</a>.
      * </p>
      * 
@@ -265,14 +267,14 @@ CreatePlatformApplicationResult createPlatformApplication(
      * requester already owns an endpoint with the same device token and
      * attributes, that endpoint's ARN is returned without creating a new
      * endpoint. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * <p>
      * When using <code>CreatePlatformEndpoint</code> with Baidu, two attributes
      * must be provided: ChannelId and UserId. The token field must also contain
      * the ChannelId. For more information, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePushBaiduEndpoint.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePushBaiduEndpoint.html"
      * >Creating an Amazon SNS Endpoint for Baidu</a>.
      * </p>
      * 
@@ -302,7 +304,7 @@ CreatePlatformEndpointResult createPlatformEndpoint(
      * <p>
      * Creates a topic to which notifications can be published. Users can create
      * at most 100,000 topics. For more information, see <a
-     * href="http://aws.amazon.com/sns/">http://aws.amazon.com/sns</a>. This
+     * href="http://aws.amazon.com/sns/">https://aws.amazon.com/sns</a>. This
      * action is idempotent, so if the requester already owns a topic with the
      * specified name, that topic's ARN is returned without creating a new
      * topic.
@@ -317,6 +319,11 @@ CreatePlatformEndpointResult createPlatformEndpoint(
      * @throws TopicLimitExceededException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -332,7 +339,7 @@ CreateTopicResult createTopic(CreateTopicRequest createTopicRequest)
      * <p>
      * Deletes the endpoint for a device and mobile app from Amazon SNS. This
      * action is idempotent. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * <p>
@@ -361,7 +368,7 @@ void deleteEndpoint(DeleteEndpointRequest deleteEndpointRequest) throws AmazonCl
      * <p>
      * Deletes a platform application object for one of the supported push
      * notification services, such as APNS and GCM. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -395,6 +402,9 @@ void deletePlatformApplication(DeletePlatformApplicationRequest deletePlatformAp
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -411,7 +421,7 @@ void deleteTopic(DeleteTopicRequest deleteTopicRequest) throws AmazonClientExcep
      * Retrieves the endpoint attributes for a device on one of the supported
      * push notification services, such as GCM and APNS. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -442,7 +452,7 @@ GetEndpointAttributesResult getEndpointAttributes(
      * Retrieves the attributes of the platform application object for the
      * supported push notification services, such as APNS and GCM. For more
      * information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -484,6 +494,7 @@ GetPlatformApplicationAttributesResult getPlatformApplicationAttributes(
      *         Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -539,6 +550,7 @@ GetSubscriptionAttributesResult getSubscriptionAttributes(
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -561,9 +573,12 @@ GetTopicAttributesResult getTopicAttributes(GetTopicAttributesRequest getTopicAt
      * <code>ListEndpointsByPlatformApplication</code> again using the NextToken
      * string received from the previous call. When there are no more records to
      * return, NextToken will be null. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listEndpointsByPlatformApplicationRequest <p>
      *            Input for ListEndpointsByPlatformApplication action.
@@ -611,6 +626,7 @@ ListEndpointsByPlatformApplicationResult listEndpointsByPlatformApplication(
      *         Simple Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -635,9 +651,12 @@ ListPhoneNumbersOptedOutResult listPhoneNumbersOptedOut(
      * using the NextToken string received from the previous call. When there
      * are no more records to return, NextToken will be null. For more
      * information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
+     * <p>
+     * This action is throttled at 15 transactions per second (TPS).
+     * </p>
      * 
      * @param listPlatformApplicationsRequest <p>
      *            Input for ListPlatformApplications action.
@@ -668,6 +687,9 @@ ListPlatformApplicationsResult listPlatformApplications(
      * <code>NextToken</code> parameter in a new <code>ListSubscriptions</code>
      * call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listSubscriptionsRequest <p>
      *            Input for ListSubscriptions action.
@@ -697,6 +719,9 @@ ListSubscriptionsResult listSubscriptions(ListSubscriptionsRequest listSubscript
      * <code>NextToken</code> parameter in a new
      * <code>ListSubscriptionsByTopic</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listSubscriptionsByTopicRequest <p>
      *            Input for ListSubscriptionsByTopic action.
@@ -720,6 +745,36 @@ ListSubscriptionsByTopicResult listSubscriptionsByTopic(
             ListSubscriptionsByTopicRequest listSubscriptionsByTopicRequest)
             throws AmazonClientException, AmazonServiceException;
 
+    /**
+     * <p>
+     * List all tags added to the specified Amazon SNS topic. For an overview,
+     * see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon Simple Notification Service Developer
+     * Guide</i>.
+     * </p>
+     * 
+     * @param listTagsForResourceRequest
+     * @return listTagsForResourceResult The response from the
+     *         ListTagsForResource service method, as returned by Amazon Simple
+     *         Notification Service.
+     * @throws ResourceNotFoundException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    ListTagsForResourceResult listTagsForResource(
+            ListTagsForResourceRequest listTagsForResourceRequest) throws AmazonClientException,
+            AmazonServiceException;
+
     /**
      * <p>
      * Returns a list of the requester's topics. Each call returns a limited
@@ -727,6 +782,9 @@ ListSubscriptionsByTopicResult listSubscriptionsByTopic(
      * <code>NextToken</code> is also returned. Use the <code>NextToken</code>
      * parameter in a new <code>ListTopics</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listTopicsRequest
      * @return listTopicsResult The response from the ListTopics service method,
@@ -762,6 +820,7 @@ ListTopicsResult listTopics(ListTopicsRequest listTopicsRequest) throws AmazonCl
      *         Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -776,23 +835,28 @@ OptInPhoneNumberResult optInPhoneNumber(OptInPhoneNumberRequest optInPhoneNumber
 
     /**
      * <p>
-     * Sends a message to all of a topic's subscribed endpoints. When a
-     * <code>messageId</code> is returned, the message has been saved and Amazon
-     * SNS will attempt to deliver it to the topic's subscribers shortly. The
-     * format of the outgoing message to each subscribed endpoint depends on the
-     * notification protocol.
+     * Sends a message to an Amazon SNS topic or sends a text message (SMS
+     * message) directly to a phone number.
+     * </p>
+     * <p>
+     * If you send a message to a topic, Amazon SNS delivers the message to each
+     * endpoint that is subscribed to the topic. The format of the message
+     * depends on the notification protocol for each subscribed endpoint.
+     * </p>
+     * <p>
+     * When a <code>messageId</code> is returned, the message has been saved and
+     * Amazon SNS will attempt to deliver it shortly.
      * </p>
      * <p>
      * To use the <code>Publish</code> action for sending a message to a mobile
      * endpoint, such as an app on a Kindle device or mobile phone, you must
      * specify the EndpointArn for the TargetArn parameter. The EndpointArn is
      * returned when making a call with the <code>CreatePlatformEndpoint</code>
-     * action. The second example below shows a request and response for
-     * publishing to a mobile endpoint.
+     * action.
      * </p>
      * <p>
      * For more information about formatting messages, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
      * >Send Custom Platform-Specific Payloads in Messages to Mobile
      * Devices</a>.
      * </p>
@@ -809,6 +873,13 @@ OptInPhoneNumberResult optInPhoneNumber(OptInPhoneNumberRequest optInPhoneNumber
      * @throws EndpointDisabledException
      * @throws PlatformApplicationDisabledException
      * @throws AuthorizationErrorException
+     * @throws KMSDisabledException
+     * @throws KMSInvalidStateException
+     * @throws KMSNotFoundException
+     * @throws KMSOptInRequiredException
+     * @throws KMSThrottlingException
+     * @throws KMSAccessDeniedException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -848,7 +919,7 @@ void removePermission(RemovePermissionRequest removePermissionRequest)
      * Sets the attributes for an endpoint for a device on one of the supported
      * push notification services, such as GCM and APNS. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -875,11 +946,11 @@ void setEndpointAttributes(SetEndpointAttributesRequest setEndpointAttributesReq
      * Sets the attributes of the platform application object for the supported
      * push notification services, such as APNS and GCM. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>. For information on configuring
      * attributes for message delivery status, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/sns-msg-status.html">Using
-     * Amazon SNS Application Attributes for Message Delivery Status</a>.
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-msg-status.html"
+     * >Using Amazon SNS Application Attributes for Message Delivery Status</a>.
      * </p>
      * 
      * @param setPlatformApplicationAttributesRequest <p>
@@ -911,7 +982,7 @@ void setPlatformApplicationAttributes(
      * the <code>Publish</code> action with the
      * <code>MessageAttributes.entry.N</code> parameter. For more information,
      * see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/sms_publish-to-phone.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/sms_publish-to-phone.html"
      * >Sending an SMS Message</a> in the <i>Amazon SNS Developer Guide</i>.
      * </p>
      * 
@@ -924,6 +995,7 @@ void setPlatformApplicationAttributes(
      * @throws InvalidParameterException
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -937,14 +1009,15 @@ SetSMSAttributesResult setSMSAttributes(SetSMSAttributesRequest setSMSAttributes
 
     /**
      * <p>
-     * Allows a subscription owner to set an attribute of the topic to a new
-     * value.
+     * Allows a subscription owner to set an attribute of the subscription to a
+     * new value.
      * </p>
      * 
      * @param setSubscriptionAttributesRequest <p>
      *            Input for SetSubscriptionAttributes action.
      *            </p>
      * @throws InvalidParameterException
+     * @throws FilterPolicyLimitExceededException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
@@ -971,6 +1044,7 @@ void setSubscriptionAttributes(SetSubscriptionAttributesRequest setSubscriptionA
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -989,6 +1063,9 @@ void setTopicAttributes(SetTopicAttributesRequest setTopicAttributesRequest)
      * the <code>ConfirmSubscription</code> action with the token from the
      * confirmation message. Confirmation tokens are valid for three days.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param subscribeRequest <p>
      *            Input for Subscribe action.
@@ -996,10 +1073,12 @@ void setTopicAttributes(SetTopicAttributesRequest setTopicAttributesRequest)
      * @return subscribeResult The response from the Subscribe service method,
      *         as returned by Amazon Simple Notification Service.
      * @throws SubscriptionLimitExceededException
+     * @throws FilterPolicyLimitExceededException
      * @throws InvalidParameterException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1011,6 +1090,74 @@ void setTopicAttributes(SetTopicAttributesRequest setTopicAttributesRequest)
     SubscribeResult subscribe(SubscribeRequest subscribeRequest) throws AmazonClientException,
             AmazonServiceException;
 
+    /**
+     * <p>
+     * Add tags to the specified Amazon SNS topic. For an overview, see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * <p>
+     * When you use topic tags, keep the following guidelines in mind:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * Adding more than 50 tags to a topic isn't recommended.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tags don't have any semantic meaning. Amazon SNS interprets tags as
+     * character strings.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tags are case-sensitive.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * A new tag with a key identical to that of an existing tag overwrites the
+     * existing tag.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tagging actions are limited to 10 TPS per AWS account. If your
+     * application requires a higher throughput, file a <a href=
+     * "https://console.aws.amazon.com/support/home#/case/create?issueType=technical"
+     * >technical support request</a>.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * For a full list of tag restrictions, see <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-limits.html#limits-topics"
+     * >Limits Related to Topics</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * 
+     * @param tagResourceRequest
+     * @return tagResourceResult The response from the TagResource service
+     *         method, as returned by Amazon Simple Notification Service.
+     * @throws ResourceNotFoundException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    TagResourceResult tagResource(TagResourceRequest tagResourceRequest)
+            throws AmazonClientException, AmazonServiceException;
+
     /**
      * <p>
      * Deletes a subscription. If the subscription requires authentication for
@@ -1022,6 +1169,9 @@ SubscribeResult subscribe(SubscribeRequest subscribeRequest) throws AmazonClient
      * resubscribe to the topic if the <code>Unsubscribe</code> request was
      * unintended.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param unsubscribeRequest <p>
      *            Input for Unsubscribe action.
@@ -1030,6 +1180,7 @@ SubscribeResult subscribe(SubscribeRequest subscribeRequest) throws AmazonClient
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1043,25 +1194,78 @@ void unsubscribe(UnsubscribeRequest unsubscribeRequest) throws AmazonClientExcep
 
     /**
      * <p>
-     * Allows a subscription owner to set an attribute of the topic to a new
-     * value.
+     * Remove tags from the specified Amazon SNS topic. For an overview, see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * 
+     * @param untagResourceRequest
+     * @return untagResourceResult The response from the UntagResource service
+     *         method, as returned by Amazon Simple Notification Service.
+     * @throws ResourceNotFoundException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    UntagResourceResult untagResource(UntagResourceRequest untagResourceRequest)
+            throws AmazonClientException, AmazonServiceException;
+
+    /**
+     * <p>
+     * Allows a subscription owner to set an attribute of the subscription to a
+     * new value.
      * </p>
      * 
      * @param subscriptionArn <p>
      *            The ARN of the subscription to modify.
      *            </p>
      * @param attributeName <p>
-     *            The name of the attribute you want to set. Only a subset of
-     *            the subscriptions attributes are mutable.
+     *            A map of attributes with their corresponding values.
      *            </p>
      *            <p>
-     *            Valid values: <code>DeliveryPolicy</code> |
-     *            <code>RawMessageDelivery</code>
+     *            The following lists the names, descriptions, and values of the
+     *            special request parameters that the
+     *            <code>SetTopicAttributes</code> action uses:
      *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>DeliveryPolicy</code> – The policy that defines how
+     *            Amazon SNS retries failed deliveries to HTTP/S endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>FilterPolicy</code> – The simple JSON object that lets
+     *            your subscriber receive only a subset of messages, rather than
+     *            receiving every message published to the topic.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>RawMessageDelivery</code> – When set to
+     *            <code>true</code>, enables raw message delivery to Amazon SQS
+     *            or HTTP/S endpoints. This eliminates the need for the
+     *            endpoints to process JSON formatting, which is otherwise
+     *            created for Amazon SNS metadata.
+     *            </p>
+     *            </li>
+     *            </ul>
      * @param attributeValue <p>
      *            The new value for the attribute in JSON format.
      *            </p>
      * @throws InvalidParameterException
+     * @throws FilterPolicyLimitExceededException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
@@ -1109,6 +1313,9 @@ GetSubscriptionAttributesResult getSubscriptionAttributes(String subscriptionArn
      * the <code>ConfirmSubscription</code> action with the token from the
      * confirmation message. Confirmation tokens are valid for three days.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param topicArn <p>
      *            The ARN of the topic you want to subscribe to.
@@ -1119,47 +1326,47 @@ GetSubscriptionAttributesResult getSubscriptionAttributes(String subscriptionArn
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>http</code> -- delivery of JSON-encoded message via HTTP
+     *            <code>http</code> – delivery of JSON-encoded message via HTTP
      *            POST
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>https</code> -- delivery of JSON-encoded message via
+     *            <code>https</code> – delivery of JSON-encoded message via
      *            HTTPS POST
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>email</code> -- delivery of message via SMTP
+     *            <code>email</code> – delivery of message via SMTP
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>email-json</code> -- delivery of JSON-encoded message
-     *            via SMTP
+     *            <code>email-json</code> – delivery of JSON-encoded message via
+     *            SMTP
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>sms</code> -- delivery of message via SMS
+     *            <code>sms</code> – delivery of message via SMS
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>sqs</code> -- delivery of JSON-encoded message to an
+     *            <code>sqs</code> – delivery of JSON-encoded message to an
      *            Amazon SQS queue
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>application</code> -- delivery of JSON-encoded message
-     *            to an EndpointArn for a mobile app and device.
+     *            <code>application</code> – delivery of JSON-encoded message to
+     *            an EndpointArn for a mobile app and device.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>lambda</code> -- delivery of JSON-encoded message to an
+     *            <code>lambda</code> – delivery of JSON-encoded message to an
      *            AWS Lambda function.
      *            </p>
      *            </li>
@@ -1172,7 +1379,7 @@ GetSubscriptionAttributesResult getSubscriptionAttributes(String subscriptionArn
      *            <li>
      *            <p>
      *            For the <code>http</code> protocol, the endpoint is an URL
-     *            beginning with "http://"
+     *            beginning with "https://"
      *            </p>
      *            </li>
      *            <li>
@@ -1221,10 +1428,12 @@ GetSubscriptionAttributesResult getSubscriptionAttributes(String subscriptionArn
      * @return subscribeResult The response from the Subscribe service method,
      *         as returned by Amazon Simple Notification Service.
      * @throws SubscriptionLimitExceededException
+     * @throws FilterPolicyLimitExceededException
      * @throws InvalidParameterException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1292,6 +1501,7 @@ void addPermission(String topicArn, String label, java.util.List<String> aWSAcco
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1337,6 +1547,9 @@ void removePermission(String topicArn, String label) throws AmazonClientExceptio
      * <code>NextToken</code> is also returned. Use the <code>NextToken</code>
      * parameter in a new <code>ListTopics</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @return listTopicsResult The response from the ListTopics service method,
      *         as returned by Amazon Simple Notification Service.
@@ -1360,6 +1573,9 @@ void removePermission(String topicArn, String label) throws AmazonClientExceptio
      * <code>NextToken</code> is also returned. Use the <code>NextToken</code>
      * parameter in a new <code>ListTopics</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param nextToken <p>
      *            Token returned by the previous <code>ListTopics</code>
@@ -1413,6 +1629,7 @@ ListTopicsResult listTopics(String nextToken) throws AmazonClientException,
      * @throws NotFoundException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws FilterPolicyLimitExceededException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1449,6 +1666,7 @@ ConfirmSubscriptionResult confirmSubscription(String topicArn, String token,
      * @throws NotFoundException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws FilterPolicyLimitExceededException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1469,13 +1687,53 @@ ConfirmSubscriptionResult confirmSubscription(String topicArn, String token)
      *            The ARN of the topic to modify.
      *            </p>
      * @param attributeName <p>
-     *            The name of the attribute you want to set. Only a subset of
-     *            the topic's attributes are mutable.
+     *            A map of attributes with their corresponding values.
      *            </p>
      *            <p>
-     *            Valid values: <code>Policy</code> | <code>DisplayName</code> |
-     *            <code>DeliveryPolicy</code>
+     *            The following lists the names, descriptions, and values of the
+     *            special request parameters that the
+     *            <code>SetTopicAttributes</code> action uses:
      *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>DeliveryPolicy</code> – The policy that defines how
+     *            Amazon SNS retries failed deliveries to HTTP/S endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>DisplayName</code> – The display name to use for a topic
+     *            with SMS subscriptions.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>Policy</code> – The policy that defines who can access
+     *            your topic. By default, only the topic owner can publish or
+     *            subscribe to the topic.
+     *            </p>
+     *            </li>
+     *            </ul>
+     *            <p>
+     *            The following attribute applies only to <a href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     *            >server-side-encryption</a>:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>KmsMasterKeyId</code> - The ID of an AWS-managed
+     *            customer master key (CMK) for Amazon SNS or a custom CMK. For
+     *            more information, see <a href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     *            >Key Terms</a>. For more examples, see <a href=
+     *            "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     *            >KeyId</a> in the <i>AWS Key Management Service API
+     *            Reference</i>.
+     *            </p>
+     *            </li>
+     *            </ul>
      * @param attributeValue <p>
      *            The new value for the attribute.
      *            </p>
@@ -1483,6 +1741,7 @@ ConfirmSubscriptionResult confirmSubscription(String topicArn, String token)
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1498,7 +1757,7 @@ void setTopicAttributes(String topicArn, String attributeName, String attributeV
      * <p>
      * Creates a topic to which notifications can be published. Users can create
      * at most 100,000 topics. For more information, see <a
-     * href="http://aws.amazon.com/sns/">http://aws.amazon.com/sns</a>. This
+     * href="http://aws.amazon.com/sns/">https://aws.amazon.com/sns</a>. This
      * action is idempotent, so if the requester already owns a topic with the
      * specified name, that topic's ARN is returned without creating a new
      * topic.
@@ -1518,6 +1777,11 @@ void setTopicAttributes(String topicArn, String attributeName, String attributeV
      * @throws TopicLimitExceededException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1543,6 +1807,9 @@ void setTopicAttributes(String topicArn, String attributeName, String attributeV
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1564,6 +1831,9 @@ void setTopicAttributes(String topicArn, String attributeName, String attributeV
      * resubscribe to the topic if the <code>Unsubscribe</code> request was
      * unintended.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param subscriptionArn <p>
      *            The ARN of the subscription to be deleted.
@@ -1572,6 +1842,7 @@ void setTopicAttributes(String topicArn, String attributeName, String attributeV
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1584,23 +1855,28 @@ void setTopicAttributes(String topicArn, String attributeName, String attributeV
 
     /**
      * <p>
-     * Sends a message to all of a topic's subscribed endpoints. When a
-     * <code>messageId</code> is returned, the message has been saved and Amazon
-     * SNS will attempt to deliver it to the topic's subscribers shortly. The
-     * format of the outgoing message to each subscribed endpoint depends on the
-     * notification protocol.
+     * Sends a message to an Amazon SNS topic or sends a text message (SMS
+     * message) directly to a phone number.
+     * </p>
+     * <p>
+     * If you send a message to a topic, Amazon SNS delivers the message to each
+     * endpoint that is subscribed to the topic. The format of the message
+     * depends on the notification protocol for each subscribed endpoint.
+     * </p>
+     * <p>
+     * When a <code>messageId</code> is returned, the message has been saved and
+     * Amazon SNS will attempt to deliver it shortly.
      * </p>
      * <p>
      * To use the <code>Publish</code> action for sending a message to a mobile
      * endpoint, such as an app on a Kindle device or mobile phone, you must
      * specify the EndpointArn for the TargetArn parameter. The EndpointArn is
      * returned when making a call with the <code>CreatePlatformEndpoint</code>
-     * action. The second example below shows a request and response for
-     * publishing to a mobile endpoint.
+     * action.
      * </p>
      * <p>
      * For more information about formatting messages, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
      * >Send Custom Platform-Specific Payloads in Messages to Mobile
      * Devices</a>.
      * </p>
@@ -1614,23 +1890,54 @@ void setTopicAttributes(String topicArn, String attributeName, String attributeV
      *            <code>PhoneNumber</code> or <code>TargetArn</code> parameters.
      *            </p>
      * @param message <p>
-     *            The message you want to send to the topic.
+     *            The message you want to send.
+     *            </p>
+     *            <important>
+     *            <p>
+     *            The <code>Message</code> parameter is always a string. If you
+     *            set <code>MessageStructure</code> to <code>json</code>, you
+     *            must string-encode the <code>Message</code> parameter.
+     *            </p>
+     *            </important>
+     *            <p>
+     *            If you are publishing to a topic and you want to send the same
+     *            message to all transport protocols, include the text of the
+     *            message as a String value. If you want to send different
+     *            messages for each transport protocol, set the value of the
+     *            <code>MessageStructure</code> parameter to <code>json</code>
+     *            and use a JSON object for the <code>Message</code> parameter.
+     *            </p>
+     *            <p/>
+     *            <p>
+     *            Constraints:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            With the exception of SMS, messages must be UTF-8 encoded
+     *            strings and at most 256 KB in size (262,144 bytes, not 262,144
+     *            characters).
      *            </p>
+     *            </li>
+     *            <li>
      *            <p>
-     *            If you want to send the same message to all transport
-     *            protocols, include the text of the message as a String value.
+     *            For SMS, each message can contain up to 140 characters. This
+     *            character limit depends on the encoding schema. For example,
+     *            an SMS message can contain 160 GSM characters, 140 ASCII
+     *            characters, or 70 UCS-2 characters.
      *            </p>
      *            <p>
-     *            If you want to send different messages for each transport
-     *            protocol, set the value of the <code>MessageStructure</code>
-     *            parameter to <code>json</code> and use a JSON object for the
-     *            <code>Message</code> parameter. See the Examples section for
-     *            the format of the JSON object.
+     *            If you publish a message that exceeds this size limit, Amazon
+     *            SNS sends the message as multiple messages, each fitting
+     *            within the size limit. Messages aren't truncated mid-word but
+     *            are cut off at whole-word boundaries.
      *            </p>
      *            <p>
-     *            Constraints: Messages must be UTF-8 encoded strings at most
-     *            256 KB in size (262144 bytes, not 262144 characters).
+     *            The total size limit for a single SMS <code>Publish</code>
+     *            action is 1,600 characters.
      *            </p>
+     *            </li>
+     *            </ul>
      *            <p>
      *            JSON-specific constraints:
      *            </p>
@@ -1699,6 +2006,13 @@ void setTopicAttributes(String topicArn, String attributeName, String attributeV
      * @throws EndpointDisabledException
      * @throws PlatformApplicationDisabledException
      * @throws AuthorizationErrorException
+     * @throws KMSDisabledException
+     * @throws KMSInvalidStateException
+     * @throws KMSNotFoundException
+     * @throws KMSOptInRequiredException
+     * @throws KMSThrottlingException
+     * @throws KMSAccessDeniedException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1712,23 +2026,28 @@ PublishResult publish(String topicArn, String message) throws AmazonClientExcept
 
     /**
      * <p>
-     * Sends a message to all of a topic's subscribed endpoints. When a
-     * <code>messageId</code> is returned, the message has been saved and Amazon
-     * SNS will attempt to deliver it to the topic's subscribers shortly. The
-     * format of the outgoing message to each subscribed endpoint depends on the
-     * notification protocol.
+     * Sends a message to an Amazon SNS topic or sends a text message (SMS
+     * message) directly to a phone number.
+     * </p>
+     * <p>
+     * If you send a message to a topic, Amazon SNS delivers the message to each
+     * endpoint that is subscribed to the topic. The format of the message
+     * depends on the notification protocol for each subscribed endpoint.
+     * </p>
+     * <p>
+     * When a <code>messageId</code> is returned, the message has been saved and
+     * Amazon SNS will attempt to deliver it shortly.
      * </p>
      * <p>
      * To use the <code>Publish</code> action for sending a message to a mobile
      * endpoint, such as an app on a Kindle device or mobile phone, you must
      * specify the EndpointArn for the TargetArn parameter. The EndpointArn is
      * returned when making a call with the <code>CreatePlatformEndpoint</code>
-     * action. The second example below shows a request and response for
-     * publishing to a mobile endpoint.
+     * action.
      * </p>
      * <p>
      * For more information about formatting messages, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
      * >Send Custom Platform-Specific Payloads in Messages to Mobile
      * Devices</a>.
      * </p>
@@ -1742,24 +2061,55 @@ PublishResult publish(String topicArn, String message) throws AmazonClientExcept
      *            <code>PhoneNumber</code> or <code>TargetArn</code> parameters.
      *            </p>
      * @param message <p>
-     *            The message you want to send to the topic.
+     *            The message you want to send.
+     *            </p>
+     *            <important>
+     *            <p>
+     *            The <code>Message</code> parameter is always a string. If you
+     *            set <code>MessageStructure</code> to <code>json</code>, you
+     *            must string-encode the <code>Message</code> parameter.
      *            </p>
+     *            </important>
+     *            <p>
+     *            If you are publishing to a topic and you want to send the same
+     *            message to all transport protocols, include the text of the
+     *            message as a String value. If you want to send different
+     *            messages for each transport protocol, set the value of the
+     *            <code>MessageStructure</code> parameter to <code>json</code>
+     *            and use a JSON object for the <code>Message</code> parameter.
+     *            </p>
+     *            <p/>
      *            <p>
-     *            If you want to send the same message to all transport
-     *            protocols, include the text of the message as a String value.
+     *            Constraints:
      *            </p>
+     *            <ul>
+     *            <li>
      *            <p>
-     *            If you want to send different messages for each transport
-     *            protocol, set the value of the <code>MessageStructure</code>
-     *            parameter to <code>json</code> and use a JSON object for the
-     *            <code>Message</code> parameter. See the Examples section for
-     *            the format of the JSON object.
+     *            With the exception of SMS, messages must be UTF-8 encoded
+     *            strings and at most 256 KB in size (262,144 bytes, not 262,144
+     *            characters).
      *            </p>
+     *            </li>
+     *            <li>
      *            <p>
-     *            Constraints: Messages must be UTF-8 encoded strings at most
-     *            256 KB in size (262144 bytes, not 262144 characters).
+     *            For SMS, each message can contain up to 140 characters. This
+     *            character limit depends on the encoding schema. For example,
+     *            an SMS message can contain 160 GSM characters, 140 ASCII
+     *            characters, or 70 UCS-2 characters.
      *            </p>
      *            <p>
+     *            If you publish a message that exceeds this size limit, Amazon
+     *            SNS sends the message as multiple messages, each fitting
+     *            within the size limit. Messages aren't truncated mid-word but
+     *            are cut off at whole-word boundaries.
+     *            </p>
+     *            <p>
+     *            The total size limit for a single SMS <code>Publish</code>
+     *            action is 1,600 characters.
+     *            </p>
+     *            </li>
+     *            </ul>
+     *            <p>
      *            JSON-specific constraints:
      *            </p>
      *            <ul>
@@ -1839,6 +2189,13 @@ PublishResult publish(String topicArn, String message) throws AmazonClientExcept
      * @throws EndpointDisabledException
      * @throws PlatformApplicationDisabledException
      * @throws AuthorizationErrorException
+     * @throws KMSDisabledException
+     * @throws KMSInvalidStateException
+     * @throws KMSNotFoundException
+     * @throws KMSOptInRequiredException
+     * @throws KMSThrottlingException
+     * @throws KMSAccessDeniedException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1858,6 +2215,9 @@ PublishResult publish(String topicArn, String message, String subject)
      * <code>NextToken</code> parameter in a new <code>ListSubscriptions</code>
      * call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @return listSubscriptionsResult The response from the ListSubscriptions
      *         service method, as returned by Amazon Simple Notification
@@ -1884,6 +2244,9 @@ ListSubscriptionsResult listSubscriptions() throws AmazonClientException,
      * <code>NextToken</code> parameter in a new <code>ListSubscriptions</code>
      * call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param nextToken <p>
      *            Token returned by the previous <code>ListSubscriptions</code>
@@ -1914,6 +2277,9 @@ ListSubscriptionsResult listSubscriptions(String nextToken) throws AmazonClientE
      * <code>NextToken</code> parameter in a new
      * <code>ListSubscriptionsByTopic</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param topicArn <p>
      *            The ARN of the topic for which you wish to find subscriptions.
@@ -1944,6 +2310,9 @@ ListSubscriptionsByTopicResult listSubscriptionsByTopic(String topicArn)
      * <code>NextToken</code> parameter in a new
      * <code>ListSubscriptionsByTopic</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param topicArn <p>
      *            The ARN of the topic for which you wish to find subscriptions.
@@ -1981,9 +2350,12 @@ ListSubscriptionsByTopicResult listSubscriptionsByTopic(String topicArn, String
      * using the NextToken string received from the previous call. When there
      * are no more records to return, NextToken will be null. For more
      * information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
+     * <p>
+     * This action is throttled at 15 transactions per second (TPS).
+     * </p>
      * 
      * @return listPlatformApplicationsResult The response from the
      *         ListPlatformApplications service method, as returned by Amazon
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNSAsync.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNSAsync.java
index b080883759..2c2de9a39e 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNSAsync.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNSAsync.java
@@ -31,10 +31,10 @@
  * Amazon SNS to easily push real-time notification messages to interested
  * subscribers over multiple delivery protocols. For more information about this
  * product see <a
- * href="http://aws.amazon.com/sns/">http://aws.amazon.com/sns</a>. For detailed
- * information about Amazon SNS features and their associated API calls, see the
- * <a href="http://docs.aws.amazon.com/sns/latest/dg/">Amazon SNS Developer
- * Guide</a>.
+ * href="http://aws.amazon.com/sns/">https://aws.amazon.com/sns</a>. For
+ * detailed information about Amazon SNS features and their associated API
+ * calls, see the <a href="https://docs.aws.amazon.com/sns/latest/dg/">Amazon
+ * SNS Developer Guide</a>.
  * </p>
  * <p>
  * We also provide SDKs that enable you to access Amazon SNS from your preferred
@@ -121,6 +121,7 @@
      *         Amazon Simple Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -158,6 +159,7 @@
      *         Amazon Simple Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -192,6 +194,7 @@
      * @throws NotFoundException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws FilterPolicyLimitExceededException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -228,6 +231,7 @@
      * @throws NotFoundException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws FilterPolicyLimitExceededException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -244,7 +248,7 @@
     /**
      * <p>
      * Creates a platform application object for one of the supported push
-     * notification services, such as APNS and GCM, to which devices and mobile
+     * notification services, such as APNS and FCM, to which devices and mobile
      * apps may register. You must specify PlatformPrincipal and
      * PlatformCredential attributes when using the
      * <code>CreatePlatformApplication</code> action. The PlatformPrincipal is
@@ -264,21 +268,21 @@
      * "secret key". The PlatformApplicationArn that is returned when using
      * <code>CreatePlatformApplication</code> is then used as an attribute for
      * the <code>CreatePlatformEndpoint</code> action. For more information, see
-     * <a href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">
+     * <a href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">
      * Using Amazon SNS Mobile Push Notifications</a>. For more information
      * about obtaining the PlatformPrincipal and PlatformCredential for each of
      * the supported push notification services, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-apns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-apns.html"
      * >Getting Started with Apple Push Notification Service</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-adm.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-adm.html"
      * >Getting Started with Amazon Device Messaging</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-baidu.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-baidu.html"
      * >Getting Started with Baidu Cloud Push</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-gcm.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-gcm.html"
      * >Getting Started with Google Cloud Messaging for Android</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-mpns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-mpns.html"
      * >Getting Started with MPNS</a>, or <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-wns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-wns.html"
      * >Getting Started with WNS</a>.
      * </p>
      * 
@@ -306,7 +310,7 @@
     /**
      * <p>
      * Creates a platform application object for one of the supported push
-     * notification services, such as APNS and GCM, to which devices and mobile
+     * notification services, such as APNS and FCM, to which devices and mobile
      * apps may register. You must specify PlatformPrincipal and
      * PlatformCredential attributes when using the
      * <code>CreatePlatformApplication</code> action. The PlatformPrincipal is
@@ -326,21 +330,21 @@
      * "secret key". The PlatformApplicationArn that is returned when using
      * <code>CreatePlatformApplication</code> is then used as an attribute for
      * the <code>CreatePlatformEndpoint</code> action. For more information, see
-     * <a href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">
+     * <a href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">
      * Using Amazon SNS Mobile Push Notifications</a>. For more information
      * about obtaining the PlatformPrincipal and PlatformCredential for each of
      * the supported push notification services, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-apns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-apns.html"
      * >Getting Started with Apple Push Notification Service</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-adm.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-adm.html"
      * >Getting Started with Amazon Device Messaging</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-baidu.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-baidu.html"
      * >Getting Started with Baidu Cloud Push</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-gcm.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-gcm.html"
      * >Getting Started with Google Cloud Messaging for Android</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-mpns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-mpns.html"
      * >Getting Started with MPNS</a>, or <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-wns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-wns.html"
      * >Getting Started with WNS</a>.
      * </p>
      * 
@@ -384,14 +388,14 @@
      * requester already owns an endpoint with the same device token and
      * attributes, that endpoint's ARN is returned without creating a new
      * endpoint. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * <p>
      * When using <code>CreatePlatformEndpoint</code> with Baidu, two attributes
      * must be provided: ChannelId and UserId. The token field must also contain
      * the ChannelId. For more information, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePushBaiduEndpoint.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePushBaiduEndpoint.html"
      * >Creating an Amazon SNS Endpoint for Baidu</a>.
      * </p>
      * 
@@ -431,14 +435,14 @@
      * requester already owns an endpoint with the same device token and
      * attributes, that endpoint's ARN is returned without creating a new
      * endpoint. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * <p>
      * When using <code>CreatePlatformEndpoint</code> with Baidu, two attributes
      * must be provided: ChannelId and UserId. The token field must also contain
      * the ChannelId. For more information, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePushBaiduEndpoint.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePushBaiduEndpoint.html"
      * >Creating an Amazon SNS Endpoint for Baidu</a>.
      * </p>
      * 
@@ -473,7 +477,7 @@
      * <p>
      * Creates a topic to which notifications can be published. Users can create
      * at most 100,000 topics. For more information, see <a
-     * href="http://aws.amazon.com/sns/">http://aws.amazon.com/sns</a>. This
+     * href="http://aws.amazon.com/sns/">https://aws.amazon.com/sns</a>. This
      * action is idempotent, so if the requester already owns a topic with the
      * specified name, that topic's ARN is returned without creating a new
      * topic.
@@ -489,6 +493,11 @@
      * @throws TopicLimitExceededException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -504,7 +513,7 @@
      * <p>
      * Creates a topic to which notifications can be published. Users can create
      * at most 100,000 topics. For more information, see <a
-     * href="http://aws.amazon.com/sns/">http://aws.amazon.com/sns</a>. This
+     * href="http://aws.amazon.com/sns/">https://aws.amazon.com/sns</a>. This
      * action is idempotent, so if the requester already owns a topic with the
      * specified name, that topic's ARN is returned without creating a new
      * topic.
@@ -524,6 +533,11 @@
      * @throws TopicLimitExceededException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -540,7 +554,7 @@
      * <p>
      * Deletes the endpoint for a device and mobile app from Amazon SNS. This
      * action is idempotent. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * <p>
@@ -572,7 +586,7 @@
      * <p>
      * Deletes the endpoint for a device and mobile app from Amazon SNS. This
      * action is idempotent. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * <p>
@@ -609,7 +623,7 @@
      * <p>
      * Deletes a platform application object for one of the supported push
      * notification services, such as APNS and GCM. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -638,7 +652,7 @@
      * <p>
      * Deletes a platform application object for one of the supported push
      * notification services, such as APNS and GCM. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -684,6 +698,9 @@
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -715,6 +732,9 @@
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -732,7 +752,7 @@
      * Retrieves the endpoint attributes for a device on one of the supported
      * push notification services, such as GCM and APNS. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -763,7 +783,7 @@
      * Retrieves the endpoint attributes for a device on one of the supported
      * push notification services, such as GCM and APNS. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -799,7 +819,7 @@
      * Retrieves the attributes of the platform application object for the
      * supported push notification services, such as APNS and GCM. For more
      * information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -830,7 +850,7 @@
      * Retrieves the attributes of the platform application object for the
      * supported push notification services, such as APNS and GCM. For more
      * information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -877,6 +897,7 @@
      *         Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -910,6 +931,7 @@
      *         Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -999,6 +1021,7 @@
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1031,6 +1054,7 @@
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1055,9 +1079,12 @@
      * <code>ListEndpointsByPlatformApplication</code> again using the NextToken
      * string received from the previous call. When there are no more records to
      * return, NextToken will be null. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listEndpointsByPlatformApplicationRequest <p>
      *            Input for ListEndpointsByPlatformApplication action.
@@ -1092,9 +1119,12 @@
      * <code>ListEndpointsByPlatformApplication</code> again using the NextToken
      * string received from the previous call. When there are no more records to
      * return, NextToken will be null. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listEndpointsByPlatformApplicationRequest <p>
      *            Input for ListEndpointsByPlatformApplication action.
@@ -1147,6 +1177,7 @@
      *         Simple Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -1188,6 +1219,7 @@
      *         Simple Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -1213,9 +1245,12 @@
      * using the NextToken string received from the previous call. When there
      * are no more records to return, NextToken will be null. For more
      * information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
+     * <p>
+     * This action is throttled at 15 transactions per second (TPS).
+     * </p>
      * 
      * @param listPlatformApplicationsRequest <p>
      *            Input for ListPlatformApplications action.
@@ -1249,9 +1284,12 @@
      * using the NextToken string received from the previous call. When there
      * are no more records to return, NextToken will be null. For more
      * information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
+     * <p>
+     * This action is throttled at 15 transactions per second (TPS).
+     * </p>
      * 
      * @param listPlatformApplicationsRequest <p>
      *            Input for ListPlatformApplications action.
@@ -1287,6 +1325,9 @@
      * <code>NextToken</code> parameter in a new <code>ListSubscriptions</code>
      * call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listSubscriptionsRequest <p>
      *            Input for ListSubscriptions action.
@@ -1317,6 +1358,9 @@
      * <code>NextToken</code> parameter in a new <code>ListSubscriptions</code>
      * call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listSubscriptionsRequest <p>
      *            Input for ListSubscriptions action.
@@ -1352,6 +1396,9 @@
      * <code>NextToken</code> parameter in a new
      * <code>ListSubscriptionsByTopic</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listSubscriptionsByTopicRequest <p>
      *            Input for ListSubscriptionsByTopic action.
@@ -1383,6 +1430,9 @@
      * <code>NextToken</code> parameter in a new
      * <code>ListSubscriptionsByTopic</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listSubscriptionsByTopicRequest <p>
      *            Input for ListSubscriptionsByTopic action.
@@ -1411,6 +1461,71 @@
             AsyncHandler<ListSubscriptionsByTopicRequest, ListSubscriptionsByTopicResult> asyncHandler)
             throws AmazonServiceException, AmazonClientException;
 
+    /**
+     * <p>
+     * List all tags added to the specified Amazon SNS topic. For an overview,
+     * see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon Simple Notification Service Developer
+     * Guide</i>.
+     * </p>
+     * 
+     * @param listTagsForResourceRequest
+     * @return A Java Future object containing the response from the
+     *         ListTagsForResource service method, as returned by Amazon Simple
+     *         Notification Service.
+     * @throws ResourceNotFoundException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    Future<ListTagsForResourceResult> listTagsForResourceAsync(
+            ListTagsForResourceRequest listTagsForResourceRequest) throws AmazonServiceException,
+            AmazonClientException;
+
+    /**
+     * <p>
+     * List all tags added to the specified Amazon SNS topic. For an overview,
+     * see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon Simple Notification Service Developer
+     * Guide</i>.
+     * </p>
+     * 
+     * @param listTagsForResourceRequest
+     * @param asyncHandler Asynchronous callback handler for events in the
+     *            life-cycle of the request. Users could provide the
+     *            implementation of the four callback methods in this interface
+     *            to process the operation result or handle the exception.
+     * @return A Java Future object containing the response from the
+     *         ListTagsForResource service method, as returned by Amazon Simple
+     *         Notification Service.
+     * @throws ResourceNotFoundException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    Future<ListTagsForResourceResult> listTagsForResourceAsync(
+            ListTagsForResourceRequest listTagsForResourceRequest,
+            AsyncHandler<ListTagsForResourceRequest, ListTagsForResourceResult> asyncHandler)
+            throws AmazonServiceException, AmazonClientException;
+
     /**
      * <p>
      * Returns a list of the requester's topics. Each call returns a limited
@@ -1418,6 +1533,9 @@
      * <code>NextToken</code> is also returned. Use the <code>NextToken</code>
      * parameter in a new <code>ListTopics</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listTopicsRequest
      * @return A Java Future object containing the response from the ListTopics
@@ -1444,6 +1562,9 @@
      * <code>NextToken</code> is also returned. Use the <code>NextToken</code>
      * parameter in a new <code>ListTopics</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listTopicsRequest
      * @param asyncHandler Asynchronous callback handler for events in the
@@ -1485,6 +1606,7 @@
      *         Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -1519,6 +1641,7 @@
      *         Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -1535,23 +1658,28 @@
 
     /**
      * <p>
-     * Sends a message to all of a topic's subscribed endpoints. When a
-     * <code>messageId</code> is returned, the message has been saved and Amazon
-     * SNS will attempt to deliver it to the topic's subscribers shortly. The
-     * format of the outgoing message to each subscribed endpoint depends on the
-     * notification protocol.
+     * Sends a message to an Amazon SNS topic or sends a text message (SMS
+     * message) directly to a phone number.
+     * </p>
+     * <p>
+     * If you send a message to a topic, Amazon SNS delivers the message to each
+     * endpoint that is subscribed to the topic. The format of the message
+     * depends on the notification protocol for each subscribed endpoint.
+     * </p>
+     * <p>
+     * When a <code>messageId</code> is returned, the message has been saved and
+     * Amazon SNS will attempt to deliver it shortly.
      * </p>
      * <p>
      * To use the <code>Publish</code> action for sending a message to a mobile
      * endpoint, such as an app on a Kindle device or mobile phone, you must
      * specify the EndpointArn for the TargetArn parameter. The EndpointArn is
      * returned when making a call with the <code>CreatePlatformEndpoint</code>
-     * action. The second example below shows a request and response for
-     * publishing to a mobile endpoint.
+     * action.
      * </p>
      * <p>
      * For more information about formatting messages, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
      * >Send Custom Platform-Specific Payloads in Messages to Mobile
      * Devices</a>.
      * </p>
@@ -1569,6 +1697,13 @@
      * @throws EndpointDisabledException
      * @throws PlatformApplicationDisabledException
      * @throws AuthorizationErrorException
+     * @throws KMSDisabledException
+     * @throws KMSInvalidStateException
+     * @throws KMSNotFoundException
+     * @throws KMSOptInRequiredException
+     * @throws KMSThrottlingException
+     * @throws KMSAccessDeniedException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1582,23 +1717,28 @@
 
     /**
      * <p>
-     * Sends a message to all of a topic's subscribed endpoints. When a
-     * <code>messageId</code> is returned, the message has been saved and Amazon
-     * SNS will attempt to deliver it to the topic's subscribers shortly. The
-     * format of the outgoing message to each subscribed endpoint depends on the
-     * notification protocol.
+     * Sends a message to an Amazon SNS topic or sends a text message (SMS
+     * message) directly to a phone number.
+     * </p>
+     * <p>
+     * If you send a message to a topic, Amazon SNS delivers the message to each
+     * endpoint that is subscribed to the topic. The format of the message
+     * depends on the notification protocol for each subscribed endpoint.
+     * </p>
+     * <p>
+     * When a <code>messageId</code> is returned, the message has been saved and
+     * Amazon SNS will attempt to deliver it shortly.
      * </p>
      * <p>
      * To use the <code>Publish</code> action for sending a message to a mobile
      * endpoint, such as an app on a Kindle device or mobile phone, you must
      * specify the EndpointArn for the TargetArn parameter. The EndpointArn is
      * returned when making a call with the <code>CreatePlatformEndpoint</code>
-     * action. The second example below shows a request and response for
-     * publishing to a mobile endpoint.
+     * action.
      * </p>
      * <p>
      * For more information about formatting messages, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
      * >Send Custom Platform-Specific Payloads in Messages to Mobile
      * Devices</a>.
      * </p>
@@ -1620,6 +1760,13 @@
      * @throws EndpointDisabledException
      * @throws PlatformApplicationDisabledException
      * @throws AuthorizationErrorException
+     * @throws KMSDisabledException
+     * @throws KMSInvalidStateException
+     * @throws KMSNotFoundException
+     * @throws KMSOptInRequiredException
+     * @throws KMSThrottlingException
+     * @throws KMSAccessDeniedException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1694,7 +1841,7 @@
      * Sets the attributes for an endpoint for a device on one of the supported
      * push notification services, such as GCM and APNS. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -1725,7 +1872,7 @@
      * Sets the attributes for an endpoint for a device on one of the supported
      * push notification services, such as GCM and APNS. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -1761,11 +1908,11 @@
      * Sets the attributes of the platform application object for the supported
      * push notification services, such as APNS and GCM. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>. For information on configuring
      * attributes for message delivery status, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/sns-msg-status.html">Using
-     * Amazon SNS Application Attributes for Message Delivery Status</a>.
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-msg-status.html"
+     * >Using Amazon SNS Application Attributes for Message Delivery Status</a>.
      * </p>
      * 
      * @param setPlatformApplicationAttributesRequest <p>
@@ -1795,11 +1942,11 @@
      * Sets the attributes of the platform application object for the supported
      * push notification services, such as APNS and GCM. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>. For information on configuring
      * attributes for message delivery status, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/sns-msg-status.html">Using
-     * Amazon SNS Application Attributes for Message Delivery Status</a>.
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-msg-status.html"
+     * >Using Amazon SNS Application Attributes for Message Delivery Status</a>.
      * </p>
      * 
      * @param setPlatformApplicationAttributesRequest <p>
@@ -1839,7 +1986,7 @@
      * the <code>Publish</code> action with the
      * <code>MessageAttributes.entry.N</code> parameter. For more information,
      * see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/sms_publish-to-phone.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/sms_publish-to-phone.html"
      * >Sending an SMS Message</a> in the <i>Amazon SNS Developer Guide</i>.
      * </p>
      * 
@@ -1852,6 +1999,7 @@
      * @throws InvalidParameterException
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1874,7 +2022,7 @@
      * the <code>Publish</code> action with the
      * <code>MessageAttributes.entry.N</code> parameter. For more information,
      * see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/sms_publish-to-phone.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/sms_publish-to-phone.html"
      * >Sending an SMS Message</a> in the <i>Amazon SNS Developer Guide</i>.
      * </p>
      * 
@@ -1891,6 +2039,7 @@
      * @throws InvalidParameterException
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1906,8 +2055,8 @@
 
     /**
      * <p>
-     * Allows a subscription owner to set an attribute of the topic to a new
-     * value.
+     * Allows a subscription owner to set an attribute of the subscription to a
+     * new value.
      * </p>
      * 
      * @param setSubscriptionAttributesRequest <p>
@@ -1917,6 +2066,7 @@
      *         SetSubscriptionAttributes service method, as returned by Amazon
      *         Simple Notification Service.
      * @throws InvalidParameterException
+     * @throws FilterPolicyLimitExceededException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
@@ -1934,8 +2084,8 @@
 
     /**
      * <p>
-     * Allows a subscription owner to set an attribute of the topic to a new
-     * value.
+     * Allows a subscription owner to set an attribute of the subscription to a
+     * new value.
      * </p>
      * 
      * @param setSubscriptionAttributesRequest <p>
@@ -1949,6 +2099,7 @@
      *         SetSubscriptionAttributes service method, as returned by Amazon
      *         Simple Notification Service.
      * @throws InvalidParameterException
+     * @throws FilterPolicyLimitExceededException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
@@ -1980,6 +2131,7 @@
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2010,6 +2162,7 @@
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2029,6 +2182,9 @@
      * the <code>ConfirmSubscription</code> action with the token from the
      * confirmation message. Confirmation tokens are valid for three days.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param subscribeRequest <p>
      *            Input for Subscribe action.
@@ -2037,10 +2193,12 @@
      *         service method, as returned by Amazon Simple Notification
      *         Service.
      * @throws SubscriptionLimitExceededException
+     * @throws FilterPolicyLimitExceededException
      * @throws InvalidParameterException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2059,6 +2217,9 @@
      * the <code>ConfirmSubscription</code> action with the token from the
      * confirmation message. Confirmation tokens are valid for three days.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param subscribeRequest <p>
      *            Input for Subscribe action.
@@ -2071,10 +2232,12 @@
      *         service method, as returned by Amazon Simple Notification
      *         Service.
      * @throws SubscriptionLimitExceededException
+     * @throws FilterPolicyLimitExceededException
      * @throws InvalidParameterException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2087,6 +2250,149 @@
             AsyncHandler<SubscribeRequest, SubscribeResult> asyncHandler)
             throws AmazonServiceException, AmazonClientException;
 
+    /**
+     * <p>
+     * Add tags to the specified Amazon SNS topic. For an overview, see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * <p>
+     * When you use topic tags, keep the following guidelines in mind:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * Adding more than 50 tags to a topic isn't recommended.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tags don't have any semantic meaning. Amazon SNS interprets tags as
+     * character strings.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tags are case-sensitive.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * A new tag with a key identical to that of an existing tag overwrites the
+     * existing tag.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tagging actions are limited to 10 TPS per AWS account. If your
+     * application requires a higher throughput, file a <a href=
+     * "https://console.aws.amazon.com/support/home#/case/create?issueType=technical"
+     * >technical support request</a>.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * For a full list of tag restrictions, see <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-limits.html#limits-topics"
+     * >Limits Related to Topics</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * 
+     * @param tagResourceRequest
+     * @return A Java Future object containing the response from the TagResource
+     *         service method, as returned by Amazon Simple Notification
+     *         Service.
+     * @throws ResourceNotFoundException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    Future<TagResourceResult> tagResourceAsync(TagResourceRequest tagResourceRequest)
+            throws AmazonServiceException, AmazonClientException;
+
+    /**
+     * <p>
+     * Add tags to the specified Amazon SNS topic. For an overview, see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * <p>
+     * When you use topic tags, keep the following guidelines in mind:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * Adding more than 50 tags to a topic isn't recommended.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tags don't have any semantic meaning. Amazon SNS interprets tags as
+     * character strings.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tags are case-sensitive.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * A new tag with a key identical to that of an existing tag overwrites the
+     * existing tag.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tagging actions are limited to 10 TPS per AWS account. If your
+     * application requires a higher throughput, file a <a href=
+     * "https://console.aws.amazon.com/support/home#/case/create?issueType=technical"
+     * >technical support request</a>.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * For a full list of tag restrictions, see <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-limits.html#limits-topics"
+     * >Limits Related to Topics</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * 
+     * @param tagResourceRequest
+     * @param asyncHandler Asynchronous callback handler for events in the
+     *            life-cycle of the request. Users could provide the
+     *            implementation of the four callback methods in this interface
+     *            to process the operation result or handle the exception.
+     * @return A Java Future object containing the response from the TagResource
+     *         service method, as returned by Amazon Simple Notification
+     *         Service.
+     * @throws ResourceNotFoundException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    Future<TagResourceResult> tagResourceAsync(TagResourceRequest tagResourceRequest,
+            AsyncHandler<TagResourceRequest, TagResourceResult> asyncHandler)
+            throws AmazonServiceException, AmazonClientException;
+
     /**
      * <p>
      * Deletes a subscription. If the subscription requires authentication for
@@ -2098,6 +2404,9 @@
      * resubscribe to the topic if the <code>Unsubscribe</code> request was
      * unintended.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param unsubscribeRequest <p>
      *            Input for Unsubscribe action.
@@ -2109,6 +2418,7 @@
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2131,6 +2441,9 @@
      * resubscribe to the topic if the <code>Unsubscribe</code> request was
      * unintended.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param unsubscribeRequest <p>
      *            Input for Unsubscribe action.
@@ -2146,6 +2459,7 @@
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2158,4 +2472,67 @@
             AsyncHandler<UnsubscribeRequest, Void> asyncHandler) throws AmazonServiceException,
             AmazonClientException;
 
+    /**
+     * <p>
+     * Remove tags from the specified Amazon SNS topic. For an overview, see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * 
+     * @param untagResourceRequest
+     * @return A Java Future object containing the response from the
+     *         UntagResource service method, as returned by Amazon Simple
+     *         Notification Service.
+     * @throws ResourceNotFoundException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    Future<UntagResourceResult> untagResourceAsync(UntagResourceRequest untagResourceRequest)
+            throws AmazonServiceException, AmazonClientException;
+
+    /**
+     * <p>
+     * Remove tags from the specified Amazon SNS topic. For an overview, see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * 
+     * @param untagResourceRequest
+     * @param asyncHandler Asynchronous callback handler for events in the
+     *            life-cycle of the request. Users could provide the
+     *            implementation of the four callback methods in this interface
+     *            to process the operation result or handle the exception.
+     * @return A Java Future object containing the response from the
+     *         UntagResource service method, as returned by Amazon Simple
+     *         Notification Service.
+     * @throws ResourceNotFoundException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    Future<UntagResourceResult> untagResourceAsync(UntagResourceRequest untagResourceRequest,
+            AsyncHandler<UntagResourceRequest, UntagResourceResult> asyncHandler)
+            throws AmazonServiceException, AmazonClientException;
+
 }
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNSAsyncClient.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNSAsyncClient.java
index 006a78e0b0..9e856a6e94 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNSAsyncClient.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNSAsyncClient.java
@@ -39,10 +39,10 @@
  * Amazon SNS to easily push real-time notification messages to interested
  * subscribers over multiple delivery protocols. For more information about this
  * product see <a
- * href="http://aws.amazon.com/sns/">http://aws.amazon.com/sns</a>. For detailed
- * information about Amazon SNS features and their associated API calls, see the
- * <a href="http://docs.aws.amazon.com/sns/latest/dg/">Amazon SNS Developer
- * Guide</a>.
+ * href="http://aws.amazon.com/sns/">https://aws.amazon.com/sns</a>. For
+ * detailed information about Amazon SNS features and their associated API
+ * calls, see the <a href="https://docs.aws.amazon.com/sns/latest/dg/">Amazon
+ * SNS Developer Guide</a>.
  * </p>
  * <p>
  * We also provide SDKs that enable you to access Amazon SNS from your preferred
@@ -364,6 +364,7 @@ public Void call() throws Exception {
      *         Amazon Simple Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -403,6 +404,7 @@ public CheckIfPhoneNumberIsOptedOutResult call() throws Exception {
      *         Amazon Simple Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -451,6 +453,7 @@ public CheckIfPhoneNumberIsOptedOutResult call() throws Exception {
      * @throws NotFoundException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws FilterPolicyLimitExceededException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -489,6 +492,7 @@ public ConfirmSubscriptionResult call() throws Exception {
      * @throws NotFoundException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws FilterPolicyLimitExceededException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -519,7 +523,7 @@ public ConfirmSubscriptionResult call() throws Exception {
     /**
      * <p>
      * Creates a platform application object for one of the supported push
-     * notification services, such as APNS and GCM, to which devices and mobile
+     * notification services, such as APNS and FCM, to which devices and mobile
      * apps may register. You must specify PlatformPrincipal and
      * PlatformCredential attributes when using the
      * <code>CreatePlatformApplication</code> action. The PlatformPrincipal is
@@ -539,21 +543,21 @@ public ConfirmSubscriptionResult call() throws Exception {
      * "secret key". The PlatformApplicationArn that is returned when using
      * <code>CreatePlatformApplication</code> is then used as an attribute for
      * the <code>CreatePlatformEndpoint</code> action. For more information, see
-     * <a href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">
+     * <a href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">
      * Using Amazon SNS Mobile Push Notifications</a>. For more information
      * about obtaining the PlatformPrincipal and PlatformCredential for each of
      * the supported push notification services, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-apns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-apns.html"
      * >Getting Started with Apple Push Notification Service</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-adm.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-adm.html"
      * >Getting Started with Amazon Device Messaging</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-baidu.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-baidu.html"
      * >Getting Started with Baidu Cloud Push</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-gcm.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-gcm.html"
      * >Getting Started with Google Cloud Messaging for Android</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-mpns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-mpns.html"
      * >Getting Started with MPNS</a>, or <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-wns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-wns.html"
      * >Getting Started with WNS</a>.
      * </p>
      * 
@@ -587,7 +591,7 @@ public CreatePlatformApplicationResult call() throws Exception {
     /**
      * <p>
      * Creates a platform application object for one of the supported push
-     * notification services, such as APNS and GCM, to which devices and mobile
+     * notification services, such as APNS and FCM, to which devices and mobile
      * apps may register. You must specify PlatformPrincipal and
      * PlatformCredential attributes when using the
      * <code>CreatePlatformApplication</code> action. The PlatformPrincipal is
@@ -607,21 +611,21 @@ public CreatePlatformApplicationResult call() throws Exception {
      * "secret key". The PlatformApplicationArn that is returned when using
      * <code>CreatePlatformApplication</code> is then used as an attribute for
      * the <code>CreatePlatformEndpoint</code> action. For more information, see
-     * <a href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">
+     * <a href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">
      * Using Amazon SNS Mobile Push Notifications</a>. For more information
      * about obtaining the PlatformPrincipal and PlatformCredential for each of
      * the supported push notification services, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-apns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-apns.html"
      * >Getting Started with Apple Push Notification Service</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-adm.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-adm.html"
      * >Getting Started with Amazon Device Messaging</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-baidu.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-baidu.html"
      * >Getting Started with Baidu Cloud Push</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-gcm.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-gcm.html"
      * >Getting Started with Google Cloud Messaging for Android</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-mpns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-mpns.html"
      * >Getting Started with MPNS</a>, or <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-wns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-wns.html"
      * >Getting Started with WNS</a>.
      * </p>
      * 
@@ -675,14 +679,14 @@ public CreatePlatformApplicationResult call() throws Exception {
      * requester already owns an endpoint with the same device token and
      * attributes, that endpoint's ARN is returned without creating a new
      * endpoint. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * <p>
      * When using <code>CreatePlatformEndpoint</code> with Baidu, two attributes
      * must be provided: ChannelId and UserId. The token field must also contain
      * the ChannelId. For more information, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePushBaiduEndpoint.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePushBaiduEndpoint.html"
      * >Creating an Amazon SNS Endpoint for Baidu</a>.
      * </p>
      * 
@@ -728,14 +732,14 @@ public CreatePlatformEndpointResult call() throws Exception {
      * requester already owns an endpoint with the same device token and
      * attributes, that endpoint's ARN is returned without creating a new
      * endpoint. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * <p>
      * When using <code>CreatePlatformEndpoint</code> with Baidu, two attributes
      * must be provided: ChannelId and UserId. The token field must also contain
      * the ChannelId. For more information, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePushBaiduEndpoint.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePushBaiduEndpoint.html"
      * >Creating an Amazon SNS Endpoint for Baidu</a>.
      * </p>
      * 
@@ -780,7 +784,7 @@ public CreatePlatformEndpointResult call() throws Exception {
      * <p>
      * Creates a topic to which notifications can be published. Users can create
      * at most 100,000 topics. For more information, see <a
-     * href="http://aws.amazon.com/sns/">http://aws.amazon.com/sns</a>. This
+     * href="http://aws.amazon.com/sns/">https://aws.amazon.com/sns</a>. This
      * action is idempotent, so if the requester already owns a topic with the
      * specified name, that topic's ARN is returned without creating a new
      * topic.
@@ -796,6 +800,11 @@ public CreatePlatformEndpointResult call() throws Exception {
      * @throws TopicLimitExceededException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -817,7 +826,7 @@ public CreateTopicResult call() throws Exception {
      * <p>
      * Creates a topic to which notifications can be published. Users can create
      * at most 100,000 topics. For more information, see <a
-     * href="http://aws.amazon.com/sns/">http://aws.amazon.com/sns</a>. This
+     * href="http://aws.amazon.com/sns/">https://aws.amazon.com/sns</a>. This
      * action is idempotent, so if the requester already owns a topic with the
      * specified name, that topic's ARN is returned without creating a new
      * topic.
@@ -833,6 +842,11 @@ public CreateTopicResult call() throws Exception {
      * @throws TopicLimitExceededException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -863,7 +877,7 @@ public CreateTopicResult call() throws Exception {
      * <p>
      * Deletes the endpoint for a device and mobile app from Amazon SNS. This
      * action is idempotent. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * <p>
@@ -902,7 +916,7 @@ public Void call() throws Exception {
      * <p>
      * Deletes the endpoint for a device and mobile app from Amazon SNS. This
      * action is idempotent. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * <p>
@@ -949,7 +963,7 @@ public Void call() throws Exception {
      * <p>
      * Deletes a platform application object for one of the supported push
      * notification services, such as APNS and GCM. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -985,7 +999,7 @@ public Void call() throws Exception {
      * <p>
      * Deletes a platform application object for one of the supported push
      * notification services, such as APNS and GCM. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -1041,6 +1055,9 @@ public Void call() throws Exception {
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1075,6 +1092,9 @@ public Void call() throws Exception {
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1106,7 +1126,7 @@ public Void call() throws Exception {
      * Retrieves the endpoint attributes for a device on one of the supported
      * push notification services, such as GCM and APNS. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -1143,7 +1163,7 @@ public GetEndpointAttributesResult call() throws Exception {
      * Retrieves the endpoint attributes for a device on one of the supported
      * push notification services, such as GCM and APNS. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -1189,7 +1209,7 @@ public GetEndpointAttributesResult call() throws Exception {
      * Retrieves the attributes of the platform application object for the
      * supported push notification services, such as APNS and GCM. For more
      * information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -1226,7 +1246,7 @@ public GetPlatformApplicationAttributesResult call() throws Exception {
      * Retrieves the attributes of the platform application object for the
      * supported push notification services, such as APNS and GCM. For more
      * information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -1283,6 +1303,7 @@ public GetPlatformApplicationAttributesResult call() throws Exception {
      *         Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -1318,6 +1339,7 @@ public GetSMSAttributesResult call() throws Exception {
      *         Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -1437,6 +1459,7 @@ public GetSubscriptionAttributesResult call() throws Exception {
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1471,6 +1494,7 @@ public GetTopicAttributesResult call() throws Exception {
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1509,9 +1533,12 @@ public GetTopicAttributesResult call() throws Exception {
      * <code>ListEndpointsByPlatformApplication</code> again using the NextToken
      * string received from the previous call. When there are no more records to
      * return, NextToken will be null. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listEndpointsByPlatformApplicationRequest <p>
      *            Input for ListEndpointsByPlatformApplication action.
@@ -1552,9 +1579,12 @@ public ListEndpointsByPlatformApplicationResult call() throws Exception {
      * <code>ListEndpointsByPlatformApplication</code> again using the NextToken
      * string received from the previous call. When there are no more records to
      * return, NextToken will be null. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listEndpointsByPlatformApplicationRequest <p>
      *            Input for ListEndpointsByPlatformApplication action.
@@ -1617,6 +1647,7 @@ public ListEndpointsByPlatformApplicationResult call() throws Exception {
      *         Simple Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -1660,6 +1691,7 @@ public ListPhoneNumbersOptedOutResult call() throws Exception {
      *         Simple Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -1699,9 +1731,12 @@ public ListPhoneNumbersOptedOutResult call() throws Exception {
      * using the NextToken string received from the previous call. When there
      * are no more records to return, NextToken will be null. For more
      * information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
+     * <p>
+     * This action is throttled at 15 transactions per second (TPS).
+     * </p>
      * 
      * @param listPlatformApplicationsRequest <p>
      *            Input for ListPlatformApplications action.
@@ -1741,9 +1776,12 @@ public ListPlatformApplicationsResult call() throws Exception {
      * using the NextToken string received from the previous call. When there
      * are no more records to return, NextToken will be null. For more
      * information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
+     * <p>
+     * This action is throttled at 15 transactions per second (TPS).
+     * </p>
      * 
      * @param listPlatformApplicationsRequest <p>
      *            Input for ListPlatformApplications action.
@@ -1789,6 +1827,9 @@ public ListPlatformApplicationsResult call() throws Exception {
      * <code>NextToken</code> parameter in a new <code>ListSubscriptions</code>
      * call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listSubscriptionsRequest <p>
      *            Input for ListSubscriptions action.
@@ -1825,6 +1866,9 @@ public ListSubscriptionsResult call() throws Exception {
      * <code>NextToken</code> parameter in a new <code>ListSubscriptions</code>
      * call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listSubscriptionsRequest <p>
      *            Input for ListSubscriptions action.
@@ -1870,6 +1914,9 @@ public ListSubscriptionsResult call() throws Exception {
      * <code>NextToken</code> parameter in a new
      * <code>ListSubscriptionsByTopic</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listSubscriptionsByTopicRequest <p>
      *            Input for ListSubscriptionsByTopic action.
@@ -1907,6 +1954,9 @@ public ListSubscriptionsByTopicResult call() throws Exception {
      * <code>NextToken</code> parameter in a new
      * <code>ListSubscriptionsByTopic</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listSubscriptionsByTopicRequest <p>
      *            Input for ListSubscriptionsByTopic action.
@@ -1945,6 +1995,87 @@ public ListSubscriptionsByTopicResult call() throws Exception {
         });
     }
 
+    /**
+     * <p>
+     * List all tags added to the specified Amazon SNS topic. For an overview,
+     * see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon Simple Notification Service Developer
+     * Guide</i>.
+     * </p>
+     * 
+     * @param listTagsForResourceRequest
+     * @return A Java Future object containing the response from the
+     *         ListTagsForResource service method, as returned by Amazon Simple
+     *         Notification Service.
+     * @throws ResourceNotFoundException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    public Future<ListTagsForResourceResult> listTagsForResourceAsync(
+            final ListTagsForResourceRequest listTagsForResourceRequest)
+            throws AmazonServiceException, AmazonClientException {
+        return executorService.submit(new Callable<ListTagsForResourceResult>() {
+            public ListTagsForResourceResult call() throws Exception {
+                return listTagsForResource(listTagsForResourceRequest);
+            }
+        });
+    }
+
+    /**
+     * <p>
+     * List all tags added to the specified Amazon SNS topic. For an overview,
+     * see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon Simple Notification Service Developer
+     * Guide</i>.
+     * </p>
+     * 
+     * @param listTagsForResourceRequest
+     * @return A Java Future object containing the response from the
+     *         ListTagsForResource service method, as returned by Amazon Simple
+     *         Notification Service.
+     * @throws ResourceNotFoundException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    public Future<ListTagsForResourceResult> listTagsForResourceAsync(
+            final ListTagsForResourceRequest listTagsForResourceRequest,
+            final AsyncHandler<ListTagsForResourceRequest, ListTagsForResourceResult> asyncHandler)
+            throws AmazonServiceException, AmazonClientException {
+        return executorService.submit(new Callable<ListTagsForResourceResult>() {
+            public ListTagsForResourceResult call() throws Exception {
+                ListTagsForResourceResult result = null;
+                try {
+                    result = listTagsForResource(listTagsForResourceRequest);
+                } catch (Exception ex) {
+                    asyncHandler.onError(ex);
+                    throw ex;
+                }
+                asyncHandler.onSuccess(listTagsForResourceRequest, result);
+                return result;
+            }
+        });
+    }
+
     /**
      * <p>
      * Returns a list of the requester's topics. Each call returns a limited
@@ -1952,6 +2083,9 @@ public ListSubscriptionsByTopicResult call() throws Exception {
      * <code>NextToken</code> is also returned. Use the <code>NextToken</code>
      * parameter in a new <code>ListTopics</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listTopicsRequest
      * @return A Java Future object containing the response from the ListTopics
@@ -1984,6 +2118,9 @@ public ListTopicsResult call() throws Exception {
      * <code>NextToken</code> is also returned. Use the <code>NextToken</code>
      * parameter in a new <code>ListTopics</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listTopicsRequest
      * @return A Java Future object containing the response from the ListTopics
@@ -2035,6 +2172,7 @@ public ListTopicsResult call() throws Exception {
      *         Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -2071,6 +2209,7 @@ public OptInPhoneNumberResult call() throws Exception {
      *         Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -2101,23 +2240,28 @@ public OptInPhoneNumberResult call() throws Exception {
 
     /**
      * <p>
-     * Sends a message to all of a topic's subscribed endpoints. When a
-     * <code>messageId</code> is returned, the message has been saved and Amazon
-     * SNS will attempt to deliver it to the topic's subscribers shortly. The
-     * format of the outgoing message to each subscribed endpoint depends on the
-     * notification protocol.
+     * Sends a message to an Amazon SNS topic or sends a text message (SMS
+     * message) directly to a phone number.
+     * </p>
+     * <p>
+     * If you send a message to a topic, Amazon SNS delivers the message to each
+     * endpoint that is subscribed to the topic. The format of the message
+     * depends on the notification protocol for each subscribed endpoint.
+     * </p>
+     * <p>
+     * When a <code>messageId</code> is returned, the message has been saved and
+     * Amazon SNS will attempt to deliver it shortly.
      * </p>
      * <p>
      * To use the <code>Publish</code> action for sending a message to a mobile
      * endpoint, such as an app on a Kindle device or mobile phone, you must
      * specify the EndpointArn for the TargetArn parameter. The EndpointArn is
      * returned when making a call with the <code>CreatePlatformEndpoint</code>
-     * action. The second example below shows a request and response for
-     * publishing to a mobile endpoint.
+     * action.
      * </p>
      * <p>
      * For more information about formatting messages, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
      * >Send Custom Platform-Specific Payloads in Messages to Mobile
      * Devices</a>.
      * </p>
@@ -2135,6 +2279,13 @@ public OptInPhoneNumberResult call() throws Exception {
      * @throws EndpointDisabledException
      * @throws PlatformApplicationDisabledException
      * @throws AuthorizationErrorException
+     * @throws KMSDisabledException
+     * @throws KMSInvalidStateException
+     * @throws KMSNotFoundException
+     * @throws KMSOptInRequiredException
+     * @throws KMSThrottlingException
+     * @throws KMSAccessDeniedException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2154,23 +2305,28 @@ public PublishResult call() throws Exception {
 
     /**
      * <p>
-     * Sends a message to all of a topic's subscribed endpoints. When a
-     * <code>messageId</code> is returned, the message has been saved and Amazon
-     * SNS will attempt to deliver it to the topic's subscribers shortly. The
-     * format of the outgoing message to each subscribed endpoint depends on the
-     * notification protocol.
+     * Sends a message to an Amazon SNS topic or sends a text message (SMS
+     * message) directly to a phone number.
+     * </p>
+     * <p>
+     * If you send a message to a topic, Amazon SNS delivers the message to each
+     * endpoint that is subscribed to the topic. The format of the message
+     * depends on the notification protocol for each subscribed endpoint.
+     * </p>
+     * <p>
+     * When a <code>messageId</code> is returned, the message has been saved and
+     * Amazon SNS will attempt to deliver it shortly.
      * </p>
      * <p>
      * To use the <code>Publish</code> action for sending a message to a mobile
      * endpoint, such as an app on a Kindle device or mobile phone, you must
      * specify the EndpointArn for the TargetArn parameter. The EndpointArn is
      * returned when making a call with the <code>CreatePlatformEndpoint</code>
-     * action. The second example below shows a request and response for
-     * publishing to a mobile endpoint.
+     * action.
      * </p>
      * <p>
      * For more information about formatting messages, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
      * >Send Custom Platform-Specific Payloads in Messages to Mobile
      * Devices</a>.
      * </p>
@@ -2188,6 +2344,13 @@ public PublishResult call() throws Exception {
      * @throws EndpointDisabledException
      * @throws PlatformApplicationDisabledException
      * @throws AuthorizationErrorException
+     * @throws KMSDisabledException
+     * @throws KMSInvalidStateException
+     * @throws KMSNotFoundException
+     * @throws KMSOptInRequiredException
+     * @throws KMSThrottlingException
+     * @throws KMSAccessDeniedException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2294,7 +2457,7 @@ public Void call() throws Exception {
      * Sets the attributes for an endpoint for a device on one of the supported
      * push notification services, such as GCM and APNS. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -2332,7 +2495,7 @@ public Void call() throws Exception {
      * Sets the attributes for an endpoint for a device on one of the supported
      * push notification services, such as GCM and APNS. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -2378,11 +2541,11 @@ public Void call() throws Exception {
      * Sets the attributes of the platform application object for the supported
      * push notification services, such as APNS and GCM. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>. For information on configuring
      * attributes for message delivery status, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/sns-msg-status.html">Using
-     * Amazon SNS Application Attributes for Message Delivery Status</a>.
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-msg-status.html"
+     * >Using Amazon SNS Application Attributes for Message Delivery Status</a>.
      * </p>
      * 
      * @param setPlatformApplicationAttributesRequest <p>
@@ -2419,11 +2582,11 @@ public Void call() throws Exception {
      * Sets the attributes of the platform application object for the supported
      * push notification services, such as APNS and GCM. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>. For information on configuring
      * attributes for message delivery status, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/sns-msg-status.html">Using
-     * Amazon SNS Application Attributes for Message Delivery Status</a>.
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-msg-status.html"
+     * >Using Amazon SNS Application Attributes for Message Delivery Status</a>.
      * </p>
      * 
      * @param setPlatformApplicationAttributesRequest <p>
@@ -2473,7 +2636,7 @@ public Void call() throws Exception {
      * the <code>Publish</code> action with the
      * <code>MessageAttributes.entry.N</code> parameter. For more information,
      * see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/sms_publish-to-phone.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/sms_publish-to-phone.html"
      * >Sending an SMS Message</a> in the <i>Amazon SNS Developer Guide</i>.
      * </p>
      * 
@@ -2486,6 +2649,7 @@ public Void call() throws Exception {
      * @throws InvalidParameterException
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2514,7 +2678,7 @@ public SetSMSAttributesResult call() throws Exception {
      * the <code>Publish</code> action with the
      * <code>MessageAttributes.entry.N</code> parameter. For more information,
      * see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/sms_publish-to-phone.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/sms_publish-to-phone.html"
      * >Sending an SMS Message</a> in the <i>Amazon SNS Developer Guide</i>.
      * </p>
      * 
@@ -2527,6 +2691,7 @@ public SetSMSAttributesResult call() throws Exception {
      * @throws InvalidParameterException
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2556,8 +2721,8 @@ public SetSMSAttributesResult call() throws Exception {
 
     /**
      * <p>
-     * Allows a subscription owner to set an attribute of the topic to a new
-     * value.
+     * Allows a subscription owner to set an attribute of the subscription to a
+     * new value.
      * </p>
      * 
      * @param setSubscriptionAttributesRequest <p>
@@ -2567,6 +2732,7 @@ public SetSMSAttributesResult call() throws Exception {
      *         SetSubscriptionAttributes service method, as returned by Amazon
      *         Simple Notification Service.
      * @throws InvalidParameterException
+     * @throws FilterPolicyLimitExceededException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
@@ -2591,8 +2757,8 @@ public Void call() throws Exception {
 
     /**
      * <p>
-     * Allows a subscription owner to set an attribute of the topic to a new
-     * value.
+     * Allows a subscription owner to set an attribute of the subscription to a
+     * new value.
      * </p>
      * 
      * @param setSubscriptionAttributesRequest <p>
@@ -2602,6 +2768,7 @@ public Void call() throws Exception {
      *         SetSubscriptionAttributes service method, as returned by Amazon
      *         Simple Notification Service.
      * @throws InvalidParameterException
+     * @throws FilterPolicyLimitExceededException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
@@ -2647,6 +2814,7 @@ public Void call() throws Exception {
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2681,6 +2849,7 @@ public Void call() throws Exception {
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2715,6 +2884,9 @@ public Void call() throws Exception {
      * the <code>ConfirmSubscription</code> action with the token from the
      * confirmation message. Confirmation tokens are valid for three days.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param subscribeRequest <p>
      *            Input for Subscribe action.
@@ -2723,10 +2895,12 @@ public Void call() throws Exception {
      *         service method, as returned by Amazon Simple Notification
      *         Service.
      * @throws SubscriptionLimitExceededException
+     * @throws FilterPolicyLimitExceededException
      * @throws InvalidParameterException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2751,6 +2925,9 @@ public SubscribeResult call() throws Exception {
      * the <code>ConfirmSubscription</code> action with the token from the
      * confirmation message. Confirmation tokens are valid for three days.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param subscribeRequest <p>
      *            Input for Subscribe action.
@@ -2759,10 +2936,12 @@ public SubscribeResult call() throws Exception {
      *         service method, as returned by Amazon Simple Notification
      *         Service.
      * @throws SubscriptionLimitExceededException
+     * @throws FilterPolicyLimitExceededException
      * @throws InvalidParameterException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2789,6 +2968,165 @@ public SubscribeResult call() throws Exception {
         });
     }
 
+    /**
+     * <p>
+     * Add tags to the specified Amazon SNS topic. For an overview, see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * <p>
+     * When you use topic tags, keep the following guidelines in mind:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * Adding more than 50 tags to a topic isn't recommended.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tags don't have any semantic meaning. Amazon SNS interprets tags as
+     * character strings.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tags are case-sensitive.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * A new tag with a key identical to that of an existing tag overwrites the
+     * existing tag.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tagging actions are limited to 10 TPS per AWS account. If your
+     * application requires a higher throughput, file a <a href=
+     * "https://console.aws.amazon.com/support/home#/case/create?issueType=technical"
+     * >technical support request</a>.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * For a full list of tag restrictions, see <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-limits.html#limits-topics"
+     * >Limits Related to Topics</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * 
+     * @param tagResourceRequest
+     * @return A Java Future object containing the response from the TagResource
+     *         service method, as returned by Amazon Simple Notification
+     *         Service.
+     * @throws ResourceNotFoundException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    public Future<TagResourceResult> tagResourceAsync(final TagResourceRequest tagResourceRequest)
+            throws AmazonServiceException, AmazonClientException {
+        return executorService.submit(new Callable<TagResourceResult>() {
+            public TagResourceResult call() throws Exception {
+                return tagResource(tagResourceRequest);
+            }
+        });
+    }
+
+    /**
+     * <p>
+     * Add tags to the specified Amazon SNS topic. For an overview, see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * <p>
+     * When you use topic tags, keep the following guidelines in mind:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * Adding more than 50 tags to a topic isn't recommended.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tags don't have any semantic meaning. Amazon SNS interprets tags as
+     * character strings.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tags are case-sensitive.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * A new tag with a key identical to that of an existing tag overwrites the
+     * existing tag.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tagging actions are limited to 10 TPS per AWS account. If your
+     * application requires a higher throughput, file a <a href=
+     * "https://console.aws.amazon.com/support/home#/case/create?issueType=technical"
+     * >technical support request</a>.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * For a full list of tag restrictions, see <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-limits.html#limits-topics"
+     * >Limits Related to Topics</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * 
+     * @param tagResourceRequest
+     * @return A Java Future object containing the response from the TagResource
+     *         service method, as returned by Amazon Simple Notification
+     *         Service.
+     * @throws ResourceNotFoundException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    public Future<TagResourceResult> tagResourceAsync(final TagResourceRequest tagResourceRequest,
+            final AsyncHandler<TagResourceRequest, TagResourceResult> asyncHandler)
+            throws AmazonServiceException, AmazonClientException {
+        return executorService.submit(new Callable<TagResourceResult>() {
+            public TagResourceResult call() throws Exception {
+                TagResourceResult result = null;
+                try {
+                    result = tagResource(tagResourceRequest);
+                } catch (Exception ex) {
+                    asyncHandler.onError(ex);
+                    throw ex;
+                }
+                asyncHandler.onSuccess(tagResourceRequest, result);
+                return result;
+            }
+        });
+    }
+
     /**
      * <p>
      * Deletes a subscription. If the subscription requires authentication for
@@ -2800,6 +3138,9 @@ public SubscribeResult call() throws Exception {
      * resubscribe to the topic if the <code>Unsubscribe</code> request was
      * unintended.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param unsubscribeRequest <p>
      *            Input for Unsubscribe action.
@@ -2811,6 +3152,7 @@ public SubscribeResult call() throws Exception {
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2840,6 +3182,9 @@ public Void call() throws Exception {
      * resubscribe to the topic if the <code>Unsubscribe</code> request was
      * unintended.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param unsubscribeRequest <p>
      *            Input for Unsubscribe action.
@@ -2851,6 +3196,7 @@ public Void call() throws Exception {
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2877,4 +3223,85 @@ public Void call() throws Exception {
         });
     }
 
+    /**
+     * <p>
+     * Remove tags from the specified Amazon SNS topic. For an overview, see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * 
+     * @param untagResourceRequest
+     * @return A Java Future object containing the response from the
+     *         UntagResource service method, as returned by Amazon Simple
+     *         Notification Service.
+     * @throws ResourceNotFoundException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    public Future<UntagResourceResult> untagResourceAsync(
+            final UntagResourceRequest untagResourceRequest) throws AmazonServiceException,
+            AmazonClientException {
+        return executorService.submit(new Callable<UntagResourceResult>() {
+            public UntagResourceResult call() throws Exception {
+                return untagResource(untagResourceRequest);
+            }
+        });
+    }
+
+    /**
+     * <p>
+     * Remove tags from the specified Amazon SNS topic. For an overview, see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * 
+     * @param untagResourceRequest
+     * @return A Java Future object containing the response from the
+     *         UntagResource service method, as returned by Amazon Simple
+     *         Notification Service.
+     * @throws ResourceNotFoundException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    public Future<UntagResourceResult> untagResourceAsync(
+            final UntagResourceRequest untagResourceRequest,
+            final AsyncHandler<UntagResourceRequest, UntagResourceResult> asyncHandler)
+            throws AmazonServiceException, AmazonClientException {
+        return executorService.submit(new Callable<UntagResourceResult>() {
+            public UntagResourceResult call() throws Exception {
+                UntagResourceResult result = null;
+                try {
+                    result = untagResource(untagResourceRequest);
+                } catch (Exception ex) {
+                    asyncHandler.onError(ex);
+                    throw ex;
+                }
+                asyncHandler.onSuccess(untagResourceRequest, result);
+                return result;
+            }
+        });
+    }
+
 }
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNSClient.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNSClient.java
index 14c5658313..8535d86643 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNSClient.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/AmazonSNSClient.java
@@ -44,10 +44,10 @@
  * Amazon SNS to easily push real-time notification messages to interested
  * subscribers over multiple delivery protocols. For more information about this
  * product see <a
- * href="http://aws.amazon.com/sns/">http://aws.amazon.com/sns</a>. For detailed
- * information about Amazon SNS features and their associated API calls, see the
- * <a href="http://docs.aws.amazon.com/sns/latest/dg/">Amazon SNS Developer
- * Guide</a>.
+ * href="http://aws.amazon.com/sns/">https://aws.amazon.com/sns</a>. For
+ * detailed information about Amazon SNS features and their associated API
+ * calls, see the <a href="https://docs.aws.amazon.com/sns/latest/dg/">Amazon
+ * SNS Developer Guide</a>.
  * </p>
  * <p>
  * We also provide SDKs that enable you to access Amazon SNS from your preferred
@@ -131,7 +131,7 @@ public AmazonSNSClient(ClientConfiguration clientConfiguration) {
      *     public void onResult(final UserStateDetails details) {
      *         AmazonSNSClient client = new AmazonSNSClient(AWSMobileClient.getInstance());
      *     }
-     *
+     * 
      *     &#064;Override
      *     public void onError(final Exception e) {
      *         e.printStackTrace();
@@ -167,7 +167,7 @@ public AmazonSNSClient(AWSCredentials awsCredentials) {
      *     public void onResult(final UserStateDetails details) {
      *         AmazonSNSClient client = new AmazonSNSClient(AWSMobileClient.getInstance());
      *     }
-     *
+     * 
      *     &#064;Override
      *     public void onError(final Exception e) {
      *         e.printStackTrace();
@@ -193,10 +193,10 @@ public AmazonSNSClient(AWSCredentials awsCredentials, ClientConfiguration client
      * specified AWS account credentials provider.
      * <p>
      * The client requests are authenticated using the {@link AWSCredentials}
-     * provided in this constructor. Static AWSCredentials can be passed for
-     * quick testing. However, it is strongly recommended to use Amazon Cognito
-     * vended temporary credentials for use in production. This can be achieved
-     * by using {@link AWSMobileClient}. Please see
+     * provided by the {@link AWSCredentialsProvider}. Static AWSCredentials can
+     * be passed for quick testing. However, it is strongly recommended to use
+     * Amazon Cognito vended temporary credentials for use in production. This
+     * can be achieved by using {@link AWSMobileClient}. Please see
      * https://aws-amplify.github.io/docs/android/authentication for
      * instructions on how to enable {@link AWSMobileClient}.
      *
@@ -206,7 +206,7 @@ public AmazonSNSClient(AWSCredentials awsCredentials, ClientConfiguration client
      *     public void onResult(final UserStateDetails details) {
      *         AmazonSNSClient client = new AmazonSNSClient(AWSMobileClient.getInstance());
      *     }
-     *
+     * 
      *     &#064;Override
      *     public void onError(final Exception e) {
      *         e.printStackTrace();
@@ -231,10 +231,10 @@ public AmazonSNSClient(AWSCredentialsProvider awsCredentialsProvider) {
      * options.
      * <p>
      * The client requests are authenticated using the {@link AWSCredentials}
-     * provided in this constructor. Static AWSCredentials can be passed for
-     * quick testing. However, it is strongly recommended to use Amazon Cognito
-     * vended temporary credentials for use in production. This can be achieved
-     * by using {@link AWSMobileClient}. Please see
+     * provided by the {@link AWSCredentialsProvider}. Static AWSCredentials can
+     * be passed for quick testing. However, it is strongly recommended to use
+     * Amazon Cognito vended temporary credentials for use in production. This
+     * can be achieved by using {@link AWSMobileClient}. Please see
      * https://aws-amplify.github.io/docs/android/authentication for
      * instructions on how to enable {@link AWSMobileClient}.
      *
@@ -244,7 +244,7 @@ public AmazonSNSClient(AWSCredentialsProvider awsCredentialsProvider) {
      *     public void onResult(final UserStateDetails details) {
      *         AmazonSNSClient client = new AmazonSNSClient(AWSMobileClient.getInstance());
      *     }
-     *
+     * 
      *     &#064;Override
      *     public void onError(final Exception e) {
      *         e.printStackTrace();
@@ -299,6 +299,28 @@ public AmazonSNSClient(AWSCredentialsProvider awsCredentialsProvider,
      * specified AWS account credentials provider, client configuration options
      * and request metric collector.
      * <p>
+     * The client requests are authenticated using the {@link AWSCredentials}
+     * provided by the {@link AWSCredentialsProvider}. Static AWSCredentials can
+     * be passed for quick testing. However, it is strongly recommended to use
+     * Amazon Cognito vended temporary credentials for use in production. This
+     * can be achieved by using {@link AWSMobileClient}. Please see
+     * https://aws-amplify.github.io/docs/android/authentication for
+     * instructions on how to enable {@link AWSMobileClient}.
+     *
+     * <pre>
+     * AWSMobileClient.getInstance().initialize(getApplicationContext(), new Callback&lt;UserStateDetails&gt;() {
+     *     &#064;Override
+     *     public void onResult(final UserStateDetails details) {
+     *         AmazonSNSClient client = new AmazonSNSClient(AWSMobileClient.getInstance());
+     *     }
+     * 
+     *     &#064;Override
+     *     public void onError(final Exception e) {
+     *         e.printStackTrace();
+     *     }
+     * });
+     * </pre>
+     * <p>
      * All service calls made using this new client object are blocking, and
      * will not return until the service call completes.
      *
@@ -321,13 +343,26 @@ public AmazonSNSClient(AWSCredentialsProvider awsCredentialsProvider,
 
     private void init() {
         exceptionUnmarshallers.add(new AuthorizationErrorExceptionUnmarshaller());
+        exceptionUnmarshallers.add(new ConcurrentAccessExceptionUnmarshaller());
         exceptionUnmarshallers.add(new EndpointDisabledExceptionUnmarshaller());
+        exceptionUnmarshallers.add(new FilterPolicyLimitExceededExceptionUnmarshaller());
         exceptionUnmarshallers.add(new InternalErrorExceptionUnmarshaller());
         exceptionUnmarshallers.add(new InvalidParameterExceptionUnmarshaller());
         exceptionUnmarshallers.add(new InvalidParameterValueExceptionUnmarshaller());
+        exceptionUnmarshallers.add(new InvalidSecurityExceptionUnmarshaller());
+        exceptionUnmarshallers.add(new KMSAccessDeniedExceptionUnmarshaller());
+        exceptionUnmarshallers.add(new KMSDisabledExceptionUnmarshaller());
+        exceptionUnmarshallers.add(new KMSInvalidStateExceptionUnmarshaller());
+        exceptionUnmarshallers.add(new KMSNotFoundExceptionUnmarshaller());
+        exceptionUnmarshallers.add(new KMSOptInRequiredExceptionUnmarshaller());
+        exceptionUnmarshallers.add(new KMSThrottlingExceptionUnmarshaller());
         exceptionUnmarshallers.add(new NotFoundExceptionUnmarshaller());
         exceptionUnmarshallers.add(new PlatformApplicationDisabledExceptionUnmarshaller());
+        exceptionUnmarshallers.add(new ResourceNotFoundExceptionUnmarshaller());
+        exceptionUnmarshallers.add(new StaleTagExceptionUnmarshaller());
         exceptionUnmarshallers.add(new SubscriptionLimitExceededExceptionUnmarshaller());
+        exceptionUnmarshallers.add(new TagLimitExceededExceptionUnmarshaller());
+        exceptionUnmarshallers.add(new TagPolicyExceptionUnmarshaller());
         exceptionUnmarshallers.add(new ThrottledExceptionUnmarshaller());
         exceptionUnmarshallers.add(new TopicLimitExceededExceptionUnmarshaller());
         exceptionUnmarshallers.add(new StandardErrorUnmarshaller());
@@ -406,6 +441,7 @@ public void addPermission(AddPermissionRequest addPermissionRequest)
      *         Amazon Simple Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -457,6 +493,7 @@ public CheckIfPhoneNumberIsOptedOutResult checkIfPhoneNumberIsOptedOut(
      * @throws NotFoundException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws FilterPolicyLimitExceededException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -490,7 +527,7 @@ public ConfirmSubscriptionResult confirmSubscription(
     /**
      * <p>
      * Creates a platform application object for one of the supported push
-     * notification services, such as APNS and GCM, to which devices and mobile
+     * notification services, such as APNS and FCM, to which devices and mobile
      * apps may register. You must specify PlatformPrincipal and
      * PlatformCredential attributes when using the
      * <code>CreatePlatformApplication</code> action. The PlatformPrincipal is
@@ -510,21 +547,21 @@ public ConfirmSubscriptionResult confirmSubscription(
      * "secret key". The PlatformApplicationArn that is returned when using
      * <code>CreatePlatformApplication</code> is then used as an attribute for
      * the <code>CreatePlatformEndpoint</code> action. For more information, see
-     * <a href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">
+     * <a href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">
      * Using Amazon SNS Mobile Push Notifications</a>. For more information
      * about obtaining the PlatformPrincipal and PlatformCredential for each of
      * the supported push notification services, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-apns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-apns.html"
      * >Getting Started with Apple Push Notification Service</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-adm.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-adm.html"
      * >Getting Started with Amazon Device Messaging</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-baidu.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-baidu.html"
      * >Getting Started with Baidu Cloud Push</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-gcm.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-gcm.html"
      * >Getting Started with Google Cloud Messaging for Android</a>, <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-mpns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-mpns.html"
      * >Getting Started with MPNS</a>, or <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-wns.html"
+     * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-wns.html"
      * >Getting Started with WNS</a>.
      * </p>
      * 
@@ -581,14 +618,14 @@ public CreatePlatformApplicationResult createPlatformApplication(
      * requester already owns an endpoint with the same device token and
      * attributes, that endpoint's ARN is returned without creating a new
      * endpoint. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * <p>
      * When using <code>CreatePlatformEndpoint</code> with Baidu, two attributes
      * must be provided: ChannelId and UserId. The token field must also contain
      * the ChannelId. For more information, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePushBaiduEndpoint.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePushBaiduEndpoint.html"
      * >Creating an Amazon SNS Endpoint for Baidu</a>.
      * </p>
      * 
@@ -636,7 +673,7 @@ public CreatePlatformEndpointResult createPlatformEndpoint(
      * <p>
      * Creates a topic to which notifications can be published. Users can create
      * at most 100,000 topics. For more information, see <a
-     * href="http://aws.amazon.com/sns/">http://aws.amazon.com/sns</a>. This
+     * href="http://aws.amazon.com/sns/">https://aws.amazon.com/sns</a>. This
      * action is idempotent, so if the requester already owns a topic with the
      * specified name, that topic's ARN is returned without creating a new
      * topic.
@@ -651,6 +688,11 @@ public CreatePlatformEndpointResult createPlatformEndpoint(
      * @throws TopicLimitExceededException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -682,7 +724,7 @@ public CreateTopicResult createTopic(CreateTopicRequest createTopicRequest)
      * <p>
      * Deletes the endpoint for a device and mobile app from Amazon SNS. This
      * action is idempotent. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * <p>
@@ -726,7 +768,7 @@ public void deleteEndpoint(DeleteEndpointRequest deleteEndpointRequest)
      * <p>
      * Deletes a platform application object for one of the supported push
      * notification services, such as APNS and GCM. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -777,6 +819,9 @@ public void deletePlatformApplication(
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -808,7 +853,7 @@ public void deleteTopic(DeleteTopicRequest deleteTopicRequest)
      * Retrieves the endpoint attributes for a device on one of the supported
      * push notification services, such as GCM and APNS. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -857,7 +902,7 @@ public GetEndpointAttributesResult getEndpointAttributes(
      * Retrieves the attributes of the platform application object for the
      * supported push notification services, such as APNS and GCM. For more
      * information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -917,6 +962,7 @@ public GetPlatformApplicationAttributesResult getPlatformApplicationAttributes(
      *         Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -1007,6 +1053,7 @@ public GetSubscriptionAttributesResult getSubscriptionAttributes(
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1047,9 +1094,12 @@ public GetTopicAttributesResult getTopicAttributes(
      * <code>ListEndpointsByPlatformApplication</code> again using the NextToken
      * string received from the previous call. When there are no more records to
      * return, NextToken will be null. For more information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listEndpointsByPlatformApplicationRequest <p>
      *            Input for ListEndpointsByPlatformApplication action.
@@ -1116,6 +1166,7 @@ public ListEndpointsByPlatformApplicationResult listEndpointsByPlatformApplicati
      *         Simple Notification Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -1158,9 +1209,12 @@ public ListPhoneNumbersOptedOutResult listPhoneNumbersOptedOut(
      * using the NextToken string received from the previous call. When there
      * are no more records to return, NextToken will be null. For more
      * information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
+     * <p>
+     * This action is throttled at 15 transactions per second (TPS).
+     * </p>
      * 
      * @param listPlatformApplicationsRequest <p>
      *            Input for ListPlatformApplications action.
@@ -1209,6 +1263,9 @@ public ListPlatformApplicationsResult listPlatformApplications(
      * <code>NextToken</code> parameter in a new <code>ListSubscriptions</code>
      * call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listSubscriptionsRequest <p>
      *            Input for ListSubscriptions action.
@@ -1256,6 +1313,9 @@ public ListSubscriptionsResult listSubscriptions(
      * <code>NextToken</code> parameter in a new
      * <code>ListSubscriptionsByTopic</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listSubscriptionsByTopicRequest <p>
      *            Input for ListSubscriptionsByTopic action.
@@ -1297,6 +1357,54 @@ public ListSubscriptionsByTopicResult listSubscriptionsByTopic(
         }
     }
 
+    /**
+     * <p>
+     * List all tags added to the specified Amazon SNS topic. For an overview,
+     * see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon Simple Notification Service Developer
+     * Guide</i>.
+     * </p>
+     * 
+     * @param listTagsForResourceRequest
+     * @return listTagsForResourceResult The response from the
+     *         ListTagsForResource service method, as returned by Amazon Simple
+     *         Notification Service.
+     * @throws ResourceNotFoundException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    public ListTagsForResourceResult listTagsForResource(
+            ListTagsForResourceRequest listTagsForResourceRequest)
+            throws AmazonServiceException, AmazonClientException {
+        ExecutionContext executionContext = createExecutionContext(listTagsForResourceRequest);
+        AWSRequestMetrics awsRequestMetrics = executionContext.getAwsRequestMetrics();
+        awsRequestMetrics.startEvent(Field.ClientExecuteTime);
+        Request<ListTagsForResourceRequest> request = null;
+        Response<ListTagsForResourceResult> response = null;
+        try {
+            request = new ListTagsForResourceRequestMarshaller()
+                    .marshall(listTagsForResourceRequest);
+            // Binds the request metrics to the current request.
+            request.setAWSRequestMetrics(awsRequestMetrics);
+            response = invoke(request, new ListTagsForResourceResultStaxUnmarshaller(),
+                    executionContext);
+            return response.getAwsResponse();
+        } finally {
+            awsRequestMetrics.endEvent(Field.ClientExecuteTime);
+            endClientExecution(awsRequestMetrics, request, response);
+        }
+    }
+
     /**
      * <p>
      * Returns a list of the requester's topics. Each call returns a limited
@@ -1304,6 +1412,9 @@ public ListSubscriptionsByTopicResult listSubscriptionsByTopic(
      * <code>NextToken</code> is also returned. Use the <code>NextToken</code>
      * parameter in a new <code>ListTopics</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param listTopicsRequest
      * @return listTopicsResult The response from the ListTopics service method,
@@ -1355,6 +1466,7 @@ public ListTopicsResult listTopics(ListTopicsRequest listTopicsRequest)
      *         Service.
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws InvalidParameterException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
@@ -1386,23 +1498,28 @@ public OptInPhoneNumberResult optInPhoneNumber(OptInPhoneNumberRequest optInPhon
 
     /**
      * <p>
-     * Sends a message to all of a topic's subscribed endpoints. When a
-     * <code>messageId</code> is returned, the message has been saved and Amazon
-     * SNS will attempt to deliver it to the topic's subscribers shortly. The
-     * format of the outgoing message to each subscribed endpoint depends on the
-     * notification protocol.
+     * Sends a message to an Amazon SNS topic or sends a text message (SMS
+     * message) directly to a phone number.
+     * </p>
+     * <p>
+     * If you send a message to a topic, Amazon SNS delivers the message to each
+     * endpoint that is subscribed to the topic. The format of the message
+     * depends on the notification protocol for each subscribed endpoint.
+     * </p>
+     * <p>
+     * When a <code>messageId</code> is returned, the message has been saved and
+     * Amazon SNS will attempt to deliver it shortly.
      * </p>
      * <p>
      * To use the <code>Publish</code> action for sending a message to a mobile
      * endpoint, such as an app on a Kindle device or mobile phone, you must
      * specify the EndpointArn for the TargetArn parameter. The EndpointArn is
      * returned when making a call with the <code>CreatePlatformEndpoint</code>
-     * action. The second example below shows a request and response for
-     * publishing to a mobile endpoint.
+     * action.
      * </p>
      * <p>
      * For more information about formatting messages, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
      * >Send Custom Platform-Specific Payloads in Messages to Mobile
      * Devices</a>.
      * </p>
@@ -1419,6 +1536,13 @@ public OptInPhoneNumberResult optInPhoneNumber(OptInPhoneNumberRequest optInPhon
      * @throws EndpointDisabledException
      * @throws PlatformApplicationDisabledException
      * @throws AuthorizationErrorException
+     * @throws KMSDisabledException
+     * @throws KMSInvalidStateException
+     * @throws KMSNotFoundException
+     * @throws KMSOptInRequiredException
+     * @throws KMSThrottlingException
+     * @throws KMSAccessDeniedException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1489,7 +1613,7 @@ public void removePermission(RemovePermissionRequest removePermissionRequest)
      * Sets the attributes for an endpoint for a device on one of the supported
      * push notification services, such as GCM and APNS. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
      * 
@@ -1532,11 +1656,11 @@ public void setEndpointAttributes(SetEndpointAttributesRequest setEndpointAttrib
      * Sets the attributes of the platform application object for the supported
      * push notification services, such as APNS and GCM. For more information,
      * see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>. For information on configuring
      * attributes for message delivery status, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/sns-msg-status.html">Using
-     * Amazon SNS Application Attributes for Message Delivery Status</a>.
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-msg-status.html"
+     * >Using Amazon SNS Application Attributes for Message Delivery Status</a>.
      * </p>
      * 
      * @param setPlatformApplicationAttributesRequest <p>
@@ -1584,7 +1708,7 @@ public void setPlatformApplicationAttributes(
      * the <code>Publish</code> action with the
      * <code>MessageAttributes.entry.N</code> parameter. For more information,
      * see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/sms_publish-to-phone.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/sms_publish-to-phone.html"
      * >Sending an SMS Message</a> in the <i>Amazon SNS Developer Guide</i>.
      * </p>
      * 
@@ -1597,6 +1721,7 @@ public void setPlatformApplicationAttributes(
      * @throws InvalidParameterException
      * @throws ThrottledException
      * @throws InternalErrorException
+     * @throws AuthorizationErrorException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1627,14 +1752,15 @@ public SetSMSAttributesResult setSMSAttributes(SetSMSAttributesRequest setSMSAtt
 
     /**
      * <p>
-     * Allows a subscription owner to set an attribute of the topic to a new
-     * value.
+     * Allows a subscription owner to set an attribute of the subscription to a
+     * new value.
      * </p>
      * 
      * @param setSubscriptionAttributesRequest <p>
      *            Input for SetSubscriptionAttributes action.
      *            </p>
      * @throws InvalidParameterException
+     * @throws FilterPolicyLimitExceededException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
@@ -1678,6 +1804,7 @@ public void setSubscriptionAttributes(
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1711,6 +1838,9 @@ public void setTopicAttributes(SetTopicAttributesRequest setTopicAttributesReque
      * the <code>ConfirmSubscription</code> action with the token from the
      * confirmation message. Confirmation tokens are valid for three days.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param subscribeRequest <p>
      *            Input for Subscribe action.
@@ -1718,10 +1848,12 @@ public void setTopicAttributes(SetTopicAttributesRequest setTopicAttributesReque
      * @return subscribeResult The response from the Subscribe service method,
      *         as returned by Amazon Simple Notification Service.
      * @throws SubscriptionLimitExceededException
+     * @throws FilterPolicyLimitExceededException
      * @throws InvalidParameterException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1749,6 +1881,90 @@ public SubscribeResult subscribe(SubscribeRequest subscribeRequest)
         }
     }
 
+    /**
+     * <p>
+     * Add tags to the specified Amazon SNS topic. For an overview, see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * <p>
+     * When you use topic tags, keep the following guidelines in mind:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * Adding more than 50 tags to a topic isn't recommended.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tags don't have any semantic meaning. Amazon SNS interprets tags as
+     * character strings.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tags are case-sensitive.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * A new tag with a key identical to that of an existing tag overwrites the
+     * existing tag.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * Tagging actions are limited to 10 TPS per AWS account. If your
+     * application requires a higher throughput, file a <a href=
+     * "https://console.aws.amazon.com/support/home#/case/create?issueType=technical"
+     * >technical support request</a>.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * For a full list of tag restrictions, see <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-limits.html#limits-topics"
+     * >Limits Related to Topics</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * 
+     * @param tagResourceRequest
+     * @return tagResourceResult The response from the TagResource service
+     *         method, as returned by Amazon Simple Notification Service.
+     * @throws ResourceNotFoundException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    public TagResourceResult tagResource(TagResourceRequest tagResourceRequest)
+            throws AmazonServiceException, AmazonClientException {
+        ExecutionContext executionContext = createExecutionContext(tagResourceRequest);
+        AWSRequestMetrics awsRequestMetrics = executionContext.getAwsRequestMetrics();
+        awsRequestMetrics.startEvent(Field.ClientExecuteTime);
+        Request<TagResourceRequest> request = null;
+        Response<TagResourceResult> response = null;
+        try {
+            request = new TagResourceRequestMarshaller().marshall(tagResourceRequest);
+            // Binds the request metrics to the current request.
+            request.setAWSRequestMetrics(awsRequestMetrics);
+            response = invoke(request, new TagResourceResultStaxUnmarshaller(), executionContext);
+            return response.getAwsResponse();
+        } finally {
+            awsRequestMetrics.endEvent(Field.ClientExecuteTime);
+            endClientExecution(awsRequestMetrics, request, response);
+        }
+    }
+
     /**
      * <p>
      * Deletes a subscription. If the subscription requires authentication for
@@ -1760,6 +1976,9 @@ public SubscribeResult subscribe(SubscribeRequest subscribeRequest)
      * resubscribe to the topic if the <code>Unsubscribe</code> request was
      * unintended.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param unsubscribeRequest <p>
      *            Input for Unsubscribe action.
@@ -1768,6 +1987,7 @@ public SubscribeResult subscribe(SubscribeRequest subscribeRequest)
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -1796,25 +2016,94 @@ public void unsubscribe(UnsubscribeRequest unsubscribeRequest)
 
     /**
      * <p>
-     * Allows a subscription owner to set an attribute of the topic to a new
-     * value.
+     * Remove tags from the specified Amazon SNS topic. For an overview, see <a
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+     * Tags</a> in the <i>Amazon SNS Developer Guide</i>.
+     * </p>
+     * 
+     * @param untagResourceRequest
+     * @return untagResourceResult The response from the UntagResource service
+     *         method, as returned by Amazon Simple Notification Service.
+     * @throws ResourceNotFoundException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws InvalidParameterException
+     * @throws AuthorizationErrorException
+     * @throws ConcurrentAccessException
+     * @throws AmazonClientException If any internal errors are encountered
+     *             inside the client while attempting to make the request or
+     *             handle the response. For example if a network connection is
+     *             not available.
+     * @throws AmazonServiceException If an error response is returned by Amazon
+     *             Simple Notification Service indicating either a problem with
+     *             the data in the request, or a server side issue.
+     */
+    public UntagResourceResult untagResource(UntagResourceRequest untagResourceRequest)
+            throws AmazonServiceException, AmazonClientException {
+        ExecutionContext executionContext = createExecutionContext(untagResourceRequest);
+        AWSRequestMetrics awsRequestMetrics = executionContext.getAwsRequestMetrics();
+        awsRequestMetrics.startEvent(Field.ClientExecuteTime);
+        Request<UntagResourceRequest> request = null;
+        Response<UntagResourceResult> response = null;
+        try {
+            request = new UntagResourceRequestMarshaller().marshall(untagResourceRequest);
+            // Binds the request metrics to the current request.
+            request.setAWSRequestMetrics(awsRequestMetrics);
+            response = invoke(request, new UntagResourceResultStaxUnmarshaller(), executionContext);
+            return response.getAwsResponse();
+        } finally {
+            awsRequestMetrics.endEvent(Field.ClientExecuteTime);
+            endClientExecution(awsRequestMetrics, request, response);
+        }
+    }
+
+    /**
+     * <p>
+     * Allows a subscription owner to set an attribute of the subscription to a
+     * new value.
      * </p>
      * 
      * @param subscriptionArn <p>
      *            The ARN of the subscription to modify.
      *            </p>
      * @param attributeName <p>
-     *            The name of the attribute you want to set. Only a subset of
-     *            the subscriptions attributes are mutable.
+     *            A map of attributes with their corresponding values.
+     *            </p>
+     *            <p>
+     *            The following lists the names, descriptions, and values of the
+     *            special request parameters that the
+     *            <code>SetTopicAttributes</code> action uses:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>DeliveryPolicy</code> – The policy that defines how
+     *            Amazon SNS retries failed deliveries to HTTP/S endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>FilterPolicy</code> – The simple JSON object that lets
+     *            your subscriber receive only a subset of messages, rather than
+     *            receiving every message published to the topic.
      *            </p>
+     *            </li>
+     *            <li>
      *            <p>
-     *            Valid values: <code>DeliveryPolicy</code> |
-     *            <code>RawMessageDelivery</code>
+     *            <code>RawMessageDelivery</code> – When set to
+     *            <code>true</code>, enables raw message delivery to Amazon SQS
+     *            or HTTP/S endpoints. This eliminates the need for the
+     *            endpoints to process JSON formatting, which is otherwise
+     *            created for Amazon SNS metadata.
      *            </p>
+     *            </li>
+     *            </ul>
      * @param attributeValue <p>
      *            The new value for the attribute in JSON format.
      *            </p>
      * @throws InvalidParameterException
+     * @throws FilterPolicyLimitExceededException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
@@ -1873,6 +2162,9 @@ public GetSubscriptionAttributesResult getSubscriptionAttributes(String subscrip
      * the <code>ConfirmSubscription</code> action with the token from the
      * confirmation message. Confirmation tokens are valid for three days.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param topicArn <p>
      *            The ARN of the topic you want to subscribe to.
@@ -1883,47 +2175,47 @@ public GetSubscriptionAttributesResult getSubscriptionAttributes(String subscrip
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>http</code> -- delivery of JSON-encoded message via HTTP
+     *            <code>http</code> – delivery of JSON-encoded message via HTTP
      *            POST
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>https</code> -- delivery of JSON-encoded message via
+     *            <code>https</code> – delivery of JSON-encoded message via
      *            HTTPS POST
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>email</code> -- delivery of message via SMTP
+     *            <code>email</code> – delivery of message via SMTP
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>email-json</code> -- delivery of JSON-encoded message
-     *            via SMTP
+     *            <code>email-json</code> – delivery of JSON-encoded message via
+     *            SMTP
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>sms</code> -- delivery of message via SMS
+     *            <code>sms</code> – delivery of message via SMS
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>sqs</code> -- delivery of JSON-encoded message to an
+     *            <code>sqs</code> – delivery of JSON-encoded message to an
      *            Amazon SQS queue
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>application</code> -- delivery of JSON-encoded message
-     *            to an EndpointArn for a mobile app and device.
+     *            <code>application</code> – delivery of JSON-encoded message to
+     *            an EndpointArn for a mobile app and device.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>lambda</code> -- delivery of JSON-encoded message to an
+     *            <code>lambda</code> – delivery of JSON-encoded message to an
      *            AWS Lambda function.
      *            </p>
      *            </li>
@@ -1936,7 +2228,7 @@ public GetSubscriptionAttributesResult getSubscriptionAttributes(String subscrip
      *            <li>
      *            <p>
      *            For the <code>http</code> protocol, the endpoint is an URL
-     *            beginning with "http://"
+     *            beginning with "https://"
      *            </p>
      *            </li>
      *            <li>
@@ -1985,10 +2277,12 @@ public GetSubscriptionAttributesResult getSubscriptionAttributes(String subscrip
      * @return subscribeResult The response from the Subscribe service method,
      *         as returned by Amazon Simple Notification Service.
      * @throws SubscriptionLimitExceededException
+     * @throws FilterPolicyLimitExceededException
      * @throws InvalidParameterException
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2069,6 +2363,7 @@ public void addPermission(String topicArn, String label, java.util.List<String>
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2123,6 +2418,9 @@ public void removePermission(String topicArn, String label)
      * <code>NextToken</code> is also returned. Use the <code>NextToken</code>
      * parameter in a new <code>ListTopics</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @return listTopicsResult The response from the ListTopics service method,
      *         as returned by Amazon Simple Notification Service.
@@ -2150,6 +2448,9 @@ public ListTopicsResult listTopics()
      * <code>NextToken</code> is also returned. Use the <code>NextToken</code>
      * parameter in a new <code>ListTopics</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param nextToken <p>
      *            Token returned by the previous <code>ListTopics</code>
@@ -2207,6 +2508,7 @@ public ListTopicsResult listTopics(String nextToken)
      * @throws NotFoundException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws FilterPolicyLimitExceededException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2250,6 +2552,7 @@ public ConfirmSubscriptionResult confirmSubscription(String topicArn, String tok
      * @throws NotFoundException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws FilterPolicyLimitExceededException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2275,13 +2578,53 @@ public ConfirmSubscriptionResult confirmSubscription(String topicArn, String tok
      *            The ARN of the topic to modify.
      *            </p>
      * @param attributeName <p>
-     *            The name of the attribute you want to set. Only a subset of
-     *            the topic's attributes are mutable.
+     *            A map of attributes with their corresponding values.
+     *            </p>
+     *            <p>
+     *            The following lists the names, descriptions, and values of the
+     *            special request parameters that the
+     *            <code>SetTopicAttributes</code> action uses:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>DeliveryPolicy</code> – The policy that defines how
+     *            Amazon SNS retries failed deliveries to HTTP/S endpoints.
      *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>DisplayName</code> – The display name to use for a topic
+     *            with SMS subscriptions.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>Policy</code> – The policy that defines who can access
+     *            your topic. By default, only the topic owner can publish or
+     *            subscribe to the topic.
+     *            </p>
+     *            </li>
+     *            </ul>
+     *            <p>
+     *            The following attribute applies only to <a href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     *            >server-side-encryption</a>:
+     *            </p>
+     *            <ul>
+     *            <li>
      *            <p>
-     *            Valid values: <code>Policy</code> | <code>DisplayName</code> |
-     *            <code>DeliveryPolicy</code>
+     *            <code>KmsMasterKeyId</code> - The ID of an AWS-managed
+     *            customer master key (CMK) for Amazon SNS or a custom CMK. For
+     *            more information, see <a href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     *            >Key Terms</a>. For more examples, see <a href=
+     *            "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     *            >KeyId</a> in the <i>AWS Key Management Service API
+     *            Reference</i>.
      *            </p>
+     *            </li>
+     *            </ul>
      * @param attributeValue <p>
      *            The new value for the attribute.
      *            </p>
@@ -2289,6 +2632,7 @@ public ConfirmSubscriptionResult confirmSubscription(String topicArn, String tok
      * @throws InternalErrorException
      * @throws NotFoundException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2310,7 +2654,7 @@ public void setTopicAttributes(String topicArn, String attributeName, String att
      * <p>
      * Creates a topic to which notifications can be published. Users can create
      * at most 100,000 topics. For more information, see <a
-     * href="http://aws.amazon.com/sns/">http://aws.amazon.com/sns</a>. This
+     * href="http://aws.amazon.com/sns/">https://aws.amazon.com/sns</a>. This
      * action is idempotent, so if the requester already owns a topic with the
      * specified name, that topic's ARN is returned without creating a new
      * topic.
@@ -2330,6 +2674,11 @@ public void setTopicAttributes(String topicArn, String attributeName, String att
      * @throws TopicLimitExceededException
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
+     * @throws InvalidSecurityException
+     * @throws TagLimitExceededException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2360,6 +2709,9 @@ public CreateTopicResult createTopic(String name)
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws StaleTagException
+     * @throws TagPolicyException
+     * @throws ConcurrentAccessException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2386,6 +2738,9 @@ public void deleteTopic(String topicArn)
      * resubscribe to the topic if the <code>Unsubscribe</code> request was
      * unintended.
      * </p>
+     * <p>
+     * This action is throttled at 100 transactions per second (TPS).
+     * </p>
      * 
      * @param subscriptionArn <p>
      *            The ARN of the subscription to be deleted.
@@ -2394,6 +2749,7 @@ public void deleteTopic(String topicArn)
      * @throws InternalErrorException
      * @throws AuthorizationErrorException
      * @throws NotFoundException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2411,23 +2767,28 @@ public void unsubscribe(String subscriptionArn)
 
     /**
      * <p>
-     * Sends a message to all of a topic's subscribed endpoints. When a
-     * <code>messageId</code> is returned, the message has been saved and Amazon
-     * SNS will attempt to deliver it to the topic's subscribers shortly. The
-     * format of the outgoing message to each subscribed endpoint depends on the
-     * notification protocol.
+     * Sends a message to an Amazon SNS topic or sends a text message (SMS
+     * message) directly to a phone number.
+     * </p>
+     * <p>
+     * If you send a message to a topic, Amazon SNS delivers the message to each
+     * endpoint that is subscribed to the topic. The format of the message
+     * depends on the notification protocol for each subscribed endpoint.
+     * </p>
+     * <p>
+     * When a <code>messageId</code> is returned, the message has been saved and
+     * Amazon SNS will attempt to deliver it shortly.
      * </p>
      * <p>
      * To use the <code>Publish</code> action for sending a message to a mobile
      * endpoint, such as an app on a Kindle device or mobile phone, you must
      * specify the EndpointArn for the TargetArn parameter. The EndpointArn is
      * returned when making a call with the <code>CreatePlatformEndpoint</code>
-     * action. The second example below shows a request and response for
-     * publishing to a mobile endpoint.
+     * action.
      * </p>
      * <p>
      * For more information about formatting messages, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
      * >Send Custom Platform-Specific Payloads in Messages to Mobile
      * Devices</a>.
      * </p>
@@ -2441,23 +2802,54 @@ public void unsubscribe(String subscriptionArn)
      *            <code>PhoneNumber</code> or <code>TargetArn</code> parameters.
      *            </p>
      * @param message <p>
-     *            The message you want to send to the topic.
+     *            The message you want to send.
+     *            </p>
+     *            <important>
+     *            <p>
+     *            The <code>Message</code> parameter is always a string. If you
+     *            set <code>MessageStructure</code> to <code>json</code>, you
+     *            must string-encode the <code>Message</code> parameter.
+     *            </p>
+     *            </important>
+     *            <p>
+     *            If you are publishing to a topic and you want to send the same
+     *            message to all transport protocols, include the text of the
+     *            message as a String value. If you want to send different
+     *            messages for each transport protocol, set the value of the
+     *            <code>MessageStructure</code> parameter to <code>json</code>
+     *            and use a JSON object for the <code>Message</code> parameter.
+     *            </p>
+     *            <p/>
+     *            <p>
+     *            Constraints:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            With the exception of SMS, messages must be UTF-8 encoded
+     *            strings and at most 256 KB in size (262,144 bytes, not 262,144
+     *            characters).
      *            </p>
+     *            </li>
+     *            <li>
      *            <p>
-     *            If you want to send the same message to all transport
-     *            protocols, include the text of the message as a String value.
+     *            For SMS, each message can contain up to 140 characters. This
+     *            character limit depends on the encoding schema. For example,
+     *            an SMS message can contain 160 GSM characters, 140 ASCII
+     *            characters, or 70 UCS-2 characters.
      *            </p>
      *            <p>
-     *            If you want to send different messages for each transport
-     *            protocol, set the value of the <code>MessageStructure</code>
-     *            parameter to <code>json</code> and use a JSON object for the
-     *            <code>Message</code> parameter. See the Examples section for
-     *            the format of the JSON object.
+     *            If you publish a message that exceeds this size limit, Amazon
+     *            SNS sends the message as multiple messages, each fitting
+     *            within the size limit. Messages aren't truncated mid-word but
+     *            are cut off at whole-word boundaries.
      *            </p>
      *            <p>
-     *            Constraints: Messages must be UTF-8 encoded strings at most
-     *            256 KB in size (262144 bytes, not 262144 characters).
+     *            The total size limit for a single SMS <code>Publish</code>
+     *            action is 1,600 characters.
      *            </p>
+     *            </li>
+     *            </ul>
      *            <p>
      *            JSON-specific constraints:
      *            </p>
@@ -2526,6 +2918,13 @@ public void unsubscribe(String subscriptionArn)
      * @throws EndpointDisabledException
      * @throws PlatformApplicationDisabledException
      * @throws AuthorizationErrorException
+     * @throws KMSDisabledException
+     * @throws KMSInvalidStateException
+     * @throws KMSNotFoundException
+     * @throws KMSOptInRequiredException
+     * @throws KMSThrottlingException
+     * @throws KMSAccessDeniedException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2544,23 +2943,28 @@ public PublishResult publish(String topicArn, String message)
 
     /**
      * <p>
-     * Sends a message to all of a topic's subscribed endpoints. When a
-     * <code>messageId</code> is returned, the message has been saved and Amazon
-     * SNS will attempt to deliver it to the topic's subscribers shortly. The
-     * format of the outgoing message to each subscribed endpoint depends on the
-     * notification protocol.
+     * Sends a message to an Amazon SNS topic or sends a text message (SMS
+     * message) directly to a phone number.
+     * </p>
+     * <p>
+     * If you send a message to a topic, Amazon SNS delivers the message to each
+     * endpoint that is subscribed to the topic. The format of the message
+     * depends on the notification protocol for each subscribed endpoint.
+     * </p>
+     * <p>
+     * When a <code>messageId</code> is returned, the message has been saved and
+     * Amazon SNS will attempt to deliver it shortly.
      * </p>
      * <p>
      * To use the <code>Publish</code> action for sending a message to a mobile
      * endpoint, such as an app on a Kindle device or mobile phone, you must
      * specify the EndpointArn for the TargetArn parameter. The EndpointArn is
      * returned when making a call with the <code>CreatePlatformEndpoint</code>
-     * action. The second example below shows a request and response for
-     * publishing to a mobile endpoint.
+     * action.
      * </p>
      * <p>
      * For more information about formatting messages, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
+     * "https://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
      * >Send Custom Platform-Specific Payloads in Messages to Mobile
      * Devices</a>.
      * </p>
@@ -2574,24 +2978,55 @@ public PublishResult publish(String topicArn, String message)
      *            <code>PhoneNumber</code> or <code>TargetArn</code> parameters.
      *            </p>
      * @param message <p>
-     *            The message you want to send to the topic.
+     *            The message you want to send.
      *            </p>
+     *            <important>
      *            <p>
-     *            If you want to send the same message to all transport
-     *            protocols, include the text of the message as a String value.
+     *            The <code>Message</code> parameter is always a string. If you
+     *            set <code>MessageStructure</code> to <code>json</code>, you
+     *            must string-encode the <code>Message</code> parameter.
      *            </p>
+     *            </important>
      *            <p>
-     *            If you want to send different messages for each transport
-     *            protocol, set the value of the <code>MessageStructure</code>
-     *            parameter to <code>json</code> and use a JSON object for the
-     *            <code>Message</code> parameter. See the Examples section for
-     *            the format of the JSON object.
+     *            If you are publishing to a topic and you want to send the same
+     *            message to all transport protocols, include the text of the
+     *            message as a String value. If you want to send different
+     *            messages for each transport protocol, set the value of the
+     *            <code>MessageStructure</code> parameter to <code>json</code>
+     *            and use a JSON object for the <code>Message</code> parameter.
+     *            </p>
+     *            <p/>
+     *            <p>
+     *            Constraints:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            With the exception of SMS, messages must be UTF-8 encoded
+     *            strings and at most 256 KB in size (262,144 bytes, not 262,144
+     *            characters).
      *            </p>
+     *            </li>
+     *            <li>
      *            <p>
-     *            Constraints: Messages must be UTF-8 encoded strings at most
-     *            256 KB in size (262144 bytes, not 262144 characters).
+     *            For SMS, each message can contain up to 140 characters. This
+     *            character limit depends on the encoding schema. For example,
+     *            an SMS message can contain 160 GSM characters, 140 ASCII
+     *            characters, or 70 UCS-2 characters.
      *            </p>
      *            <p>
+     *            If you publish a message that exceeds this size limit, Amazon
+     *            SNS sends the message as multiple messages, each fitting
+     *            within the size limit. Messages aren't truncated mid-word but
+     *            are cut off at whole-word boundaries.
+     *            </p>
+     *            <p>
+     *            The total size limit for a single SMS <code>Publish</code>
+     *            action is 1,600 characters.
+     *            </p>
+     *            </li>
+     *            </ul>
+     *            <p>
      *            JSON-specific constraints:
      *            </p>
      *            <ul>
@@ -2671,6 +3106,13 @@ public PublishResult publish(String topicArn, String message)
      * @throws EndpointDisabledException
      * @throws PlatformApplicationDisabledException
      * @throws AuthorizationErrorException
+     * @throws KMSDisabledException
+     * @throws KMSInvalidStateException
+     * @throws KMSNotFoundException
+     * @throws KMSOptInRequiredException
+     * @throws KMSThrottlingException
+     * @throws KMSAccessDeniedException
+     * @throws InvalidSecurityException
      * @throws AmazonClientException If any internal errors are encountered
      *             inside the client while attempting to make the request or
      *             handle the response. For example if a network connection is
@@ -2696,6 +3138,9 @@ public PublishResult publish(String topicArn, String message, String subject)
      * <code>NextToken</code> parameter in a new <code>ListSubscriptions</code>
      * call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @return listSubscriptionsResult The response from the ListSubscriptions
      *         service method, as returned by Amazon Simple Notification
@@ -2725,6 +3170,9 @@ public ListSubscriptionsResult listSubscriptions()
      * <code>NextToken</code> parameter in a new <code>ListSubscriptions</code>
      * call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param nextToken <p>
      *            Token returned by the previous <code>ListSubscriptions</code>
@@ -2759,6 +3207,9 @@ public ListSubscriptionsResult listSubscriptions(String nextToken)
      * <code>NextToken</code> parameter in a new
      * <code>ListSubscriptionsByTopic</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param topicArn <p>
      *            The ARN of the topic for which you wish to find subscriptions.
@@ -2793,6 +3244,9 @@ public ListSubscriptionsByTopicResult listSubscriptionsByTopic(String topicArn)
      * <code>NextToken</code> parameter in a new
      * <code>ListSubscriptionsByTopic</code> call to get further results.
      * </p>
+     * <p>
+     * This action is throttled at 30 transactions per second (TPS).
+     * </p>
      * 
      * @param topicArn <p>
      *            The ARN of the topic for which you wish to find subscriptions.
@@ -2835,9 +3289,12 @@ public ListSubscriptionsByTopicResult listSubscriptionsByTopic(String topicArn,
      * using the NextToken string received from the previous call. When there
      * are no more records to return, NextToken will be null. For more
      * information, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+     * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
      * Amazon SNS Mobile Push Notifications</a>.
      * </p>
+     * <p>
+     * This action is throttled at 15 transactions per second (TPS).
+     * </p>
      * 
      * @return listPlatformApplicationsResult The response from the
      *         ListPlatformApplications service method, as returned by Amazon
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ConcurrentAccessException.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ConcurrentAccessException.java
new file mode 100644
index 0000000000..f68ebe13d2
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ConcurrentAccessException.java
@@ -0,0 +1,38 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import com.amazonaws.AmazonServiceException;
+
+/**
+ * <p>
+ * Can't perform multiple operations on a tag simultaneously. Perform the
+ * operations sequentially.
+ * </p>
+ */
+public class ConcurrentAccessException extends AmazonServiceException {
+    private static final long serialVersionUID = 1L;
+
+    /**
+     * Constructs a new ConcurrentAccessException with the specified error
+     * message.
+     *
+     * @param message Describes the error encountered.
+     */
+    public ConcurrentAccessException(String message) {
+        super(message);
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/CreatePlatformApplicationRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/CreatePlatformApplicationRequest.java
index 77dd18cf45..6ab71d1626 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/CreatePlatformApplicationRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/CreatePlatformApplicationRequest.java
@@ -22,7 +22,7 @@
 /**
  * <p>
  * Creates a platform application object for one of the supported push
- * notification services, such as APNS and GCM, to which devices and mobile apps
+ * notification services, such as APNS and FCM, to which devices and mobile apps
  * may register. You must specify PlatformPrincipal and PlatformCredential
  * attributes when using the <code>CreatePlatformApplication</code> action. The
  * PlatformPrincipal is received from the notification service. For
@@ -41,21 +41,21 @@
  * "secret key". The PlatformApplicationArn that is returned when using
  * <code>CreatePlatformApplication</code> is then used as an attribute for the
  * <code>CreatePlatformEndpoint</code> action. For more information, see <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+ * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
  * Amazon SNS Mobile Push Notifications</a>. For more information about
  * obtaining the PlatformPrincipal and PlatformCredential for each of the
  * supported push notification services, see <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-apns.html">Getting
- * Started with Apple Push Notification Service</a>, <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-adm.html">Getting
+ * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-apns.html"
+ * >Getting Started with Apple Push Notification Service</a>, <a
+ * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-adm.html">Getting
  * Started with Amazon Device Messaging</a>, <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-baidu.html"
+ * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-baidu.html"
  * >Getting Started with Baidu Cloud Push</a>, <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-gcm.html">Getting
+ * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-gcm.html">Getting
  * Started with Google Cloud Messaging for Android</a>, <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-mpns.html">Getting
- * Started with MPNS</a>, or <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/mobile-push-wns.html">Getting
+ * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-mpns.html"
+ * >Getting Started with MPNS</a>, or <a
+ * href="https://docs.aws.amazon.com/sns/latest/dg/mobile-push-wns.html">Getting
  * Started with WNS</a>.
  * </p>
  */
@@ -82,7 +82,7 @@
     /**
      * <p>
      * For a list of attributes, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
      * >SetPlatformApplicationAttributes</a>
      * </p>
      */
@@ -205,13 +205,13 @@ public CreatePlatformApplicationRequest withPlatform(String platform) {
     /**
      * <p>
      * For a list of attributes, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
      * >SetPlatformApplicationAttributes</a>
      * </p>
      *
      * @return <p>
      *         For a list of attributes, see <a href=
-     *         "http://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
+     *         "https://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
      *         >SetPlatformApplicationAttributes</a>
      *         </p>
      */
@@ -222,13 +222,13 @@ public CreatePlatformApplicationRequest withPlatform(String platform) {
     /**
      * <p>
      * For a list of attributes, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
      * >SetPlatformApplicationAttributes</a>
      * </p>
      *
      * @param attributes <p>
      *            For a list of attributes, see <a href=
-     *            "http://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
+     *            "https://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
      *            >SetPlatformApplicationAttributes</a>
      *            </p>
      */
@@ -239,7 +239,7 @@ public void setAttributes(java.util.Map<String, String> attributes) {
     /**
      * <p>
      * For a list of attributes, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
      * >SetPlatformApplicationAttributes</a>
      * </p>
      * <p>
@@ -248,7 +248,7 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *
      * @param attributes <p>
      *            For a list of attributes, see <a href=
-     *            "http://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
+     *            "https://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
      *            >SetPlatformApplicationAttributes</a>
      *            </p>
      * @return A reference to this updated object so that method calls can be
@@ -262,7 +262,7 @@ public CreatePlatformApplicationRequest withAttributes(java.util.Map<String, Str
     /**
      * <p>
      * For a list of attributes, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetPlatformApplicationAttributes.html"
      * >SetPlatformApplicationAttributes</a>
      * </p>
      * <p>
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/CreatePlatformEndpointRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/CreatePlatformEndpointRequest.java
index 6c55e28f54..1c46667e5e 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/CreatePlatformEndpointRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/CreatePlatformEndpointRequest.java
@@ -32,14 +32,14 @@
  * already owns an endpoint with the same device token and attributes, that
  * endpoint's ARN is returned without creating a new endpoint. For more
  * information, see <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+ * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
  * Amazon SNS Mobile Push Notifications</a>.
  * </p>
  * <p>
  * When using <code>CreatePlatformEndpoint</code> with Baidu, two attributes
  * must be provided: ChannelId and UserId. The token field must also contain the
  * ChannelId. For more information, see <a href=
- * "http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePushBaiduEndpoint.html"
+ * "https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePushBaiduEndpoint.html"
  * >Creating an Amazon SNS Endpoint for Baidu</a>.
  * </p>
  */
@@ -75,7 +75,7 @@
     /**
      * <p>
      * For a list of attributes, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
      * >SetEndpointAttributes</a>.
      * </p>
      */
@@ -264,13 +264,13 @@ public CreatePlatformEndpointRequest withCustomUserData(String customUserData) {
     /**
      * <p>
      * For a list of attributes, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
      * >SetEndpointAttributes</a>.
      * </p>
      *
      * @return <p>
      *         For a list of attributes, see <a href=
-     *         "http://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
+     *         "https://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
      *         >SetEndpointAttributes</a>.
      *         </p>
      */
@@ -281,13 +281,13 @@ public CreatePlatformEndpointRequest withCustomUserData(String customUserData) {
     /**
      * <p>
      * For a list of attributes, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
      * >SetEndpointAttributes</a>.
      * </p>
      *
      * @param attributes <p>
      *            For a list of attributes, see <a href=
-     *            "http://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
+     *            "https://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
      *            >SetEndpointAttributes</a>.
      *            </p>
      */
@@ -298,7 +298,7 @@ public void setAttributes(java.util.Map<String, String> attributes) {
     /**
      * <p>
      * For a list of attributes, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
      * >SetEndpointAttributes</a>.
      * </p>
      * <p>
@@ -307,7 +307,7 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *
      * @param attributes <p>
      *            For a list of attributes, see <a href=
-     *            "http://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
+     *            "https://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
      *            >SetEndpointAttributes</a>.
      *            </p>
      * @return A reference to this updated object so that method calls can be
@@ -321,7 +321,7 @@ public CreatePlatformEndpointRequest withAttributes(java.util.Map<String, String
     /**
      * <p>
      * For a list of attributes, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetEndpointAttributes.html"
      * >SetEndpointAttributes</a>.
      * </p>
      * <p>
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/CreateTopicRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/CreateTopicRequest.java
index d38b2aeb70..56d4dc6ace 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/CreateTopicRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/CreateTopicRequest.java
@@ -23,7 +23,7 @@
  * <p>
  * Creates a topic to which notifications can be published. Users can create at
  * most 100,000 topics. For more information, see <a
- * href="http://aws.amazon.com/sns/">http://aws.amazon.com/sns</a>. This action
+ * href="http://aws.amazon.com/sns/">https://aws.amazon.com/sns</a>. This action
  * is idempotent, so if the requester already owns a topic with the specified
  * name, that topic's ARN is returned without creating a new topic.
  * </p>
@@ -41,6 +41,62 @@
      */
     private String name;
 
+    /**
+     * <p>
+     * A map of attributes with their corresponding values.
+     * </p>
+     * <p>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>CreateTopic</code> action uses:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>DisplayName</code> – The display name to use for a topic with SMS
+     * subscriptions.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>Policy</code> – The policy that defines who can access your topic.
+     * By default, only the topic owner can publish or subscribe to the topic.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * The following attribute applies only to <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     * >server-side-encryption</a>:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>KmsMasterKeyId</code> - The ID of an AWS-managed customer master
+     * key (CMK) for Amazon SNS or a custom CMK. For more information, see <a
+     * href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     * >Key Terms</a>. For more examples, see <a href=
+     * "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     * >KeyId</a> in the <i>AWS Key Management Service API Reference</i>.
+     * </p>
+     * </li>
+     * </ul>
+     */
+    private java.util.Map<String, String> attributes = new java.util.HashMap<String, String>();
+
+    /**
+     * <p>
+     * The list of tags to add to a new topic.
+     * </p>
+     */
+    private java.util.List<Tag> tags = new java.util.ArrayList<Tag>();
+
     /**
      * Default constructor for CreateTopicRequest object. Callers should use the
      * setter or fluent setter (with...) methods to initialize any additional
@@ -142,6 +198,465 @@ public CreateTopicRequest withName(String name) {
         return this;
     }
 
+    /**
+     * <p>
+     * A map of attributes with their corresponding values.
+     * </p>
+     * <p>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>CreateTopic</code> action uses:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>DisplayName</code> – The display name to use for a topic with SMS
+     * subscriptions.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>Policy</code> – The policy that defines who can access your topic.
+     * By default, only the topic owner can publish or subscribe to the topic.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * The following attribute applies only to <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     * >server-side-encryption</a>:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>KmsMasterKeyId</code> - The ID of an AWS-managed customer master
+     * key (CMK) for Amazon SNS or a custom CMK. For more information, see <a
+     * href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     * >Key Terms</a>. For more examples, see <a href=
+     * "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     * >KeyId</a> in the <i>AWS Key Management Service API Reference</i>.
+     * </p>
+     * </li>
+     * </ul>
+     *
+     * @return <p>
+     *         A map of attributes with their corresponding values.
+     *         </p>
+     *         <p>
+     *         The following lists the names, descriptions, and values of the
+     *         special request parameters that the <code>CreateTopic</code>
+     *         action uses:
+     *         </p>
+     *         <ul>
+     *         <li>
+     *         <p>
+     *         <code>DeliveryPolicy</code> – The policy that defines how Amazon
+     *         SNS retries failed deliveries to HTTP/S endpoints.
+     *         </p>
+     *         </li>
+     *         <li>
+     *         <p>
+     *         <code>DisplayName</code> – The display name to use for a topic
+     *         with SMS subscriptions.
+     *         </p>
+     *         </li>
+     *         <li>
+     *         <p>
+     *         <code>Policy</code> – The policy that defines who can access your
+     *         topic. By default, only the topic owner can publish or subscribe
+     *         to the topic.
+     *         </p>
+     *         </li>
+     *         </ul>
+     *         <p>
+     *         The following attribute applies only to <a href=
+     *         "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     *         >server-side-encryption</a>:
+     *         </p>
+     *         <ul>
+     *         <li>
+     *         <p>
+     *         <code>KmsMasterKeyId</code> - The ID of an AWS-managed customer
+     *         master key (CMK) for Amazon SNS or a custom CMK. For more
+     *         information, see <a href=
+     *         "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     *         >Key Terms</a>. For more examples, see <a href=
+     *         "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     *         >KeyId</a> in the <i>AWS Key Management Service API
+     *         Reference</i>.
+     *         </p>
+     *         </li>
+     *         </ul>
+     */
+    public java.util.Map<String, String> getAttributes() {
+        return attributes;
+    }
+
+    /**
+     * <p>
+     * A map of attributes with their corresponding values.
+     * </p>
+     * <p>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>CreateTopic</code> action uses:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>DisplayName</code> – The display name to use for a topic with SMS
+     * subscriptions.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>Policy</code> – The policy that defines who can access your topic.
+     * By default, only the topic owner can publish or subscribe to the topic.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * The following attribute applies only to <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     * >server-side-encryption</a>:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>KmsMasterKeyId</code> - The ID of an AWS-managed customer master
+     * key (CMK) for Amazon SNS or a custom CMK. For more information, see <a
+     * href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     * >Key Terms</a>. For more examples, see <a href=
+     * "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     * >KeyId</a> in the <i>AWS Key Management Service API Reference</i>.
+     * </p>
+     * </li>
+     * </ul>
+     *
+     * @param attributes <p>
+     *            A map of attributes with their corresponding values.
+     *            </p>
+     *            <p>
+     *            The following lists the names, descriptions, and values of the
+     *            special request parameters that the <code>CreateTopic</code>
+     *            action uses:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>DeliveryPolicy</code> – The policy that defines how
+     *            Amazon SNS retries failed deliveries to HTTP/S endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>DisplayName</code> – The display name to use for a topic
+     *            with SMS subscriptions.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>Policy</code> – The policy that defines who can access
+     *            your topic. By default, only the topic owner can publish or
+     *            subscribe to the topic.
+     *            </p>
+     *            </li>
+     *            </ul>
+     *            <p>
+     *            The following attribute applies only to <a href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     *            >server-side-encryption</a>:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>KmsMasterKeyId</code> - The ID of an AWS-managed
+     *            customer master key (CMK) for Amazon SNS or a custom CMK. For
+     *            more information, see <a href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     *            >Key Terms</a>. For more examples, see <a href=
+     *            "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     *            >KeyId</a> in the <i>AWS Key Management Service API
+     *            Reference</i>.
+     *            </p>
+     *            </li>
+     *            </ul>
+     */
+    public void setAttributes(java.util.Map<String, String> attributes) {
+        this.attributes = attributes;
+    }
+
+    /**
+     * <p>
+     * A map of attributes with their corresponding values.
+     * </p>
+     * <p>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>CreateTopic</code> action uses:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>DisplayName</code> – The display name to use for a topic with SMS
+     * subscriptions.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>Policy</code> – The policy that defines who can access your topic.
+     * By default, only the topic owner can publish or subscribe to the topic.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * The following attribute applies only to <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     * >server-side-encryption</a>:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>KmsMasterKeyId</code> - The ID of an AWS-managed customer master
+     * key (CMK) for Amazon SNS or a custom CMK. For more information, see <a
+     * href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     * >Key Terms</a>. For more examples, see <a href=
+     * "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     * >KeyId</a> in the <i>AWS Key Management Service API Reference</i>.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     *
+     * @param attributes <p>
+     *            A map of attributes with their corresponding values.
+     *            </p>
+     *            <p>
+     *            The following lists the names, descriptions, and values of the
+     *            special request parameters that the <code>CreateTopic</code>
+     *            action uses:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>DeliveryPolicy</code> – The policy that defines how
+     *            Amazon SNS retries failed deliveries to HTTP/S endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>DisplayName</code> – The display name to use for a topic
+     *            with SMS subscriptions.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>Policy</code> – The policy that defines who can access
+     *            your topic. By default, only the topic owner can publish or
+     *            subscribe to the topic.
+     *            </p>
+     *            </li>
+     *            </ul>
+     *            <p>
+     *            The following attribute applies only to <a href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     *            >server-side-encryption</a>:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>KmsMasterKeyId</code> - The ID of an AWS-managed
+     *            customer master key (CMK) for Amazon SNS or a custom CMK. For
+     *            more information, see <a href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     *            >Key Terms</a>. For more examples, see <a href=
+     *            "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     *            >KeyId</a> in the <i>AWS Key Management Service API
+     *            Reference</i>.
+     *            </p>
+     *            </li>
+     *            </ul>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public CreateTopicRequest withAttributes(java.util.Map<String, String> attributes) {
+        this.attributes = attributes;
+        return this;
+    }
+
+    /**
+     * <p>
+     * A map of attributes with their corresponding values.
+     * </p>
+     * <p>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>CreateTopic</code> action uses:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>DisplayName</code> – The display name to use for a topic with SMS
+     * subscriptions.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>Policy</code> – The policy that defines who can access your topic.
+     * By default, only the topic owner can publish or subscribe to the topic.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * The following attribute applies only to <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     * >server-side-encryption</a>:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>KmsMasterKeyId</code> - The ID of an AWS-managed customer master
+     * key (CMK) for Amazon SNS or a custom CMK. For more information, see <a
+     * href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     * >Key Terms</a>. For more examples, see <a href=
+     * "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     * >KeyId</a> in the <i>AWS Key Management Service API Reference</i>.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * The method adds a new key-value pair into Attributes parameter, and
+     * returns a reference to this object so that method calls can be chained
+     * together.
+     *
+     * @param key The key of the entry to be added into Attributes.
+     * @param value The corresponding value of the entry to be added into
+     *            Attributes.
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public CreateTopicRequest addAttributesEntry(String key, String value) {
+        if (null == this.attributes) {
+            this.attributes = new java.util.HashMap<String, String>();
+        }
+        if (this.attributes.containsKey(key))
+            throw new IllegalArgumentException("Duplicated keys (" + key.toString()
+                    + ") are provided.");
+        this.attributes.put(key, value);
+        return this;
+    }
+
+    /**
+     * Removes all the entries added into Attributes.
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     */
+    public CreateTopicRequest clearAttributesEntries() {
+        this.attributes = null;
+        return this;
+    }
+
+    /**
+     * <p>
+     * The list of tags to add to a new topic.
+     * </p>
+     *
+     * @return <p>
+     *         The list of tags to add to a new topic.
+     *         </p>
+     */
+    public java.util.List<Tag> getTags() {
+        return tags;
+    }
+
+    /**
+     * <p>
+     * The list of tags to add to a new topic.
+     * </p>
+     *
+     * @param tags <p>
+     *            The list of tags to add to a new topic.
+     *            </p>
+     */
+    public void setTags(java.util.Collection<Tag> tags) {
+        if (tags == null) {
+            this.tags = null;
+            return;
+        }
+
+        this.tags = new java.util.ArrayList<Tag>(tags);
+    }
+
+    /**
+     * <p>
+     * The list of tags to add to a new topic.
+     * </p>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     *
+     * @param tags <p>
+     *            The list of tags to add to a new topic.
+     *            </p>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public CreateTopicRequest withTags(Tag... tags) {
+        if (getTags() == null) {
+            this.tags = new java.util.ArrayList<Tag>(tags.length);
+        }
+        for (Tag value : tags) {
+            this.tags.add(value);
+        }
+        return this;
+    }
+
+    /**
+     * <p>
+     * The list of tags to add to a new topic.
+     * </p>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     *
+     * @param tags <p>
+     *            The list of tags to add to a new topic.
+     *            </p>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public CreateTopicRequest withTags(java.util.Collection<Tag> tags) {
+        setTags(tags);
+        return this;
+    }
+
     /**
      * Returns a string representation of this object; useful for testing and
      * debugging.
@@ -154,7 +669,11 @@ public String toString() {
         StringBuilder sb = new StringBuilder();
         sb.append("{");
         if (getName() != null)
-            sb.append("Name: " + getName());
+            sb.append("Name: " + getName() + ",");
+        if (getAttributes() != null)
+            sb.append("Attributes: " + getAttributes() + ",");
+        if (getTags() != null)
+            sb.append("Tags: " + getTags());
         sb.append("}");
         return sb.toString();
     }
@@ -165,6 +684,8 @@ public int hashCode() {
         int hashCode = 1;
 
         hashCode = prime * hashCode + ((getName() == null) ? 0 : getName().hashCode());
+        hashCode = prime * hashCode + ((getAttributes() == null) ? 0 : getAttributes().hashCode());
+        hashCode = prime * hashCode + ((getTags() == null) ? 0 : getTags().hashCode());
         return hashCode;
     }
 
@@ -183,6 +704,15 @@ public boolean equals(Object obj) {
             return false;
         if (other.getName() != null && other.getName().equals(this.getName()) == false)
             return false;
+        if (other.getAttributes() == null ^ this.getAttributes() == null)
+            return false;
+        if (other.getAttributes() != null
+                && other.getAttributes().equals(this.getAttributes()) == false)
+            return false;
+        if (other.getTags() == null ^ this.getTags() == null)
+            return false;
+        if (other.getTags() != null && other.getTags().equals(this.getTags()) == false)
+            return false;
         return true;
     }
 }
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/DeleteEndpointRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/DeleteEndpointRequest.java
index a42ddb5410..5ecdbb1a51 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/DeleteEndpointRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/DeleteEndpointRequest.java
@@ -23,7 +23,7 @@
  * <p>
  * Deletes the endpoint for a device and mobile app from Amazon SNS. This action
  * is idempotent. For more information, see <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+ * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
  * Amazon SNS Mobile Push Notifications</a>.
  * </p>
  * <p>
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/DeletePlatformApplicationRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/DeletePlatformApplicationRequest.java
index 85b8507ec9..efa5a46839 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/DeletePlatformApplicationRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/DeletePlatformApplicationRequest.java
@@ -23,7 +23,7 @@
  * <p>
  * Deletes a platform application object for one of the supported push
  * notification services, such as APNS and GCM. For more information, see <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+ * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
  * Amazon SNS Mobile Push Notifications</a>.
  * </p>
  */
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/FilterPolicyLimitExceededException.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/FilterPolicyLimitExceededException.java
new file mode 100644
index 0000000000..1b18469efe
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/FilterPolicyLimitExceededException.java
@@ -0,0 +1,39 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import com.amazonaws.AmazonServiceException;
+
+/**
+ * <p>
+ * Indicates that the number of filter polices in your AWS account exceeds the
+ * limit. To add more filter polices, submit an SNS Limit Increase case in the
+ * AWS Support Center.
+ * </p>
+ */
+public class FilterPolicyLimitExceededException extends AmazonServiceException {
+    private static final long serialVersionUID = 1L;
+
+    /**
+     * Constructs a new FilterPolicyLimitExceededException with the specified
+     * error message.
+     *
+     * @param message Describes the error encountered.
+     */
+    public FilterPolicyLimitExceededException(String message) {
+        super(message);
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetEndpointAttributesRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetEndpointAttributesRequest.java
index 758af2eace..b9dbc9f24d 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetEndpointAttributesRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetEndpointAttributesRequest.java
@@ -23,7 +23,7 @@
  * <p>
  * Retrieves the endpoint attributes for a device on one of the supported push
  * notification services, such as GCM and APNS. For more information, see <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+ * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
  * Amazon SNS Mobile Push Notifications</a>.
  * </p>
  */
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetEndpointAttributesResult.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetEndpointAttributesResult.java
index beb36f3cf8..7587609db5 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetEndpointAttributesResult.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetEndpointAttributesResult.java
@@ -30,14 +30,14 @@
      * <ul>
      * <li>
      * <p>
-     * <code>CustomUserData</code> -- arbitrary user data to associate with the
+     * <code>CustomUserData</code> – arbitrary user data to associate with the
      * endpoint. Amazon SNS does not use this data. The data must be in UTF-8
      * format and less than 2KB.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Enabled</code> -- flag that enables/disables delivery to the
+     * <code>Enabled</code> – flag that enables/disables delivery to the
      * endpoint. Amazon SNS will set this to false when a notification service
      * indicates to Amazon SNS that the endpoint is invalid. Users can set it
      * back to true, typically after updating Token.
@@ -45,8 +45,8 @@
      * </li>
      * <li>
      * <p>
-     * <code>Token</code> -- device token, also referred to as a registration
-     * id, for an app and mobile device. This is returned from the notification
+     * <code>Token</code> – device token, also referred to as a registration id,
+     * for an app and mobile device. This is returned from the notification
      * service when an app and mobile device are registered with the
      * notification service.
      * </p>
@@ -62,14 +62,14 @@
      * <ul>
      * <li>
      * <p>
-     * <code>CustomUserData</code> -- arbitrary user data to associate with the
+     * <code>CustomUserData</code> – arbitrary user data to associate with the
      * endpoint. Amazon SNS does not use this data. The data must be in UTF-8
      * format and less than 2KB.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Enabled</code> -- flag that enables/disables delivery to the
+     * <code>Enabled</code> – flag that enables/disables delivery to the
      * endpoint. Amazon SNS will set this to false when a notification service
      * indicates to Amazon SNS that the endpoint is invalid. Users can set it
      * back to true, typically after updating Token.
@@ -77,8 +77,8 @@
      * </li>
      * <li>
      * <p>
-     * <code>Token</code> -- device token, also referred to as a registration
-     * id, for an app and mobile device. This is returned from the notification
+     * <code>Token</code> – device token, also referred to as a registration id,
+     * for an app and mobile device. This is returned from the notification
      * service when an app and mobile device are registered with the
      * notification service.
      * </p>
@@ -91,23 +91,22 @@
      *         <ul>
      *         <li>
      *         <p>
-     *         <code>CustomUserData</code> -- arbitrary user data to associate
+     *         <code>CustomUserData</code> – arbitrary user data to associate
      *         with the endpoint. Amazon SNS does not use this data. The data
      *         must be in UTF-8 format and less than 2KB.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>Enabled</code> -- flag that enables/disables delivery to
-     *         the endpoint. Amazon SNS will set this to false when a
-     *         notification service indicates to Amazon SNS that the endpoint is
-     *         invalid. Users can set it back to true, typically after updating
-     *         Token.
+     *         <code>Enabled</code> – flag that enables/disables delivery to the
+     *         endpoint. Amazon SNS will set this to false when a notification
+     *         service indicates to Amazon SNS that the endpoint is invalid.
+     *         Users can set it back to true, typically after updating Token.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>Token</code> -- device token, also referred to as a
+     *         <code>Token</code> – device token, also referred to as a
      *         registration id, for an app and mobile device. This is returned
      *         from the notification service when an app and mobile device are
      *         registered with the notification service.
@@ -126,14 +125,14 @@
      * <ul>
      * <li>
      * <p>
-     * <code>CustomUserData</code> -- arbitrary user data to associate with the
+     * <code>CustomUserData</code> – arbitrary user data to associate with the
      * endpoint. Amazon SNS does not use this data. The data must be in UTF-8
      * format and less than 2KB.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Enabled</code> -- flag that enables/disables delivery to the
+     * <code>Enabled</code> – flag that enables/disables delivery to the
      * endpoint. Amazon SNS will set this to false when a notification service
      * indicates to Amazon SNS that the endpoint is invalid. Users can set it
      * back to true, typically after updating Token.
@@ -141,8 +140,8 @@
      * </li>
      * <li>
      * <p>
-     * <code>Token</code> -- device token, also referred to as a registration
-     * id, for an app and mobile device. This is returned from the notification
+     * <code>Token</code> – device token, also referred to as a registration id,
+     * for an app and mobile device. This is returned from the notification
      * service when an app and mobile device are registered with the
      * notification service.
      * </p>
@@ -155,14 +154,14 @@
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>CustomUserData</code> -- arbitrary user data to
-     *            associate with the endpoint. Amazon SNS does not use this
-     *            data. The data must be in UTF-8 format and less than 2KB.
+     *            <code>CustomUserData</code> – arbitrary user data to associate
+     *            with the endpoint. Amazon SNS does not use this data. The data
+     *            must be in UTF-8 format and less than 2KB.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Enabled</code> -- flag that enables/disables delivery to
+     *            <code>Enabled</code> – flag that enables/disables delivery to
      *            the endpoint. Amazon SNS will set this to false when a
      *            notification service indicates to Amazon SNS that the endpoint
      *            is invalid. Users can set it back to true, typically after
@@ -171,7 +170,7 @@
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Token</code> -- device token, also referred to as a
+     *            <code>Token</code> – device token, also referred to as a
      *            registration id, for an app and mobile device. This is
      *            returned from the notification service when an app and mobile
      *            device are registered with the notification service.
@@ -190,14 +189,14 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      * <ul>
      * <li>
      * <p>
-     * <code>CustomUserData</code> -- arbitrary user data to associate with the
+     * <code>CustomUserData</code> – arbitrary user data to associate with the
      * endpoint. Amazon SNS does not use this data. The data must be in UTF-8
      * format and less than 2KB.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Enabled</code> -- flag that enables/disables delivery to the
+     * <code>Enabled</code> – flag that enables/disables delivery to the
      * endpoint. Amazon SNS will set this to false when a notification service
      * indicates to Amazon SNS that the endpoint is invalid. Users can set it
      * back to true, typically after updating Token.
@@ -205,8 +204,8 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      * </li>
      * <li>
      * <p>
-     * <code>Token</code> -- device token, also referred to as a registration
-     * id, for an app and mobile device. This is returned from the notification
+     * <code>Token</code> – device token, also referred to as a registration id,
+     * for an app and mobile device. This is returned from the notification
      * service when an app and mobile device are registered with the
      * notification service.
      * </p>
@@ -222,14 +221,14 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>CustomUserData</code> -- arbitrary user data to
-     *            associate with the endpoint. Amazon SNS does not use this
-     *            data. The data must be in UTF-8 format and less than 2KB.
+     *            <code>CustomUserData</code> – arbitrary user data to associate
+     *            with the endpoint. Amazon SNS does not use this data. The data
+     *            must be in UTF-8 format and less than 2KB.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Enabled</code> -- flag that enables/disables delivery to
+     *            <code>Enabled</code> – flag that enables/disables delivery to
      *            the endpoint. Amazon SNS will set this to false when a
      *            notification service indicates to Amazon SNS that the endpoint
      *            is invalid. Users can set it back to true, typically after
@@ -238,7 +237,7 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Token</code> -- device token, also referred to as a
+     *            <code>Token</code> – device token, also referred to as a
      *            registration id, for an app and mobile device. This is
      *            returned from the notification service when an app and mobile
      *            device are registered with the notification service.
@@ -260,14 +259,14 @@ public GetEndpointAttributesResult withAttributes(java.util.Map<String, String>
      * <ul>
      * <li>
      * <p>
-     * <code>CustomUserData</code> -- arbitrary user data to associate with the
+     * <code>CustomUserData</code> – arbitrary user data to associate with the
      * endpoint. Amazon SNS does not use this data. The data must be in UTF-8
      * format and less than 2KB.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Enabled</code> -- flag that enables/disables delivery to the
+     * <code>Enabled</code> – flag that enables/disables delivery to the
      * endpoint. Amazon SNS will set this to false when a notification service
      * indicates to Amazon SNS that the endpoint is invalid. Users can set it
      * back to true, typically after updating Token.
@@ -275,8 +274,8 @@ public GetEndpointAttributesResult withAttributes(java.util.Map<String, String>
      * </li>
      * <li>
      * <p>
-     * <code>Token</code> -- device token, also referred to as a registration
-     * id, for an app and mobile device. This is returned from the notification
+     * <code>Token</code> – device token, also referred to as a registration id,
+     * for an app and mobile device. This is returned from the notification
      * service when an app and mobile device are registered with the
      * notification service.
      * </p>
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetPlatformApplicationAttributesRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetPlatformApplicationAttributesRequest.java
index 34f161d1d7..c81f914a1b 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetPlatformApplicationAttributesRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetPlatformApplicationAttributesRequest.java
@@ -23,7 +23,7 @@
  * <p>
  * Retrieves the attributes of the platform application object for the supported
  * push notification services, such as APNS and GCM. For more information, see
- * <a href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+ * <a href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
  * Amazon SNS Mobile Push Notifications</a>.
  * </p>
  */
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetPlatformApplicationAttributesResult.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetPlatformApplicationAttributesResult.java
index 2d3b6dee6d..c81b011838 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetPlatformApplicationAttributesResult.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetPlatformApplicationAttributesResult.java
@@ -30,25 +30,25 @@
      * <ul>
      * <li>
      * <p>
-     * <code>EventEndpointCreated</code> -- Topic ARN to which EndpointCreated
+     * <code>EventEndpointCreated</code> – Topic ARN to which EndpointCreated
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointDeleted</code> -- Topic ARN to which EndpointDeleted
+     * <code>EventEndpointDeleted</code> – Topic ARN to which EndpointDeleted
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointUpdated</code> -- Topic ARN to which EndpointUpdate
+     * <code>EventEndpointUpdated</code> – Topic ARN to which EndpointUpdate
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventDeliveryFailure</code> -- Topic ARN to which DeliveryFailure
+     * <code>EventDeliveryFailure</code> – Topic ARN to which DeliveryFailure
      * event notifications should be sent upon Direct Publish delivery failure
      * (permanent) to one of the application's endpoints.
      * </p>
@@ -64,25 +64,25 @@
      * <ul>
      * <li>
      * <p>
-     * <code>EventEndpointCreated</code> -- Topic ARN to which EndpointCreated
+     * <code>EventEndpointCreated</code> – Topic ARN to which EndpointCreated
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointDeleted</code> -- Topic ARN to which EndpointDeleted
+     * <code>EventEndpointDeleted</code> – Topic ARN to which EndpointDeleted
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointUpdated</code> -- Topic ARN to which EndpointUpdate
+     * <code>EventEndpointUpdated</code> – Topic ARN to which EndpointUpdate
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventDeliveryFailure</code> -- Topic ARN to which DeliveryFailure
+     * <code>EventDeliveryFailure</code> – Topic ARN to which DeliveryFailure
      * event notifications should be sent upon Direct Publish delivery failure
      * (permanent) to one of the application's endpoints.
      * </p>
@@ -95,25 +95,25 @@
      *         <ul>
      *         <li>
      *         <p>
-     *         <code>EventEndpointCreated</code> -- Topic ARN to which
+     *         <code>EventEndpointCreated</code> – Topic ARN to which
      *         EndpointCreated event notifications should be sent.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>EventEndpointDeleted</code> -- Topic ARN to which
+     *         <code>EventEndpointDeleted</code> – Topic ARN to which
      *         EndpointDeleted event notifications should be sent.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>EventEndpointUpdated</code> -- Topic ARN to which
+     *         <code>EventEndpointUpdated</code> – Topic ARN to which
      *         EndpointUpdate event notifications should be sent.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>EventDeliveryFailure</code> -- Topic ARN to which
+     *         <code>EventDeliveryFailure</code> – Topic ARN to which
      *         DeliveryFailure event notifications should be sent upon Direct
      *         Publish delivery failure (permanent) to one of the application's
      *         endpoints.
@@ -132,25 +132,25 @@
      * <ul>
      * <li>
      * <p>
-     * <code>EventEndpointCreated</code> -- Topic ARN to which EndpointCreated
+     * <code>EventEndpointCreated</code> – Topic ARN to which EndpointCreated
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointDeleted</code> -- Topic ARN to which EndpointDeleted
+     * <code>EventEndpointDeleted</code> – Topic ARN to which EndpointDeleted
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointUpdated</code> -- Topic ARN to which EndpointUpdate
+     * <code>EventEndpointUpdated</code> – Topic ARN to which EndpointUpdate
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventDeliveryFailure</code> -- Topic ARN to which DeliveryFailure
+     * <code>EventDeliveryFailure</code> – Topic ARN to which DeliveryFailure
      * event notifications should be sent upon Direct Publish delivery failure
      * (permanent) to one of the application's endpoints.
      * </p>
@@ -163,25 +163,25 @@
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>EventEndpointCreated</code> -- Topic ARN to which
+     *            <code>EventEndpointCreated</code> – Topic ARN to which
      *            EndpointCreated event notifications should be sent.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EventEndpointDeleted</code> -- Topic ARN to which
+     *            <code>EventEndpointDeleted</code> – Topic ARN to which
      *            EndpointDeleted event notifications should be sent.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EventEndpointUpdated</code> -- Topic ARN to which
+     *            <code>EventEndpointUpdated</code> – Topic ARN to which
      *            EndpointUpdate event notifications should be sent.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EventDeliveryFailure</code> -- Topic ARN to which
+     *            <code>EventDeliveryFailure</code> – Topic ARN to which
      *            DeliveryFailure event notifications should be sent upon Direct
      *            Publish delivery failure (permanent) to one of the
      *            application's endpoints.
@@ -200,25 +200,25 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      * <ul>
      * <li>
      * <p>
-     * <code>EventEndpointCreated</code> -- Topic ARN to which EndpointCreated
+     * <code>EventEndpointCreated</code> – Topic ARN to which EndpointCreated
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointDeleted</code> -- Topic ARN to which EndpointDeleted
+     * <code>EventEndpointDeleted</code> – Topic ARN to which EndpointDeleted
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointUpdated</code> -- Topic ARN to which EndpointUpdate
+     * <code>EventEndpointUpdated</code> – Topic ARN to which EndpointUpdate
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventDeliveryFailure</code> -- Topic ARN to which DeliveryFailure
+     * <code>EventDeliveryFailure</code> – Topic ARN to which DeliveryFailure
      * event notifications should be sent upon Direct Publish delivery failure
      * (permanent) to one of the application's endpoints.
      * </p>
@@ -234,25 +234,25 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>EventEndpointCreated</code> -- Topic ARN to which
+     *            <code>EventEndpointCreated</code> – Topic ARN to which
      *            EndpointCreated event notifications should be sent.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EventEndpointDeleted</code> -- Topic ARN to which
+     *            <code>EventEndpointDeleted</code> – Topic ARN to which
      *            EndpointDeleted event notifications should be sent.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EventEndpointUpdated</code> -- Topic ARN to which
+     *            <code>EventEndpointUpdated</code> – Topic ARN to which
      *            EndpointUpdate event notifications should be sent.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EventDeliveryFailure</code> -- Topic ARN to which
+     *            <code>EventDeliveryFailure</code> – Topic ARN to which
      *            DeliveryFailure event notifications should be sent upon Direct
      *            Publish delivery failure (permanent) to one of the
      *            application's endpoints.
@@ -275,25 +275,25 @@ public GetPlatformApplicationAttributesResult withAttributes(
      * <ul>
      * <li>
      * <p>
-     * <code>EventEndpointCreated</code> -- Topic ARN to which EndpointCreated
+     * <code>EventEndpointCreated</code> – Topic ARN to which EndpointCreated
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointDeleted</code> -- Topic ARN to which EndpointDeleted
+     * <code>EventEndpointDeleted</code> – Topic ARN to which EndpointDeleted
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointUpdated</code> -- Topic ARN to which EndpointUpdate
+     * <code>EventEndpointUpdated</code> – Topic ARN to which EndpointUpdate
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventDeliveryFailure</code> -- Topic ARN to which DeliveryFailure
+     * <code>EventDeliveryFailure</code> – Topic ARN to which DeliveryFailure
      * event notifications should be sent upon Direct Publish delivery failure
      * (permanent) to one of the application's endpoints.
      * </p>
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetSMSAttributesRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetSMSAttributesRequest.java
index 652d36505e..aa0df3a26c 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetSMSAttributesRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetSMSAttributesRequest.java
@@ -35,7 +35,7 @@
      * </p>
      * <p>
      * For all attribute names, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
      * >SetSMSAttributes</a>.
      * </p>
      * <p>
@@ -51,7 +51,7 @@
      * </p>
      * <p>
      * For all attribute names, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
      * >SetSMSAttributes</a>.
      * </p>
      * <p>
@@ -64,7 +64,7 @@
      *         </p>
      *         <p>
      *         For all attribute names, see <a href=
-     *         "http://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
+     *         "https://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
      *         >SetSMSAttributes</a>.
      *         </p>
      *         <p>
@@ -83,7 +83,7 @@
      * </p>
      * <p>
      * For all attribute names, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
      * >SetSMSAttributes</a>.
      * </p>
      * <p>
@@ -96,7 +96,7 @@
      *            </p>
      *            <p>
      *            For all attribute names, see <a href=
-     *            "http://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
+     *            "https://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
      *            >SetSMSAttributes</a>.
      *            </p>
      *            <p>
@@ -120,7 +120,7 @@ public void setAttributes(java.util.Collection<String> attributes) {
      * </p>
      * <p>
      * For all attribute names, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
      * >SetSMSAttributes</a>.
      * </p>
      * <p>
@@ -136,7 +136,7 @@ public void setAttributes(java.util.Collection<String> attributes) {
      *            </p>
      *            <p>
      *            For all attribute names, see <a href=
-     *            "http://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
+     *            "https://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
      *            >SetSMSAttributes</a>.
      *            </p>
      *            <p>
@@ -163,7 +163,7 @@ public GetSMSAttributesRequest withAttributes(String... attributes) {
      * </p>
      * <p>
      * For all attribute names, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
+     * "https://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
      * >SetSMSAttributes</a>.
      * </p>
      * <p>
@@ -179,7 +179,7 @@ public GetSMSAttributesRequest withAttributes(String... attributes) {
      *            </p>
      *            <p>
      *            For all attribute names, see <a href=
-     *            "http://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
+     *            "https://docs.aws.amazon.com/sns/latest/api/API_SetSMSAttributes.html"
      *            >SetSMSAttributes</a>.
      *            </p>
      *            <p>
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetSubscriptionAttributesResult.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetSubscriptionAttributesResult.java
index 7aa6ecaa21..79a19fd4df 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetSubscriptionAttributesResult.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetSubscriptionAttributesResult.java
@@ -31,37 +31,57 @@
      * <ul>
      * <li>
      * <p>
-     * <code>SubscriptionArn</code> -- the subscription's ARN
+     * <code>ConfirmationWasAuthenticated</code> – <code>true</code> if the
+     * subscription confirmation request was authenticated.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>TopicArn</code> -- the topic ARN that the subscription is
-     * associated with
+     * <code>DeliveryPolicy</code> – The JSON serialization of the
+     * subscription's delivery policy.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Owner</code> -- the AWS account ID of the subscription's owner
+     * <code>EffectiveDeliveryPolicy</code> – The JSON serialization of the
+     * effective delivery policy that takes into account the topic delivery
+     * policy and account system defaults.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>ConfirmationWasAuthenticated</code> -- true if the subscription
-     * confirmation request was authenticated
+     * <code>FilterPolicy</code> – The filter policy JSON that is assigned to
+     * the subscription.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DeliveryPolicy</code> -- the JSON serialization of the
-     * subscription's delivery policy
+     * <code>Owner</code> – The AWS account ID of the subscription's owner.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EffectiveDeliveryPolicy</code> -- the JSON serialization of the
-     * effective delivery policy that takes into account the topic delivery
-     * policy and account system defaults
+     * <code>PendingConfirmation</code> – <code>true</code> if the subscription
+     * hasn't been confirmed. To confirm a pending subscription, call the
+     * <code>ConfirmSubscription</code> action with a confirmation token.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>RawMessageDelivery</code> – <code>true</code> if raw message
+     * delivery is enabled for the subscription. Raw messages are free of JSON
+     * formatting and can be sent to HTTP/S and Amazon SQS endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>SubscriptionArn</code> – The subscription's ARN.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>TopicArn</code> – The topic ARN that the subscription is associated
+     * with.
      * </p>
      * </li>
      * </ul>
@@ -76,37 +96,57 @@
      * <ul>
      * <li>
      * <p>
-     * <code>SubscriptionArn</code> -- the subscription's ARN
+     * <code>ConfirmationWasAuthenticated</code> – <code>true</code> if the
+     * subscription confirmation request was authenticated.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>TopicArn</code> -- the topic ARN that the subscription is
-     * associated with
+     * <code>DeliveryPolicy</code> – The JSON serialization of the
+     * subscription's delivery policy.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Owner</code> -- the AWS account ID of the subscription's owner
+     * <code>EffectiveDeliveryPolicy</code> – The JSON serialization of the
+     * effective delivery policy that takes into account the topic delivery
+     * policy and account system defaults.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>ConfirmationWasAuthenticated</code> -- true if the subscription
-     * confirmation request was authenticated
+     * <code>FilterPolicy</code> – The filter policy JSON that is assigned to
+     * the subscription.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DeliveryPolicy</code> -- the JSON serialization of the
-     * subscription's delivery policy
+     * <code>Owner</code> – The AWS account ID of the subscription's owner.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EffectiveDeliveryPolicy</code> -- the JSON serialization of the
-     * effective delivery policy that takes into account the topic delivery
-     * policy and account system defaults
+     * <code>PendingConfirmation</code> – <code>true</code> if the subscription
+     * hasn't been confirmed. To confirm a pending subscription, call the
+     * <code>ConfirmSubscription</code> action with a confirmation token.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>RawMessageDelivery</code> – <code>true</code> if raw message
+     * delivery is enabled for the subscription. Raw messages are free of JSON
+     * formatting and can be sent to HTTP/S and Amazon SQS endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>SubscriptionArn</code> – The subscription's ARN.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>TopicArn</code> – The topic ARN that the subscription is associated
+     * with.
      * </p>
      * </li>
      * </ul>
@@ -118,38 +158,60 @@
      *         <ul>
      *         <li>
      *         <p>
-     *         <code>SubscriptionArn</code> -- the subscription's ARN
+     *         <code>ConfirmationWasAuthenticated</code> – <code>true</code> if
+     *         the subscription confirmation request was authenticated.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>TopicArn</code> -- the topic ARN that the subscription is
-     *         associated with
+     *         <code>DeliveryPolicy</code> – The JSON serialization of the
+     *         subscription's delivery policy.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>Owner</code> -- the AWS account ID of the subscription's
-     *         owner
+     *         <code>EffectiveDeliveryPolicy</code> – The JSON serialization of
+     *         the effective delivery policy that takes into account the topic
+     *         delivery policy and account system defaults.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>ConfirmationWasAuthenticated</code> -- true if the
-     *         subscription confirmation request was authenticated
+     *         <code>FilterPolicy</code> – The filter policy JSON that is
+     *         assigned to the subscription.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>DeliveryPolicy</code> -- the JSON serialization of the
-     *         subscription's delivery policy
+     *         <code>Owner</code> – The AWS account ID of the subscription's
+     *         owner.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>EffectiveDeliveryPolicy</code> -- the JSON serialization of
-     *         the effective delivery policy that takes into account the topic
-     *         delivery policy and account system defaults
+     *         <code>PendingConfirmation</code> – <code>true</code> if the
+     *         subscription hasn't been confirmed. To confirm a pending
+     *         subscription, call the <code>ConfirmSubscription</code> action
+     *         with a confirmation token.
+     *         </p>
+     *         </li>
+     *         <li>
+     *         <p>
+     *         <code>RawMessageDelivery</code> – <code>true</code> if raw
+     *         message delivery is enabled for the subscription. Raw messages
+     *         are free of JSON formatting and can be sent to HTTP/S and Amazon
+     *         SQS endpoints.
+     *         </p>
+     *         </li>
+     *         <li>
+     *         <p>
+     *         <code>SubscriptionArn</code> – The subscription's ARN.
+     *         </p>
+     *         </li>
+     *         <li>
+     *         <p>
+     *         <code>TopicArn</code> – The topic ARN that the subscription is
+     *         associated with.
      *         </p>
      *         </li>
      *         </ul>
@@ -166,37 +228,57 @@
      * <ul>
      * <li>
      * <p>
-     * <code>SubscriptionArn</code> -- the subscription's ARN
+     * <code>ConfirmationWasAuthenticated</code> – <code>true</code> if the
+     * subscription confirmation request was authenticated.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>TopicArn</code> -- the topic ARN that the subscription is
-     * associated with
+     * <code>DeliveryPolicy</code> – The JSON serialization of the
+     * subscription's delivery policy.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Owner</code> -- the AWS account ID of the subscription's owner
+     * <code>EffectiveDeliveryPolicy</code> – The JSON serialization of the
+     * effective delivery policy that takes into account the topic delivery
+     * policy and account system defaults.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>ConfirmationWasAuthenticated</code> -- true if the subscription
-     * confirmation request was authenticated
+     * <code>FilterPolicy</code> – The filter policy JSON that is assigned to
+     * the subscription.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DeliveryPolicy</code> -- the JSON serialization of the
-     * subscription's delivery policy
+     * <code>Owner</code> – The AWS account ID of the subscription's owner.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EffectiveDeliveryPolicy</code> -- the JSON serialization of the
-     * effective delivery policy that takes into account the topic delivery
-     * policy and account system defaults
+     * <code>PendingConfirmation</code> – <code>true</code> if the subscription
+     * hasn't been confirmed. To confirm a pending subscription, call the
+     * <code>ConfirmSubscription</code> action with a confirmation token.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>RawMessageDelivery</code> – <code>true</code> if raw message
+     * delivery is enabled for the subscription. Raw messages are free of JSON
+     * formatting and can be sent to HTTP/S and Amazon SQS endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>SubscriptionArn</code> – The subscription's ARN.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>TopicArn</code> – The topic ARN that the subscription is associated
+     * with.
      * </p>
      * </li>
      * </ul>
@@ -208,38 +290,60 @@
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>SubscriptionArn</code> -- the subscription's ARN
+     *            <code>ConfirmationWasAuthenticated</code> – <code>true</code>
+     *            if the subscription confirmation request was authenticated.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>TopicArn</code> -- the topic ARN that the subscription
-     *            is associated with
+     *            <code>DeliveryPolicy</code> – The JSON serialization of the
+     *            subscription's delivery policy.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Owner</code> -- the AWS account ID of the subscription's
-     *            owner
+     *            <code>EffectiveDeliveryPolicy</code> – The JSON serialization
+     *            of the effective delivery policy that takes into account the
+     *            topic delivery policy and account system defaults.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>ConfirmationWasAuthenticated</code> -- true if the
-     *            subscription confirmation request was authenticated
+     *            <code>FilterPolicy</code> – The filter policy JSON that is
+     *            assigned to the subscription.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>DeliveryPolicy</code> -- the JSON serialization of the
-     *            subscription's delivery policy
+     *            <code>Owner</code> – The AWS account ID of the subscription's
+     *            owner.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EffectiveDeliveryPolicy</code> -- the JSON serialization
-     *            of the effective delivery policy that takes into account the
-     *            topic delivery policy and account system defaults
+     *            <code>PendingConfirmation</code> – <code>true</code> if the
+     *            subscription hasn't been confirmed. To confirm a pending
+     *            subscription, call the <code>ConfirmSubscription</code> action
+     *            with a confirmation token.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>RawMessageDelivery</code> – <code>true</code> if raw
+     *            message delivery is enabled for the subscription. Raw messages
+     *            are free of JSON formatting and can be sent to HTTP/S and
+     *            Amazon SQS endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>SubscriptionArn</code> – The subscription's ARN.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>TopicArn</code> – The topic ARN that the subscription is
+     *            associated with.
      *            </p>
      *            </li>
      *            </ul>
@@ -256,37 +360,57 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      * <ul>
      * <li>
      * <p>
-     * <code>SubscriptionArn</code> -- the subscription's ARN
+     * <code>ConfirmationWasAuthenticated</code> – <code>true</code> if the
+     * subscription confirmation request was authenticated.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>TopicArn</code> -- the topic ARN that the subscription is
-     * associated with
+     * <code>DeliveryPolicy</code> – The JSON serialization of the
+     * subscription's delivery policy.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Owner</code> -- the AWS account ID of the subscription's owner
+     * <code>EffectiveDeliveryPolicy</code> – The JSON serialization of the
+     * effective delivery policy that takes into account the topic delivery
+     * policy and account system defaults.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>ConfirmationWasAuthenticated</code> -- true if the subscription
-     * confirmation request was authenticated
+     * <code>FilterPolicy</code> – The filter policy JSON that is assigned to
+     * the subscription.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DeliveryPolicy</code> -- the JSON serialization of the
-     * subscription's delivery policy
+     * <code>Owner</code> – The AWS account ID of the subscription's owner.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EffectiveDeliveryPolicy</code> -- the JSON serialization of the
-     * effective delivery policy that takes into account the topic delivery
-     * policy and account system defaults
+     * <code>PendingConfirmation</code> – <code>true</code> if the subscription
+     * hasn't been confirmed. To confirm a pending subscription, call the
+     * <code>ConfirmSubscription</code> action with a confirmation token.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>RawMessageDelivery</code> – <code>true</code> if raw message
+     * delivery is enabled for the subscription. Raw messages are free of JSON
+     * formatting and can be sent to HTTP/S and Amazon SQS endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>SubscriptionArn</code> – The subscription's ARN.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>TopicArn</code> – The topic ARN that the subscription is associated
+     * with.
      * </p>
      * </li>
      * </ul>
@@ -301,38 +425,60 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>SubscriptionArn</code> -- the subscription's ARN
+     *            <code>ConfirmationWasAuthenticated</code> – <code>true</code>
+     *            if the subscription confirmation request was authenticated.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>TopicArn</code> -- the topic ARN that the subscription
-     *            is associated with
+     *            <code>DeliveryPolicy</code> – The JSON serialization of the
+     *            subscription's delivery policy.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Owner</code> -- the AWS account ID of the subscription's
-     *            owner
+     *            <code>EffectiveDeliveryPolicy</code> – The JSON serialization
+     *            of the effective delivery policy that takes into account the
+     *            topic delivery policy and account system defaults.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>ConfirmationWasAuthenticated</code> -- true if the
-     *            subscription confirmation request was authenticated
+     *            <code>FilterPolicy</code> – The filter policy JSON that is
+     *            assigned to the subscription.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>DeliveryPolicy</code> -- the JSON serialization of the
-     *            subscription's delivery policy
+     *            <code>Owner</code> – The AWS account ID of the subscription's
+     *            owner.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EffectiveDeliveryPolicy</code> -- the JSON serialization
-     *            of the effective delivery policy that takes into account the
-     *            topic delivery policy and account system defaults
+     *            <code>PendingConfirmation</code> – <code>true</code> if the
+     *            subscription hasn't been confirmed. To confirm a pending
+     *            subscription, call the <code>ConfirmSubscription</code> action
+     *            with a confirmation token.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>RawMessageDelivery</code> – <code>true</code> if raw
+     *            message delivery is enabled for the subscription. Raw messages
+     *            are free of JSON formatting and can be sent to HTTP/S and
+     *            Amazon SQS endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>SubscriptionArn</code> – The subscription's ARN.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>TopicArn</code> – The topic ARN that the subscription is
+     *            associated with.
      *            </p>
      *            </li>
      *            </ul>
@@ -352,37 +498,57 @@ public GetSubscriptionAttributesResult withAttributes(java.util.Map<String, Stri
      * <ul>
      * <li>
      * <p>
-     * <code>SubscriptionArn</code> -- the subscription's ARN
+     * <code>ConfirmationWasAuthenticated</code> – <code>true</code> if the
+     * subscription confirmation request was authenticated.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>TopicArn</code> -- the topic ARN that the subscription is
-     * associated with
+     * <code>DeliveryPolicy</code> – The JSON serialization of the
+     * subscription's delivery policy.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Owner</code> -- the AWS account ID of the subscription's owner
+     * <code>EffectiveDeliveryPolicy</code> – The JSON serialization of the
+     * effective delivery policy that takes into account the topic delivery
+     * policy and account system defaults.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>ConfirmationWasAuthenticated</code> -- true if the subscription
-     * confirmation request was authenticated
+     * <code>FilterPolicy</code> – The filter policy JSON that is assigned to
+     * the subscription.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DeliveryPolicy</code> -- the JSON serialization of the
-     * subscription's delivery policy
+     * <code>Owner</code> – The AWS account ID of the subscription's owner.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EffectiveDeliveryPolicy</code> -- the JSON serialization of the
-     * effective delivery policy that takes into account the topic delivery
-     * policy and account system defaults
+     * <code>PendingConfirmation</code> – <code>true</code> if the subscription
+     * hasn't been confirmed. To confirm a pending subscription, call the
+     * <code>ConfirmSubscription</code> action with a confirmation token.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>RawMessageDelivery</code> – <code>true</code> if raw message
+     * delivery is enabled for the subscription. Raw messages are free of JSON
+     * formatting and can be sent to HTTP/S and Amazon SQS endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>SubscriptionArn</code> – The subscription's ARN.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>TopicArn</code> – The topic ARN that the subscription is associated
+     * with.
      * </p>
      * </li>
      * </ul>
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetTopicAttributesResult.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetTopicAttributesResult.java
index 0ee70539e4..0856b7ebdb 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetTopicAttributesResult.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/GetTopicAttributesResult.java
@@ -31,53 +31,53 @@
      * <ul>
      * <li>
      * <p>
-     * <code>TopicArn</code> -- the topic's ARN
+     * <code>TopicArn</code> – the topic's ARN
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Owner</code> -- the AWS account ID of the topic's owner
+     * <code>Owner</code> – the AWS account ID of the topic's owner
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Policy</code> -- the JSON serialization of the topic's access
+     * <code>Policy</code> – the JSON serialization of the topic's access
      * control policy
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DisplayName</code> -- the human-readable name used in the "From"
+     * <code>DisplayName</code> – the human-readable name used in the "From"
      * field for notifications to email and email-json endpoints
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsPending</code> -- the number of subscriptions pending
+     * <code>SubscriptionsPending</code> – the number of subscriptions pending
      * confirmation on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsConfirmed</code> -- the number of confirmed
+     * <code>SubscriptionsConfirmed</code> – the number of confirmed
      * subscriptions on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsDeleted</code> -- the number of deleted subscriptions
+     * <code>SubscriptionsDeleted</code> – the number of deleted subscriptions
      * on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DeliveryPolicy</code> -- the JSON serialization of the topic's
+     * <code>DeliveryPolicy</code> – the JSON serialization of the topic's
      * delivery policy
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EffectiveDeliveryPolicy</code> -- the JSON serialization of the
+     * <code>EffectiveDeliveryPolicy</code> – the JSON serialization of the
      * effective delivery policy that takes into account system defaults
      * </p>
      * </li>
@@ -93,53 +93,53 @@
      * <ul>
      * <li>
      * <p>
-     * <code>TopicArn</code> -- the topic's ARN
+     * <code>TopicArn</code> – the topic's ARN
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Owner</code> -- the AWS account ID of the topic's owner
+     * <code>Owner</code> – the AWS account ID of the topic's owner
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Policy</code> -- the JSON serialization of the topic's access
+     * <code>Policy</code> – the JSON serialization of the topic's access
      * control policy
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DisplayName</code> -- the human-readable name used in the "From"
+     * <code>DisplayName</code> – the human-readable name used in the "From"
      * field for notifications to email and email-json endpoints
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsPending</code> -- the number of subscriptions pending
+     * <code>SubscriptionsPending</code> – the number of subscriptions pending
      * confirmation on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsConfirmed</code> -- the number of confirmed
+     * <code>SubscriptionsConfirmed</code> – the number of confirmed
      * subscriptions on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsDeleted</code> -- the number of deleted subscriptions
+     * <code>SubscriptionsDeleted</code> – the number of deleted subscriptions
      * on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DeliveryPolicy</code> -- the JSON serialization of the topic's
+     * <code>DeliveryPolicy</code> – the JSON serialization of the topic's
      * delivery policy
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EffectiveDeliveryPolicy</code> -- the JSON serialization of the
+     * <code>EffectiveDeliveryPolicy</code> – the JSON serialization of the
      * effective delivery policy that takes into account system defaults
      * </p>
      * </li>
@@ -152,53 +152,53 @@
      *         <ul>
      *         <li>
      *         <p>
-     *         <code>TopicArn</code> -- the topic's ARN
+     *         <code>TopicArn</code> – the topic's ARN
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>Owner</code> -- the AWS account ID of the topic's owner
+     *         <code>Owner</code> – the AWS account ID of the topic's owner
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>Policy</code> -- the JSON serialization of the topic's
+     *         <code>Policy</code> – the JSON serialization of the topic's
      *         access control policy
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>DisplayName</code> -- the human-readable name used in the
+     *         <code>DisplayName</code> – the human-readable name used in the
      *         "From" field for notifications to email and email-json endpoints
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>SubscriptionsPending</code> -- the number of subscriptions
+     *         <code>SubscriptionsPending</code> – the number of subscriptions
      *         pending confirmation on this topic
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>SubscriptionsConfirmed</code> -- the number of confirmed
+     *         <code>SubscriptionsConfirmed</code> – the number of confirmed
      *         subscriptions on this topic
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>SubscriptionsDeleted</code> -- the number of deleted
+     *         <code>SubscriptionsDeleted</code> – the number of deleted
      *         subscriptions on this topic
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>DeliveryPolicy</code> -- the JSON serialization of the
+     *         <code>DeliveryPolicy</code> – the JSON serialization of the
      *         topic's delivery policy
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>EffectiveDeliveryPolicy</code> -- the JSON serialization of
+     *         <code>EffectiveDeliveryPolicy</code> – the JSON serialization of
      *         the effective delivery policy that takes into account system
      *         defaults
      *         </p>
@@ -217,53 +217,53 @@
      * <ul>
      * <li>
      * <p>
-     * <code>TopicArn</code> -- the topic's ARN
+     * <code>TopicArn</code> – the topic's ARN
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Owner</code> -- the AWS account ID of the topic's owner
+     * <code>Owner</code> – the AWS account ID of the topic's owner
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Policy</code> -- the JSON serialization of the topic's access
+     * <code>Policy</code> – the JSON serialization of the topic's access
      * control policy
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DisplayName</code> -- the human-readable name used in the "From"
+     * <code>DisplayName</code> – the human-readable name used in the "From"
      * field for notifications to email and email-json endpoints
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsPending</code> -- the number of subscriptions pending
+     * <code>SubscriptionsPending</code> – the number of subscriptions pending
      * confirmation on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsConfirmed</code> -- the number of confirmed
+     * <code>SubscriptionsConfirmed</code> – the number of confirmed
      * subscriptions on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsDeleted</code> -- the number of deleted subscriptions
+     * <code>SubscriptionsDeleted</code> – the number of deleted subscriptions
      * on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DeliveryPolicy</code> -- the JSON serialization of the topic's
+     * <code>DeliveryPolicy</code> – the JSON serialization of the topic's
      * delivery policy
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EffectiveDeliveryPolicy</code> -- the JSON serialization of the
+     * <code>EffectiveDeliveryPolicy</code> – the JSON serialization of the
      * effective delivery policy that takes into account system defaults
      * </p>
      * </li>
@@ -276,54 +276,54 @@
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>TopicArn</code> -- the topic's ARN
+     *            <code>TopicArn</code> – the topic's ARN
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Owner</code> -- the AWS account ID of the topic's owner
+     *            <code>Owner</code> – the AWS account ID of the topic's owner
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Policy</code> -- the JSON serialization of the topic's
+     *            <code>Policy</code> – the JSON serialization of the topic's
      *            access control policy
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>DisplayName</code> -- the human-readable name used in
-     *            the "From" field for notifications to email and email-json
+     *            <code>DisplayName</code> – the human-readable name used in the
+     *            "From" field for notifications to email and email-json
      *            endpoints
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>SubscriptionsPending</code> -- the number of
+     *            <code>SubscriptionsPending</code> – the number of
      *            subscriptions pending confirmation on this topic
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>SubscriptionsConfirmed</code> -- the number of confirmed
+     *            <code>SubscriptionsConfirmed</code> – the number of confirmed
      *            subscriptions on this topic
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>SubscriptionsDeleted</code> -- the number of deleted
+     *            <code>SubscriptionsDeleted</code> – the number of deleted
      *            subscriptions on this topic
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>DeliveryPolicy</code> -- the JSON serialization of the
+     *            <code>DeliveryPolicy</code> – the JSON serialization of the
      *            topic's delivery policy
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EffectiveDeliveryPolicy</code> -- the JSON serialization
+     *            <code>EffectiveDeliveryPolicy</code> – the JSON serialization
      *            of the effective delivery policy that takes into account
      *            system defaults
      *            </p>
@@ -342,53 +342,53 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      * <ul>
      * <li>
      * <p>
-     * <code>TopicArn</code> -- the topic's ARN
+     * <code>TopicArn</code> – the topic's ARN
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Owner</code> -- the AWS account ID of the topic's owner
+     * <code>Owner</code> – the AWS account ID of the topic's owner
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Policy</code> -- the JSON serialization of the topic's access
+     * <code>Policy</code> – the JSON serialization of the topic's access
      * control policy
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DisplayName</code> -- the human-readable name used in the "From"
+     * <code>DisplayName</code> – the human-readable name used in the "From"
      * field for notifications to email and email-json endpoints
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsPending</code> -- the number of subscriptions pending
+     * <code>SubscriptionsPending</code> – the number of subscriptions pending
      * confirmation on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsConfirmed</code> -- the number of confirmed
+     * <code>SubscriptionsConfirmed</code> – the number of confirmed
      * subscriptions on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsDeleted</code> -- the number of deleted subscriptions
+     * <code>SubscriptionsDeleted</code> – the number of deleted subscriptions
      * on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DeliveryPolicy</code> -- the JSON serialization of the topic's
+     * <code>DeliveryPolicy</code> – the JSON serialization of the topic's
      * delivery policy
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EffectiveDeliveryPolicy</code> -- the JSON serialization of the
+     * <code>EffectiveDeliveryPolicy</code> – the JSON serialization of the
      * effective delivery policy that takes into account system defaults
      * </p>
      * </li>
@@ -404,54 +404,54 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>TopicArn</code> -- the topic's ARN
+     *            <code>TopicArn</code> – the topic's ARN
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Owner</code> -- the AWS account ID of the topic's owner
+     *            <code>Owner</code> – the AWS account ID of the topic's owner
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Policy</code> -- the JSON serialization of the topic's
+     *            <code>Policy</code> – the JSON serialization of the topic's
      *            access control policy
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>DisplayName</code> -- the human-readable name used in
-     *            the "From" field for notifications to email and email-json
+     *            <code>DisplayName</code> – the human-readable name used in the
+     *            "From" field for notifications to email and email-json
      *            endpoints
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>SubscriptionsPending</code> -- the number of
+     *            <code>SubscriptionsPending</code> – the number of
      *            subscriptions pending confirmation on this topic
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>SubscriptionsConfirmed</code> -- the number of confirmed
+     *            <code>SubscriptionsConfirmed</code> – the number of confirmed
      *            subscriptions on this topic
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>SubscriptionsDeleted</code> -- the number of deleted
+     *            <code>SubscriptionsDeleted</code> – the number of deleted
      *            subscriptions on this topic
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>DeliveryPolicy</code> -- the JSON serialization of the
+     *            <code>DeliveryPolicy</code> – the JSON serialization of the
      *            topic's delivery policy
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EffectiveDeliveryPolicy</code> -- the JSON serialization
+     *            <code>EffectiveDeliveryPolicy</code> – the JSON serialization
      *            of the effective delivery policy that takes into account
      *            system defaults
      *            </p>
@@ -473,53 +473,53 @@ public GetTopicAttributesResult withAttributes(java.util.Map<String, String> att
      * <ul>
      * <li>
      * <p>
-     * <code>TopicArn</code> -- the topic's ARN
+     * <code>TopicArn</code> – the topic's ARN
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Owner</code> -- the AWS account ID of the topic's owner
+     * <code>Owner</code> – the AWS account ID of the topic's owner
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Policy</code> -- the JSON serialization of the topic's access
+     * <code>Policy</code> – the JSON serialization of the topic's access
      * control policy
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DisplayName</code> -- the human-readable name used in the "From"
+     * <code>DisplayName</code> – the human-readable name used in the "From"
      * field for notifications to email and email-json endpoints
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsPending</code> -- the number of subscriptions pending
+     * <code>SubscriptionsPending</code> – the number of subscriptions pending
      * confirmation on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsConfirmed</code> -- the number of confirmed
+     * <code>SubscriptionsConfirmed</code> – the number of confirmed
      * subscriptions on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SubscriptionsDeleted</code> -- the number of deleted subscriptions
+     * <code>SubscriptionsDeleted</code> – the number of deleted subscriptions
      * on this topic
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>DeliveryPolicy</code> -- the JSON serialization of the topic's
+     * <code>DeliveryPolicy</code> – the JSON serialization of the topic's
      * delivery policy
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EffectiveDeliveryPolicy</code> -- the JSON serialization of the
+     * <code>EffectiveDeliveryPolicy</code> – the JSON serialization of the
      * effective delivery policy that takes into account system defaults
      * </p>
      * </li>
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/InvalidSecurityException.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/InvalidSecurityException.java
new file mode 100644
index 0000000000..ba84d91492
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/InvalidSecurityException.java
@@ -0,0 +1,38 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import com.amazonaws.AmazonServiceException;
+
+/**
+ * <p>
+ * The credential signature isn't valid. You must use an HTTPS endpoint and sign
+ * your request using Signature Version 4.
+ * </p>
+ */
+public class InvalidSecurityException extends AmazonServiceException {
+    private static final long serialVersionUID = 1L;
+
+    /**
+     * Constructs a new InvalidSecurityException with the specified error
+     * message.
+     *
+     * @param message Describes the error encountered.
+     */
+    public InvalidSecurityException(String message) {
+        super(message);
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSAccessDeniedException.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSAccessDeniedException.java
new file mode 100644
index 0000000000..c65fcae8c8
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSAccessDeniedException.java
@@ -0,0 +1,38 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import com.amazonaws.AmazonServiceException;
+
+/**
+ * <p>
+ * The ciphertext references a key that doesn't exist or that you don't have
+ * access to.
+ * </p>
+ */
+public class KMSAccessDeniedException extends AmazonServiceException {
+    private static final long serialVersionUID = 1L;
+
+    /**
+     * Constructs a new KMSAccessDeniedException with the specified error
+     * message.
+     *
+     * @param message Describes the error encountered.
+     */
+    public KMSAccessDeniedException(String message) {
+        super(message);
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSDisabledException.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSDisabledException.java
new file mode 100644
index 0000000000..5f5acaaba5
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSDisabledException.java
@@ -0,0 +1,37 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import com.amazonaws.AmazonServiceException;
+
+/**
+ * <p>
+ * The request was rejected because the specified customer master key (CMK)
+ * isn't enabled.
+ * </p>
+ */
+public class KMSDisabledException extends AmazonServiceException {
+    private static final long serialVersionUID = 1L;
+
+    /**
+     * Constructs a new KMSDisabledException with the specified error message.
+     *
+     * @param message Describes the error encountered.
+     */
+    public KMSDisabledException(String message) {
+        super(message);
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSInvalidStateException.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSInvalidStateException.java
new file mode 100644
index 0000000000..0ce4e0bbb6
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSInvalidStateException.java
@@ -0,0 +1,41 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import com.amazonaws.AmazonServiceException;
+
+/**
+ * <p>
+ * The request was rejected because the state of the specified resource isn't
+ * valid for this request. For more information, see <a
+ * href="https://docs.aws.amazon.com/kms/latest/developerguide/key-state.html"
+ * >How Key State Affects Use of a Customer Master Key</a> in the <i>AWS Key
+ * Management Service Developer Guide</i>.
+ * </p>
+ */
+public class KMSInvalidStateException extends AmazonServiceException {
+    private static final long serialVersionUID = 1L;
+
+    /**
+     * Constructs a new KMSInvalidStateException with the specified error
+     * message.
+     *
+     * @param message Describes the error encountered.
+     */
+    public KMSInvalidStateException(String message) {
+        super(message);
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSNotFoundException.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSNotFoundException.java
new file mode 100644
index 0000000000..cb2a1aea90
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSNotFoundException.java
@@ -0,0 +1,37 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import com.amazonaws.AmazonServiceException;
+
+/**
+ * <p>
+ * The request was rejected because the specified entity or resource can't be
+ * found.
+ * </p>
+ */
+public class KMSNotFoundException extends AmazonServiceException {
+    private static final long serialVersionUID = 1L;
+
+    /**
+     * Constructs a new KMSNotFoundException with the specified error message.
+     *
+     * @param message Describes the error encountered.
+     */
+    public KMSNotFoundException(String message) {
+        super(message);
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSOptInRequiredException.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSOptInRequiredException.java
new file mode 100644
index 0000000000..5746034e03
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSOptInRequiredException.java
@@ -0,0 +1,37 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import com.amazonaws.AmazonServiceException;
+
+/**
+ * <p>
+ * The AWS access key ID needs a subscription for the service.
+ * </p>
+ */
+public class KMSOptInRequiredException extends AmazonServiceException {
+    private static final long serialVersionUID = 1L;
+
+    /**
+     * Constructs a new KMSOptInRequiredException with the specified error
+     * message.
+     *
+     * @param message Describes the error encountered.
+     */
+    public KMSOptInRequiredException(String message) {
+        super(message);
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSThrottlingException.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSThrottlingException.java
new file mode 100644
index 0000000000..0c3cf01e10
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/KMSThrottlingException.java
@@ -0,0 +1,39 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import com.amazonaws.AmazonServiceException;
+
+/**
+ * <p>
+ * The request was denied due to request throttling. For more information about
+ * throttling, see <a href=
+ * "https://docs.aws.amazon.com/kms/latest/developerguide/limits.html#requests-per-second"
+ * >Limits</a> in the <i>AWS Key Management Service Developer Guide.</i>
+ * </p>
+ */
+public class KMSThrottlingException extends AmazonServiceException {
+    private static final long serialVersionUID = 1L;
+
+    /**
+     * Constructs a new KMSThrottlingException with the specified error message.
+     *
+     * @param message Describes the error encountered.
+     */
+    public KMSThrottlingException(String message) {
+        super(message);
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListEndpointsByPlatformApplicationRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListEndpointsByPlatformApplicationRequest.java
index 2ceec59ea1..100f59885e 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListEndpointsByPlatformApplicationRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListEndpointsByPlatformApplicationRequest.java
@@ -30,9 +30,12 @@
  * <code>ListEndpointsByPlatformApplication</code> again using the NextToken
  * string received from the previous call. When there are no more records to
  * return, NextToken will be null. For more information, see <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+ * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
  * Amazon SNS Mobile Push Notifications</a>.
  * </p>
+ * <p>
+ * This action is throttled at 30 transactions per second (TPS).
+ * </p>
  */
 public class ListEndpointsByPlatformApplicationRequest extends AmazonWebServiceRequest implements
         Serializable {
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListPlatformApplicationsRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListPlatformApplicationsRequest.java
index edbfb25b95..4cf91fc568 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListPlatformApplicationsRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListPlatformApplicationsRequest.java
@@ -29,9 +29,12 @@
  * next page, you call <code>ListPlatformApplications</code> using the NextToken
  * string received from the previous call. When there are no more records to
  * return, NextToken will be null. For more information, see <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+ * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
  * Amazon SNS Mobile Push Notifications</a>.
  * </p>
+ * <p>
+ * This action is throttled at 15 transactions per second (TPS).
+ * </p>
  */
 public class ListPlatformApplicationsRequest extends AmazonWebServiceRequest implements
         Serializable {
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListSubscriptionsByTopicRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListSubscriptionsByTopicRequest.java
index de9ed18af7..ec2a1e78cd 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListSubscriptionsByTopicRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListSubscriptionsByTopicRequest.java
@@ -27,6 +27,9 @@
  * parameter in a new <code>ListSubscriptionsByTopic</code> call to get further
  * results.
  * </p>
+ * <p>
+ * This action is throttled at 30 transactions per second (TPS).
+ * </p>
  */
 public class ListSubscriptionsByTopicRequest extends AmazonWebServiceRequest implements
         Serializable {
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListSubscriptionsRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListSubscriptionsRequest.java
index c7f554a6c3..af73615094 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListSubscriptionsRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListSubscriptionsRequest.java
@@ -27,6 +27,9 @@
  * parameter in a new <code>ListSubscriptions</code> call to get further
  * results.
  * </p>
+ * <p>
+ * This action is throttled at 30 transactions per second (TPS).
+ * </p>
  */
 public class ListSubscriptionsRequest extends AmazonWebServiceRequest implements Serializable {
     /**
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListTagsForResourceRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListTagsForResourceRequest.java
new file mode 100644
index 0000000000..89501ec4e1
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListTagsForResourceRequest.java
@@ -0,0 +1,139 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import java.io.Serializable;
+
+import com.amazonaws.AmazonWebServiceRequest;
+
+/**
+ * <p>
+ * List all tags added to the specified Amazon SNS topic. For an overview, see
+ * <a href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+ * Tags</a> in the <i>Amazon Simple Notification Service Developer Guide</i>.
+ * </p>
+ */
+public class ListTagsForResourceRequest extends AmazonWebServiceRequest implements Serializable {
+    /**
+     * <p>
+     * The ARN of the topic for which to list tags.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 1011<br/>
+     */
+    private String resourceArn;
+
+    /**
+     * <p>
+     * The ARN of the topic for which to list tags.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 1011<br/>
+     *
+     * @return <p>
+     *         The ARN of the topic for which to list tags.
+     *         </p>
+     */
+    public String getResourceArn() {
+        return resourceArn;
+    }
+
+    /**
+     * <p>
+     * The ARN of the topic for which to list tags.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 1011<br/>
+     *
+     * @param resourceArn <p>
+     *            The ARN of the topic for which to list tags.
+     *            </p>
+     */
+    public void setResourceArn(String resourceArn) {
+        this.resourceArn = resourceArn;
+    }
+
+    /**
+     * <p>
+     * The ARN of the topic for which to list tags.
+     * </p>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 1011<br/>
+     *
+     * @param resourceArn <p>
+     *            The ARN of the topic for which to list tags.
+     *            </p>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public ListTagsForResourceRequest withResourceArn(String resourceArn) {
+        this.resourceArn = resourceArn;
+        return this;
+    }
+
+    /**
+     * Returns a string representation of this object; useful for testing and
+     * debugging.
+     *
+     * @return A string representation of this object.
+     * @see java.lang.Object#toString()
+     */
+    @Override
+    public String toString() {
+        StringBuilder sb = new StringBuilder();
+        sb.append("{");
+        if (getResourceArn() != null)
+            sb.append("ResourceArn: " + getResourceArn());
+        sb.append("}");
+        return sb.toString();
+    }
+
+    @Override
+    public int hashCode() {
+        final int prime = 31;
+        int hashCode = 1;
+
+        hashCode = prime * hashCode
+                + ((getResourceArn() == null) ? 0 : getResourceArn().hashCode());
+        return hashCode;
+    }
+
+    @Override
+    public boolean equals(Object obj) {
+        if (this == obj)
+            return true;
+        if (obj == null)
+            return false;
+
+        if (obj instanceof ListTagsForResourceRequest == false)
+            return false;
+        ListTagsForResourceRequest other = (ListTagsForResourceRequest) obj;
+
+        if (other.getResourceArn() == null ^ this.getResourceArn() == null)
+            return false;
+        if (other.getResourceArn() != null
+                && other.getResourceArn().equals(this.getResourceArn()) == false)
+            return false;
+        return true;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListTagsForResourceResult.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListTagsForResourceResult.java
new file mode 100644
index 0000000000..f40eb86616
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListTagsForResourceResult.java
@@ -0,0 +1,145 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import java.io.Serializable;
+
+public class ListTagsForResourceResult implements Serializable {
+    /**
+     * <p>
+     * The tags associated with the specified topic.
+     * </p>
+     */
+    private java.util.List<Tag> tags = new java.util.ArrayList<Tag>();
+
+    /**
+     * <p>
+     * The tags associated with the specified topic.
+     * </p>
+     *
+     * @return <p>
+     *         The tags associated with the specified topic.
+     *         </p>
+     */
+    public java.util.List<Tag> getTags() {
+        return tags;
+    }
+
+    /**
+     * <p>
+     * The tags associated with the specified topic.
+     * </p>
+     *
+     * @param tags <p>
+     *            The tags associated with the specified topic.
+     *            </p>
+     */
+    public void setTags(java.util.Collection<Tag> tags) {
+        if (tags == null) {
+            this.tags = null;
+            return;
+        }
+
+        this.tags = new java.util.ArrayList<Tag>(tags);
+    }
+
+    /**
+     * <p>
+     * The tags associated with the specified topic.
+     * </p>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     *
+     * @param tags <p>
+     *            The tags associated with the specified topic.
+     *            </p>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public ListTagsForResourceResult withTags(Tag... tags) {
+        if (getTags() == null) {
+            this.tags = new java.util.ArrayList<Tag>(tags.length);
+        }
+        for (Tag value : tags) {
+            this.tags.add(value);
+        }
+        return this;
+    }
+
+    /**
+     * <p>
+     * The tags associated with the specified topic.
+     * </p>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     *
+     * @param tags <p>
+     *            The tags associated with the specified topic.
+     *            </p>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public ListTagsForResourceResult withTags(java.util.Collection<Tag> tags) {
+        setTags(tags);
+        return this;
+    }
+
+    /**
+     * Returns a string representation of this object; useful for testing and
+     * debugging.
+     *
+     * @return A string representation of this object.
+     * @see java.lang.Object#toString()
+     */
+    @Override
+    public String toString() {
+        StringBuilder sb = new StringBuilder();
+        sb.append("{");
+        if (getTags() != null)
+            sb.append("Tags: " + getTags());
+        sb.append("}");
+        return sb.toString();
+    }
+
+    @Override
+    public int hashCode() {
+        final int prime = 31;
+        int hashCode = 1;
+
+        hashCode = prime * hashCode + ((getTags() == null) ? 0 : getTags().hashCode());
+        return hashCode;
+    }
+
+    @Override
+    public boolean equals(Object obj) {
+        if (this == obj)
+            return true;
+        if (obj == null)
+            return false;
+
+        if (obj instanceof ListTagsForResourceResult == false)
+            return false;
+        ListTagsForResourceResult other = (ListTagsForResourceResult) obj;
+
+        if (other.getTags() == null ^ this.getTags() == null)
+            return false;
+        if (other.getTags() != null && other.getTags().equals(this.getTags()) == false)
+            return false;
+        return true;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListTopicsRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListTopicsRequest.java
index a61d26e15a..d835f50158 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListTopicsRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ListTopicsRequest.java
@@ -26,6 +26,9 @@
  * returned. Use the <code>NextToken</code> parameter in a new
  * <code>ListTopics</code> call to get further results.
  * </p>
+ * <p>
+ * This action is throttled at 30 transactions per second (TPS).
+ * </p>
  */
 public class ListTopicsRequest extends AmazonWebServiceRequest implements Serializable {
     /**
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/MessageAttributeValue.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/MessageAttributeValue.java
index 7f69ac0463..3138801b40 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/MessageAttributeValue.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/MessageAttributeValue.java
@@ -22,7 +22,7 @@
  * The user-specified message attribute value. For string data types, the value
  * attribute has the same restrictions on the content as the message body. For
  * more information, see <a
- * href="http://docs.aws.amazon.com/sns/latest/api/API_Publish.html"
+ * href="https://docs.aws.amazon.com/sns/latest/api/API_Publish.html"
  * >Publish</a>.
  * </p>
  * <p>
@@ -31,16 +31,16 @@
  * including name, type, and value, are included in the message size
  * restriction, which is currently 256 KB (262,144 bytes). For more information,
  * see <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html"
+ * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html"
  * >Using Amazon SNS Message Attributes</a>.
  * </p>
  */
 public class MessageAttributeValue implements Serializable {
     /**
      * <p>
-     * Amazon SNS supports the following logical data types: String, Number, and
-     * Binary. For more information, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html#SNSMessageAttributes.DataTypes"
+     * Amazon SNS supports the following logical data types: String,
+     * String.Array, Number, and Binary. For more information, see <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html#SNSMessageAttributes.DataTypes"
      * >Message Attribute Data Types</a>.
      * </p>
      */
@@ -50,8 +50,8 @@
      * <p>
      * Strings are Unicode with UTF8 binary encoding. For a list of code values,
      * see <a
-     * href="http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters"
-     * >http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters</a>.
+     * href="https://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters"
+     * >ASCII Printable Characters</a>.
      * </p>
      */
     private String stringValue;
@@ -66,16 +66,17 @@
 
     /**
      * <p>
-     * Amazon SNS supports the following logical data types: String, Number, and
-     * Binary. For more information, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html#SNSMessageAttributes.DataTypes"
+     * Amazon SNS supports the following logical data types: String,
+     * String.Array, Number, and Binary. For more information, see <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html#SNSMessageAttributes.DataTypes"
      * >Message Attribute Data Types</a>.
      * </p>
      *
      * @return <p>
      *         Amazon SNS supports the following logical data types: String,
-     *         Number, and Binary. For more information, see <a href=
-     *         "http://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html#SNSMessageAttributes.DataTypes"
+     *         String.Array, Number, and Binary. For more information, see <a
+     *         href=
+     *         "https://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html#SNSMessageAttributes.DataTypes"
      *         >Message Attribute Data Types</a>.
      *         </p>
      */
@@ -85,16 +86,17 @@ public String getDataType() {
 
     /**
      * <p>
-     * Amazon SNS supports the following logical data types: String, Number, and
-     * Binary. For more information, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html#SNSMessageAttributes.DataTypes"
+     * Amazon SNS supports the following logical data types: String,
+     * String.Array, Number, and Binary. For more information, see <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html#SNSMessageAttributes.DataTypes"
      * >Message Attribute Data Types</a>.
      * </p>
      *
      * @param dataType <p>
      *            Amazon SNS supports the following logical data types: String,
-     *            Number, and Binary. For more information, see <a href=
-     *            "http://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html#SNSMessageAttributes.DataTypes"
+     *            String.Array, Number, and Binary. For more information, see <a
+     *            href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html#SNSMessageAttributes.DataTypes"
      *            >Message Attribute Data Types</a>.
      *            </p>
      */
@@ -104,9 +106,9 @@ public void setDataType(String dataType) {
 
     /**
      * <p>
-     * Amazon SNS supports the following logical data types: String, Number, and
-     * Binary. For more information, see <a href=
-     * "http://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html#SNSMessageAttributes.DataTypes"
+     * Amazon SNS supports the following logical data types: String,
+     * String.Array, Number, and Binary. For more information, see <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html#SNSMessageAttributes.DataTypes"
      * >Message Attribute Data Types</a>.
      * </p>
      * <p>
@@ -115,8 +117,9 @@ public void setDataType(String dataType) {
      *
      * @param dataType <p>
      *            Amazon SNS supports the following logical data types: String,
-     *            Number, and Binary. For more information, see <a href=
-     *            "http://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html#SNSMessageAttributes.DataTypes"
+     *            String.Array, Number, and Binary. For more information, see <a
+     *            href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/SNSMessageAttributes.html#SNSMessageAttributes.DataTypes"
      *            >Message Attribute Data Types</a>.
      *            </p>
      * @return A reference to this updated object so that method calls can be
@@ -131,16 +134,15 @@ public MessageAttributeValue withDataType(String dataType) {
      * <p>
      * Strings are Unicode with UTF8 binary encoding. For a list of code values,
      * see <a
-     * href="http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters"
-     * >http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters</a>.
+     * href="https://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters"
+     * >ASCII Printable Characters</a>.
      * </p>
      *
      * @return <p>
      *         Strings are Unicode with UTF8 binary encoding. For a list of code
      *         values, see <a href=
-     *         "http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters"
-     *         >http
-     *         ://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters</a>.
+     *         "https://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters"
+     *         >ASCII Printable Characters</a>.
      *         </p>
      */
     public String getStringValue() {
@@ -151,16 +153,15 @@ public String getStringValue() {
      * <p>
      * Strings are Unicode with UTF8 binary encoding. For a list of code values,
      * see <a
-     * href="http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters"
-     * >http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters</a>.
+     * href="https://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters"
+     * >ASCII Printable Characters</a>.
      * </p>
      *
      * @param stringValue <p>
      *            Strings are Unicode with UTF8 binary encoding. For a list of
      *            code values, see <a href=
-     *            "http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters"
-     *            >http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters
-     *            </a>.
+     *            "https://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters"
+     *            >ASCII Printable Characters</a>.
      *            </p>
      */
     public void setStringValue(String stringValue) {
@@ -171,8 +172,8 @@ public void setStringValue(String stringValue) {
      * <p>
      * Strings are Unicode with UTF8 binary encoding. For a list of code values,
      * see <a
-     * href="http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters"
-     * >http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters</a>.
+     * href="https://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters"
+     * >ASCII Printable Characters</a>.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -181,9 +182,8 @@ public void setStringValue(String stringValue) {
      * @param stringValue <p>
      *            Strings are Unicode with UTF8 binary encoding. For a list of
      *            code values, see <a href=
-     *            "http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters"
-     *            >http://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters
-     *            </a>.
+     *            "https://en.wikipedia.org/wiki/ASCII#ASCII_printable_characters"
+     *            >ASCII Printable Characters</a>.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/PublishRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/PublishRequest.java
index dc5918288b..7a8004819c 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/PublishRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/PublishRequest.java
@@ -21,23 +21,27 @@
 
 /**
  * <p>
- * Sends a message to all of a topic's subscribed endpoints. When a
- * <code>messageId</code> is returned, the message has been saved and Amazon SNS
- * will attempt to deliver it to the topic's subscribers shortly. The format of
- * the outgoing message to each subscribed endpoint depends on the notification
- * protocol.
+ * Sends a message to an Amazon SNS topic or sends a text message (SMS message)
+ * directly to a phone number.
+ * </p>
+ * <p>
+ * If you send a message to a topic, Amazon SNS delivers the message to each
+ * endpoint that is subscribed to the topic. The format of the message depends
+ * on the notification protocol for each subscribed endpoint.
+ * </p>
+ * <p>
+ * When a <code>messageId</code> is returned, the message has been saved and
+ * Amazon SNS will attempt to deliver it shortly.
  * </p>
  * <p>
  * To use the <code>Publish</code> action for sending a message to a mobile
  * endpoint, such as an app on a Kindle device or mobile phone, you must specify
  * the EndpointArn for the TargetArn parameter. The EndpointArn is returned when
- * making a call with the <code>CreatePlatformEndpoint</code> action. The second
- * example below shows a request and response for publishing to a mobile
- * endpoint.
+ * making a call with the <code>CreatePlatformEndpoint</code> action.
  * </p>
  * <p>
  * For more information about formatting messages, see <a href=
- * "http://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
+ * "https://docs.aws.amazon.com/sns/latest/dg/mobile-push-send-custommessage.html"
  * >Send Custom Platform-Specific Payloads in Messages to Mobile Devices</a>.
  * </p>
  */
@@ -55,9 +59,6 @@
     private String topicArn;
 
     /**
-     * <p>
-     * Either TopicArn or EndpointArn, but not both.
-     * </p>
      * <p>
      * If you don't specify a value for the <code>TargetArn</code> parameter,
      * you must specify a value for the <code>PhoneNumber</code> or
@@ -81,22 +82,52 @@
 
     /**
      * <p>
-     * The message you want to send to the topic.
+     * The message you want to send.
      * </p>
+     * <important>
      * <p>
-     * If you want to send the same message to all transport protocols, include
-     * the text of the message as a String value.
+     * The <code>Message</code> parameter is always a string. If you set
+     * <code>MessageStructure</code> to <code>json</code>, you must
+     * string-encode the <code>Message</code> parameter.
      * </p>
+     * </important>
      * <p>
-     * If you want to send different messages for each transport protocol, set
-     * the value of the <code>MessageStructure</code> parameter to
+     * If you are publishing to a topic and you want to send the same message to
+     * all transport protocols, include the text of the message as a String
+     * value. If you want to send different messages for each transport
+     * protocol, set the value of the <code>MessageStructure</code> parameter to
      * <code>json</code> and use a JSON object for the <code>Message</code>
-     * parameter. See the Examples section for the format of the JSON object.
+     * parameter.
+     * </p>
+     * <p/>
+     * <p>
+     * Constraints:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * With the exception of SMS, messages must be UTF-8 encoded strings and at
+     * most 256 KB in size (262,144 bytes, not 262,144 characters).
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * For SMS, each message can contain up to 140 characters. This character
+     * limit depends on the encoding schema. For example, an SMS message can
+     * contain 160 GSM characters, 140 ASCII characters, or 70 UCS-2 characters.
+     * </p>
+     * <p>
+     * If you publish a message that exceeds this size limit, Amazon SNS sends
+     * the message as multiple messages, each fitting within the size limit.
+     * Messages aren't truncated mid-word but are cut off at whole-word
+     * boundaries.
      * </p>
      * <p>
-     * Constraints: Messages must be UTF-8 encoded strings at most 256 KB in
-     * size (262144 bytes, not 262144 characters).
+     * The total size limit for a single SMS <code>Publish</code> action is
+     * 1,600 characters.
      * </p>
+     * </li>
+     * </ul>
      * <p>
      * JSON-specific constraints:
      * </p>
@@ -198,7 +229,7 @@
      * <p>
      * For information about sending different messages for each protocol using
      * the AWS Management Console, go to <a href=
-     * "http://docs.aws.amazon.com/sns/latest/gsg/Publish.html#sns-message-formatting-by-protocol"
+     * "https://docs.aws.amazon.com/sns/latest/gsg/Publish.html#sns-message-formatting-by-protocol"
      * >Create Different Messages for Each Protocol</a> in the <i>Amazon Simple
      * Notification Service Getting Started Guide</i>.
      * </p>
@@ -237,23 +268,54 @@ public PublishRequest() {
      *            <code>PhoneNumber</code> or <code>TargetArn</code> parameters.
      *            </p>
      * @param message <p>
-     *            The message you want to send to the topic.
+     *            The message you want to send.
+     *            </p>
+     *            <important>
+     *            <p>
+     *            The <code>Message</code> parameter is always a string. If you
+     *            set <code>MessageStructure</code> to <code>json</code>, you
+     *            must string-encode the <code>Message</code> parameter.
+     *            </p>
+     *            </important>
+     *            <p>
+     *            If you are publishing to a topic and you want to send the same
+     *            message to all transport protocols, include the text of the
+     *            message as a String value. If you want to send different
+     *            messages for each transport protocol, set the value of the
+     *            <code>MessageStructure</code> parameter to <code>json</code>
+     *            and use a JSON object for the <code>Message</code> parameter.
+     *            </p>
+     *            <p/>
+     *            <p>
+     *            Constraints:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            With the exception of SMS, messages must be UTF-8 encoded
+     *            strings and at most 256 KB in size (262,144 bytes, not 262,144
+     *            characters).
      *            </p>
+     *            </li>
+     *            <li>
      *            <p>
-     *            If you want to send the same message to all transport
-     *            protocols, include the text of the message as a String value.
+     *            For SMS, each message can contain up to 140 characters. This
+     *            character limit depends on the encoding schema. For example,
+     *            an SMS message can contain 160 GSM characters, 140 ASCII
+     *            characters, or 70 UCS-2 characters.
      *            </p>
      *            <p>
-     *            If you want to send different messages for each transport
-     *            protocol, set the value of the <code>MessageStructure</code>
-     *            parameter to <code>json</code> and use a JSON object for the
-     *            <code>Message</code> parameter. See the Examples section for
-     *            the format of the JSON object.
+     *            If you publish a message that exceeds this size limit, Amazon
+     *            SNS sends the message as multiple messages, each fitting
+     *            within the size limit. Messages aren't truncated mid-word but
+     *            are cut off at whole-word boundaries.
      *            </p>
      *            <p>
-     *            Constraints: Messages must be UTF-8 encoded strings at most
-     *            256 KB in size (262144 bytes, not 262144 characters).
+     *            The total size limit for a single SMS <code>Publish</code>
+     *            action is 1,600 characters.
      *            </p>
+     *            </li>
+     *            </ul>
      *            <p>
      *            JSON-specific constraints:
      *            </p>
@@ -333,23 +395,54 @@ public PublishRequest(String topicArn, String message) {
      *            <code>PhoneNumber</code> or <code>TargetArn</code> parameters.
      *            </p>
      * @param message <p>
-     *            The message you want to send to the topic.
+     *            The message you want to send.
+     *            </p>
+     *            <important>
+     *            <p>
+     *            The <code>Message</code> parameter is always a string. If you
+     *            set <code>MessageStructure</code> to <code>json</code>, you
+     *            must string-encode the <code>Message</code> parameter.
      *            </p>
+     *            </important>
      *            <p>
-     *            If you want to send the same message to all transport
-     *            protocols, include the text of the message as a String value.
+     *            If you are publishing to a topic and you want to send the same
+     *            message to all transport protocols, include the text of the
+     *            message as a String value. If you want to send different
+     *            messages for each transport protocol, set the value of the
+     *            <code>MessageStructure</code> parameter to <code>json</code>
+     *            and use a JSON object for the <code>Message</code> parameter.
      *            </p>
+     *            <p/>
      *            <p>
-     *            If you want to send different messages for each transport
-     *            protocol, set the value of the <code>MessageStructure</code>
-     *            parameter to <code>json</code> and use a JSON object for the
-     *            <code>Message</code> parameter. See the Examples section for
-     *            the format of the JSON object.
+     *            Constraints:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            With the exception of SMS, messages must be UTF-8 encoded
+     *            strings and at most 256 KB in size (262,144 bytes, not 262,144
+     *            characters).
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            For SMS, each message can contain up to 140 characters. This
+     *            character limit depends on the encoding schema. For example,
+     *            an SMS message can contain 160 GSM characters, 140 ASCII
+     *            characters, or 70 UCS-2 characters.
+     *            </p>
+     *            <p>
+     *            If you publish a message that exceeds this size limit, Amazon
+     *            SNS sends the message as multiple messages, each fitting
+     *            within the size limit. Messages aren't truncated mid-word but
+     *            are cut off at whole-word boundaries.
      *            </p>
      *            <p>
-     *            Constraints: Messages must be UTF-8 encoded strings at most
-     *            256 KB in size (262144 bytes, not 262144 characters).
+     *            The total size limit for a single SMS <code>Publish</code>
+     *            action is 1,600 characters.
      *            </p>
+     *            </li>
+     *            </ul>
      *            <p>
      *            JSON-specific constraints:
      *            </p>
@@ -504,9 +597,6 @@ public PublishRequest withTopicArn(String topicArn) {
     }
 
     /**
-     * <p>
-     * Either TopicArn or EndpointArn, but not both.
-     * </p>
      * <p>
      * If you don't specify a value for the <code>TargetArn</code> parameter,
      * you must specify a value for the <code>PhoneNumber</code> or
@@ -514,9 +604,6 @@ public PublishRequest withTopicArn(String topicArn) {
      * </p>
      *
      * @return <p>
-     *         Either TopicArn or EndpointArn, but not both.
-     *         </p>
-     *         <p>
      *         If you don't specify a value for the <code>TargetArn</code>
      *         parameter, you must specify a value for the
      *         <code>PhoneNumber</code> or <code>TopicArn</code> parameters.
@@ -527,9 +614,6 @@ public String getTargetArn() {
     }
 
     /**
-     * <p>
-     * Either TopicArn or EndpointArn, but not both.
-     * </p>
      * <p>
      * If you don't specify a value for the <code>TargetArn</code> parameter,
      * you must specify a value for the <code>PhoneNumber</code> or
@@ -537,9 +621,6 @@ public String getTargetArn() {
      * </p>
      *
      * @param targetArn <p>
-     *            Either TopicArn or EndpointArn, but not both.
-     *            </p>
-     *            <p>
      *            If you don't specify a value for the <code>TargetArn</code>
      *            parameter, you must specify a value for the
      *            <code>PhoneNumber</code> or <code>TopicArn</code> parameters.
@@ -550,9 +631,6 @@ public void setTargetArn(String targetArn) {
     }
 
     /**
-     * <p>
-     * Either TopicArn or EndpointArn, but not both.
-     * </p>
      * <p>
      * If you don't specify a value for the <code>TargetArn</code> parameter,
      * you must specify a value for the <code>PhoneNumber</code> or
@@ -563,9 +641,6 @@ public void setTargetArn(String targetArn) {
      * together.
      *
      * @param targetArn <p>
-     *            Either TopicArn or EndpointArn, but not both.
-     *            </p>
-     *            <p>
      *            If you don't specify a value for the <code>TargetArn</code>
      *            parameter, you must specify a value for the
      *            <code>PhoneNumber</code> or <code>TopicArn</code> parameters.
@@ -661,22 +736,52 @@ public PublishRequest withPhoneNumber(String phoneNumber) {
 
     /**
      * <p>
-     * The message you want to send to the topic.
+     * The message you want to send.
      * </p>
+     * <important>
      * <p>
-     * If you want to send the same message to all transport protocols, include
-     * the text of the message as a String value.
+     * The <code>Message</code> parameter is always a string. If you set
+     * <code>MessageStructure</code> to <code>json</code>, you must
+     * string-encode the <code>Message</code> parameter.
      * </p>
+     * </important>
      * <p>
-     * If you want to send different messages for each transport protocol, set
-     * the value of the <code>MessageStructure</code> parameter to
+     * If you are publishing to a topic and you want to send the same message to
+     * all transport protocols, include the text of the message as a String
+     * value. If you want to send different messages for each transport
+     * protocol, set the value of the <code>MessageStructure</code> parameter to
      * <code>json</code> and use a JSON object for the <code>Message</code>
-     * parameter. See the Examples section for the format of the JSON object.
+     * parameter.
+     * </p>
+     * <p/>
+     * <p>
+     * Constraints:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * With the exception of SMS, messages must be UTF-8 encoded strings and at
+     * most 256 KB in size (262,144 bytes, not 262,144 characters).
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * For SMS, each message can contain up to 140 characters. This character
+     * limit depends on the encoding schema. For example, an SMS message can
+     * contain 160 GSM characters, 140 ASCII characters, or 70 UCS-2 characters.
+     * </p>
+     * <p>
+     * If you publish a message that exceeds this size limit, Amazon SNS sends
+     * the message as multiple messages, each fitting within the size limit.
+     * Messages aren't truncated mid-word but are cut off at whole-word
+     * boundaries.
      * </p>
      * <p>
-     * Constraints: Messages must be UTF-8 encoded strings at most 256 KB in
-     * size (262144 bytes, not 262144 characters).
+     * The total size limit for a single SMS <code>Publish</code> action is
+     * 1,600 characters.
      * </p>
+     * </li>
+     * </ul>
      * <p>
      * JSON-specific constraints:
      * </p>
@@ -734,23 +839,54 @@ public PublishRequest withPhoneNumber(String phoneNumber) {
      * </ul>
      *
      * @return <p>
-     *         The message you want to send to the topic.
+     *         The message you want to send.
+     *         </p>
+     *         <important>
+     *         <p>
+     *         The <code>Message</code> parameter is always a string. If you set
+     *         <code>MessageStructure</code> to <code>json</code>, you must
+     *         string-encode the <code>Message</code> parameter.
+     *         </p>
+     *         </important>
+     *         <p>
+     *         If you are publishing to a topic and you want to send the same
+     *         message to all transport protocols, include the text of the
+     *         message as a String value. If you want to send different messages
+     *         for each transport protocol, set the value of the
+     *         <code>MessageStructure</code> parameter to <code>json</code> and
+     *         use a JSON object for the <code>Message</code> parameter.
+     *         </p>
+     *         <p/>
+     *         <p>
+     *         Constraints:
+     *         </p>
+     *         <ul>
+     *         <li>
+     *         <p>
+     *         With the exception of SMS, messages must be UTF-8 encoded strings
+     *         and at most 256 KB in size (262,144 bytes, not 262,144
+     *         characters).
      *         </p>
+     *         </li>
+     *         <li>
      *         <p>
-     *         If you want to send the same message to all transport protocols,
-     *         include the text of the message as a String value.
+     *         For SMS, each message can contain up to 140 characters. This
+     *         character limit depends on the encoding schema. For example, an
+     *         SMS message can contain 160 GSM characters, 140 ASCII characters,
+     *         or 70 UCS-2 characters.
      *         </p>
      *         <p>
-     *         If you want to send different messages for each transport
-     *         protocol, set the value of the <code>MessageStructure</code>
-     *         parameter to <code>json</code> and use a JSON object for the
-     *         <code>Message</code> parameter. See the Examples section for the
-     *         format of the JSON object.
+     *         If you publish a message that exceeds this size limit, Amazon SNS
+     *         sends the message as multiple messages, each fitting within the
+     *         size limit. Messages aren't truncated mid-word but are cut off at
+     *         whole-word boundaries.
      *         </p>
      *         <p>
-     *         Constraints: Messages must be UTF-8 encoded strings at most 256
-     *         KB in size (262144 bytes, not 262144 characters).
+     *         The total size limit for a single SMS <code>Publish</code> action
+     *         is 1,600 characters.
      *         </p>
+     *         </li>
+     *         </ul>
      *         <p>
      *         JSON-specific constraints:
      *         </p>
@@ -816,22 +952,52 @@ public String getMessage() {
 
     /**
      * <p>
-     * The message you want to send to the topic.
+     * The message you want to send.
      * </p>
+     * <important>
      * <p>
-     * If you want to send the same message to all transport protocols, include
-     * the text of the message as a String value.
+     * The <code>Message</code> parameter is always a string. If you set
+     * <code>MessageStructure</code> to <code>json</code>, you must
+     * string-encode the <code>Message</code> parameter.
      * </p>
+     * </important>
      * <p>
-     * If you want to send different messages for each transport protocol, set
-     * the value of the <code>MessageStructure</code> parameter to
+     * If you are publishing to a topic and you want to send the same message to
+     * all transport protocols, include the text of the message as a String
+     * value. If you want to send different messages for each transport
+     * protocol, set the value of the <code>MessageStructure</code> parameter to
      * <code>json</code> and use a JSON object for the <code>Message</code>
-     * parameter. See the Examples section for the format of the JSON object.
+     * parameter.
+     * </p>
+     * <p/>
+     * <p>
+     * Constraints:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * With the exception of SMS, messages must be UTF-8 encoded strings and at
+     * most 256 KB in size (262,144 bytes, not 262,144 characters).
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * For SMS, each message can contain up to 140 characters. This character
+     * limit depends on the encoding schema. For example, an SMS message can
+     * contain 160 GSM characters, 140 ASCII characters, or 70 UCS-2 characters.
+     * </p>
+     * <p>
+     * If you publish a message that exceeds this size limit, Amazon SNS sends
+     * the message as multiple messages, each fitting within the size limit.
+     * Messages aren't truncated mid-word but are cut off at whole-word
+     * boundaries.
      * </p>
      * <p>
-     * Constraints: Messages must be UTF-8 encoded strings at most 256 KB in
-     * size (262144 bytes, not 262144 characters).
+     * The total size limit for a single SMS <code>Publish</code> action is
+     * 1,600 characters.
      * </p>
+     * </li>
+     * </ul>
      * <p>
      * JSON-specific constraints:
      * </p>
@@ -889,24 +1055,55 @@ public String getMessage() {
      * </ul>
      *
      * @param message <p>
-     *            The message you want to send to the topic.
+     *            The message you want to send.
+     *            </p>
+     *            <important>
+     *            <p>
+     *            The <code>Message</code> parameter is always a string. If you
+     *            set <code>MessageStructure</code> to <code>json</code>, you
+     *            must string-encode the <code>Message</code> parameter.
+     *            </p>
+     *            </important>
+     *            <p>
+     *            If you are publishing to a topic and you want to send the same
+     *            message to all transport protocols, include the text of the
+     *            message as a String value. If you want to send different
+     *            messages for each transport protocol, set the value of the
+     *            <code>MessageStructure</code> parameter to <code>json</code>
+     *            and use a JSON object for the <code>Message</code> parameter.
+     *            </p>
+     *            <p/>
+     *            <p>
+     *            Constraints:
      *            </p>
+     *            <ul>
+     *            <li>
      *            <p>
-     *            If you want to send the same message to all transport
-     *            protocols, include the text of the message as a String value.
+     *            With the exception of SMS, messages must be UTF-8 encoded
+     *            strings and at most 256 KB in size (262,144 bytes, not 262,144
+     *            characters).
      *            </p>
+     *            </li>
+     *            <li>
      *            <p>
-     *            If you want to send different messages for each transport
-     *            protocol, set the value of the <code>MessageStructure</code>
-     *            parameter to <code>json</code> and use a JSON object for the
-     *            <code>Message</code> parameter. See the Examples section for
-     *            the format of the JSON object.
+     *            For SMS, each message can contain up to 140 characters. This
+     *            character limit depends on the encoding schema. For example,
+     *            an SMS message can contain 160 GSM characters, 140 ASCII
+     *            characters, or 70 UCS-2 characters.
      *            </p>
      *            <p>
-     *            Constraints: Messages must be UTF-8 encoded strings at most
-     *            256 KB in size (262144 bytes, not 262144 characters).
+     *            If you publish a message that exceeds this size limit, Amazon
+     *            SNS sends the message as multiple messages, each fitting
+     *            within the size limit. Messages aren't truncated mid-word but
+     *            are cut off at whole-word boundaries.
      *            </p>
      *            <p>
+     *            The total size limit for a single SMS <code>Publish</code>
+     *            action is 1,600 characters.
+     *            </p>
+     *            </li>
+     *            </ul>
+     *            <p>
      *            JSON-specific constraints:
      *            </p>
      *            <ul>
@@ -972,23 +1169,53 @@ public void setMessage(String message) {
 
     /**
      * <p>
-     * The message you want to send to the topic.
+     * The message you want to send.
      * </p>
+     * <important>
      * <p>
-     * If you want to send the same message to all transport protocols, include
-     * the text of the message as a String value.
+     * The <code>Message</code> parameter is always a string. If you set
+     * <code>MessageStructure</code> to <code>json</code>, you must
+     * string-encode the <code>Message</code> parameter.
      * </p>
+     * </important>
      * <p>
-     * If you want to send different messages for each transport protocol, set
-     * the value of the <code>MessageStructure</code> parameter to
+     * If you are publishing to a topic and you want to send the same message to
+     * all transport protocols, include the text of the message as a String
+     * value. If you want to send different messages for each transport
+     * protocol, set the value of the <code>MessageStructure</code> parameter to
      * <code>json</code> and use a JSON object for the <code>Message</code>
-     * parameter. See the Examples section for the format of the JSON object.
+     * parameter.
+     * </p>
+     * <p/>
+     * <p>
+     * Constraints:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * With the exception of SMS, messages must be UTF-8 encoded strings and at
+     * most 256 KB in size (262,144 bytes, not 262,144 characters).
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * For SMS, each message can contain up to 140 characters. This character
+     * limit depends on the encoding schema. For example, an SMS message can
+     * contain 160 GSM characters, 140 ASCII characters, or 70 UCS-2 characters.
      * </p>
      * <p>
-     * Constraints: Messages must be UTF-8 encoded strings at most 256 KB in
-     * size (262144 bytes, not 262144 characters).
+     * If you publish a message that exceeds this size limit, Amazon SNS sends
+     * the message as multiple messages, each fitting within the size limit.
+     * Messages aren't truncated mid-word but are cut off at whole-word
+     * boundaries.
      * </p>
      * <p>
+     * The total size limit for a single SMS <code>Publish</code> action is
+     * 1,600 characters.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
      * JSON-specific constraints:
      * </p>
      * <ul>
@@ -1048,24 +1275,55 @@ public void setMessage(String message) {
      * together.
      *
      * @param message <p>
-     *            The message you want to send to the topic.
+     *            The message you want to send.
+     *            </p>
+     *            <important>
+     *            <p>
+     *            The <code>Message</code> parameter is always a string. If you
+     *            set <code>MessageStructure</code> to <code>json</code>, you
+     *            must string-encode the <code>Message</code> parameter.
+     *            </p>
+     *            </important>
+     *            <p>
+     *            If you are publishing to a topic and you want to send the same
+     *            message to all transport protocols, include the text of the
+     *            message as a String value. If you want to send different
+     *            messages for each transport protocol, set the value of the
+     *            <code>MessageStructure</code> parameter to <code>json</code>
+     *            and use a JSON object for the <code>Message</code> parameter.
+     *            </p>
+     *            <p/>
+     *            <p>
+     *            Constraints:
      *            </p>
+     *            <ul>
+     *            <li>
      *            <p>
-     *            If you want to send the same message to all transport
-     *            protocols, include the text of the message as a String value.
+     *            With the exception of SMS, messages must be UTF-8 encoded
+     *            strings and at most 256 KB in size (262,144 bytes, not 262,144
+     *            characters).
      *            </p>
+     *            </li>
+     *            <li>
      *            <p>
-     *            If you want to send different messages for each transport
-     *            protocol, set the value of the <code>MessageStructure</code>
-     *            parameter to <code>json</code> and use a JSON object for the
-     *            <code>Message</code> parameter. See the Examples section for
-     *            the format of the JSON object.
+     *            For SMS, each message can contain up to 140 characters. This
+     *            character limit depends on the encoding schema. For example,
+     *            an SMS message can contain 160 GSM characters, 140 ASCII
+     *            characters, or 70 UCS-2 characters.
      *            </p>
      *            <p>
-     *            Constraints: Messages must be UTF-8 encoded strings at most
-     *            256 KB in size (262144 bytes, not 262144 characters).
+     *            If you publish a message that exceeds this size limit, Amazon
+     *            SNS sends the message as multiple messages, each fitting
+     *            within the size limit. Messages aren't truncated mid-word but
+     *            are cut off at whole-word boundaries.
      *            </p>
      *            <p>
+     *            The total size limit for a single SMS <code>Publish</code>
+     *            action is 1,600 characters.
+     *            </p>
+     *            </li>
+     *            </ul>
+     *            <p>
      *            JSON-specific constraints:
      *            </p>
      *            <ul>
@@ -1253,7 +1511,7 @@ public PublishRequest withSubject(String subject) {
      * <p>
      * For information about sending different messages for each protocol using
      * the AWS Management Console, go to <a href=
-     * "http://docs.aws.amazon.com/sns/latest/gsg/Publish.html#sns-message-formatting-by-protocol"
+     * "https://docs.aws.amazon.com/sns/latest/gsg/Publish.html#sns-message-formatting-by-protocol"
      * >Create Different Messages for Each Protocol</a> in the <i>Amazon Simple
      * Notification Service Getting Started Guide</i>.
      * </p>
@@ -1289,7 +1547,7 @@ public PublishRequest withSubject(String subject) {
      *         <p>
      *         For information about sending different messages for each
      *         protocol using the AWS Management Console, go to <a href=
-     *         "http://docs.aws.amazon.com/sns/latest/gsg/Publish.html#sns-message-formatting-by-protocol"
+     *         "https://docs.aws.amazon.com/sns/latest/gsg/Publish.html#sns-message-formatting-by-protocol"
      *         >Create Different Messages for Each Protocol</a> in the <i>Amazon
      *         Simple Notification Service Getting Started Guide</i>.
      *         </p>
@@ -1330,7 +1588,7 @@ public String getMessageStructure() {
      * <p>
      * For information about sending different messages for each protocol using
      * the AWS Management Console, go to <a href=
-     * "http://docs.aws.amazon.com/sns/latest/gsg/Publish.html#sns-message-formatting-by-protocol"
+     * "https://docs.aws.amazon.com/sns/latest/gsg/Publish.html#sns-message-formatting-by-protocol"
      * >Create Different Messages for Each Protocol</a> in the <i>Amazon Simple
      * Notification Service Getting Started Guide</i>.
      * </p>
@@ -1368,7 +1626,7 @@ public String getMessageStructure() {
      *            <p>
      *            For information about sending different messages for each
      *            protocol using the AWS Management Console, go to <a href=
-     *            "http://docs.aws.amazon.com/sns/latest/gsg/Publish.html#sns-message-formatting-by-protocol"
+     *            "https://docs.aws.amazon.com/sns/latest/gsg/Publish.html#sns-message-formatting-by-protocol"
      *            >Create Different Messages for Each Protocol</a> in the
      *            <i>Amazon Simple Notification Service Getting Started
      *            Guide</i>.
@@ -1410,7 +1668,7 @@ public void setMessageStructure(String messageStructure) {
      * <p>
      * For information about sending different messages for each protocol using
      * the AWS Management Console, go to <a href=
-     * "http://docs.aws.amazon.com/sns/latest/gsg/Publish.html#sns-message-formatting-by-protocol"
+     * "https://docs.aws.amazon.com/sns/latest/gsg/Publish.html#sns-message-formatting-by-protocol"
      * >Create Different Messages for Each Protocol</a> in the <i>Amazon Simple
      * Notification Service Getting Started Guide</i>.
      * </p>
@@ -1451,7 +1709,7 @@ public void setMessageStructure(String messageStructure) {
      *            <p>
      *            For information about sending different messages for each
      *            protocol using the AWS Management Console, go to <a href=
-     *            "http://docs.aws.amazon.com/sns/latest/gsg/Publish.html#sns-message-formatting-by-protocol"
+     *            "https://docs.aws.amazon.com/sns/latest/gsg/Publish.html#sns-message-formatting-by-protocol"
      *            >Create Different Messages for Each Protocol</a> in the
      *            <i>Amazon Simple Notification Service Getting Started
      *            Guide</i>.
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ResourceNotFoundException.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ResourceNotFoundException.java
new file mode 100644
index 0000000000..643d7aba3f
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/ResourceNotFoundException.java
@@ -0,0 +1,37 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import com.amazonaws.AmazonServiceException;
+
+/**
+ * <p>
+ * Can't tag resource. Verify that the topic exists.
+ * </p>
+ */
+public class ResourceNotFoundException extends AmazonServiceException {
+    private static final long serialVersionUID = 1L;
+
+    /**
+     * Constructs a new ResourceNotFoundException with the specified error
+     * message.
+     *
+     * @param message Describes the error encountered.
+     */
+    public ResourceNotFoundException(String message) {
+        super(message);
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetEndpointAttributesRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetEndpointAttributesRequest.java
index 8759d09ecc..e38f1ac642 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetEndpointAttributesRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetEndpointAttributesRequest.java
@@ -23,7 +23,7 @@
  * <p>
  * Sets the attributes for an endpoint for a device on one of the supported push
  * notification services, such as GCM and APNS. For more information, see <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+ * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
  * Amazon SNS Mobile Push Notifications</a>.
  * </p>
  */
@@ -43,14 +43,14 @@
      * <ul>
      * <li>
      * <p>
-     * <code>CustomUserData</code> -- arbitrary user data to associate with the
+     * <code>CustomUserData</code> – arbitrary user data to associate with the
      * endpoint. Amazon SNS does not use this data. The data must be in UTF-8
      * format and less than 2KB.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Enabled</code> -- flag that enables/disables delivery to the
+     * <code>Enabled</code> – flag that enables/disables delivery to the
      * endpoint. Amazon SNS will set this to false when a notification service
      * indicates to Amazon SNS that the endpoint is invalid. Users can set it
      * back to true, typically after updating Token.
@@ -58,8 +58,8 @@
      * </li>
      * <li>
      * <p>
-     * <code>Token</code> -- device token, also referred to as a registration
-     * id, for an app and mobile device. This is returned from the notification
+     * <code>Token</code> – device token, also referred to as a registration id,
+     * for an app and mobile device. This is returned from the notification
      * service when an app and mobile device are registered with the
      * notification service.
      * </p>
@@ -121,14 +121,14 @@ public SetEndpointAttributesRequest withEndpointArn(String endpointArn) {
      * <ul>
      * <li>
      * <p>
-     * <code>CustomUserData</code> -- arbitrary user data to associate with the
+     * <code>CustomUserData</code> – arbitrary user data to associate with the
      * endpoint. Amazon SNS does not use this data. The data must be in UTF-8
      * format and less than 2KB.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Enabled</code> -- flag that enables/disables delivery to the
+     * <code>Enabled</code> – flag that enables/disables delivery to the
      * endpoint. Amazon SNS will set this to false when a notification service
      * indicates to Amazon SNS that the endpoint is invalid. Users can set it
      * back to true, typically after updating Token.
@@ -136,8 +136,8 @@ public SetEndpointAttributesRequest withEndpointArn(String endpointArn) {
      * </li>
      * <li>
      * <p>
-     * <code>Token</code> -- device token, also referred to as a registration
-     * id, for an app and mobile device. This is returned from the notification
+     * <code>Token</code> – device token, also referred to as a registration id,
+     * for an app and mobile device. This is returned from the notification
      * service when an app and mobile device are registered with the
      * notification service.
      * </p>
@@ -151,23 +151,22 @@ public SetEndpointAttributesRequest withEndpointArn(String endpointArn) {
      *         <ul>
      *         <li>
      *         <p>
-     *         <code>CustomUserData</code> -- arbitrary user data to associate
+     *         <code>CustomUserData</code> – arbitrary user data to associate
      *         with the endpoint. Amazon SNS does not use this data. The data
      *         must be in UTF-8 format and less than 2KB.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>Enabled</code> -- flag that enables/disables delivery to
-     *         the endpoint. Amazon SNS will set this to false when a
-     *         notification service indicates to Amazon SNS that the endpoint is
-     *         invalid. Users can set it back to true, typically after updating
-     *         Token.
+     *         <code>Enabled</code> – flag that enables/disables delivery to the
+     *         endpoint. Amazon SNS will set this to false when a notification
+     *         service indicates to Amazon SNS that the endpoint is invalid.
+     *         Users can set it back to true, typically after updating Token.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>Token</code> -- device token, also referred to as a
+     *         <code>Token</code> – device token, also referred to as a
      *         registration id, for an app and mobile device. This is returned
      *         from the notification service when an app and mobile device are
      *         registered with the notification service.
@@ -187,14 +186,14 @@ public SetEndpointAttributesRequest withEndpointArn(String endpointArn) {
      * <ul>
      * <li>
      * <p>
-     * <code>CustomUserData</code> -- arbitrary user data to associate with the
+     * <code>CustomUserData</code> – arbitrary user data to associate with the
      * endpoint. Amazon SNS does not use this data. The data must be in UTF-8
      * format and less than 2KB.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Enabled</code> -- flag that enables/disables delivery to the
+     * <code>Enabled</code> – flag that enables/disables delivery to the
      * endpoint. Amazon SNS will set this to false when a notification service
      * indicates to Amazon SNS that the endpoint is invalid. Users can set it
      * back to true, typically after updating Token.
@@ -202,8 +201,8 @@ public SetEndpointAttributesRequest withEndpointArn(String endpointArn) {
      * </li>
      * <li>
      * <p>
-     * <code>Token</code> -- device token, also referred to as a registration
-     * id, for an app and mobile device. This is returned from the notification
+     * <code>Token</code> – device token, also referred to as a registration id,
+     * for an app and mobile device. This is returned from the notification
      * service when an app and mobile device are registered with the
      * notification service.
      * </p>
@@ -217,14 +216,14 @@ public SetEndpointAttributesRequest withEndpointArn(String endpointArn) {
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>CustomUserData</code> -- arbitrary user data to
-     *            associate with the endpoint. Amazon SNS does not use this
-     *            data. The data must be in UTF-8 format and less than 2KB.
+     *            <code>CustomUserData</code> – arbitrary user data to associate
+     *            with the endpoint. Amazon SNS does not use this data. The data
+     *            must be in UTF-8 format and less than 2KB.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Enabled</code> -- flag that enables/disables delivery to
+     *            <code>Enabled</code> – flag that enables/disables delivery to
      *            the endpoint. Amazon SNS will set this to false when a
      *            notification service indicates to Amazon SNS that the endpoint
      *            is invalid. Users can set it back to true, typically after
@@ -233,7 +232,7 @@ public SetEndpointAttributesRequest withEndpointArn(String endpointArn) {
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Token</code> -- device token, also referred to as a
+     *            <code>Token</code> – device token, also referred to as a
      *            registration id, for an app and mobile device. This is
      *            returned from the notification service when an app and mobile
      *            device are registered with the notification service.
@@ -253,14 +252,14 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      * <ul>
      * <li>
      * <p>
-     * <code>CustomUserData</code> -- arbitrary user data to associate with the
+     * <code>CustomUserData</code> – arbitrary user data to associate with the
      * endpoint. Amazon SNS does not use this data. The data must be in UTF-8
      * format and less than 2KB.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Enabled</code> -- flag that enables/disables delivery to the
+     * <code>Enabled</code> – flag that enables/disables delivery to the
      * endpoint. Amazon SNS will set this to false when a notification service
      * indicates to Amazon SNS that the endpoint is invalid. Users can set it
      * back to true, typically after updating Token.
@@ -268,8 +267,8 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      * </li>
      * <li>
      * <p>
-     * <code>Token</code> -- device token, also referred to as a registration
-     * id, for an app and mobile device. This is returned from the notification
+     * <code>Token</code> – device token, also referred to as a registration id,
+     * for an app and mobile device. This is returned from the notification
      * service when an app and mobile device are registered with the
      * notification service.
      * </p>
@@ -286,14 +285,14 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>CustomUserData</code> -- arbitrary user data to
-     *            associate with the endpoint. Amazon SNS does not use this
-     *            data. The data must be in UTF-8 format and less than 2KB.
+     *            <code>CustomUserData</code> – arbitrary user data to associate
+     *            with the endpoint. Amazon SNS does not use this data. The data
+     *            must be in UTF-8 format and less than 2KB.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Enabled</code> -- flag that enables/disables delivery to
+     *            <code>Enabled</code> – flag that enables/disables delivery to
      *            the endpoint. Amazon SNS will set this to false when a
      *            notification service indicates to Amazon SNS that the endpoint
      *            is invalid. Users can set it back to true, typically after
@@ -302,7 +301,7 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Token</code> -- device token, also referred to as a
+     *            <code>Token</code> – device token, also referred to as a
      *            registration id, for an app and mobile device. This is
      *            returned from the notification service when an app and mobile
      *            device are registered with the notification service.
@@ -325,14 +324,14 @@ public SetEndpointAttributesRequest withAttributes(java.util.Map<String, String>
      * <ul>
      * <li>
      * <p>
-     * <code>CustomUserData</code> -- arbitrary user data to associate with the
+     * <code>CustomUserData</code> – arbitrary user data to associate with the
      * endpoint. Amazon SNS does not use this data. The data must be in UTF-8
      * format and less than 2KB.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Enabled</code> -- flag that enables/disables delivery to the
+     * <code>Enabled</code> – flag that enables/disables delivery to the
      * endpoint. Amazon SNS will set this to false when a notification service
      * indicates to Amazon SNS that the endpoint is invalid. Users can set it
      * back to true, typically after updating Token.
@@ -340,8 +339,8 @@ public SetEndpointAttributesRequest withAttributes(java.util.Map<String, String>
      * </li>
      * <li>
      * <p>
-     * <code>Token</code> -- device token, also referred to as a registration
-     * id, for an app and mobile device. This is returned from the notification
+     * <code>Token</code> – device token, also referred to as a registration id,
+     * for an app and mobile device. This is returned from the notification
      * service when an app and mobile device are registered with the
      * notification service.
      * </p>
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetPlatformApplicationAttributesRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetPlatformApplicationAttributesRequest.java
index fb0bc2277b..8281438c26 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetPlatformApplicationAttributesRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetPlatformApplicationAttributesRequest.java
@@ -23,10 +23,10 @@
  * <p>
  * Sets the attributes of the platform application object for the supported push
  * notification services, such as APNS and GCM. For more information, see <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
+ * href="https://docs.aws.amazon.com/sns/latest/dg/SNSMobilePush.html">Using
  * Amazon SNS Mobile Push Notifications</a>. For information on configuring
  * attributes for message delivery status, see <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/sns-msg-status.html">Using
+ * href="https://docs.aws.amazon.com/sns/latest/dg/sns-msg-status.html">Using
  * Amazon SNS Application Attributes for Message Delivery Status</a>.
  * </p>
  */
@@ -47,7 +47,7 @@
      * <ul>
      * <li>
      * <p>
-     * <code>PlatformCredential</code> -- The credential received from the
+     * <code>PlatformCredential</code> – The credential received from the
      * notification service. For APNS/APNS_SANDBOX, PlatformCredential is
      * private key. For GCM, PlatformCredential is "API key". For ADM,
      * PlatformCredential is "client secret".
@@ -55,7 +55,7 @@
      * </li>
      * <li>
      * <p>
-     * <code>PlatformPrincipal</code> -- The principal received from the
+     * <code>PlatformPrincipal</code> – The principal received from the
      * notification service. For APNS/APNS_SANDBOX, PlatformPrincipal is SSL
      * certificate. For GCM, PlatformPrincipal is not applicable. For ADM,
      * PlatformPrincipal is "client id".
@@ -63,44 +63,44 @@
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointCreated</code> -- Topic ARN to which EndpointCreated
+     * <code>EventEndpointCreated</code> – Topic ARN to which EndpointCreated
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointDeleted</code> -- Topic ARN to which EndpointDeleted
+     * <code>EventEndpointDeleted</code> – Topic ARN to which EndpointDeleted
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointUpdated</code> -- Topic ARN to which EndpointUpdate
+     * <code>EventEndpointUpdated</code> – Topic ARN to which EndpointUpdate
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventDeliveryFailure</code> -- Topic ARN to which DeliveryFailure
+     * <code>EventDeliveryFailure</code> – Topic ARN to which DeliveryFailure
      * event notifications should be sent upon Direct Publish delivery failure
      * (permanent) to one of the application's endpoints.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SuccessFeedbackRoleArn</code> -- IAM role ARN used to give Amazon
+     * <code>SuccessFeedbackRoleArn</code> – IAM role ARN used to give Amazon
      * SNS write access to use CloudWatch Logs on your behalf.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>FailureFeedbackRoleArn</code> -- IAM role ARN used to give Amazon
+     * <code>FailureFeedbackRoleArn</code> – IAM role ARN used to give Amazon
      * SNS write access to use CloudWatch Logs on your behalf.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SuccessFeedbackSampleRate</code> -- Sample rate percentage (0-100)
+     * <code>SuccessFeedbackSampleRate</code> – Sample rate percentage (0-100)
      * of successfully delivered messages.
      * </p>
      * </li>
@@ -165,7 +165,7 @@ public SetPlatformApplicationAttributesRequest withPlatformApplicationArn(
      * <ul>
      * <li>
      * <p>
-     * <code>PlatformCredential</code> -- The credential received from the
+     * <code>PlatformCredential</code> – The credential received from the
      * notification service. For APNS/APNS_SANDBOX, PlatformCredential is
      * private key. For GCM, PlatformCredential is "API key". For ADM,
      * PlatformCredential is "client secret".
@@ -173,7 +173,7 @@ public SetPlatformApplicationAttributesRequest withPlatformApplicationArn(
      * </li>
      * <li>
      * <p>
-     * <code>PlatformPrincipal</code> -- The principal received from the
+     * <code>PlatformPrincipal</code> – The principal received from the
      * notification service. For APNS/APNS_SANDBOX, PlatformPrincipal is SSL
      * certificate. For GCM, PlatformPrincipal is not applicable. For ADM,
      * PlatformPrincipal is "client id".
@@ -181,44 +181,44 @@ public SetPlatformApplicationAttributesRequest withPlatformApplicationArn(
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointCreated</code> -- Topic ARN to which EndpointCreated
+     * <code>EventEndpointCreated</code> – Topic ARN to which EndpointCreated
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointDeleted</code> -- Topic ARN to which EndpointDeleted
+     * <code>EventEndpointDeleted</code> – Topic ARN to which EndpointDeleted
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointUpdated</code> -- Topic ARN to which EndpointUpdate
+     * <code>EventEndpointUpdated</code> – Topic ARN to which EndpointUpdate
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventDeliveryFailure</code> -- Topic ARN to which DeliveryFailure
+     * <code>EventDeliveryFailure</code> – Topic ARN to which DeliveryFailure
      * event notifications should be sent upon Direct Publish delivery failure
      * (permanent) to one of the application's endpoints.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SuccessFeedbackRoleArn</code> -- IAM role ARN used to give Amazon
+     * <code>SuccessFeedbackRoleArn</code> – IAM role ARN used to give Amazon
      * SNS write access to use CloudWatch Logs on your behalf.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>FailureFeedbackRoleArn</code> -- IAM role ARN used to give Amazon
+     * <code>FailureFeedbackRoleArn</code> – IAM role ARN used to give Amazon
      * SNS write access to use CloudWatch Logs on your behalf.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SuccessFeedbackSampleRate</code> -- Sample rate percentage (0-100)
+     * <code>SuccessFeedbackSampleRate</code> – Sample rate percentage (0-100)
      * of successfully delivered messages.
      * </p>
      * </li>
@@ -231,7 +231,7 @@ public SetPlatformApplicationAttributesRequest withPlatformApplicationArn(
      *         <ul>
      *         <li>
      *         <p>
-     *         <code>PlatformCredential</code> -- The credential received from
+     *         <code>PlatformCredential</code> – The credential received from
      *         the notification service. For APNS/APNS_SANDBOX,
      *         PlatformCredential is private key. For GCM, PlatformCredential is
      *         "API key". For ADM, PlatformCredential is "client secret".
@@ -239,7 +239,7 @@ public SetPlatformApplicationAttributesRequest withPlatformApplicationArn(
      *         </li>
      *         <li>
      *         <p>
-     *         <code>PlatformPrincipal</code> -- The principal received from the
+     *         <code>PlatformPrincipal</code> – The principal received from the
      *         notification service. For APNS/APNS_SANDBOX, PlatformPrincipal is
      *         SSL certificate. For GCM, PlatformPrincipal is not applicable.
      *         For ADM, PlatformPrincipal is "client id".
@@ -247,25 +247,25 @@ public SetPlatformApplicationAttributesRequest withPlatformApplicationArn(
      *         </li>
      *         <li>
      *         <p>
-     *         <code>EventEndpointCreated</code> -- Topic ARN to which
+     *         <code>EventEndpointCreated</code> – Topic ARN to which
      *         EndpointCreated event notifications should be sent.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>EventEndpointDeleted</code> -- Topic ARN to which
+     *         <code>EventEndpointDeleted</code> – Topic ARN to which
      *         EndpointDeleted event notifications should be sent.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>EventEndpointUpdated</code> -- Topic ARN to which
+     *         <code>EventEndpointUpdated</code> – Topic ARN to which
      *         EndpointUpdate event notifications should be sent.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>EventDeliveryFailure</code> -- Topic ARN to which
+     *         <code>EventDeliveryFailure</code> – Topic ARN to which
      *         DeliveryFailure event notifications should be sent upon Direct
      *         Publish delivery failure (permanent) to one of the application's
      *         endpoints.
@@ -273,19 +273,19 @@ public SetPlatformApplicationAttributesRequest withPlatformApplicationArn(
      *         </li>
      *         <li>
      *         <p>
-     *         <code>SuccessFeedbackRoleArn</code> -- IAM role ARN used to give
+     *         <code>SuccessFeedbackRoleArn</code> – IAM role ARN used to give
      *         Amazon SNS write access to use CloudWatch Logs on your behalf.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>FailureFeedbackRoleArn</code> -- IAM role ARN used to give
+     *         <code>FailureFeedbackRoleArn</code> – IAM role ARN used to give
      *         Amazon SNS write access to use CloudWatch Logs on your behalf.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>SuccessFeedbackSampleRate</code> -- Sample rate percentage
+     *         <code>SuccessFeedbackSampleRate</code> – Sample rate percentage
      *         (0-100) of successfully delivered messages.
      *         </p>
      *         </li>
@@ -303,7 +303,7 @@ public SetPlatformApplicationAttributesRequest withPlatformApplicationArn(
      * <ul>
      * <li>
      * <p>
-     * <code>PlatformCredential</code> -- The credential received from the
+     * <code>PlatformCredential</code> – The credential received from the
      * notification service. For APNS/APNS_SANDBOX, PlatformCredential is
      * private key. For GCM, PlatformCredential is "API key". For ADM,
      * PlatformCredential is "client secret".
@@ -311,7 +311,7 @@ public SetPlatformApplicationAttributesRequest withPlatformApplicationArn(
      * </li>
      * <li>
      * <p>
-     * <code>PlatformPrincipal</code> -- The principal received from the
+     * <code>PlatformPrincipal</code> – The principal received from the
      * notification service. For APNS/APNS_SANDBOX, PlatformPrincipal is SSL
      * certificate. For GCM, PlatformPrincipal is not applicable. For ADM,
      * PlatformPrincipal is "client id".
@@ -319,44 +319,44 @@ public SetPlatformApplicationAttributesRequest withPlatformApplicationArn(
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointCreated</code> -- Topic ARN to which EndpointCreated
+     * <code>EventEndpointCreated</code> – Topic ARN to which EndpointCreated
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointDeleted</code> -- Topic ARN to which EndpointDeleted
+     * <code>EventEndpointDeleted</code> – Topic ARN to which EndpointDeleted
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointUpdated</code> -- Topic ARN to which EndpointUpdate
+     * <code>EventEndpointUpdated</code> – Topic ARN to which EndpointUpdate
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventDeliveryFailure</code> -- Topic ARN to which DeliveryFailure
+     * <code>EventDeliveryFailure</code> – Topic ARN to which DeliveryFailure
      * event notifications should be sent upon Direct Publish delivery failure
      * (permanent) to one of the application's endpoints.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SuccessFeedbackRoleArn</code> -- IAM role ARN used to give Amazon
+     * <code>SuccessFeedbackRoleArn</code> – IAM role ARN used to give Amazon
      * SNS write access to use CloudWatch Logs on your behalf.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>FailureFeedbackRoleArn</code> -- IAM role ARN used to give Amazon
+     * <code>FailureFeedbackRoleArn</code> – IAM role ARN used to give Amazon
      * SNS write access to use CloudWatch Logs on your behalf.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SuccessFeedbackSampleRate</code> -- Sample rate percentage (0-100)
+     * <code>SuccessFeedbackSampleRate</code> – Sample rate percentage (0-100)
      * of successfully delivered messages.
      * </p>
      * </li>
@@ -369,15 +369,15 @@ public SetPlatformApplicationAttributesRequest withPlatformApplicationArn(
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>PlatformCredential</code> -- The credential received
-     *            from the notification service. For APNS/APNS_SANDBOX,
+     *            <code>PlatformCredential</code> – The credential received from
+     *            the notification service. For APNS/APNS_SANDBOX,
      *            PlatformCredential is private key. For GCM, PlatformCredential
      *            is "API key". For ADM, PlatformCredential is "client secret".
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>PlatformPrincipal</code> -- The principal received from
+     *            <code>PlatformPrincipal</code> – The principal received from
      *            the notification service. For APNS/APNS_SANDBOX,
      *            PlatformPrincipal is SSL certificate. For GCM,
      *            PlatformPrincipal is not applicable. For ADM,
@@ -386,25 +386,25 @@ public SetPlatformApplicationAttributesRequest withPlatformApplicationArn(
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EventEndpointCreated</code> -- Topic ARN to which
+     *            <code>EventEndpointCreated</code> – Topic ARN to which
      *            EndpointCreated event notifications should be sent.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EventEndpointDeleted</code> -- Topic ARN to which
+     *            <code>EventEndpointDeleted</code> – Topic ARN to which
      *            EndpointDeleted event notifications should be sent.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EventEndpointUpdated</code> -- Topic ARN to which
+     *            <code>EventEndpointUpdated</code> – Topic ARN to which
      *            EndpointUpdate event notifications should be sent.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EventDeliveryFailure</code> -- Topic ARN to which
+     *            <code>EventDeliveryFailure</code> – Topic ARN to which
      *            DeliveryFailure event notifications should be sent upon Direct
      *            Publish delivery failure (permanent) to one of the
      *            application's endpoints.
@@ -412,21 +412,21 @@ public SetPlatformApplicationAttributesRequest withPlatformApplicationArn(
      *            </li>
      *            <li>
      *            <p>
-     *            <code>SuccessFeedbackRoleArn</code> -- IAM role ARN used to
+     *            <code>SuccessFeedbackRoleArn</code> – IAM role ARN used to
      *            give Amazon SNS write access to use CloudWatch Logs on your
      *            behalf.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>FailureFeedbackRoleArn</code> -- IAM role ARN used to
+     *            <code>FailureFeedbackRoleArn</code> – IAM role ARN used to
      *            give Amazon SNS write access to use CloudWatch Logs on your
      *            behalf.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>SuccessFeedbackSampleRate</code> -- Sample rate
+     *            <code>SuccessFeedbackSampleRate</code> – Sample rate
      *            percentage (0-100) of successfully delivered messages.
      *            </p>
      *            </li>
@@ -444,7 +444,7 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      * <ul>
      * <li>
      * <p>
-     * <code>PlatformCredential</code> -- The credential received from the
+     * <code>PlatformCredential</code> – The credential received from the
      * notification service. For APNS/APNS_SANDBOX, PlatformCredential is
      * private key. For GCM, PlatformCredential is "API key". For ADM,
      * PlatformCredential is "client secret".
@@ -452,7 +452,7 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      * </li>
      * <li>
      * <p>
-     * <code>PlatformPrincipal</code> -- The principal received from the
+     * <code>PlatformPrincipal</code> – The principal received from the
      * notification service. For APNS/APNS_SANDBOX, PlatformPrincipal is SSL
      * certificate. For GCM, PlatformPrincipal is not applicable. For ADM,
      * PlatformPrincipal is "client id".
@@ -460,44 +460,44 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointCreated</code> -- Topic ARN to which EndpointCreated
+     * <code>EventEndpointCreated</code> – Topic ARN to which EndpointCreated
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointDeleted</code> -- Topic ARN to which EndpointDeleted
+     * <code>EventEndpointDeleted</code> – Topic ARN to which EndpointDeleted
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointUpdated</code> -- Topic ARN to which EndpointUpdate
+     * <code>EventEndpointUpdated</code> – Topic ARN to which EndpointUpdate
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventDeliveryFailure</code> -- Topic ARN to which DeliveryFailure
+     * <code>EventDeliveryFailure</code> – Topic ARN to which DeliveryFailure
      * event notifications should be sent upon Direct Publish delivery failure
      * (permanent) to one of the application's endpoints.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SuccessFeedbackRoleArn</code> -- IAM role ARN used to give Amazon
+     * <code>SuccessFeedbackRoleArn</code> – IAM role ARN used to give Amazon
      * SNS write access to use CloudWatch Logs on your behalf.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>FailureFeedbackRoleArn</code> -- IAM role ARN used to give Amazon
+     * <code>FailureFeedbackRoleArn</code> – IAM role ARN used to give Amazon
      * SNS write access to use CloudWatch Logs on your behalf.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SuccessFeedbackSampleRate</code> -- Sample rate percentage (0-100)
+     * <code>SuccessFeedbackSampleRate</code> – Sample rate percentage (0-100)
      * of successfully delivered messages.
      * </p>
      * </li>
@@ -513,15 +513,15 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>PlatformCredential</code> -- The credential received
-     *            from the notification service. For APNS/APNS_SANDBOX,
+     *            <code>PlatformCredential</code> – The credential received from
+     *            the notification service. For APNS/APNS_SANDBOX,
      *            PlatformCredential is private key. For GCM, PlatformCredential
      *            is "API key". For ADM, PlatformCredential is "client secret".
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>PlatformPrincipal</code> -- The principal received from
+     *            <code>PlatformPrincipal</code> – The principal received from
      *            the notification service. For APNS/APNS_SANDBOX,
      *            PlatformPrincipal is SSL certificate. For GCM,
      *            PlatformPrincipal is not applicable. For ADM,
@@ -530,25 +530,25 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EventEndpointCreated</code> -- Topic ARN to which
+     *            <code>EventEndpointCreated</code> – Topic ARN to which
      *            EndpointCreated event notifications should be sent.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EventEndpointDeleted</code> -- Topic ARN to which
+     *            <code>EventEndpointDeleted</code> – Topic ARN to which
      *            EndpointDeleted event notifications should be sent.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EventEndpointUpdated</code> -- Topic ARN to which
+     *            <code>EventEndpointUpdated</code> – Topic ARN to which
      *            EndpointUpdate event notifications should be sent.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>EventDeliveryFailure</code> -- Topic ARN to which
+     *            <code>EventDeliveryFailure</code> – Topic ARN to which
      *            DeliveryFailure event notifications should be sent upon Direct
      *            Publish delivery failure (permanent) to one of the
      *            application's endpoints.
@@ -556,21 +556,21 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *            </li>
      *            <li>
      *            <p>
-     *            <code>SuccessFeedbackRoleArn</code> -- IAM role ARN used to
+     *            <code>SuccessFeedbackRoleArn</code> – IAM role ARN used to
      *            give Amazon SNS write access to use CloudWatch Logs on your
      *            behalf.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>FailureFeedbackRoleArn</code> -- IAM role ARN used to
+     *            <code>FailureFeedbackRoleArn</code> – IAM role ARN used to
      *            give Amazon SNS write access to use CloudWatch Logs on your
      *            behalf.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>SuccessFeedbackSampleRate</code> -- Sample rate
+     *            <code>SuccessFeedbackSampleRate</code> – Sample rate
      *            percentage (0-100) of successfully delivered messages.
      *            </p>
      *            </li>
@@ -592,7 +592,7 @@ public SetPlatformApplicationAttributesRequest withAttributes(
      * <ul>
      * <li>
      * <p>
-     * <code>PlatformCredential</code> -- The credential received from the
+     * <code>PlatformCredential</code> – The credential received from the
      * notification service. For APNS/APNS_SANDBOX, PlatformCredential is
      * private key. For GCM, PlatformCredential is "API key". For ADM,
      * PlatformCredential is "client secret".
@@ -600,7 +600,7 @@ public SetPlatformApplicationAttributesRequest withAttributes(
      * </li>
      * <li>
      * <p>
-     * <code>PlatformPrincipal</code> -- The principal received from the
+     * <code>PlatformPrincipal</code> – The principal received from the
      * notification service. For APNS/APNS_SANDBOX, PlatformPrincipal is SSL
      * certificate. For GCM, PlatformPrincipal is not applicable. For ADM,
      * PlatformPrincipal is "client id".
@@ -608,44 +608,44 @@ public SetPlatformApplicationAttributesRequest withAttributes(
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointCreated</code> -- Topic ARN to which EndpointCreated
+     * <code>EventEndpointCreated</code> – Topic ARN to which EndpointCreated
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointDeleted</code> -- Topic ARN to which EndpointDeleted
+     * <code>EventEndpointDeleted</code> – Topic ARN to which EndpointDeleted
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventEndpointUpdated</code> -- Topic ARN to which EndpointUpdate
+     * <code>EventEndpointUpdated</code> – Topic ARN to which EndpointUpdate
      * event notifications should be sent.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>EventDeliveryFailure</code> -- Topic ARN to which DeliveryFailure
+     * <code>EventDeliveryFailure</code> – Topic ARN to which DeliveryFailure
      * event notifications should be sent upon Direct Publish delivery failure
      * (permanent) to one of the application's endpoints.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SuccessFeedbackRoleArn</code> -- IAM role ARN used to give Amazon
+     * <code>SuccessFeedbackRoleArn</code> – IAM role ARN used to give Amazon
      * SNS write access to use CloudWatch Logs on your behalf.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>FailureFeedbackRoleArn</code> -- IAM role ARN used to give Amazon
+     * <code>FailureFeedbackRoleArn</code> – IAM role ARN used to give Amazon
      * SNS write access to use CloudWatch Logs on your behalf.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>SuccessFeedbackSampleRate</code> -- Sample rate percentage (0-100)
+     * <code>SuccessFeedbackSampleRate</code> – Sample rate percentage (0-100)
      * of successfully delivered messages.
      * </p>
      * </li>
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetSMSAttributesRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetSMSAttributesRequest.java
index 86d989b1af..c3dafff1d1 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetSMSAttributesRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetSMSAttributesRequest.java
@@ -28,7 +28,7 @@
  * You can override some of these settings for a single message when you use the
  * <code>Publish</code> action with the <code>MessageAttributes.entry.N</code>
  * parameter. For more information, see <a
- * href="http://docs.aws.amazon.com/sns/latest/dg/sms_publish-to-phone.html"
+ * href="https://docs.aws.amazon.com/sns/latest/dg/sms_publish-to-phone.html"
  * >Sending an SMS Message</a> in the <i>Amazon SNS Developer Guide</i>.
  * </p>
  */
@@ -52,6 +52,14 @@
      * </p>
      * </important>
      * <p>
+     * By default, the spend limit is set to the maximum allowed by Amazon SNS.
+     * If you want to raise the limit, submit an <a href=
+     * "https://console.aws.amazon.com/support/home#/case/create?issueType=service-limit-increase&amp;limitType=service-code-sns"
+     * >SNS Limit Increase case</a>. For <b>New limit value</b>, enter your
+     * desired monthly spend limit. In the <b>Use Case Description</b> field,
+     * explain that you are requesting an SMS monthly spend limit increase.
+     * </p>
+     * <p>
      * <code>DeliveryStatusIAMRole</code> – The ARN of the IAM role that allows
      * Amazon SNS to write logs about SMS deliveries in CloudWatch Logs. For
      * each SMS message that you send, Amazon SNS writes a log that includes the
@@ -79,17 +87,17 @@
      * <ul>
      * <li>
      * <p>
-     * <code>Promotional</code> – Noncritical messages, such as marketing
-     * messages. Amazon SNS optimizes the message delivery to incur the lowest
-     * cost.
+     * <code>Promotional</code> – (Default) Noncritical messages, such as
+     * marketing messages. Amazon SNS optimizes the message delivery to incur
+     * the lowest cost.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Transactional</code> – (Default) Critical messages that support
-     * customer transactions, such as one-time passcodes for multi-factor
-     * authentication. Amazon SNS optimizes the message delivery to achieve the
-     * highest reliability.
+     * <code>Transactional</code> – Critical messages that support customer
+     * transactions, such as one-time passcodes for multi-factor authentication.
+     * Amazon SNS optimizes the message delivery to achieve the highest
+     * reliability.
      * </p>
      * </li>
      * </ul>
@@ -150,9 +158,8 @@
      * </p>
      * <p>
      * For an example bucket policy and usage report, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/sms_stats.html">Viewing
-     * Statistics About SMS Message Delivery</a> in the <i>Amazon SNS Developer
-     * Guide</i>.
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sms_stats.html"
+     * >Monitoring SMS Activity</a> in the <i>Amazon SNS Developer Guide</i>.
      * </p>
      */
     private java.util.Map<String, String> attributes = new java.util.HashMap<String, String>();
@@ -176,6 +183,14 @@
      * </p>
      * </important>
      * <p>
+     * By default, the spend limit is set to the maximum allowed by Amazon SNS.
+     * If you want to raise the limit, submit an <a href=
+     * "https://console.aws.amazon.com/support/home#/case/create?issueType=service-limit-increase&amp;limitType=service-code-sns"
+     * >SNS Limit Increase case</a>. For <b>New limit value</b>, enter your
+     * desired monthly spend limit. In the <b>Use Case Description</b> field,
+     * explain that you are requesting an SMS monthly spend limit increase.
+     * </p>
+     * <p>
      * <code>DeliveryStatusIAMRole</code> – The ARN of the IAM role that allows
      * Amazon SNS to write logs about SMS deliveries in CloudWatch Logs. For
      * each SMS message that you send, Amazon SNS writes a log that includes the
@@ -203,17 +218,17 @@
      * <ul>
      * <li>
      * <p>
-     * <code>Promotional</code> – Noncritical messages, such as marketing
-     * messages. Amazon SNS optimizes the message delivery to incur the lowest
-     * cost.
+     * <code>Promotional</code> – (Default) Noncritical messages, such as
+     * marketing messages. Amazon SNS optimizes the message delivery to incur
+     * the lowest cost.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Transactional</code> – (Default) Critical messages that support
-     * customer transactions, such as one-time passcodes for multi-factor
-     * authentication. Amazon SNS optimizes the message delivery to achieve the
-     * highest reliability.
+     * <code>Transactional</code> – Critical messages that support customer
+     * transactions, such as one-time passcodes for multi-factor authentication.
+     * Amazon SNS optimizes the message delivery to achieve the highest
+     * reliability.
      * </p>
      * </li>
      * </ul>
@@ -274,9 +289,8 @@
      * </p>
      * <p>
      * For an example bucket policy and usage report, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/sms_stats.html">Viewing
-     * Statistics About SMS Message Delivery</a> in the <i>Amazon SNS Developer
-     * Guide</i>.
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sms_stats.html"
+     * >Monitoring SMS Activity</a> in the <i>Amazon SNS Developer Guide</i>.
      * </p>
      *
      * @return <p>
@@ -298,6 +312,15 @@
      *         </p>
      *         </important>
      *         <p>
+     *         By default, the spend limit is set to the maximum allowed by
+     *         Amazon SNS. If you want to raise the limit, submit an <a href=
+     *         "https://console.aws.amazon.com/support/home#/case/create?issueType=service-limit-increase&amp;limitType=service-code-sns"
+     *         >SNS Limit Increase case</a>. For <b>New limit value</b>, enter
+     *         your desired monthly spend limit. In the <b>Use Case
+     *         Description</b> field, explain that you are requesting an SMS
+     *         monthly spend limit increase.
+     *         </p>
+     *         <p>
      *         <code>DeliveryStatusIAMRole</code> – The ARN of the IAM role that
      *         allows Amazon SNS to write logs about SMS deliveries in
      *         CloudWatch Logs. For each SMS message that you send, Amazon SNS
@@ -327,15 +350,15 @@
      *         <ul>
      *         <li>
      *         <p>
-     *         <code>Promotional</code> – Noncritical messages, such as
-     *         marketing messages. Amazon SNS optimizes the message delivery to
-     *         incur the lowest cost.
+     *         <code>Promotional</code> – (Default) Noncritical messages, such
+     *         as marketing messages. Amazon SNS optimizes the message delivery
+     *         to incur the lowest cost.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>Transactional</code> – (Default) Critical messages that
-     *         support customer transactions, such as one-time passcodes for
+     *         <code>Transactional</code> – Critical messages that support
+     *         customer transactions, such as one-time passcodes for
      *         multi-factor authentication. Amazon SNS optimizes the message
      *         delivery to achieve the highest reliability.
      *         </p>
@@ -399,9 +422,9 @@
      *         </p>
      *         <p>
      *         For an example bucket policy and usage report, see <a
-     *         href="http://docs.aws.amazon.com/sns/latest/dg/sms_stats.html"
-     *         >Viewing Statistics About SMS Message Delivery</a> in the
-     *         <i>Amazon SNS Developer Guide</i>.
+     *         href="https://docs.aws.amazon.com/sns/latest/dg/sms_stats.html"
+     *         >Monitoring SMS Activity</a> in the <i>Amazon SNS Developer
+     *         Guide</i>.
      *         </p>
      */
     public java.util.Map<String, String> getAttributes() {
@@ -427,6 +450,14 @@
      * </p>
      * </important>
      * <p>
+     * By default, the spend limit is set to the maximum allowed by Amazon SNS.
+     * If you want to raise the limit, submit an <a href=
+     * "https://console.aws.amazon.com/support/home#/case/create?issueType=service-limit-increase&amp;limitType=service-code-sns"
+     * >SNS Limit Increase case</a>. For <b>New limit value</b>, enter your
+     * desired monthly spend limit. In the <b>Use Case Description</b> field,
+     * explain that you are requesting an SMS monthly spend limit increase.
+     * </p>
+     * <p>
      * <code>DeliveryStatusIAMRole</code> – The ARN of the IAM role that allows
      * Amazon SNS to write logs about SMS deliveries in CloudWatch Logs. For
      * each SMS message that you send, Amazon SNS writes a log that includes the
@@ -454,17 +485,17 @@
      * <ul>
      * <li>
      * <p>
-     * <code>Promotional</code> – Noncritical messages, such as marketing
-     * messages. Amazon SNS optimizes the message delivery to incur the lowest
-     * cost.
+     * <code>Promotional</code> – (Default) Noncritical messages, such as
+     * marketing messages. Amazon SNS optimizes the message delivery to incur
+     * the lowest cost.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Transactional</code> – (Default) Critical messages that support
-     * customer transactions, such as one-time passcodes for multi-factor
-     * authentication. Amazon SNS optimizes the message delivery to achieve the
-     * highest reliability.
+     * <code>Transactional</code> – Critical messages that support customer
+     * transactions, such as one-time passcodes for multi-factor authentication.
+     * Amazon SNS optimizes the message delivery to achieve the highest
+     * reliability.
      * </p>
      * </li>
      * </ul>
@@ -525,9 +556,8 @@
      * </p>
      * <p>
      * For an example bucket policy and usage report, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/sms_stats.html">Viewing
-     * Statistics About SMS Message Delivery</a> in the <i>Amazon SNS Developer
-     * Guide</i>.
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sms_stats.html"
+     * >Monitoring SMS Activity</a> in the <i>Amazon SNS Developer Guide</i>.
      * </p>
      *
      * @param attributes <p>
@@ -550,6 +580,15 @@
      *            </p>
      *            </important>
      *            <p>
+     *            By default, the spend limit is set to the maximum allowed by
+     *            Amazon SNS. If you want to raise the limit, submit an <a href=
+     *            "https://console.aws.amazon.com/support/home#/case/create?issueType=service-limit-increase&amp;limitType=service-code-sns"
+     *            >SNS Limit Increase case</a>. For <b>New limit value</b>,
+     *            enter your desired monthly spend limit. In the <b>Use Case
+     *            Description</b> field, explain that you are requesting an SMS
+     *            monthly spend limit increase.
+     *            </p>
+     *            <p>
      *            <code>DeliveryStatusIAMRole</code> – The ARN of the IAM role
      *            that allows Amazon SNS to write logs about SMS deliveries in
      *            CloudWatch Logs. For each SMS message that you send, Amazon
@@ -580,15 +619,15 @@
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>Promotional</code> – Noncritical messages, such as
-     *            marketing messages. Amazon SNS optimizes the message delivery
-     *            to incur the lowest cost.
+     *            <code>Promotional</code> – (Default) Noncritical messages,
+     *            such as marketing messages. Amazon SNS optimizes the message
+     *            delivery to incur the lowest cost.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Transactional</code> – (Default) Critical messages that
-     *            support customer transactions, such as one-time passcodes for
+     *            <code>Transactional</code> – Critical messages that support
+     *            customer transactions, such as one-time passcodes for
      *            multi-factor authentication. Amazon SNS optimizes the message
      *            delivery to achieve the highest reliability.
      *            </p>
@@ -653,9 +692,9 @@
      *            </p>
      *            <p>
      *            For an example bucket policy and usage report, see <a href=
-     *            "http://docs.aws.amazon.com/sns/latest/dg/sms_stats.html"
-     *            >Viewing Statistics About SMS Message Delivery</a> in the
-     *            <i>Amazon SNS Developer Guide</i>.
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sms_stats.html"
+     *            >Monitoring SMS Activity</a> in the <i>Amazon SNS Developer
+     *            Guide</i>.
      *            </p>
      */
     public void setAttributes(java.util.Map<String, String> attributes) {
@@ -681,6 +720,14 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      * </p>
      * </important>
      * <p>
+     * By default, the spend limit is set to the maximum allowed by Amazon SNS.
+     * If you want to raise the limit, submit an <a href=
+     * "https://console.aws.amazon.com/support/home#/case/create?issueType=service-limit-increase&amp;limitType=service-code-sns"
+     * >SNS Limit Increase case</a>. For <b>New limit value</b>, enter your
+     * desired monthly spend limit. In the <b>Use Case Description</b> field,
+     * explain that you are requesting an SMS monthly spend limit increase.
+     * </p>
+     * <p>
      * <code>DeliveryStatusIAMRole</code> – The ARN of the IAM role that allows
      * Amazon SNS to write logs about SMS deliveries in CloudWatch Logs. For
      * each SMS message that you send, Amazon SNS writes a log that includes the
@@ -708,17 +755,17 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      * <ul>
      * <li>
      * <p>
-     * <code>Promotional</code> – Noncritical messages, such as marketing
-     * messages. Amazon SNS optimizes the message delivery to incur the lowest
-     * cost.
+     * <code>Promotional</code> – (Default) Noncritical messages, such as
+     * marketing messages. Amazon SNS optimizes the message delivery to incur
+     * the lowest cost.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Transactional</code> – (Default) Critical messages that support
-     * customer transactions, such as one-time passcodes for multi-factor
-     * authentication. Amazon SNS optimizes the message delivery to achieve the
-     * highest reliability.
+     * <code>Transactional</code> – Critical messages that support customer
+     * transactions, such as one-time passcodes for multi-factor authentication.
+     * Amazon SNS optimizes the message delivery to achieve the highest
+     * reliability.
      * </p>
      * </li>
      * </ul>
@@ -779,9 +826,8 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      * </p>
      * <p>
      * For an example bucket policy and usage report, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/sms_stats.html">Viewing
-     * Statistics About SMS Message Delivery</a> in the <i>Amazon SNS Developer
-     * Guide</i>.
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sms_stats.html"
+     * >Monitoring SMS Activity</a> in the <i>Amazon SNS Developer Guide</i>.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -807,6 +853,15 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *            </p>
      *            </important>
      *            <p>
+     *            By default, the spend limit is set to the maximum allowed by
+     *            Amazon SNS. If you want to raise the limit, submit an <a href=
+     *            "https://console.aws.amazon.com/support/home#/case/create?issueType=service-limit-increase&amp;limitType=service-code-sns"
+     *            >SNS Limit Increase case</a>. For <b>New limit value</b>,
+     *            enter your desired monthly spend limit. In the <b>Use Case
+     *            Description</b> field, explain that you are requesting an SMS
+     *            monthly spend limit increase.
+     *            </p>
+     *            <p>
      *            <code>DeliveryStatusIAMRole</code> – The ARN of the IAM role
      *            that allows Amazon SNS to write logs about SMS deliveries in
      *            CloudWatch Logs. For each SMS message that you send, Amazon
@@ -837,15 +892,15 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>Promotional</code> – Noncritical messages, such as
-     *            marketing messages. Amazon SNS optimizes the message delivery
-     *            to incur the lowest cost.
+     *            <code>Promotional</code> – (Default) Noncritical messages,
+     *            such as marketing messages. Amazon SNS optimizes the message
+     *            delivery to incur the lowest cost.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>Transactional</code> – (Default) Critical messages that
-     *            support customer transactions, such as one-time passcodes for
+     *            <code>Transactional</code> – Critical messages that support
+     *            customer transactions, such as one-time passcodes for
      *            multi-factor authentication. Amazon SNS optimizes the message
      *            delivery to achieve the highest reliability.
      *            </p>
@@ -910,9 +965,9 @@ public void setAttributes(java.util.Map<String, String> attributes) {
      *            </p>
      *            <p>
      *            For an example bucket policy and usage report, see <a href=
-     *            "http://docs.aws.amazon.com/sns/latest/dg/sms_stats.html"
-     *            >Viewing Statistics About SMS Message Delivery</a> in the
-     *            <i>Amazon SNS Developer Guide</i>.
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sms_stats.html"
+     *            >Monitoring SMS Activity</a> in the <i>Amazon SNS Developer
+     *            Guide</i>.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -941,6 +996,14 @@ public SetSMSAttributesRequest withAttributes(java.util.Map<String, String> attr
      * </p>
      * </important>
      * <p>
+     * By default, the spend limit is set to the maximum allowed by Amazon SNS.
+     * If you want to raise the limit, submit an <a href=
+     * "https://console.aws.amazon.com/support/home#/case/create?issueType=service-limit-increase&amp;limitType=service-code-sns"
+     * >SNS Limit Increase case</a>. For <b>New limit value</b>, enter your
+     * desired monthly spend limit. In the <b>Use Case Description</b> field,
+     * explain that you are requesting an SMS monthly spend limit increase.
+     * </p>
+     * <p>
      * <code>DeliveryStatusIAMRole</code> – The ARN of the IAM role that allows
      * Amazon SNS to write logs about SMS deliveries in CloudWatch Logs. For
      * each SMS message that you send, Amazon SNS writes a log that includes the
@@ -968,17 +1031,17 @@ public SetSMSAttributesRequest withAttributes(java.util.Map<String, String> attr
      * <ul>
      * <li>
      * <p>
-     * <code>Promotional</code> – Noncritical messages, such as marketing
-     * messages. Amazon SNS optimizes the message delivery to incur the lowest
-     * cost.
+     * <code>Promotional</code> – (Default) Noncritical messages, such as
+     * marketing messages. Amazon SNS optimizes the message delivery to incur
+     * the lowest cost.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>Transactional</code> – (Default) Critical messages that support
-     * customer transactions, such as one-time passcodes for multi-factor
-     * authentication. Amazon SNS optimizes the message delivery to achieve the
-     * highest reliability.
+     * <code>Transactional</code> – Critical messages that support customer
+     * transactions, such as one-time passcodes for multi-factor authentication.
+     * Amazon SNS optimizes the message delivery to achieve the highest
+     * reliability.
      * </p>
      * </li>
      * </ul>
@@ -1039,9 +1102,8 @@ public SetSMSAttributesRequest withAttributes(java.util.Map<String, String> attr
      * </p>
      * <p>
      * For an example bucket policy and usage report, see <a
-     * href="http://docs.aws.amazon.com/sns/latest/dg/sms_stats.html">Viewing
-     * Statistics About SMS Message Delivery</a> in the <i>Amazon SNS Developer
-     * Guide</i>.
+     * href="https://docs.aws.amazon.com/sns/latest/dg/sms_stats.html"
+     * >Monitoring SMS Activity</a> in the <i>Amazon SNS Developer Guide</i>.
      * </p>
      * <p>
      * The method adds a new key-value pair into attributes parameter, and
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetSubscriptionAttributesRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetSubscriptionAttributesRequest.java
index 46857a2c8b..6957e7fe15 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetSubscriptionAttributesRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetSubscriptionAttributesRequest.java
@@ -21,7 +21,8 @@
 
 /**
  * <p>
- * Allows a subscription owner to set an attribute of the topic to a new value.
+ * Allows a subscription owner to set an attribute of the subscription to a new
+ * value.
  * </p>
  */
 public class SetSubscriptionAttributesRequest extends AmazonWebServiceRequest implements
@@ -35,13 +36,35 @@
 
     /**
      * <p>
-     * The name of the attribute you want to set. Only a subset of the
-     * subscriptions attributes are mutable.
+     * A map of attributes with their corresponding values.
      * </p>
      * <p>
-     * Valid values: <code>DeliveryPolicy</code> |
-     * <code>RawMessageDelivery</code>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>SetTopicAttributes</code> action uses:
      * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>FilterPolicy</code> – The simple JSON object that lets your
+     * subscriber receive only a subset of messages, rather than receiving every
+     * message published to the topic.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>RawMessageDelivery</code> – When set to <code>true</code>, enables
+     * raw message delivery to Amazon SQS or HTTP/S endpoints. This eliminates
+     * the need for the endpoints to process JSON formatting, which is otherwise
+     * created for Amazon SNS metadata.
+     * </p>
+     * </li>
+     * </ul>
      */
     private String attributeName;
 
@@ -69,13 +92,37 @@ public SetSubscriptionAttributesRequest() {
      *            The ARN of the subscription to modify.
      *            </p>
      * @param attributeName <p>
-     *            The name of the attribute you want to set. Only a subset of
-     *            the subscriptions attributes are mutable.
+     *            A map of attributes with their corresponding values.
+     *            </p>
+     *            <p>
+     *            The following lists the names, descriptions, and values of the
+     *            special request parameters that the
+     *            <code>SetTopicAttributes</code> action uses:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>DeliveryPolicy</code> – The policy that defines how
+     *            Amazon SNS retries failed deliveries to HTTP/S endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>FilterPolicy</code> – The simple JSON object that lets
+     *            your subscriber receive only a subset of messages, rather than
+     *            receiving every message published to the topic.
      *            </p>
+     *            </li>
+     *            <li>
      *            <p>
-     *            Valid values: <code>DeliveryPolicy</code> |
-     *            <code>RawMessageDelivery</code>
+     *            <code>RawMessageDelivery</code> – When set to
+     *            <code>true</code>, enables raw message delivery to Amazon SQS
+     *            or HTTP/S endpoints. This eliminates the need for the
+     *            endpoints to process JSON formatting, which is otherwise
+     *            created for Amazon SNS metadata.
      *            </p>
+     *            </li>
+     *            </ul>
      * @param attributeValue <p>
      *            The new value for the attribute in JSON format.
      *            </p>
@@ -134,22 +181,67 @@ public SetSubscriptionAttributesRequest withSubscriptionArn(String subscriptionA
 
     /**
      * <p>
-     * The name of the attribute you want to set. Only a subset of the
-     * subscriptions attributes are mutable.
+     * A map of attributes with their corresponding values.
      * </p>
      * <p>
-     * Valid values: <code>DeliveryPolicy</code> |
-     * <code>RawMessageDelivery</code>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>SetTopicAttributes</code> action uses:
      * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>FilterPolicy</code> – The simple JSON object that lets your
+     * subscriber receive only a subset of messages, rather than receiving every
+     * message published to the topic.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>RawMessageDelivery</code> – When set to <code>true</code>, enables
+     * raw message delivery to Amazon SQS or HTTP/S endpoints. This eliminates
+     * the need for the endpoints to process JSON formatting, which is otherwise
+     * created for Amazon SNS metadata.
+     * </p>
+     * </li>
+     * </ul>
      *
      * @return <p>
-     *         The name of the attribute you want to set. Only a subset of the
-     *         subscriptions attributes are mutable.
+     *         A map of attributes with their corresponding values.
+     *         </p>
+     *         <p>
+     *         The following lists the names, descriptions, and values of the
+     *         special request parameters that the
+     *         <code>SetTopicAttributes</code> action uses:
+     *         </p>
+     *         <ul>
+     *         <li>
+     *         <p>
+     *         <code>DeliveryPolicy</code> – The policy that defines how Amazon
+     *         SNS retries failed deliveries to HTTP/S endpoints.
+     *         </p>
+     *         </li>
+     *         <li>
+     *         <p>
+     *         <code>FilterPolicy</code> – The simple JSON object that lets your
+     *         subscriber receive only a subset of messages, rather than
+     *         receiving every message published to the topic.
      *         </p>
+     *         </li>
+     *         <li>
      *         <p>
-     *         Valid values: <code>DeliveryPolicy</code> |
-     *         <code>RawMessageDelivery</code>
+     *         <code>RawMessageDelivery</code> – When set to <code>true</code>,
+     *         enables raw message delivery to Amazon SQS or HTTP/S endpoints.
+     *         This eliminates the need for the endpoints to process JSON
+     *         formatting, which is otherwise created for Amazon SNS metadata.
      *         </p>
+     *         </li>
+     *         </ul>
      */
     public String getAttributeName() {
         return attributeName;
@@ -157,22 +249,68 @@ public String getAttributeName() {
 
     /**
      * <p>
-     * The name of the attribute you want to set. Only a subset of the
-     * subscriptions attributes are mutable.
+     * A map of attributes with their corresponding values.
      * </p>
      * <p>
-     * Valid values: <code>DeliveryPolicy</code> |
-     * <code>RawMessageDelivery</code>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>SetTopicAttributes</code> action uses:
      * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>FilterPolicy</code> – The simple JSON object that lets your
+     * subscriber receive only a subset of messages, rather than receiving every
+     * message published to the topic.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>RawMessageDelivery</code> – When set to <code>true</code>, enables
+     * raw message delivery to Amazon SQS or HTTP/S endpoints. This eliminates
+     * the need for the endpoints to process JSON formatting, which is otherwise
+     * created for Amazon SNS metadata.
+     * </p>
+     * </li>
+     * </ul>
      *
      * @param attributeName <p>
-     *            The name of the attribute you want to set. Only a subset of
-     *            the subscriptions attributes are mutable.
+     *            A map of attributes with their corresponding values.
+     *            </p>
+     *            <p>
+     *            The following lists the names, descriptions, and values of the
+     *            special request parameters that the
+     *            <code>SetTopicAttributes</code> action uses:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>DeliveryPolicy</code> – The policy that defines how
+     *            Amazon SNS retries failed deliveries to HTTP/S endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>FilterPolicy</code> – The simple JSON object that lets
+     *            your subscriber receive only a subset of messages, rather than
+     *            receiving every message published to the topic.
      *            </p>
+     *            </li>
+     *            <li>
      *            <p>
-     *            Valid values: <code>DeliveryPolicy</code> |
-     *            <code>RawMessageDelivery</code>
+     *            <code>RawMessageDelivery</code> – When set to
+     *            <code>true</code>, enables raw message delivery to Amazon SQS
+     *            or HTTP/S endpoints. This eliminates the need for the
+     *            endpoints to process JSON formatting, which is otherwise
+     *            created for Amazon SNS metadata.
      *            </p>
+     *            </li>
+     *            </ul>
      */
     public void setAttributeName(String attributeName) {
         this.attributeName = attributeName;
@@ -180,25 +318,71 @@ public void setAttributeName(String attributeName) {
 
     /**
      * <p>
-     * The name of the attribute you want to set. Only a subset of the
-     * subscriptions attributes are mutable.
+     * A map of attributes with their corresponding values.
      * </p>
      * <p>
-     * Valid values: <code>DeliveryPolicy</code> |
-     * <code>RawMessageDelivery</code>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>SetTopicAttributes</code> action uses:
      * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>FilterPolicy</code> – The simple JSON object that lets your
+     * subscriber receive only a subset of messages, rather than receiving every
+     * message published to the topic.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>RawMessageDelivery</code> – When set to <code>true</code>, enables
+     * raw message delivery to Amazon SQS or HTTP/S endpoints. This eliminates
+     * the need for the endpoints to process JSON formatting, which is otherwise
+     * created for Amazon SNS metadata.
+     * </p>
+     * </li>
+     * </ul>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param attributeName <p>
-     *            The name of the attribute you want to set. Only a subset of
-     *            the subscriptions attributes are mutable.
+     *            A map of attributes with their corresponding values.
+     *            </p>
+     *            <p>
+     *            The following lists the names, descriptions, and values of the
+     *            special request parameters that the
+     *            <code>SetTopicAttributes</code> action uses:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>DeliveryPolicy</code> – The policy that defines how
+     *            Amazon SNS retries failed deliveries to HTTP/S endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>FilterPolicy</code> – The simple JSON object that lets
+     *            your subscriber receive only a subset of messages, rather than
+     *            receiving every message published to the topic.
      *            </p>
+     *            </li>
+     *            <li>
      *            <p>
-     *            Valid values: <code>DeliveryPolicy</code> |
-     *            <code>RawMessageDelivery</code>
+     *            <code>RawMessageDelivery</code> – When set to
+     *            <code>true</code>, enables raw message delivery to Amazon SQS
+     *            or HTTP/S endpoints. This eliminates the need for the
+     *            endpoints to process JSON formatting, which is otherwise
+     *            created for Amazon SNS metadata.
      *            </p>
+     *            </li>
+     *            </ul>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
      */
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetTopicAttributesRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetTopicAttributesRequest.java
index 2438178830..2d55b8ff59 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetTopicAttributesRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SetTopicAttributesRequest.java
@@ -34,13 +34,50 @@
 
     /**
      * <p>
-     * The name of the attribute you want to set. Only a subset of the topic's
-     * attributes are mutable.
+     * A map of attributes with their corresponding values.
      * </p>
      * <p>
-     * Valid values: <code>Policy</code> | <code>DisplayName</code> |
-     * <code>DeliveryPolicy</code>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>SetTopicAttributes</code> action uses:
      * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>DisplayName</code> – The display name to use for a topic with SMS
+     * subscriptions.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>Policy</code> – The policy that defines who can access your topic.
+     * By default, only the topic owner can publish or subscribe to the topic.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * The following attribute applies only to <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     * >server-side-encryption</a>:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>KmsMasterKeyId</code> - The ID of an AWS-managed customer master
+     * key (CMK) for Amazon SNS or a custom CMK. For more information, see <a
+     * href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     * >Key Terms</a>. For more examples, see <a href=
+     * "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     * >KeyId</a> in the <i>AWS Key Management Service API Reference</i>.
+     * </p>
+     * </li>
+     * </ul>
      */
     private String attributeName;
 
@@ -68,13 +105,53 @@ public SetTopicAttributesRequest() {
      *            The ARN of the topic to modify.
      *            </p>
      * @param attributeName <p>
-     *            The name of the attribute you want to set. Only a subset of
-     *            the topic's attributes are mutable.
+     *            A map of attributes with their corresponding values.
+     *            </p>
+     *            <p>
+     *            The following lists the names, descriptions, and values of the
+     *            special request parameters that the
+     *            <code>SetTopicAttributes</code> action uses:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>DeliveryPolicy</code> – The policy that defines how
+     *            Amazon SNS retries failed deliveries to HTTP/S endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>DisplayName</code> – The display name to use for a topic
+     *            with SMS subscriptions.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>Policy</code> – The policy that defines who can access
+     *            your topic. By default, only the topic owner can publish or
+     *            subscribe to the topic.
+     *            </p>
+     *            </li>
+     *            </ul>
+     *            <p>
+     *            The following attribute applies only to <a href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     *            >server-side-encryption</a>:
      *            </p>
+     *            <ul>
+     *            <li>
      *            <p>
-     *            Valid values: <code>Policy</code> | <code>DisplayName</code> |
-     *            <code>DeliveryPolicy</code>
+     *            <code>KmsMasterKeyId</code> - The ID of an AWS-managed
+     *            customer master key (CMK) for Amazon SNS or a custom CMK. For
+     *            more information, see <a href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     *            >Key Terms</a>. For more examples, see <a href=
+     *            "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     *            >KeyId</a> in the <i>AWS Key Management Service API
+     *            Reference</i>.
      *            </p>
+     *            </li>
+     *            </ul>
      * @param attributeValue <p>
      *            The new value for the attribute.
      *            </p>
@@ -132,22 +209,99 @@ public SetTopicAttributesRequest withTopicArn(String topicArn) {
 
     /**
      * <p>
-     * The name of the attribute you want to set. Only a subset of the topic's
-     * attributes are mutable.
+     * A map of attributes with their corresponding values.
      * </p>
      * <p>
-     * Valid values: <code>Policy</code> | <code>DisplayName</code> |
-     * <code>DeliveryPolicy</code>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>SetTopicAttributes</code> action uses:
      * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>DisplayName</code> – The display name to use for a topic with SMS
+     * subscriptions.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>Policy</code> – The policy that defines who can access your topic.
+     * By default, only the topic owner can publish or subscribe to the topic.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * The following attribute applies only to <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     * >server-side-encryption</a>:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>KmsMasterKeyId</code> - The ID of an AWS-managed customer master
+     * key (CMK) for Amazon SNS or a custom CMK. For more information, see <a
+     * href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     * >Key Terms</a>. For more examples, see <a href=
+     * "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     * >KeyId</a> in the <i>AWS Key Management Service API Reference</i>.
+     * </p>
+     * </li>
+     * </ul>
      *
      * @return <p>
-     *         The name of the attribute you want to set. Only a subset of the
-     *         topic's attributes are mutable.
+     *         A map of attributes with their corresponding values.
+     *         </p>
+     *         <p>
+     *         The following lists the names, descriptions, and values of the
+     *         special request parameters that the
+     *         <code>SetTopicAttributes</code> action uses:
+     *         </p>
+     *         <ul>
+     *         <li>
+     *         <p>
+     *         <code>DeliveryPolicy</code> – The policy that defines how Amazon
+     *         SNS retries failed deliveries to HTTP/S endpoints.
+     *         </p>
+     *         </li>
+     *         <li>
+     *         <p>
+     *         <code>DisplayName</code> – The display name to use for a topic
+     *         with SMS subscriptions.
+     *         </p>
+     *         </li>
+     *         <li>
+     *         <p>
+     *         <code>Policy</code> – The policy that defines who can access your
+     *         topic. By default, only the topic owner can publish or subscribe
+     *         to the topic.
+     *         </p>
+     *         </li>
+     *         </ul>
+     *         <p>
+     *         The following attribute applies only to <a href=
+     *         "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     *         >server-side-encryption</a>:
      *         </p>
+     *         <ul>
+     *         <li>
      *         <p>
-     *         Valid values: <code>Policy</code> | <code>DisplayName</code> |
-     *         <code>DeliveryPolicy</code>
+     *         <code>KmsMasterKeyId</code> - The ID of an AWS-managed customer
+     *         master key (CMK) for Amazon SNS or a custom CMK. For more
+     *         information, see <a href=
+     *         "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     *         >Key Terms</a>. For more examples, see <a href=
+     *         "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     *         >KeyId</a> in the <i>AWS Key Management Service API
+     *         Reference</i>.
      *         </p>
+     *         </li>
+     *         </ul>
      */
     public String getAttributeName() {
         return attributeName;
@@ -155,22 +309,99 @@ public String getAttributeName() {
 
     /**
      * <p>
-     * The name of the attribute you want to set. Only a subset of the topic's
-     * attributes are mutable.
+     * A map of attributes with their corresponding values.
      * </p>
      * <p>
-     * Valid values: <code>Policy</code> | <code>DisplayName</code> |
-     * <code>DeliveryPolicy</code>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>SetTopicAttributes</code> action uses:
      * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>DisplayName</code> – The display name to use for a topic with SMS
+     * subscriptions.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>Policy</code> – The policy that defines who can access your topic.
+     * By default, only the topic owner can publish or subscribe to the topic.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * The following attribute applies only to <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     * >server-side-encryption</a>:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>KmsMasterKeyId</code> - The ID of an AWS-managed customer master
+     * key (CMK) for Amazon SNS or a custom CMK. For more information, see <a
+     * href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     * >Key Terms</a>. For more examples, see <a href=
+     * "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     * >KeyId</a> in the <i>AWS Key Management Service API Reference</i>.
+     * </p>
+     * </li>
+     * </ul>
      *
      * @param attributeName <p>
-     *            The name of the attribute you want to set. Only a subset of
-     *            the topic's attributes are mutable.
+     *            A map of attributes with their corresponding values.
+     *            </p>
+     *            <p>
+     *            The following lists the names, descriptions, and values of the
+     *            special request parameters that the
+     *            <code>SetTopicAttributes</code> action uses:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>DeliveryPolicy</code> – The policy that defines how
+     *            Amazon SNS retries failed deliveries to HTTP/S endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>DisplayName</code> – The display name to use for a topic
+     *            with SMS subscriptions.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>Policy</code> – The policy that defines who can access
+     *            your topic. By default, only the topic owner can publish or
+     *            subscribe to the topic.
+     *            </p>
+     *            </li>
+     *            </ul>
+     *            <p>
+     *            The following attribute applies only to <a href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     *            >server-side-encryption</a>:
      *            </p>
+     *            <ul>
+     *            <li>
      *            <p>
-     *            Valid values: <code>Policy</code> | <code>DisplayName</code> |
-     *            <code>DeliveryPolicy</code>
+     *            <code>KmsMasterKeyId</code> - The ID of an AWS-managed
+     *            customer master key (CMK) for Amazon SNS or a custom CMK. For
+     *            more information, see <a href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     *            >Key Terms</a>. For more examples, see <a href=
+     *            "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     *            >KeyId</a> in the <i>AWS Key Management Service API
+     *            Reference</i>.
      *            </p>
+     *            </li>
+     *            </ul>
      */
     public void setAttributeName(String attributeName) {
         this.attributeName = attributeName;
@@ -178,25 +409,102 @@ public void setAttributeName(String attributeName) {
 
     /**
      * <p>
-     * The name of the attribute you want to set. Only a subset of the topic's
-     * attributes are mutable.
+     * A map of attributes with their corresponding values.
      * </p>
      * <p>
-     * Valid values: <code>Policy</code> | <code>DisplayName</code> |
-     * <code>DeliveryPolicy</code>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>SetTopicAttributes</code> action uses:
      * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>DisplayName</code> – The display name to use for a topic with SMS
+     * subscriptions.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>Policy</code> – The policy that defines who can access your topic.
+     * By default, only the topic owner can publish or subscribe to the topic.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * The following attribute applies only to <a href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     * >server-side-encryption</a>:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>KmsMasterKeyId</code> - The ID of an AWS-managed customer master
+     * key (CMK) for Amazon SNS or a custom CMK. For more information, see <a
+     * href=
+     * "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     * >Key Terms</a>. For more examples, see <a href=
+     * "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     * >KeyId</a> in the <i>AWS Key Management Service API Reference</i>.
+     * </p>
+     * </li>
+     * </ul>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param attributeName <p>
-     *            The name of the attribute you want to set. Only a subset of
-     *            the topic's attributes are mutable.
+     *            A map of attributes with their corresponding values.
+     *            </p>
+     *            <p>
+     *            The following lists the names, descriptions, and values of the
+     *            special request parameters that the
+     *            <code>SetTopicAttributes</code> action uses:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>DeliveryPolicy</code> – The policy that defines how
+     *            Amazon SNS retries failed deliveries to HTTP/S endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>DisplayName</code> – The display name to use for a topic
+     *            with SMS subscriptions.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>Policy</code> – The policy that defines who can access
+     *            your topic. By default, only the topic owner can publish or
+     *            subscribe to the topic.
+     *            </p>
+     *            </li>
+     *            </ul>
+     *            <p>
+     *            The following attribute applies only to <a href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html"
+     *            >server-side-encryption</a>:
      *            </p>
+     *            <ul>
+     *            <li>
      *            <p>
-     *            Valid values: <code>Policy</code> | <code>DisplayName</code> |
-     *            <code>DeliveryPolicy</code>
+     *            <code>KmsMasterKeyId</code> - The ID of an AWS-managed
+     *            customer master key (CMK) for Amazon SNS or a custom CMK. For
+     *            more information, see <a href=
+     *            "https://docs.aws.amazon.com/sns/latest/dg/sns-server-side-encryption.html#sse-key-terms"
+     *            >Key Terms</a>. For more examples, see <a href=
+     *            "https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters"
+     *            >KeyId</a> in the <i>AWS Key Management Service API
+     *            Reference</i>.
      *            </p>
+     *            </li>
+     *            </ul>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
      */
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/StaleTagException.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/StaleTagException.java
new file mode 100644
index 0000000000..7837ed3d81
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/StaleTagException.java
@@ -0,0 +1,37 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import com.amazonaws.AmazonServiceException;
+
+/**
+ * <p>
+ * A tag has been added to a resource with the same ARN as a deleted resource.
+ * Wait a short while and then retry the operation.
+ * </p>
+ */
+public class StaleTagException extends AmazonServiceException {
+    private static final long serialVersionUID = 1L;
+
+    /**
+     * Constructs a new StaleTagException with the specified error message.
+     *
+     * @param message Describes the error encountered.
+     */
+    public StaleTagException(String message) {
+        super(message);
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SubscribeRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SubscribeRequest.java
index c4cbde4c89..cbf6af0162 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SubscribeRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SubscribeRequest.java
@@ -26,6 +26,9 @@
  * <code>ConfirmSubscription</code> action with the token from the confirmation
  * message. Confirmation tokens are valid for three days.
  * </p>
+ * <p>
+ * This action is throttled at 100 transactions per second (TPS).
+ * </p>
  */
 public class SubscribeRequest extends AmazonWebServiceRequest implements Serializable {
     /**
@@ -42,44 +45,44 @@
      * <ul>
      * <li>
      * <p>
-     * <code>http</code> -- delivery of JSON-encoded message via HTTP POST
+     * <code>http</code> – delivery of JSON-encoded message via HTTP POST
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>https</code> -- delivery of JSON-encoded message via HTTPS POST
+     * <code>https</code> – delivery of JSON-encoded message via HTTPS POST
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>email</code> -- delivery of message via SMTP
+     * <code>email</code> – delivery of message via SMTP
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>email-json</code> -- delivery of JSON-encoded message via SMTP
+     * <code>email-json</code> – delivery of JSON-encoded message via SMTP
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>sms</code> -- delivery of message via SMS
+     * <code>sms</code> – delivery of message via SMS
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>sqs</code> -- delivery of JSON-encoded message to an Amazon SQS
+     * <code>sqs</code> – delivery of JSON-encoded message to an Amazon SQS
      * queue
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>application</code> -- delivery of JSON-encoded message to an
+     * <code>application</code> – delivery of JSON-encoded message to an
      * EndpointArn for a mobile app and device.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>lambda</code> -- delivery of JSON-encoded message to an AWS Lambda
+     * <code>lambda</code> – delivery of JSON-encoded message to an AWS Lambda
      * function.
      * </p>
      * </li>
@@ -96,7 +99,7 @@
      * <li>
      * <p>
      * For the <code>http</code> protocol, the endpoint is an URL beginning with
-     * "http://"
+     * "https://"
      * </p>
      * </li>
      * <li>
@@ -144,6 +147,63 @@
      */
     private String endpoint;
 
+    /**
+     * <p>
+     * A map of attributes with their corresponding values.
+     * </p>
+     * <p>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>SetTopicAttributes</code> action uses:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>FilterPolicy</code> – The simple JSON object that lets your
+     * subscriber receive only a subset of messages, rather than receiving every
+     * message published to the topic.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>RawMessageDelivery</code> – When set to <code>true</code>, enables
+     * raw message delivery to Amazon SQS or HTTP/S endpoints. This eliminates
+     * the need for the endpoints to process JSON formatting, which is otherwise
+     * created for Amazon SNS metadata.
+     * </p>
+     * </li>
+     * </ul>
+     */
+    private java.util.Map<String, String> attributes = new java.util.HashMap<String, String>();
+
+    /**
+     * <p>
+     * Sets whether the response from the <code>Subscribe</code> request
+     * includes the subscription ARN, even if the subscription is not yet
+     * confirmed.
+     * </p>
+     * <p>
+     * If you set this parameter to <code>false</code>, the response includes
+     * the ARN for confirmed subscriptions, but it includes an ARN value of
+     * "pending subscription" for subscriptions that are not yet confirmed. A
+     * subscription becomes confirmed when the subscriber calls the
+     * <code>ConfirmSubscription</code> action with a confirmation token.
+     * </p>
+     * <p>
+     * If you set this parameter to <code>true</code>, the response includes the
+     * ARN in all cases, even if the subscription is not yet confirmed.
+     * </p>
+     * <p>
+     * The default value is <code>false</code>.
+     * </p>
+     */
+    private Boolean returnSubscriptionArn;
+
     /**
      * Default constructor for SubscribeRequest object. Callers should use the
      * setter or fluent setter (with...) methods to initialize any additional
@@ -166,47 +226,47 @@ public SubscribeRequest() {
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>http</code> -- delivery of JSON-encoded message via HTTP
+     *            <code>http</code> – delivery of JSON-encoded message via HTTP
      *            POST
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>https</code> -- delivery of JSON-encoded message via
+     *            <code>https</code> – delivery of JSON-encoded message via
      *            HTTPS POST
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>email</code> -- delivery of message via SMTP
+     *            <code>email</code> – delivery of message via SMTP
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>email-json</code> -- delivery of JSON-encoded message
-     *            via SMTP
+     *            <code>email-json</code> – delivery of JSON-encoded message via
+     *            SMTP
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>sms</code> -- delivery of message via SMS
+     *            <code>sms</code> – delivery of message via SMS
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>sqs</code> -- delivery of JSON-encoded message to an
+     *            <code>sqs</code> – delivery of JSON-encoded message to an
      *            Amazon SQS queue
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>application</code> -- delivery of JSON-encoded message
-     *            to an EndpointArn for a mobile app and device.
+     *            <code>application</code> – delivery of JSON-encoded message to
+     *            an EndpointArn for a mobile app and device.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>lambda</code> -- delivery of JSON-encoded message to an
+     *            <code>lambda</code> – delivery of JSON-encoded message to an
      *            AWS Lambda function.
      *            </p>
      *            </li>
@@ -219,7 +279,7 @@ public SubscribeRequest() {
      *            <li>
      *            <p>
      *            For the <code>http</code> protocol, the endpoint is an URL
-     *            beginning with "http://"
+     *            beginning with "https://"
      *            </p>
      *            </li>
      *            <li>
@@ -324,44 +384,44 @@ public SubscribeRequest withTopicArn(String topicArn) {
      * <ul>
      * <li>
      * <p>
-     * <code>http</code> -- delivery of JSON-encoded message via HTTP POST
+     * <code>http</code> – delivery of JSON-encoded message via HTTP POST
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>https</code> -- delivery of JSON-encoded message via HTTPS POST
+     * <code>https</code> – delivery of JSON-encoded message via HTTPS POST
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>email</code> -- delivery of message via SMTP
+     * <code>email</code> – delivery of message via SMTP
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>email-json</code> -- delivery of JSON-encoded message via SMTP
+     * <code>email-json</code> – delivery of JSON-encoded message via SMTP
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>sms</code> -- delivery of message via SMS
+     * <code>sms</code> – delivery of message via SMS
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>sqs</code> -- delivery of JSON-encoded message to an Amazon SQS
+     * <code>sqs</code> – delivery of JSON-encoded message to an Amazon SQS
      * queue
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>application</code> -- delivery of JSON-encoded message to an
+     * <code>application</code> – delivery of JSON-encoded message to an
      * EndpointArn for a mobile app and device.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>lambda</code> -- delivery of JSON-encoded message to an AWS Lambda
+     * <code>lambda</code> – delivery of JSON-encoded message to an AWS Lambda
      * function.
      * </p>
      * </li>
@@ -373,47 +433,47 @@ public SubscribeRequest withTopicArn(String topicArn) {
      *         <ul>
      *         <li>
      *         <p>
-     *         <code>http</code> -- delivery of JSON-encoded message via HTTP
+     *         <code>http</code> – delivery of JSON-encoded message via HTTP
      *         POST
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>https</code> -- delivery of JSON-encoded message via HTTPS
+     *         <code>https</code> – delivery of JSON-encoded message via HTTPS
      *         POST
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>email</code> -- delivery of message via SMTP
+     *         <code>email</code> – delivery of message via SMTP
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>email-json</code> -- delivery of JSON-encoded message via
+     *         <code>email-json</code> – delivery of JSON-encoded message via
      *         SMTP
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>sms</code> -- delivery of message via SMS
+     *         <code>sms</code> – delivery of message via SMS
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>sqs</code> -- delivery of JSON-encoded message to an Amazon
+     *         <code>sqs</code> – delivery of JSON-encoded message to an Amazon
      *         SQS queue
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>application</code> -- delivery of JSON-encoded message to
-     *         an EndpointArn for a mobile app and device.
+     *         <code>application</code> – delivery of JSON-encoded message to an
+     *         EndpointArn for a mobile app and device.
      *         </p>
      *         </li>
      *         <li>
      *         <p>
-     *         <code>lambda</code> -- delivery of JSON-encoded message to an AWS
+     *         <code>lambda</code> – delivery of JSON-encoded message to an AWS
      *         Lambda function.
      *         </p>
      *         </li>
@@ -430,44 +490,44 @@ public String getProtocol() {
      * <ul>
      * <li>
      * <p>
-     * <code>http</code> -- delivery of JSON-encoded message via HTTP POST
+     * <code>http</code> – delivery of JSON-encoded message via HTTP POST
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>https</code> -- delivery of JSON-encoded message via HTTPS POST
+     * <code>https</code> – delivery of JSON-encoded message via HTTPS POST
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>email</code> -- delivery of message via SMTP
+     * <code>email</code> – delivery of message via SMTP
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>email-json</code> -- delivery of JSON-encoded message via SMTP
+     * <code>email-json</code> – delivery of JSON-encoded message via SMTP
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>sms</code> -- delivery of message via SMS
+     * <code>sms</code> – delivery of message via SMS
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>sqs</code> -- delivery of JSON-encoded message to an Amazon SQS
+     * <code>sqs</code> – delivery of JSON-encoded message to an Amazon SQS
      * queue
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>application</code> -- delivery of JSON-encoded message to an
+     * <code>application</code> – delivery of JSON-encoded message to an
      * EndpointArn for a mobile app and device.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>lambda</code> -- delivery of JSON-encoded message to an AWS Lambda
+     * <code>lambda</code> – delivery of JSON-encoded message to an AWS Lambda
      * function.
      * </p>
      * </li>
@@ -479,47 +539,47 @@ public String getProtocol() {
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>http</code> -- delivery of JSON-encoded message via HTTP
+     *            <code>http</code> – delivery of JSON-encoded message via HTTP
      *            POST
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>https</code> -- delivery of JSON-encoded message via
+     *            <code>https</code> – delivery of JSON-encoded message via
      *            HTTPS POST
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>email</code> -- delivery of message via SMTP
+     *            <code>email</code> – delivery of message via SMTP
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>email-json</code> -- delivery of JSON-encoded message
-     *            via SMTP
+     *            <code>email-json</code> – delivery of JSON-encoded message via
+     *            SMTP
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>sms</code> -- delivery of message via SMS
+     *            <code>sms</code> – delivery of message via SMS
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>sqs</code> -- delivery of JSON-encoded message to an
+     *            <code>sqs</code> – delivery of JSON-encoded message to an
      *            Amazon SQS queue
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>application</code> -- delivery of JSON-encoded message
-     *            to an EndpointArn for a mobile app and device.
+     *            <code>application</code> – delivery of JSON-encoded message to
+     *            an EndpointArn for a mobile app and device.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>lambda</code> -- delivery of JSON-encoded message to an
+     *            <code>lambda</code> – delivery of JSON-encoded message to an
      *            AWS Lambda function.
      *            </p>
      *            </li>
@@ -536,44 +596,44 @@ public void setProtocol(String protocol) {
      * <ul>
      * <li>
      * <p>
-     * <code>http</code> -- delivery of JSON-encoded message via HTTP POST
+     * <code>http</code> – delivery of JSON-encoded message via HTTP POST
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>https</code> -- delivery of JSON-encoded message via HTTPS POST
+     * <code>https</code> – delivery of JSON-encoded message via HTTPS POST
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>email</code> -- delivery of message via SMTP
+     * <code>email</code> – delivery of message via SMTP
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>email-json</code> -- delivery of JSON-encoded message via SMTP
+     * <code>email-json</code> – delivery of JSON-encoded message via SMTP
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>sms</code> -- delivery of message via SMS
+     * <code>sms</code> – delivery of message via SMS
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>sqs</code> -- delivery of JSON-encoded message to an Amazon SQS
+     * <code>sqs</code> – delivery of JSON-encoded message to an Amazon SQS
      * queue
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>application</code> -- delivery of JSON-encoded message to an
+     * <code>application</code> – delivery of JSON-encoded message to an
      * EndpointArn for a mobile app and device.
      * </p>
      * </li>
      * <li>
      * <p>
-     * <code>lambda</code> -- delivery of JSON-encoded message to an AWS Lambda
+     * <code>lambda</code> – delivery of JSON-encoded message to an AWS Lambda
      * function.
      * </p>
      * </li>
@@ -588,47 +648,47 @@ public void setProtocol(String protocol) {
      *            <ul>
      *            <li>
      *            <p>
-     *            <code>http</code> -- delivery of JSON-encoded message via HTTP
+     *            <code>http</code> – delivery of JSON-encoded message via HTTP
      *            POST
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>https</code> -- delivery of JSON-encoded message via
+     *            <code>https</code> – delivery of JSON-encoded message via
      *            HTTPS POST
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>email</code> -- delivery of message via SMTP
+     *            <code>email</code> – delivery of message via SMTP
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>email-json</code> -- delivery of JSON-encoded message
-     *            via SMTP
+     *            <code>email-json</code> – delivery of JSON-encoded message via
+     *            SMTP
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>sms</code> -- delivery of message via SMS
+     *            <code>sms</code> – delivery of message via SMS
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>sqs</code> -- delivery of JSON-encoded message to an
+     *            <code>sqs</code> – delivery of JSON-encoded message to an
      *            Amazon SQS queue
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>application</code> -- delivery of JSON-encoded message
-     *            to an EndpointArn for a mobile app and device.
+     *            <code>application</code> – delivery of JSON-encoded message to
+     *            an EndpointArn for a mobile app and device.
      *            </p>
      *            </li>
      *            <li>
      *            <p>
-     *            <code>lambda</code> -- delivery of JSON-encoded message to an
+     *            <code>lambda</code> – delivery of JSON-encoded message to an
      *            AWS Lambda function.
      *            </p>
      *            </li>
@@ -650,7 +710,7 @@ public SubscribeRequest withProtocol(String protocol) {
      * <li>
      * <p>
      * For the <code>http</code> protocol, the endpoint is an URL beginning with
-     * "http://"
+     * "https://"
      * </p>
      * </li>
      * <li>
@@ -704,7 +764,7 @@ public SubscribeRequest withProtocol(String protocol) {
      *         <li>
      *         <p>
      *         For the <code>http</code> protocol, the endpoint is an URL
-     *         beginning with "http://"
+     *         beginning with "https://"
      *         </p>
      *         </li>
      *         <li>
@@ -764,7 +824,7 @@ public String getEndpoint() {
      * <li>
      * <p>
      * For the <code>http</code> protocol, the endpoint is an URL beginning with
-     * "http://"
+     * "https://"
      * </p>
      * </li>
      * <li>
@@ -818,7 +878,7 @@ public String getEndpoint() {
      *            <li>
      *            <p>
      *            For the <code>http</code> protocol, the endpoint is an URL
-     *            beginning with "http://"
+     *            beginning with "https://"
      *            </p>
      *            </li>
      *            <li>
@@ -878,7 +938,7 @@ public void setEndpoint(String endpoint) {
      * <li>
      * <p>
      * For the <code>http</code> protocol, the endpoint is an URL beginning with
-     * "http://"
+     * "https://"
      * </p>
      * </li>
      * <li>
@@ -935,7 +995,7 @@ public void setEndpoint(String endpoint) {
      *            <li>
      *            <p>
      *            For the <code>http</code> protocol, the endpoint is an URL
-     *            beginning with "http://"
+     *            beginning with "https://"
      *            </p>
      *            </li>
      *            <li>
@@ -989,6 +1049,476 @@ public SubscribeRequest withEndpoint(String endpoint) {
         return this;
     }
 
+    /**
+     * <p>
+     * A map of attributes with their corresponding values.
+     * </p>
+     * <p>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>SetTopicAttributes</code> action uses:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>FilterPolicy</code> – The simple JSON object that lets your
+     * subscriber receive only a subset of messages, rather than receiving every
+     * message published to the topic.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>RawMessageDelivery</code> – When set to <code>true</code>, enables
+     * raw message delivery to Amazon SQS or HTTP/S endpoints. This eliminates
+     * the need for the endpoints to process JSON formatting, which is otherwise
+     * created for Amazon SNS metadata.
+     * </p>
+     * </li>
+     * </ul>
+     *
+     * @return <p>
+     *         A map of attributes with their corresponding values.
+     *         </p>
+     *         <p>
+     *         The following lists the names, descriptions, and values of the
+     *         special request parameters that the
+     *         <code>SetTopicAttributes</code> action uses:
+     *         </p>
+     *         <ul>
+     *         <li>
+     *         <p>
+     *         <code>DeliveryPolicy</code> – The policy that defines how Amazon
+     *         SNS retries failed deliveries to HTTP/S endpoints.
+     *         </p>
+     *         </li>
+     *         <li>
+     *         <p>
+     *         <code>FilterPolicy</code> – The simple JSON object that lets your
+     *         subscriber receive only a subset of messages, rather than
+     *         receiving every message published to the topic.
+     *         </p>
+     *         </li>
+     *         <li>
+     *         <p>
+     *         <code>RawMessageDelivery</code> – When set to <code>true</code>,
+     *         enables raw message delivery to Amazon SQS or HTTP/S endpoints.
+     *         This eliminates the need for the endpoints to process JSON
+     *         formatting, which is otherwise created for Amazon SNS metadata.
+     *         </p>
+     *         </li>
+     *         </ul>
+     */
+    public java.util.Map<String, String> getAttributes() {
+        return attributes;
+    }
+
+    /**
+     * <p>
+     * A map of attributes with their corresponding values.
+     * </p>
+     * <p>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>SetTopicAttributes</code> action uses:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>FilterPolicy</code> – The simple JSON object that lets your
+     * subscriber receive only a subset of messages, rather than receiving every
+     * message published to the topic.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>RawMessageDelivery</code> – When set to <code>true</code>, enables
+     * raw message delivery to Amazon SQS or HTTP/S endpoints. This eliminates
+     * the need for the endpoints to process JSON formatting, which is otherwise
+     * created for Amazon SNS metadata.
+     * </p>
+     * </li>
+     * </ul>
+     *
+     * @param attributes <p>
+     *            A map of attributes with their corresponding values.
+     *            </p>
+     *            <p>
+     *            The following lists the names, descriptions, and values of the
+     *            special request parameters that the
+     *            <code>SetTopicAttributes</code> action uses:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>DeliveryPolicy</code> – The policy that defines how
+     *            Amazon SNS retries failed deliveries to HTTP/S endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>FilterPolicy</code> – The simple JSON object that lets
+     *            your subscriber receive only a subset of messages, rather than
+     *            receiving every message published to the topic.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>RawMessageDelivery</code> – When set to
+     *            <code>true</code>, enables raw message delivery to Amazon SQS
+     *            or HTTP/S endpoints. This eliminates the need for the
+     *            endpoints to process JSON formatting, which is otherwise
+     *            created for Amazon SNS metadata.
+     *            </p>
+     *            </li>
+     *            </ul>
+     */
+    public void setAttributes(java.util.Map<String, String> attributes) {
+        this.attributes = attributes;
+    }
+
+    /**
+     * <p>
+     * A map of attributes with their corresponding values.
+     * </p>
+     * <p>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>SetTopicAttributes</code> action uses:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>FilterPolicy</code> – The simple JSON object that lets your
+     * subscriber receive only a subset of messages, rather than receiving every
+     * message published to the topic.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>RawMessageDelivery</code> – When set to <code>true</code>, enables
+     * raw message delivery to Amazon SQS or HTTP/S endpoints. This eliminates
+     * the need for the endpoints to process JSON formatting, which is otherwise
+     * created for Amazon SNS metadata.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     *
+     * @param attributes <p>
+     *            A map of attributes with their corresponding values.
+     *            </p>
+     *            <p>
+     *            The following lists the names, descriptions, and values of the
+     *            special request parameters that the
+     *            <code>SetTopicAttributes</code> action uses:
+     *            </p>
+     *            <ul>
+     *            <li>
+     *            <p>
+     *            <code>DeliveryPolicy</code> – The policy that defines how
+     *            Amazon SNS retries failed deliveries to HTTP/S endpoints.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>FilterPolicy</code> – The simple JSON object that lets
+     *            your subscriber receive only a subset of messages, rather than
+     *            receiving every message published to the topic.
+     *            </p>
+     *            </li>
+     *            <li>
+     *            <p>
+     *            <code>RawMessageDelivery</code> – When set to
+     *            <code>true</code>, enables raw message delivery to Amazon SQS
+     *            or HTTP/S endpoints. This eliminates the need for the
+     *            endpoints to process JSON formatting, which is otherwise
+     *            created for Amazon SNS metadata.
+     *            </p>
+     *            </li>
+     *            </ul>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public SubscribeRequest withAttributes(java.util.Map<String, String> attributes) {
+        this.attributes = attributes;
+        return this;
+    }
+
+    /**
+     * <p>
+     * A map of attributes with their corresponding values.
+     * </p>
+     * <p>
+     * The following lists the names, descriptions, and values of the special
+     * request parameters that the <code>SetTopicAttributes</code> action uses:
+     * </p>
+     * <ul>
+     * <li>
+     * <p>
+     * <code>DeliveryPolicy</code> – The policy that defines how Amazon SNS
+     * retries failed deliveries to HTTP/S endpoints.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>FilterPolicy</code> – The simple JSON object that lets your
+     * subscriber receive only a subset of messages, rather than receiving every
+     * message published to the topic.
+     * </p>
+     * </li>
+     * <li>
+     * <p>
+     * <code>RawMessageDelivery</code> – When set to <code>true</code>, enables
+     * raw message delivery to Amazon SQS or HTTP/S endpoints. This eliminates
+     * the need for the endpoints to process JSON formatting, which is otherwise
+     * created for Amazon SNS metadata.
+     * </p>
+     * </li>
+     * </ul>
+     * <p>
+     * The method adds a new key-value pair into Attributes parameter, and
+     * returns a reference to this object so that method calls can be chained
+     * together.
+     *
+     * @param key The key of the entry to be added into Attributes.
+     * @param value The corresponding value of the entry to be added into
+     *            Attributes.
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public SubscribeRequest addAttributesEntry(String key, String value) {
+        if (null == this.attributes) {
+            this.attributes = new java.util.HashMap<String, String>();
+        }
+        if (this.attributes.containsKey(key))
+            throw new IllegalArgumentException("Duplicated keys (" + key.toString()
+                    + ") are provided.");
+        this.attributes.put(key, value);
+        return this;
+    }
+
+    /**
+     * Removes all the entries added into Attributes.
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     */
+    public SubscribeRequest clearAttributesEntries() {
+        this.attributes = null;
+        return this;
+    }
+
+    /**
+     * <p>
+     * Sets whether the response from the <code>Subscribe</code> request
+     * includes the subscription ARN, even if the subscription is not yet
+     * confirmed.
+     * </p>
+     * <p>
+     * If you set this parameter to <code>false</code>, the response includes
+     * the ARN for confirmed subscriptions, but it includes an ARN value of
+     * "pending subscription" for subscriptions that are not yet confirmed. A
+     * subscription becomes confirmed when the subscriber calls the
+     * <code>ConfirmSubscription</code> action with a confirmation token.
+     * </p>
+     * <p>
+     * If you set this parameter to <code>true</code>, the response includes the
+     * ARN in all cases, even if the subscription is not yet confirmed.
+     * </p>
+     * <p>
+     * The default value is <code>false</code>.
+     * </p>
+     *
+     * @return <p>
+     *         Sets whether the response from the <code>Subscribe</code> request
+     *         includes the subscription ARN, even if the subscription is not
+     *         yet confirmed.
+     *         </p>
+     *         <p>
+     *         If you set this parameter to <code>false</code>, the response
+     *         includes the ARN for confirmed subscriptions, but it includes an
+     *         ARN value of "pending subscription" for subscriptions that are
+     *         not yet confirmed. A subscription becomes confirmed when the
+     *         subscriber calls the <code>ConfirmSubscription</code> action with
+     *         a confirmation token.
+     *         </p>
+     *         <p>
+     *         If you set this parameter to <code>true</code>, the response
+     *         includes the ARN in all cases, even if the subscription is not
+     *         yet confirmed.
+     *         </p>
+     *         <p>
+     *         The default value is <code>false</code>.
+     *         </p>
+     */
+    public Boolean isReturnSubscriptionArn() {
+        return returnSubscriptionArn;
+    }
+
+    /**
+     * <p>
+     * Sets whether the response from the <code>Subscribe</code> request
+     * includes the subscription ARN, even if the subscription is not yet
+     * confirmed.
+     * </p>
+     * <p>
+     * If you set this parameter to <code>false</code>, the response includes
+     * the ARN for confirmed subscriptions, but it includes an ARN value of
+     * "pending subscription" for subscriptions that are not yet confirmed. A
+     * subscription becomes confirmed when the subscriber calls the
+     * <code>ConfirmSubscription</code> action with a confirmation token.
+     * </p>
+     * <p>
+     * If you set this parameter to <code>true</code>, the response includes the
+     * ARN in all cases, even if the subscription is not yet confirmed.
+     * </p>
+     * <p>
+     * The default value is <code>false</code>.
+     * </p>
+     *
+     * @return <p>
+     *         Sets whether the response from the <code>Subscribe</code> request
+     *         includes the subscription ARN, even if the subscription is not
+     *         yet confirmed.
+     *         </p>
+     *         <p>
+     *         If you set this parameter to <code>false</code>, the response
+     *         includes the ARN for confirmed subscriptions, but it includes an
+     *         ARN value of "pending subscription" for subscriptions that are
+     *         not yet confirmed. A subscription becomes confirmed when the
+     *         subscriber calls the <code>ConfirmSubscription</code> action with
+     *         a confirmation token.
+     *         </p>
+     *         <p>
+     *         If you set this parameter to <code>true</code>, the response
+     *         includes the ARN in all cases, even if the subscription is not
+     *         yet confirmed.
+     *         </p>
+     *         <p>
+     *         The default value is <code>false</code>.
+     *         </p>
+     */
+    public Boolean getReturnSubscriptionArn() {
+        return returnSubscriptionArn;
+    }
+
+    /**
+     * <p>
+     * Sets whether the response from the <code>Subscribe</code> request
+     * includes the subscription ARN, even if the subscription is not yet
+     * confirmed.
+     * </p>
+     * <p>
+     * If you set this parameter to <code>false</code>, the response includes
+     * the ARN for confirmed subscriptions, but it includes an ARN value of
+     * "pending subscription" for subscriptions that are not yet confirmed. A
+     * subscription becomes confirmed when the subscriber calls the
+     * <code>ConfirmSubscription</code> action with a confirmation token.
+     * </p>
+     * <p>
+     * If you set this parameter to <code>true</code>, the response includes the
+     * ARN in all cases, even if the subscription is not yet confirmed.
+     * </p>
+     * <p>
+     * The default value is <code>false</code>.
+     * </p>
+     *
+     * @param returnSubscriptionArn <p>
+     *            Sets whether the response from the <code>Subscribe</code>
+     *            request includes the subscription ARN, even if the
+     *            subscription is not yet confirmed.
+     *            </p>
+     *            <p>
+     *            If you set this parameter to <code>false</code>, the response
+     *            includes the ARN for confirmed subscriptions, but it includes
+     *            an ARN value of "pending subscription" for subscriptions that
+     *            are not yet confirmed. A subscription becomes confirmed when
+     *            the subscriber calls the <code>ConfirmSubscription</code>
+     *            action with a confirmation token.
+     *            </p>
+     *            <p>
+     *            If you set this parameter to <code>true</code>, the response
+     *            includes the ARN in all cases, even if the subscription is not
+     *            yet confirmed.
+     *            </p>
+     *            <p>
+     *            The default value is <code>false</code>.
+     *            </p>
+     */
+    public void setReturnSubscriptionArn(Boolean returnSubscriptionArn) {
+        this.returnSubscriptionArn = returnSubscriptionArn;
+    }
+
+    /**
+     * <p>
+     * Sets whether the response from the <code>Subscribe</code> request
+     * includes the subscription ARN, even if the subscription is not yet
+     * confirmed.
+     * </p>
+     * <p>
+     * If you set this parameter to <code>false</code>, the response includes
+     * the ARN for confirmed subscriptions, but it includes an ARN value of
+     * "pending subscription" for subscriptions that are not yet confirmed. A
+     * subscription becomes confirmed when the subscriber calls the
+     * <code>ConfirmSubscription</code> action with a confirmation token.
+     * </p>
+     * <p>
+     * If you set this parameter to <code>true</code>, the response includes the
+     * ARN in all cases, even if the subscription is not yet confirmed.
+     * </p>
+     * <p>
+     * The default value is <code>false</code>.
+     * </p>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     *
+     * @param returnSubscriptionArn <p>
+     *            Sets whether the response from the <code>Subscribe</code>
+     *            request includes the subscription ARN, even if the
+     *            subscription is not yet confirmed.
+     *            </p>
+     *            <p>
+     *            If you set this parameter to <code>false</code>, the response
+     *            includes the ARN for confirmed subscriptions, but it includes
+     *            an ARN value of "pending subscription" for subscriptions that
+     *            are not yet confirmed. A subscription becomes confirmed when
+     *            the subscriber calls the <code>ConfirmSubscription</code>
+     *            action with a confirmation token.
+     *            </p>
+     *            <p>
+     *            If you set this parameter to <code>true</code>, the response
+     *            includes the ARN in all cases, even if the subscription is not
+     *            yet confirmed.
+     *            </p>
+     *            <p>
+     *            The default value is <code>false</code>.
+     *            </p>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public SubscribeRequest withReturnSubscriptionArn(Boolean returnSubscriptionArn) {
+        this.returnSubscriptionArn = returnSubscriptionArn;
+        return this;
+    }
+
     /**
      * Returns a string representation of this object; useful for testing and
      * debugging.
@@ -1005,7 +1535,11 @@ public String toString() {
         if (getProtocol() != null)
             sb.append("Protocol: " + getProtocol() + ",");
         if (getEndpoint() != null)
-            sb.append("Endpoint: " + getEndpoint());
+            sb.append("Endpoint: " + getEndpoint() + ",");
+        if (getAttributes() != null)
+            sb.append("Attributes: " + getAttributes() + ",");
+        if (getReturnSubscriptionArn() != null)
+            sb.append("ReturnSubscriptionArn: " + getReturnSubscriptionArn());
         sb.append("}");
         return sb.toString();
     }
@@ -1018,6 +1552,10 @@ public int hashCode() {
         hashCode = prime * hashCode + ((getTopicArn() == null) ? 0 : getTopicArn().hashCode());
         hashCode = prime * hashCode + ((getProtocol() == null) ? 0 : getProtocol().hashCode());
         hashCode = prime * hashCode + ((getEndpoint() == null) ? 0 : getEndpoint().hashCode());
+        hashCode = prime * hashCode + ((getAttributes() == null) ? 0 : getAttributes().hashCode());
+        hashCode = prime
+                * hashCode
+                + ((getReturnSubscriptionArn() == null) ? 0 : getReturnSubscriptionArn().hashCode());
         return hashCode;
     }
 
@@ -1044,6 +1582,16 @@ public boolean equals(Object obj) {
             return false;
         if (other.getEndpoint() != null && other.getEndpoint().equals(this.getEndpoint()) == false)
             return false;
+        if (other.getAttributes() == null ^ this.getAttributes() == null)
+            return false;
+        if (other.getAttributes() != null
+                && other.getAttributes().equals(this.getAttributes()) == false)
+            return false;
+        if (other.getReturnSubscriptionArn() == null ^ this.getReturnSubscriptionArn() == null)
+            return false;
+        if (other.getReturnSubscriptionArn() != null
+                && other.getReturnSubscriptionArn().equals(this.getReturnSubscriptionArn()) == false)
+            return false;
         return true;
     }
 }
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SubscribeResult.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SubscribeResult.java
index 3f2ddc9855..5b07052999 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SubscribeResult.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/SubscribeResult.java
@@ -25,22 +25,31 @@
 public class SubscribeResult implements Serializable {
     /**
      * <p>
-     * The ARN of the subscription, if the service was able to create a
-     * subscription immediately (without requiring endpoint owner confirmation).
+     * The ARN of the subscription if it is confirmed, or the string
+     * "pending confirmation" if the subscription requires confirmation.
+     * However, if the API request parameter <code>ReturnSubscriptionArn</code>
+     * is true, then the value is always the subscription ARN, even if the
+     * subscription requires confirmation.
      * </p>
      */
     private String subscriptionArn;
 
     /**
      * <p>
-     * The ARN of the subscription, if the service was able to create a
-     * subscription immediately (without requiring endpoint owner confirmation).
+     * The ARN of the subscription if it is confirmed, or the string
+     * "pending confirmation" if the subscription requires confirmation.
+     * However, if the API request parameter <code>ReturnSubscriptionArn</code>
+     * is true, then the value is always the subscription ARN, even if the
+     * subscription requires confirmation.
      * </p>
      *
      * @return <p>
-     *         The ARN of the subscription, if the service was able to create a
-     *         subscription immediately (without requiring endpoint owner
-     *         confirmation).
+     *         The ARN of the subscription if it is confirmed, or the string
+     *         "pending confirmation" if the subscription requires confirmation.
+     *         However, if the API request parameter
+     *         <code>ReturnSubscriptionArn</code> is true, then the value is
+     *         always the subscription ARN, even if the subscription requires
+     *         confirmation.
      *         </p>
      */
     public String getSubscriptionArn() {
@@ -49,14 +58,20 @@ public String getSubscriptionArn() {
 
     /**
      * <p>
-     * The ARN of the subscription, if the service was able to create a
-     * subscription immediately (without requiring endpoint owner confirmation).
+     * The ARN of the subscription if it is confirmed, or the string
+     * "pending confirmation" if the subscription requires confirmation.
+     * However, if the API request parameter <code>ReturnSubscriptionArn</code>
+     * is true, then the value is always the subscription ARN, even if the
+     * subscription requires confirmation.
      * </p>
      *
      * @param subscriptionArn <p>
-     *            The ARN of the subscription, if the service was able to create
-     *            a subscription immediately (without requiring endpoint owner
-     *            confirmation).
+     *            The ARN of the subscription if it is confirmed, or the string
+     *            "pending confirmation" if the subscription requires
+     *            confirmation. However, if the API request parameter
+     *            <code>ReturnSubscriptionArn</code> is true, then the value is
+     *            always the subscription ARN, even if the subscription requires
+     *            confirmation.
      *            </p>
      */
     public void setSubscriptionArn(String subscriptionArn) {
@@ -65,17 +80,23 @@ public void setSubscriptionArn(String subscriptionArn) {
 
     /**
      * <p>
-     * The ARN of the subscription, if the service was able to create a
-     * subscription immediately (without requiring endpoint owner confirmation).
+     * The ARN of the subscription if it is confirmed, or the string
+     * "pending confirmation" if the subscription requires confirmation.
+     * However, if the API request parameter <code>ReturnSubscriptionArn</code>
+     * is true, then the value is always the subscription ARN, even if the
+     * subscription requires confirmation.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param subscriptionArn <p>
-     *            The ARN of the subscription, if the service was able to create
-     *            a subscription immediately (without requiring endpoint owner
-     *            confirmation).
+     *            The ARN of the subscription if it is confirmed, or the string
+     *            "pending confirmation" if the subscription requires
+     *            confirmation. However, if the API request parameter
+     *            <code>ReturnSubscriptionArn</code> is true, then the value is
+     *            always the subscription ARN, even if the subscription requires
+     *            confirmation.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/Tag.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/Tag.java
new file mode 100644
index 0000000000..1dec4b84af
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/Tag.java
@@ -0,0 +1,204 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import java.io.Serializable;
+
+/**
+ * <p>
+ * The list of tags to be added to the specified topic.
+ * </p>
+ */
+public class Tag implements Serializable {
+    /**
+     * <p>
+     * The required key portion of the tag.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 128<br/>
+     */
+    private String key;
+
+    /**
+     * <p>
+     * The optional value portion of the tag.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>0 - 256<br/>
+     */
+    private String value;
+
+    /**
+     * <p>
+     * The required key portion of the tag.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 128<br/>
+     *
+     * @return <p>
+     *         The required key portion of the tag.
+     *         </p>
+     */
+    public String getKey() {
+        return key;
+    }
+
+    /**
+     * <p>
+     * The required key portion of the tag.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 128<br/>
+     *
+     * @param key <p>
+     *            The required key portion of the tag.
+     *            </p>
+     */
+    public void setKey(String key) {
+        this.key = key;
+    }
+
+    /**
+     * <p>
+     * The required key portion of the tag.
+     * </p>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 128<br/>
+     *
+     * @param key <p>
+     *            The required key portion of the tag.
+     *            </p>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public Tag withKey(String key) {
+        this.key = key;
+        return this;
+    }
+
+    /**
+     * <p>
+     * The optional value portion of the tag.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>0 - 256<br/>
+     *
+     * @return <p>
+     *         The optional value portion of the tag.
+     *         </p>
+     */
+    public String getValue() {
+        return value;
+    }
+
+    /**
+     * <p>
+     * The optional value portion of the tag.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>0 - 256<br/>
+     *
+     * @param value <p>
+     *            The optional value portion of the tag.
+     *            </p>
+     */
+    public void setValue(String value) {
+        this.value = value;
+    }
+
+    /**
+     * <p>
+     * The optional value portion of the tag.
+     * </p>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>0 - 256<br/>
+     *
+     * @param value <p>
+     *            The optional value portion of the tag.
+     *            </p>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public Tag withValue(String value) {
+        this.value = value;
+        return this;
+    }
+
+    /**
+     * Returns a string representation of this object; useful for testing and
+     * debugging.
+     *
+     * @return A string representation of this object.
+     * @see java.lang.Object#toString()
+     */
+    @Override
+    public String toString() {
+        StringBuilder sb = new StringBuilder();
+        sb.append("{");
+        if (getKey() != null)
+            sb.append("Key: " + getKey() + ",");
+        if (getValue() != null)
+            sb.append("Value: " + getValue());
+        sb.append("}");
+        return sb.toString();
+    }
+
+    @Override
+    public int hashCode() {
+        final int prime = 31;
+        int hashCode = 1;
+
+        hashCode = prime * hashCode + ((getKey() == null) ? 0 : getKey().hashCode());
+        hashCode = prime * hashCode + ((getValue() == null) ? 0 : getValue().hashCode());
+        return hashCode;
+    }
+
+    @Override
+    public boolean equals(Object obj) {
+        if (this == obj)
+            return true;
+        if (obj == null)
+            return false;
+
+        if (obj instanceof Tag == false)
+            return false;
+        Tag other = (Tag) obj;
+
+        if (other.getKey() == null ^ this.getKey() == null)
+            return false;
+        if (other.getKey() != null && other.getKey().equals(this.getKey()) == false)
+            return false;
+        if (other.getValue() == null ^ this.getValue() == null)
+            return false;
+        if (other.getValue() != null && other.getValue().equals(this.getValue()) == false)
+            return false;
+        return true;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/TagLimitExceededException.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/TagLimitExceededException.java
new file mode 100644
index 0000000000..27c8e2f5ff
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/TagLimitExceededException.java
@@ -0,0 +1,37 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import com.amazonaws.AmazonServiceException;
+
+/**
+ * <p>
+ * Can't add more than 50 tags to a topic.
+ * </p>
+ */
+public class TagLimitExceededException extends AmazonServiceException {
+    private static final long serialVersionUID = 1L;
+
+    /**
+     * Constructs a new TagLimitExceededException with the specified error
+     * message.
+     *
+     * @param message Describes the error encountered.
+     */
+    public TagLimitExceededException(String message) {
+        super(message);
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/TagPolicyException.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/TagPolicyException.java
new file mode 100644
index 0000000000..6e22ba68a4
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/TagPolicyException.java
@@ -0,0 +1,37 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import com.amazonaws.AmazonServiceException;
+
+/**
+ * <p>
+ * The request doesn't comply with the IAM tag policy. Correct your request and
+ * then retry it.
+ * </p>
+ */
+public class TagPolicyException extends AmazonServiceException {
+    private static final long serialVersionUID = 1L;
+
+    /**
+     * Constructs a new TagPolicyException with the specified error message.
+     *
+     * @param message Describes the error encountered.
+     */
+    public TagPolicyException(String message) {
+        super(message);
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/TagResourceRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/TagResourceRequest.java
new file mode 100644
index 0000000000..acb41c0f08
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/TagResourceRequest.java
@@ -0,0 +1,276 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import java.io.Serializable;
+
+import com.amazonaws.AmazonWebServiceRequest;
+
+/**
+ * <p>
+ * Add tags to the specified Amazon SNS topic. For an overview, see <a
+ * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+ * Tags</a> in the <i>Amazon SNS Developer Guide</i>.
+ * </p>
+ * <p>
+ * When you use topic tags, keep the following guidelines in mind:
+ * </p>
+ * <ul>
+ * <li>
+ * <p>
+ * Adding more than 50 tags to a topic isn't recommended.
+ * </p>
+ * </li>
+ * <li>
+ * <p>
+ * Tags don't have any semantic meaning. Amazon SNS interprets tags as character
+ * strings.
+ * </p>
+ * </li>
+ * <li>
+ * <p>
+ * Tags are case-sensitive.
+ * </p>
+ * </li>
+ * <li>
+ * <p>
+ * A new tag with a key identical to that of an existing tag overwrites the
+ * existing tag.
+ * </p>
+ * </li>
+ * <li>
+ * <p>
+ * Tagging actions are limited to 10 TPS per AWS account. If your application
+ * requires a higher throughput, file a <a href=
+ * "https://console.aws.amazon.com/support/home#/case/create?issueType=technical"
+ * >technical support request</a>.
+ * </p>
+ * </li>
+ * </ul>
+ * <p>
+ * For a full list of tag restrictions, see <a href=
+ * "https://docs.aws.amazon.com/sns/latest/dg/sns-limits.html#limits-topics"
+ * >Limits Related to Topics</a> in the <i>Amazon SNS Developer Guide</i>.
+ * </p>
+ */
+public class TagResourceRequest extends AmazonWebServiceRequest implements Serializable {
+    /**
+     * <p>
+     * The ARN of the topic to which to add tags.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 1011<br/>
+     */
+    private String resourceArn;
+
+    /**
+     * <p>
+     * The tags to be added to the specified topic. A tag consists of a required
+     * key and an optional value.
+     * </p>
+     */
+    private java.util.List<Tag> tags = new java.util.ArrayList<Tag>();
+
+    /**
+     * <p>
+     * The ARN of the topic to which to add tags.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 1011<br/>
+     *
+     * @return <p>
+     *         The ARN of the topic to which to add tags.
+     *         </p>
+     */
+    public String getResourceArn() {
+        return resourceArn;
+    }
+
+    /**
+     * <p>
+     * The ARN of the topic to which to add tags.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 1011<br/>
+     *
+     * @param resourceArn <p>
+     *            The ARN of the topic to which to add tags.
+     *            </p>
+     */
+    public void setResourceArn(String resourceArn) {
+        this.resourceArn = resourceArn;
+    }
+
+    /**
+     * <p>
+     * The ARN of the topic to which to add tags.
+     * </p>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 1011<br/>
+     *
+     * @param resourceArn <p>
+     *            The ARN of the topic to which to add tags.
+     *            </p>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public TagResourceRequest withResourceArn(String resourceArn) {
+        this.resourceArn = resourceArn;
+        return this;
+    }
+
+    /**
+     * <p>
+     * The tags to be added to the specified topic. A tag consists of a required
+     * key and an optional value.
+     * </p>
+     *
+     * @return <p>
+     *         The tags to be added to the specified topic. A tag consists of a
+     *         required key and an optional value.
+     *         </p>
+     */
+    public java.util.List<Tag> getTags() {
+        return tags;
+    }
+
+    /**
+     * <p>
+     * The tags to be added to the specified topic. A tag consists of a required
+     * key and an optional value.
+     * </p>
+     *
+     * @param tags <p>
+     *            The tags to be added to the specified topic. A tag consists of
+     *            a required key and an optional value.
+     *            </p>
+     */
+    public void setTags(java.util.Collection<Tag> tags) {
+        if (tags == null) {
+            this.tags = null;
+            return;
+        }
+
+        this.tags = new java.util.ArrayList<Tag>(tags);
+    }
+
+    /**
+     * <p>
+     * The tags to be added to the specified topic. A tag consists of a required
+     * key and an optional value.
+     * </p>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     *
+     * @param tags <p>
+     *            The tags to be added to the specified topic. A tag consists of
+     *            a required key and an optional value.
+     *            </p>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public TagResourceRequest withTags(Tag... tags) {
+        if (getTags() == null) {
+            this.tags = new java.util.ArrayList<Tag>(tags.length);
+        }
+        for (Tag value : tags) {
+            this.tags.add(value);
+        }
+        return this;
+    }
+
+    /**
+     * <p>
+     * The tags to be added to the specified topic. A tag consists of a required
+     * key and an optional value.
+     * </p>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     *
+     * @param tags <p>
+     *            The tags to be added to the specified topic. A tag consists of
+     *            a required key and an optional value.
+     *            </p>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public TagResourceRequest withTags(java.util.Collection<Tag> tags) {
+        setTags(tags);
+        return this;
+    }
+
+    /**
+     * Returns a string representation of this object; useful for testing and
+     * debugging.
+     *
+     * @return A string representation of this object.
+     * @see java.lang.Object#toString()
+     */
+    @Override
+    public String toString() {
+        StringBuilder sb = new StringBuilder();
+        sb.append("{");
+        if (getResourceArn() != null)
+            sb.append("ResourceArn: " + getResourceArn() + ",");
+        if (getTags() != null)
+            sb.append("Tags: " + getTags());
+        sb.append("}");
+        return sb.toString();
+    }
+
+    @Override
+    public int hashCode() {
+        final int prime = 31;
+        int hashCode = 1;
+
+        hashCode = prime * hashCode
+                + ((getResourceArn() == null) ? 0 : getResourceArn().hashCode());
+        hashCode = prime * hashCode + ((getTags() == null) ? 0 : getTags().hashCode());
+        return hashCode;
+    }
+
+    @Override
+    public boolean equals(Object obj) {
+        if (this == obj)
+            return true;
+        if (obj == null)
+            return false;
+
+        if (obj instanceof TagResourceRequest == false)
+            return false;
+        TagResourceRequest other = (TagResourceRequest) obj;
+
+        if (other.getResourceArn() == null ^ this.getResourceArn() == null)
+            return false;
+        if (other.getResourceArn() != null
+                && other.getResourceArn().equals(this.getResourceArn()) == false)
+            return false;
+        if (other.getTags() == null ^ this.getTags() == null)
+            return false;
+        if (other.getTags() != null && other.getTags().equals(this.getTags()) == false)
+            return false;
+        return true;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/TagResourceResult.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/TagResourceResult.java
new file mode 100644
index 0000000000..ea3d24e395
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/TagResourceResult.java
@@ -0,0 +1,57 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import java.io.Serializable;
+
+public class TagResourceResult implements Serializable {
+    /**
+     * Returns a string representation of this object; useful for testing and
+     * debugging.
+     *
+     * @return A string representation of this object.
+     * @see java.lang.Object#toString()
+     */
+    @Override
+    public String toString() {
+        StringBuilder sb = new StringBuilder();
+        sb.append("{");
+        sb.append("}");
+        return sb.toString();
+    }
+
+    @Override
+    public int hashCode() {
+        final int prime = 31;
+        int hashCode = 1;
+
+        return hashCode;
+    }
+
+    @Override
+    public boolean equals(Object obj) {
+        if (this == obj)
+            return true;
+        if (obj == null)
+            return false;
+
+        if (obj instanceof TagResourceResult == false)
+            return false;
+        TagResourceResult other = (TagResourceResult) obj;
+
+        return true;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/UnsubscribeRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/UnsubscribeRequest.java
index 9ac07faebf..9bdecf2710 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/UnsubscribeRequest.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/UnsubscribeRequest.java
@@ -29,6 +29,9 @@
  * delivered to the endpoint, so that the endpoint owner can easily resubscribe
  * to the topic if the <code>Unsubscribe</code> request was unintended.
  * </p>
+ * <p>
+ * This action is throttled at 100 transactions per second (TPS).
+ * </p>
  */
 public class UnsubscribeRequest extends AmazonWebServiceRequest implements Serializable {
     /**
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/UntagResourceRequest.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/UntagResourceRequest.java
new file mode 100644
index 0000000000..c396e24f13
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/UntagResourceRequest.java
@@ -0,0 +1,227 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import java.io.Serializable;
+
+import com.amazonaws.AmazonWebServiceRequest;
+
+/**
+ * <p>
+ * Remove tags from the specified Amazon SNS topic. For an overview, see <a
+ * href="https://docs.aws.amazon.com/sns/latest/dg/sns-tags.html">Amazon SNS
+ * Tags</a> in the <i>Amazon SNS Developer Guide</i>.
+ * </p>
+ */
+public class UntagResourceRequest extends AmazonWebServiceRequest implements Serializable {
+    /**
+     * <p>
+     * The ARN of the topic from which to remove tags.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 1011<br/>
+     */
+    private String resourceArn;
+
+    /**
+     * <p>
+     * The list of tag keys to remove from the specified topic.
+     * </p>
+     */
+    private java.util.List<String> tagKeys = new java.util.ArrayList<String>();
+
+    /**
+     * <p>
+     * The ARN of the topic from which to remove tags.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 1011<br/>
+     *
+     * @return <p>
+     *         The ARN of the topic from which to remove tags.
+     *         </p>
+     */
+    public String getResourceArn() {
+        return resourceArn;
+    }
+
+    /**
+     * <p>
+     * The ARN of the topic from which to remove tags.
+     * </p>
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 1011<br/>
+     *
+     * @param resourceArn <p>
+     *            The ARN of the topic from which to remove tags.
+     *            </p>
+     */
+    public void setResourceArn(String resourceArn) {
+        this.resourceArn = resourceArn;
+    }
+
+    /**
+     * <p>
+     * The ARN of the topic from which to remove tags.
+     * </p>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     * <p>
+     * <b>Constraints:</b><br/>
+     * <b>Length: </b>1 - 1011<br/>
+     *
+     * @param resourceArn <p>
+     *            The ARN of the topic from which to remove tags.
+     *            </p>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public UntagResourceRequest withResourceArn(String resourceArn) {
+        this.resourceArn = resourceArn;
+        return this;
+    }
+
+    /**
+     * <p>
+     * The list of tag keys to remove from the specified topic.
+     * </p>
+     *
+     * @return <p>
+     *         The list of tag keys to remove from the specified topic.
+     *         </p>
+     */
+    public java.util.List<String> getTagKeys() {
+        return tagKeys;
+    }
+
+    /**
+     * <p>
+     * The list of tag keys to remove from the specified topic.
+     * </p>
+     *
+     * @param tagKeys <p>
+     *            The list of tag keys to remove from the specified topic.
+     *            </p>
+     */
+    public void setTagKeys(java.util.Collection<String> tagKeys) {
+        if (tagKeys == null) {
+            this.tagKeys = null;
+            return;
+        }
+
+        this.tagKeys = new java.util.ArrayList<String>(tagKeys);
+    }
+
+    /**
+     * <p>
+     * The list of tag keys to remove from the specified topic.
+     * </p>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     *
+     * @param tagKeys <p>
+     *            The list of tag keys to remove from the specified topic.
+     *            </p>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public UntagResourceRequest withTagKeys(String... tagKeys) {
+        if (getTagKeys() == null) {
+            this.tagKeys = new java.util.ArrayList<String>(tagKeys.length);
+        }
+        for (String value : tagKeys) {
+            this.tagKeys.add(value);
+        }
+        return this;
+    }
+
+    /**
+     * <p>
+     * The list of tag keys to remove from the specified topic.
+     * </p>
+     * <p>
+     * Returns a reference to this object so that method calls can be chained
+     * together.
+     *
+     * @param tagKeys <p>
+     *            The list of tag keys to remove from the specified topic.
+     *            </p>
+     * @return A reference to this updated object so that method calls can be
+     *         chained together.
+     */
+    public UntagResourceRequest withTagKeys(java.util.Collection<String> tagKeys) {
+        setTagKeys(tagKeys);
+        return this;
+    }
+
+    /**
+     * Returns a string representation of this object; useful for testing and
+     * debugging.
+     *
+     * @return A string representation of this object.
+     * @see java.lang.Object#toString()
+     */
+    @Override
+    public String toString() {
+        StringBuilder sb = new StringBuilder();
+        sb.append("{");
+        if (getResourceArn() != null)
+            sb.append("ResourceArn: " + getResourceArn() + ",");
+        if (getTagKeys() != null)
+            sb.append("TagKeys: " + getTagKeys());
+        sb.append("}");
+        return sb.toString();
+    }
+
+    @Override
+    public int hashCode() {
+        final int prime = 31;
+        int hashCode = 1;
+
+        hashCode = prime * hashCode
+                + ((getResourceArn() == null) ? 0 : getResourceArn().hashCode());
+        hashCode = prime * hashCode + ((getTagKeys() == null) ? 0 : getTagKeys().hashCode());
+        return hashCode;
+    }
+
+    @Override
+    public boolean equals(Object obj) {
+        if (this == obj)
+            return true;
+        if (obj == null)
+            return false;
+
+        if (obj instanceof UntagResourceRequest == false)
+            return false;
+        UntagResourceRequest other = (UntagResourceRequest) obj;
+
+        if (other.getResourceArn() == null ^ this.getResourceArn() == null)
+            return false;
+        if (other.getResourceArn() != null
+                && other.getResourceArn().equals(this.getResourceArn()) == false)
+            return false;
+        if (other.getTagKeys() == null ^ this.getTagKeys() == null)
+            return false;
+        if (other.getTagKeys() != null && other.getTagKeys().equals(this.getTagKeys()) == false)
+            return false;
+        return true;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/UntagResourceResult.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/UntagResourceResult.java
new file mode 100644
index 0000000000..e75085cfe7
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/UntagResourceResult.java
@@ -0,0 +1,57 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model;
+
+import java.io.Serializable;
+
+public class UntagResourceResult implements Serializable {
+    /**
+     * Returns a string representation of this object; useful for testing and
+     * debugging.
+     *
+     * @return A string representation of this object.
+     * @see java.lang.Object#toString()
+     */
+    @Override
+    public String toString() {
+        StringBuilder sb = new StringBuilder();
+        sb.append("{");
+        sb.append("}");
+        return sb.toString();
+    }
+
+    @Override
+    public int hashCode() {
+        final int prime = 31;
+        int hashCode = 1;
+
+        return hashCode;
+    }
+
+    @Override
+    public boolean equals(Object obj) {
+        if (this == obj)
+            return true;
+        if (obj == null)
+            return false;
+
+        if (obj instanceof UntagResourceResult == false)
+            return false;
+        UntagResourceResult other = (UntagResourceResult) obj;
+
+        return true;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/ConcurrentAccessExceptionUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/ConcurrentAccessExceptionUnmarshaller.java
new file mode 100644
index 0000000000..5e77eb128d
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/ConcurrentAccessExceptionUnmarshaller.java
@@ -0,0 +1,43 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import org.w3c.dom.Node;
+
+import com.amazonaws.AmazonServiceException;
+import com.amazonaws.util.XpathUtils;
+import com.amazonaws.transform.StandardErrorUnmarshaller;
+
+import com.amazonaws.services.sns.model.ConcurrentAccessException;
+
+public class ConcurrentAccessExceptionUnmarshaller extends StandardErrorUnmarshaller {
+
+    public ConcurrentAccessExceptionUnmarshaller() {
+        super(ConcurrentAccessException.class);
+    }
+
+    public AmazonServiceException unmarshall(Node node) throws Exception {
+        // Bail out if this isn't the right error code that this
+        // marshaller understands.
+        String errorCode = parseErrorCode(node);
+        if (errorCode == null || !errorCode.equals("ConcurrentAccess"))
+            return null;
+
+        ConcurrentAccessException e = (ConcurrentAccessException) super.unmarshall(node);
+
+        return e;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/CreateTopicRequestMarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/CreateTopicRequestMarshaller.java
index dca2d86fdf..6c0e943d73 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/CreateTopicRequestMarshaller.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/CreateTopicRequestMarshaller.java
@@ -45,6 +45,40 @@
             String name = createTopicRequest.getName();
             request.addParameter(prefix, StringUtils.fromString(name));
         }
+        if (createTopicRequest.getAttributes() != null) {
+            prefix = "Attributes";
+            java.util.Map<String, String> attributes = createTopicRequest.getAttributes();
+            int attributesIndex = 1;
+            String attributesPrefix = prefix + ".entry.";
+            for (java.util.Map.Entry<String, String> attributesEntry : attributes.entrySet()) {
+                prefix = attributesPrefix + attributesIndex;
+                if (attributesEntry.getKey() != null) {
+                    request.addParameter(prefix + ".key",
+                            StringUtils.fromString(attributesEntry.getKey()));
+                }
+                prefix += ".value";
+                if (attributesEntry.getValue() != null) {
+                    String attributesValue = attributesEntry.getValue();
+                    request.addParameter(prefix, StringUtils.fromString(attributesValue));
+                }
+                attributesIndex++;
+            }
+            prefix = attributesPrefix;
+        }
+        if (createTopicRequest.getTags() != null) {
+            prefix = "Tags";
+            java.util.List<Tag> tags = createTopicRequest.getTags();
+            int tagsIndex = 1;
+            String tagsPrefix = prefix;
+            for (Tag tagsItem : tags) {
+                prefix = tagsPrefix + ".member." + tagsIndex;
+                if (tagsItem != null) {
+                    TagStaxMarshaller.getInstance().marshall(tagsItem, request, prefix + ".");
+                }
+                tagsIndex++;
+            }
+            prefix = tagsPrefix;
+        }
 
         return request;
     }
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/FilterPolicyLimitExceededExceptionUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/FilterPolicyLimitExceededExceptionUnmarshaller.java
new file mode 100644
index 0000000000..b928c4df7e
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/FilterPolicyLimitExceededExceptionUnmarshaller.java
@@ -0,0 +1,44 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import org.w3c.dom.Node;
+
+import com.amazonaws.AmazonServiceException;
+import com.amazonaws.util.XpathUtils;
+import com.amazonaws.transform.StandardErrorUnmarshaller;
+
+import com.amazonaws.services.sns.model.FilterPolicyLimitExceededException;
+
+public class FilterPolicyLimitExceededExceptionUnmarshaller extends StandardErrorUnmarshaller {
+
+    public FilterPolicyLimitExceededExceptionUnmarshaller() {
+        super(FilterPolicyLimitExceededException.class);
+    }
+
+    public AmazonServiceException unmarshall(Node node) throws Exception {
+        // Bail out if this isn't the right error code that this
+        // marshaller understands.
+        String errorCode = parseErrorCode(node);
+        if (errorCode == null || !errorCode.equals("FilterPolicyLimitExceeded"))
+            return null;
+
+        FilterPolicyLimitExceededException e = (FilterPolicyLimitExceededException) super
+                .unmarshall(node);
+
+        return e;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/InvalidSecurityExceptionUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/InvalidSecurityExceptionUnmarshaller.java
new file mode 100644
index 0000000000..aad883ad25
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/InvalidSecurityExceptionUnmarshaller.java
@@ -0,0 +1,43 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import org.w3c.dom.Node;
+
+import com.amazonaws.AmazonServiceException;
+import com.amazonaws.util.XpathUtils;
+import com.amazonaws.transform.StandardErrorUnmarshaller;
+
+import com.amazonaws.services.sns.model.InvalidSecurityException;
+
+public class InvalidSecurityExceptionUnmarshaller extends StandardErrorUnmarshaller {
+
+    public InvalidSecurityExceptionUnmarshaller() {
+        super(InvalidSecurityException.class);
+    }
+
+    public AmazonServiceException unmarshall(Node node) throws Exception {
+        // Bail out if this isn't the right error code that this
+        // marshaller understands.
+        String errorCode = parseErrorCode(node);
+        if (errorCode == null || !errorCode.equals("InvalidSecurity"))
+            return null;
+
+        InvalidSecurityException e = (InvalidSecurityException) super.unmarshall(node);
+
+        return e;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSAccessDeniedExceptionUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSAccessDeniedExceptionUnmarshaller.java
new file mode 100644
index 0000000000..638075fad0
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSAccessDeniedExceptionUnmarshaller.java
@@ -0,0 +1,43 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import org.w3c.dom.Node;
+
+import com.amazonaws.AmazonServiceException;
+import com.amazonaws.util.XpathUtils;
+import com.amazonaws.transform.StandardErrorUnmarshaller;
+
+import com.amazonaws.services.sns.model.KMSAccessDeniedException;
+
+public class KMSAccessDeniedExceptionUnmarshaller extends StandardErrorUnmarshaller {
+
+    public KMSAccessDeniedExceptionUnmarshaller() {
+        super(KMSAccessDeniedException.class);
+    }
+
+    public AmazonServiceException unmarshall(Node node) throws Exception {
+        // Bail out if this isn't the right error code that this
+        // marshaller understands.
+        String errorCode = parseErrorCode(node);
+        if (errorCode == null || !errorCode.equals("KMSAccessDenied"))
+            return null;
+
+        KMSAccessDeniedException e = (KMSAccessDeniedException) super.unmarshall(node);
+
+        return e;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSDisabledExceptionUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSDisabledExceptionUnmarshaller.java
new file mode 100644
index 0000000000..94e808d6d3
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSDisabledExceptionUnmarshaller.java
@@ -0,0 +1,43 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import org.w3c.dom.Node;
+
+import com.amazonaws.AmazonServiceException;
+import com.amazonaws.util.XpathUtils;
+import com.amazonaws.transform.StandardErrorUnmarshaller;
+
+import com.amazonaws.services.sns.model.KMSDisabledException;
+
+public class KMSDisabledExceptionUnmarshaller extends StandardErrorUnmarshaller {
+
+    public KMSDisabledExceptionUnmarshaller() {
+        super(KMSDisabledException.class);
+    }
+
+    public AmazonServiceException unmarshall(Node node) throws Exception {
+        // Bail out if this isn't the right error code that this
+        // marshaller understands.
+        String errorCode = parseErrorCode(node);
+        if (errorCode == null || !errorCode.equals("KMSDisabled"))
+            return null;
+
+        KMSDisabledException e = (KMSDisabledException) super.unmarshall(node);
+
+        return e;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSInvalidStateExceptionUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSInvalidStateExceptionUnmarshaller.java
new file mode 100644
index 0000000000..58378b71c8
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSInvalidStateExceptionUnmarshaller.java
@@ -0,0 +1,43 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import org.w3c.dom.Node;
+
+import com.amazonaws.AmazonServiceException;
+import com.amazonaws.util.XpathUtils;
+import com.amazonaws.transform.StandardErrorUnmarshaller;
+
+import com.amazonaws.services.sns.model.KMSInvalidStateException;
+
+public class KMSInvalidStateExceptionUnmarshaller extends StandardErrorUnmarshaller {
+
+    public KMSInvalidStateExceptionUnmarshaller() {
+        super(KMSInvalidStateException.class);
+    }
+
+    public AmazonServiceException unmarshall(Node node) throws Exception {
+        // Bail out if this isn't the right error code that this
+        // marshaller understands.
+        String errorCode = parseErrorCode(node);
+        if (errorCode == null || !errorCode.equals("KMSInvalidState"))
+            return null;
+
+        KMSInvalidStateException e = (KMSInvalidStateException) super.unmarshall(node);
+
+        return e;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSNotFoundExceptionUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSNotFoundExceptionUnmarshaller.java
new file mode 100644
index 0000000000..91fd3c4921
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSNotFoundExceptionUnmarshaller.java
@@ -0,0 +1,43 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import org.w3c.dom.Node;
+
+import com.amazonaws.AmazonServiceException;
+import com.amazonaws.util.XpathUtils;
+import com.amazonaws.transform.StandardErrorUnmarshaller;
+
+import com.amazonaws.services.sns.model.KMSNotFoundException;
+
+public class KMSNotFoundExceptionUnmarshaller extends StandardErrorUnmarshaller {
+
+    public KMSNotFoundExceptionUnmarshaller() {
+        super(KMSNotFoundException.class);
+    }
+
+    public AmazonServiceException unmarshall(Node node) throws Exception {
+        // Bail out if this isn't the right error code that this
+        // marshaller understands.
+        String errorCode = parseErrorCode(node);
+        if (errorCode == null || !errorCode.equals("KMSNotFound"))
+            return null;
+
+        KMSNotFoundException e = (KMSNotFoundException) super.unmarshall(node);
+
+        return e;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSOptInRequiredExceptionUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSOptInRequiredExceptionUnmarshaller.java
new file mode 100644
index 0000000000..38c62184a2
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSOptInRequiredExceptionUnmarshaller.java
@@ -0,0 +1,43 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import org.w3c.dom.Node;
+
+import com.amazonaws.AmazonServiceException;
+import com.amazonaws.util.XpathUtils;
+import com.amazonaws.transform.StandardErrorUnmarshaller;
+
+import com.amazonaws.services.sns.model.KMSOptInRequiredException;
+
+public class KMSOptInRequiredExceptionUnmarshaller extends StandardErrorUnmarshaller {
+
+    public KMSOptInRequiredExceptionUnmarshaller() {
+        super(KMSOptInRequiredException.class);
+    }
+
+    public AmazonServiceException unmarshall(Node node) throws Exception {
+        // Bail out if this isn't the right error code that this
+        // marshaller understands.
+        String errorCode = parseErrorCode(node);
+        if (errorCode == null || !errorCode.equals("KMSOptInRequired"))
+            return null;
+
+        KMSOptInRequiredException e = (KMSOptInRequiredException) super.unmarshall(node);
+
+        return e;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSThrottlingExceptionUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSThrottlingExceptionUnmarshaller.java
new file mode 100644
index 0000000000..d06b377fb1
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/KMSThrottlingExceptionUnmarshaller.java
@@ -0,0 +1,43 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import org.w3c.dom.Node;
+
+import com.amazonaws.AmazonServiceException;
+import com.amazonaws.util.XpathUtils;
+import com.amazonaws.transform.StandardErrorUnmarshaller;
+
+import com.amazonaws.services.sns.model.KMSThrottlingException;
+
+public class KMSThrottlingExceptionUnmarshaller extends StandardErrorUnmarshaller {
+
+    public KMSThrottlingExceptionUnmarshaller() {
+        super(KMSThrottlingException.class);
+    }
+
+    public AmazonServiceException unmarshall(Node node) throws Exception {
+        // Bail out if this isn't the right error code that this
+        // marshaller understands.
+        String errorCode = parseErrorCode(node);
+        if (errorCode == null || !errorCode.equals("KMSThrottling"))
+            return null;
+
+        KMSThrottlingException e = (KMSThrottlingException) super.unmarshall(node);
+
+        return e;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/ListTagsForResourceRequestMarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/ListTagsForResourceRequestMarshaller.java
new file mode 100644
index 0000000000..72872cb6b7
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/ListTagsForResourceRequestMarshaller.java
@@ -0,0 +1,52 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import com.amazonaws.AmazonClientException;
+import com.amazonaws.Request;
+import com.amazonaws.DefaultRequest;
+import com.amazonaws.services.sns.model.*;
+import com.amazonaws.transform.Marshaller;
+import com.amazonaws.util.StringUtils;
+
+/**
+ * StAX request marshaller for ListTagsForResourceRequest
+ */
+public class ListTagsForResourceRequestMarshaller implements
+        Marshaller<Request<ListTagsForResourceRequest>, ListTagsForResourceRequest> {
+
+    public Request<ListTagsForResourceRequest> marshall(
+            ListTagsForResourceRequest listTagsForResourceRequest) {
+        if (listTagsForResourceRequest == null) {
+            throw new AmazonClientException(
+                    "Invalid argument passed to marshall(ListTagsForResourceRequest)");
+        }
+
+        Request<ListTagsForResourceRequest> request = new DefaultRequest<ListTagsForResourceRequest>(
+                listTagsForResourceRequest, "AmazonSNS");
+        request.addParameter("Action", "ListTagsForResource");
+        request.addParameter("Version", "2010-03-31");
+
+        String prefix;
+        if (listTagsForResourceRequest.getResourceArn() != null) {
+            prefix = "ResourceArn";
+            String resourceArn = listTagsForResourceRequest.getResourceArn();
+            request.addParameter(prefix, StringUtils.fromString(resourceArn));
+        }
+
+        return request;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/ListTagsForResourceResultStaxUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/ListTagsForResourceResultStaxUnmarshaller.java
new file mode 100644
index 0000000000..97b473044b
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/ListTagsForResourceResultStaxUnmarshaller.java
@@ -0,0 +1,71 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import java.util.Map;
+import java.util.Map.Entry;
+
+import org.xmlpull.v1.XmlPullParser;
+
+import com.amazonaws.services.sns.model.*;
+import com.amazonaws.transform.Unmarshaller;
+import com.amazonaws.transform.MapEntry;
+import com.amazonaws.transform.StaxUnmarshallerContext;
+import com.amazonaws.transform.SimpleTypeStaxUnmarshallers.*;
+
+/**
+ * StAX unmarshaller for response ListTagsForResourceResult
+ */
+public class ListTagsForResourceResultStaxUnmarshaller implements
+        Unmarshaller<ListTagsForResourceResult, StaxUnmarshallerContext> {
+
+    public ListTagsForResourceResult unmarshall(StaxUnmarshallerContext context) throws Exception {
+        ListTagsForResourceResult listTagsForResourceResult = new ListTagsForResourceResult();
+
+        int originalDepth = context.getCurrentDepth();
+        int targetDepth = originalDepth + 1;
+
+        if (context.isStartOfDocument())
+            targetDepth += 2;
+
+        while (true) {
+            int xmlEvent = context.nextEvent();
+            if (xmlEvent == XmlPullParser.END_DOCUMENT)
+                break;
+
+            if (xmlEvent == XmlPullParser.START_TAG) {
+                if (context.testExpression("Tags/member", targetDepth)) {
+                    listTagsForResourceResult.withTags(TagStaxUnmarshaller.getInstance()
+                            .unmarshall(context));
+                    continue;
+                }
+            } else if (xmlEvent == XmlPullParser.END_TAG) {
+                if (context.getCurrentDepth() < originalDepth) {
+                    break;
+                }
+            }
+        }
+        return listTagsForResourceResult;
+    }
+
+    private static ListTagsForResourceResultStaxUnmarshaller instance;
+
+    public static ListTagsForResourceResultStaxUnmarshaller getInstance() {
+        if (instance == null)
+            instance = new ListTagsForResourceResultStaxUnmarshaller();
+        return instance;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/ResourceNotFoundExceptionUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/ResourceNotFoundExceptionUnmarshaller.java
new file mode 100644
index 0000000000..ec985ac8b5
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/ResourceNotFoundExceptionUnmarshaller.java
@@ -0,0 +1,43 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import org.w3c.dom.Node;
+
+import com.amazonaws.AmazonServiceException;
+import com.amazonaws.util.XpathUtils;
+import com.amazonaws.transform.StandardErrorUnmarshaller;
+
+import com.amazonaws.services.sns.model.ResourceNotFoundException;
+
+public class ResourceNotFoundExceptionUnmarshaller extends StandardErrorUnmarshaller {
+
+    public ResourceNotFoundExceptionUnmarshaller() {
+        super(ResourceNotFoundException.class);
+    }
+
+    public AmazonServiceException unmarshall(Node node) throws Exception {
+        // Bail out if this isn't the right error code that this
+        // marshaller understands.
+        String errorCode = parseErrorCode(node);
+        if (errorCode == null || !errorCode.equals("ResourceNotFound"))
+            return null;
+
+        ResourceNotFoundException e = (ResourceNotFoundException) super.unmarshall(node);
+
+        return e;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/StaleTagExceptionUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/StaleTagExceptionUnmarshaller.java
new file mode 100644
index 0000000000..28d4664e09
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/StaleTagExceptionUnmarshaller.java
@@ -0,0 +1,43 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import org.w3c.dom.Node;
+
+import com.amazonaws.AmazonServiceException;
+import com.amazonaws.util.XpathUtils;
+import com.amazonaws.transform.StandardErrorUnmarshaller;
+
+import com.amazonaws.services.sns.model.StaleTagException;
+
+public class StaleTagExceptionUnmarshaller extends StandardErrorUnmarshaller {
+
+    public StaleTagExceptionUnmarshaller() {
+        super(StaleTagException.class);
+    }
+
+    public AmazonServiceException unmarshall(Node node) throws Exception {
+        // Bail out if this isn't the right error code that this
+        // marshaller understands.
+        String errorCode = parseErrorCode(node);
+        if (errorCode == null || !errorCode.equals("StaleTag"))
+            return null;
+
+        StaleTagException e = (StaleTagException) super.unmarshall(node);
+
+        return e;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/SubscribeRequestMarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/SubscribeRequestMarshaller.java
index a38763a873..8773f61783 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/SubscribeRequestMarshaller.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/SubscribeRequestMarshaller.java
@@ -54,6 +54,31 @@
             String endpoint = subscribeRequest.getEndpoint();
             request.addParameter(prefix, StringUtils.fromString(endpoint));
         }
+        if (subscribeRequest.getAttributes() != null) {
+            prefix = "Attributes";
+            java.util.Map<String, String> attributes = subscribeRequest.getAttributes();
+            int attributesIndex = 1;
+            String attributesPrefix = prefix + ".entry.";
+            for (java.util.Map.Entry<String, String> attributesEntry : attributes.entrySet()) {
+                prefix = attributesPrefix + attributesIndex;
+                if (attributesEntry.getKey() != null) {
+                    request.addParameter(prefix + ".key",
+                            StringUtils.fromString(attributesEntry.getKey()));
+                }
+                prefix += ".value";
+                if (attributesEntry.getValue() != null) {
+                    String attributesValue = attributesEntry.getValue();
+                    request.addParameter(prefix, StringUtils.fromString(attributesValue));
+                }
+                attributesIndex++;
+            }
+            prefix = attributesPrefix;
+        }
+        if (subscribeRequest.getReturnSubscriptionArn() != null) {
+            prefix = "ReturnSubscriptionArn";
+            Boolean returnSubscriptionArn = subscribeRequest.getReturnSubscriptionArn();
+            request.addParameter(prefix, StringUtils.fromBoolean(returnSubscriptionArn));
+        }
 
         return request;
     }
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagLimitExceededExceptionUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagLimitExceededExceptionUnmarshaller.java
new file mode 100644
index 0000000000..48d983b44f
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagLimitExceededExceptionUnmarshaller.java
@@ -0,0 +1,43 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import org.w3c.dom.Node;
+
+import com.amazonaws.AmazonServiceException;
+import com.amazonaws.util.XpathUtils;
+import com.amazonaws.transform.StandardErrorUnmarshaller;
+
+import com.amazonaws.services.sns.model.TagLimitExceededException;
+
+public class TagLimitExceededExceptionUnmarshaller extends StandardErrorUnmarshaller {
+
+    public TagLimitExceededExceptionUnmarshaller() {
+        super(TagLimitExceededException.class);
+    }
+
+    public AmazonServiceException unmarshall(Node node) throws Exception {
+        // Bail out if this isn't the right error code that this
+        // marshaller understands.
+        String errorCode = parseErrorCode(node);
+        if (errorCode == null || !errorCode.equals("TagLimitExceeded"))
+            return null;
+
+        TagLimitExceededException e = (TagLimitExceededException) super.unmarshall(node);
+
+        return e;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagPolicyExceptionUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagPolicyExceptionUnmarshaller.java
new file mode 100644
index 0000000000..7a3c05174c
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagPolicyExceptionUnmarshaller.java
@@ -0,0 +1,43 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import org.w3c.dom.Node;
+
+import com.amazonaws.AmazonServiceException;
+import com.amazonaws.util.XpathUtils;
+import com.amazonaws.transform.StandardErrorUnmarshaller;
+
+import com.amazonaws.services.sns.model.TagPolicyException;
+
+public class TagPolicyExceptionUnmarshaller extends StandardErrorUnmarshaller {
+
+    public TagPolicyExceptionUnmarshaller() {
+        super(TagPolicyException.class);
+    }
+
+    public AmazonServiceException unmarshall(Node node) throws Exception {
+        // Bail out if this isn't the right error code that this
+        // marshaller understands.
+        String errorCode = parseErrorCode(node);
+        if (errorCode == null || !errorCode.equals("TagPolicy"))
+            return null;
+
+        TagPolicyException e = (TagPolicyException) super.unmarshall(node);
+
+        return e;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagResourceRequestMarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagResourceRequestMarshaller.java
new file mode 100644
index 0000000000..0a50d3c941
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagResourceRequestMarshaller.java
@@ -0,0 +1,65 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import com.amazonaws.AmazonClientException;
+import com.amazonaws.Request;
+import com.amazonaws.DefaultRequest;
+import com.amazonaws.services.sns.model.*;
+import com.amazonaws.transform.Marshaller;
+import com.amazonaws.util.StringUtils;
+
+/**
+ * StAX request marshaller for TagResourceRequest
+ */
+public class TagResourceRequestMarshaller implements
+        Marshaller<Request<TagResourceRequest>, TagResourceRequest> {
+
+    public Request<TagResourceRequest> marshall(TagResourceRequest tagResourceRequest) {
+        if (tagResourceRequest == null) {
+            throw new AmazonClientException(
+                    "Invalid argument passed to marshall(TagResourceRequest)");
+        }
+
+        Request<TagResourceRequest> request = new DefaultRequest<TagResourceRequest>(
+                tagResourceRequest, "AmazonSNS");
+        request.addParameter("Action", "TagResource");
+        request.addParameter("Version", "2010-03-31");
+
+        String prefix;
+        if (tagResourceRequest.getResourceArn() != null) {
+            prefix = "ResourceArn";
+            String resourceArn = tagResourceRequest.getResourceArn();
+            request.addParameter(prefix, StringUtils.fromString(resourceArn));
+        }
+        if (tagResourceRequest.getTags() != null) {
+            prefix = "Tags";
+            java.util.List<Tag> tags = tagResourceRequest.getTags();
+            int tagsIndex = 1;
+            String tagsPrefix = prefix;
+            for (Tag tagsItem : tags) {
+                prefix = tagsPrefix + ".member." + tagsIndex;
+                if (tagsItem != null) {
+                    TagStaxMarshaller.getInstance().marshall(tagsItem, request, prefix + ".");
+                }
+                tagsIndex++;
+            }
+            prefix = tagsPrefix;
+        }
+
+        return request;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagResourceResultStaxUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagResourceResultStaxUnmarshaller.java
new file mode 100644
index 0000000000..95149d135c
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagResourceResultStaxUnmarshaller.java
@@ -0,0 +1,66 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import java.util.Map;
+import java.util.Map.Entry;
+
+import org.xmlpull.v1.XmlPullParser;
+
+import com.amazonaws.services.sns.model.*;
+import com.amazonaws.transform.Unmarshaller;
+import com.amazonaws.transform.MapEntry;
+import com.amazonaws.transform.StaxUnmarshallerContext;
+import com.amazonaws.transform.SimpleTypeStaxUnmarshallers.*;
+
+/**
+ * StAX unmarshaller for response TagResourceResult
+ */
+public class TagResourceResultStaxUnmarshaller implements
+        Unmarshaller<TagResourceResult, StaxUnmarshallerContext> {
+
+    public TagResourceResult unmarshall(StaxUnmarshallerContext context) throws Exception {
+        TagResourceResult tagResourceResult = new TagResourceResult();
+
+        int originalDepth = context.getCurrentDepth();
+        int targetDepth = originalDepth + 1;
+
+        if (context.isStartOfDocument())
+            targetDepth += 2;
+
+        while (true) {
+            int xmlEvent = context.nextEvent();
+            if (xmlEvent == XmlPullParser.END_DOCUMENT)
+                break;
+
+            if (xmlEvent == XmlPullParser.START_TAG) {
+            } else if (xmlEvent == XmlPullParser.END_TAG) {
+                if (context.getCurrentDepth() < originalDepth) {
+                    break;
+                }
+            }
+        }
+        return tagResourceResult;
+    }
+
+    private static TagResourceResultStaxUnmarshaller instance;
+
+    public static TagResourceResultStaxUnmarshaller getInstance() {
+        if (instance == null)
+            instance = new TagResourceResultStaxUnmarshaller();
+        return instance;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagStaxMarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagStaxMarshaller.java
new file mode 100644
index 0000000000..aebedfbe86
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagStaxMarshaller.java
@@ -0,0 +1,48 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import com.amazonaws.Request;
+import com.amazonaws.services.sns.model.*;
+import com.amazonaws.util.StringUtils;
+
+/**
+ * StAX marshaller for POJO Tag
+ */
+class TagStaxMarshaller {
+
+    public void marshall(Tag _tag, Request<?> request, String _prefix) {
+        String prefix;
+        if (_tag.getKey() != null) {
+            prefix = _prefix + "Key";
+            String key = _tag.getKey();
+            request.addParameter(prefix, StringUtils.fromString(key));
+        }
+        if (_tag.getValue() != null) {
+            prefix = _prefix + "Value";
+            String value = _tag.getValue();
+            request.addParameter(prefix, StringUtils.fromString(value));
+        }
+    }
+
+    private static TagStaxMarshaller instance;
+
+    public static TagStaxMarshaller getInstance() {
+        if (instance == null)
+            instance = new TagStaxMarshaller();
+        return instance;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagStaxUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagStaxUnmarshaller.java
new file mode 100644
index 0000000000..1375a95500
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/TagStaxUnmarshaller.java
@@ -0,0 +1,73 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import java.util.Map;
+import java.util.Map.Entry;
+
+import org.xmlpull.v1.XmlPullParser;
+
+import com.amazonaws.services.sns.model.*;
+import com.amazonaws.transform.Unmarshaller;
+import com.amazonaws.transform.MapEntry;
+import com.amazonaws.transform.StaxUnmarshallerContext;
+import com.amazonaws.transform.SimpleTypeStaxUnmarshallers.*;
+
+/**
+ * StAX unmarshaller for model Tag
+ */
+class TagStaxUnmarshaller implements Unmarshaller<Tag, StaxUnmarshallerContext> {
+
+    public Tag unmarshall(StaxUnmarshallerContext context) throws Exception {
+        Tag tag = new Tag();
+
+        int originalDepth = context.getCurrentDepth();
+        int targetDepth = originalDepth + 1;
+
+        if (context.isStartOfDocument())
+            targetDepth += 2;
+
+        while (true) {
+            int xmlEvent = context.nextEvent();
+            if (xmlEvent == XmlPullParser.END_DOCUMENT)
+                break;
+
+            if (xmlEvent == XmlPullParser.START_TAG) {
+                if (context.testExpression("Key", targetDepth)) {
+                    tag.setKey(StringStaxUnmarshaller.getInstance().unmarshall(context));
+                    continue;
+                }
+                if (context.testExpression("Value", targetDepth)) {
+                    tag.setValue(StringStaxUnmarshaller.getInstance().unmarshall(context));
+                    continue;
+                }
+            } else if (xmlEvent == XmlPullParser.END_TAG) {
+                if (context.getCurrentDepth() < originalDepth) {
+                    break;
+                }
+            }
+        }
+        return tag;
+    }
+
+    private static TagStaxUnmarshaller instance;
+
+    public static TagStaxUnmarshaller getInstance() {
+        if (instance == null)
+            instance = new TagStaxUnmarshaller();
+        return instance;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/UntagResourceRequestMarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/UntagResourceRequestMarshaller.java
new file mode 100644
index 0000000000..0e6ca85fac
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/UntagResourceRequestMarshaller.java
@@ -0,0 +1,65 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import com.amazonaws.AmazonClientException;
+import com.amazonaws.Request;
+import com.amazonaws.DefaultRequest;
+import com.amazonaws.services.sns.model.*;
+import com.amazonaws.transform.Marshaller;
+import com.amazonaws.util.StringUtils;
+
+/**
+ * StAX request marshaller for UntagResourceRequest
+ */
+public class UntagResourceRequestMarshaller implements
+        Marshaller<Request<UntagResourceRequest>, UntagResourceRequest> {
+
+    public Request<UntagResourceRequest> marshall(UntagResourceRequest untagResourceRequest) {
+        if (untagResourceRequest == null) {
+            throw new AmazonClientException(
+                    "Invalid argument passed to marshall(UntagResourceRequest)");
+        }
+
+        Request<UntagResourceRequest> request = new DefaultRequest<UntagResourceRequest>(
+                untagResourceRequest, "AmazonSNS");
+        request.addParameter("Action", "UntagResource");
+        request.addParameter("Version", "2010-03-31");
+
+        String prefix;
+        if (untagResourceRequest.getResourceArn() != null) {
+            prefix = "ResourceArn";
+            String resourceArn = untagResourceRequest.getResourceArn();
+            request.addParameter(prefix, StringUtils.fromString(resourceArn));
+        }
+        if (untagResourceRequest.getTagKeys() != null) {
+            prefix = "TagKeys";
+            java.util.List<String> tagKeys = untagResourceRequest.getTagKeys();
+            int tagKeysIndex = 1;
+            String tagKeysPrefix = prefix;
+            for (String tagKeysItem : tagKeys) {
+                prefix = tagKeysPrefix + ".member." + tagKeysIndex;
+                if (tagKeysItem != null) {
+                    request.addParameter(prefix, StringUtils.fromString(tagKeysItem));
+                }
+                tagKeysIndex++;
+            }
+            prefix = tagKeysPrefix;
+        }
+
+        return request;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/UntagResourceResultStaxUnmarshaller.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/UntagResourceResultStaxUnmarshaller.java
new file mode 100644
index 0000000000..31f5fcfea3
--- /dev/null
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/model/transform/UntagResourceResultStaxUnmarshaller.java
@@ -0,0 +1,66 @@
+/*
+ * Copyright 2010-2019 Amazon.com, Inc. or its affiliates. All Rights Reserved.
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License").
+ * You may not use this file except in compliance with the License.
+ * A copy of the License is located at
+ *
+ *  http://aws.amazon.com/apache2.0
+ *
+ * or in the "license" file accompanying this file. This file is distributed
+ * on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either
+ * express or implied. See the License for the specific language governing
+ * permissions and limitations under the License.
+ */
+
+package com.amazonaws.services.sns.model.transform;
+
+import java.util.Map;
+import java.util.Map.Entry;
+
+import org.xmlpull.v1.XmlPullParser;
+
+import com.amazonaws.services.sns.model.*;
+import com.amazonaws.transform.Unmarshaller;
+import com.amazonaws.transform.MapEntry;
+import com.amazonaws.transform.StaxUnmarshallerContext;
+import com.amazonaws.transform.SimpleTypeStaxUnmarshallers.*;
+
+/**
+ * StAX unmarshaller for response UntagResourceResult
+ */
+public class UntagResourceResultStaxUnmarshaller implements
+        Unmarshaller<UntagResourceResult, StaxUnmarshallerContext> {
+
+    public UntagResourceResult unmarshall(StaxUnmarshallerContext context) throws Exception {
+        UntagResourceResult untagResourceResult = new UntagResourceResult();
+
+        int originalDepth = context.getCurrentDepth();
+        int targetDepth = originalDepth + 1;
+
+        if (context.isStartOfDocument())
+            targetDepth += 2;
+
+        while (true) {
+            int xmlEvent = context.nextEvent();
+            if (xmlEvent == XmlPullParser.END_DOCUMENT)
+                break;
+
+            if (xmlEvent == XmlPullParser.START_TAG) {
+            } else if (xmlEvent == XmlPullParser.END_TAG) {
+                if (context.getCurrentDepth() < originalDepth) {
+                    break;
+                }
+            }
+        }
+        return untagResourceResult;
+    }
+
+    private static UntagResourceResultStaxUnmarshaller instance;
+
+    public static UntagResourceResultStaxUnmarshaller getInstance() {
+        if (instance == null)
+            instance = new UntagResourceResultStaxUnmarshaller();
+        return instance;
+    }
+}
diff --git a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/package-info.java b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/package-info.java
index 458f25d4bb..924a12a2cb 100644
--- a/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/package-info.java
+++ b/aws-android-sdk-sns/src/main/java/com/amazonaws/services/sns/package-info.java
@@ -1,5 +1,5 @@
 /**
- * <fullname>Amazon Simple Notification Service</fullname> <p>Amazon Simple Notification Service (Amazon SNS) is a web service that enables you to build distributed web-enabled applications. Applications can use Amazon SNS to easily push real-time notification messages to interested subscribers over multiple delivery protocols. For more information about this product see <a href="http://aws.amazon.com/sns/">http://aws.amazon.com/sns</a>. For detailed information about Amazon SNS features and their associated API calls, see the <a href="http://docs.aws.amazon.com/sns/latest/dg/">Amazon SNS Developer Guide</a>. </p> <p>We also provide SDKs that enable you to access Amazon SNS from your preferred programming language. The SDKs contain functionality that automatically takes care of tasks such as: cryptographically signing your service requests, retrying requests, and handling error responses. For a list of available SDKs, go to <a href="http://aws.amazon.com/tools/">Tools for Amazon Web Services</a>. </p>
+ * <fullname>Amazon Simple Notification Service</fullname> <p>Amazon Simple Notification Service (Amazon SNS) is a web service that enables you to build distributed web-enabled applications. Applications can use Amazon SNS to easily push real-time notification messages to interested subscribers over multiple delivery protocols. For more information about this product see <a href="http://aws.amazon.com/sns/">https://aws.amazon.com/sns</a>. For detailed information about Amazon SNS features and their associated API calls, see the <a href="https://docs.aws.amazon.com/sns/latest/dg/">Amazon SNS Developer Guide</a>. </p> <p>We also provide SDKs that enable you to access Amazon SNS from your preferred programming language. The SDKs contain functionality that automatically takes care of tasks such as: cryptographically signing your service requests, retrying requests, and handling error responses. For a list of available SDKs, go to <a href="http://aws.amazon.com/tools/">Tools for Amazon Web Services</a>. </p>
  */
 
 package com.amazonaws.services.sns;
