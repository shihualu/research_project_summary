diff --git a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailService.java b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailService.java
index f0770c56e3..378d436cb7 100644
--- a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailService.java
+++ b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailService.java
@@ -1414,7 +1414,7 @@ ListConfigurationSetsResult listConfigurationSets(
      *            <p>
      *            For more information about custom verification email
      *            templates, see <a href=
-     *            "ses/latest/DeveloperGuide/custom-verification-emails.html"
+     *            "https://docs.aws.amazon.com/ses/latest/DeveloperGuide/custom-verification-emails.html"
      *            >Using Custom Verification Email Templates</a> in the
      *            <i>Amazon SES Developer Guide</i>.
      *            </p>
@@ -2121,18 +2121,17 @@ SendEmailResult sendEmail(SendEmailRequest sendEmailRequest) throws AmazonClient
      * </ul>
      * <important>
      * <p>
-     * Do not include these X-headers in the DKIM signature; Amazon SES will
-     * remove them before sending the email.
+     * Don't include these X-headers in the DKIM signature. Amazon SES removes
+     * these before it sends the email.
      * </p>
      * </important>
      * <p>
-     * For most common sending authorization scenarios, we recommend that you
-     * specify the <code>SourceIdentityArn</code> parameter and not the
-     * <code>FromIdentityArn</code> or <code>ReturnPathIdentityArn</code>
-     * parameters. If you only specify the <code>SourceIdentityArn</code>
-     * parameter, Amazon SES will set the From and Return Path addresses to the
-     * identity specified in <code>SourceIdentityArn</code>. For more
-     * information about sending authorization, see the <a href=
+     * If you only specify the <code>SourceIdentityArn</code> parameter, Amazon
+     * SES sets the From and Return-Path addresses to the same identity that you
+     * specified.
+     * </p>
+     * <p>
+     * For more information about sending authorization, see the <a href=
      * "https://docs.aws.amazon.com/ses/latest/DeveloperGuide/sending-authorization.html"
      * >Using Sending Authorization with Amazon SES</a> in the <i>Amazon SES
      * Developer Guide.</i>
@@ -2321,29 +2320,23 @@ SetActiveReceiptRuleSetResult setActiveReceiptRuleSet(
 
     /**
      * <p>
-     * Enables or disables Easy DKIM signing of email sent from an identity:
+     * Enables or disables Easy DKIM signing of email sent from an identity. If
+     * Easy DKIM signing is enabled for a domain, then Amazon SES uses DKIM to
+     * sign all email that it sends from addresses on that domain. If Easy DKIM
+     * signing is enabled for an email address, then Amazon SES uses DKIM to
+     * sign all email it sends from that address.
      * </p>
-     * <ul>
-     * <li>
-     * <p>
-     * If Easy DKIM signing is enabled for a domain name identity (such as
-     * <code>example.com</code>), then Amazon SES will DKIM-sign all email sent
-     * by addresses under that domain name (for example,
-     * <code>user@example.com</code>).
-     * </p>
-     * </li>
-     * <li>
+     * <note>
      * <p>
-     * If Easy DKIM signing is enabled for an email address, then Amazon SES
-     * will DKIM-sign all email sent by that email address.
+     * For email addresses (for example, <code>user@example.com</code>), you can
+     * only enable DKIM signing if the corresponding domain (in this case,
+     * <code>example.com</code>) has been set up to use Easy DKIM.
      * </p>
-     * </li>
-     * </ul>
+     * </note>
      * <p>
-     * For email addresses (for example, <code>user@example.com</code>), you can
-     * only enable Easy DKIM signing if the corresponding domain (in this case,
-     * <code>example.com</code>) has been set up for Easy DKIM using the AWS
-     * Console or the <code>VerifyDomainDkim</code> operation.
+     * You can enable DKIM signing for an identity at any time after you start
+     * the verification process for the identity, even if the verification
+     * process isn't complete.
      * </p>
      * <p>
      * You can execute this operation no more than once per second.
diff --git a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailServiceAsync.java b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailServiceAsync.java
index 82d2f83faa..06a52460b5 100644
--- a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailServiceAsync.java
+++ b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailServiceAsync.java
@@ -2862,7 +2862,7 @@
      *            <p>
      *            For more information about custom verification email
      *            templates, see <a href=
-     *            "ses/latest/DeveloperGuide/custom-verification-emails.html"
+     *            "https://docs.aws.amazon.com/ses/latest/DeveloperGuide/custom-verification-emails.html"
      *            >Using Custom Verification Email Templates</a> in the
      *            <i>Amazon SES Developer Guide</i>.
      *            </p>
@@ -2904,7 +2904,7 @@
      *            <p>
      *            For more information about custom verification email
      *            templates, see <a href=
-     *            "ses/latest/DeveloperGuide/custom-verification-emails.html"
+     *            "https://docs.aws.amazon.com/ses/latest/DeveloperGuide/custom-verification-emails.html"
      *            >Using Custom Verification Email Templates</a> in the
      *            <i>Amazon SES Developer Guide</i>.
      *            </p>
@@ -4263,18 +4263,17 @@
      * </ul>
      * <important>
      * <p>
-     * Do not include these X-headers in the DKIM signature; Amazon SES will
-     * remove them before sending the email.
+     * Don't include these X-headers in the DKIM signature. Amazon SES removes
+     * these before it sends the email.
      * </p>
      * </important>
      * <p>
-     * For most common sending authorization scenarios, we recommend that you
-     * specify the <code>SourceIdentityArn</code> parameter and not the
-     * <code>FromIdentityArn</code> or <code>ReturnPathIdentityArn</code>
-     * parameters. If you only specify the <code>SourceIdentityArn</code>
-     * parameter, Amazon SES will set the From and Return Path addresses to the
-     * identity specified in <code>SourceIdentityArn</code>. For more
-     * information about sending authorization, see the <a href=
+     * If you only specify the <code>SourceIdentityArn</code> parameter, Amazon
+     * SES sets the From and Return-Path addresses to the same identity that you
+     * specified.
+     * </p>
+     * <p>
+     * For more information about sending authorization, see the <a href=
      * "https://docs.aws.amazon.com/ses/latest/DeveloperGuide/sending-authorization.html"
      * >Using Sending Authorization with Amazon SES</a> in the <i>Amazon SES
      * Developer Guide.</i>
@@ -4437,18 +4436,17 @@
      * </ul>
      * <important>
      * <p>
-     * Do not include these X-headers in the DKIM signature; Amazon SES will
-     * remove them before sending the email.
+     * Don't include these X-headers in the DKIM signature. Amazon SES removes
+     * these before it sends the email.
      * </p>
      * </important>
      * <p>
-     * For most common sending authorization scenarios, we recommend that you
-     * specify the <code>SourceIdentityArn</code> parameter and not the
-     * <code>FromIdentityArn</code> or <code>ReturnPathIdentityArn</code>
-     * parameters. If you only specify the <code>SourceIdentityArn</code>
-     * parameter, Amazon SES will set the From and Return Path addresses to the
-     * identity specified in <code>SourceIdentityArn</code>. For more
-     * information about sending authorization, see the <a href=
+     * If you only specify the <code>SourceIdentityArn</code> parameter, Amazon
+     * SES sets the From and Return-Path addresses to the same identity that you
+     * specified.
+     * </p>
+     * <p>
+     * For more information about sending authorization, see the <a href=
      * "https://docs.aws.amazon.com/ses/latest/DeveloperGuide/sending-authorization.html"
      * >Using Sending Authorization with Amazon SES</a> in the <i>Amazon SES
      * Developer Guide.</i>
@@ -4800,29 +4798,23 @@
 
     /**
      * <p>
-     * Enables or disables Easy DKIM signing of email sent from an identity:
-     * </p>
-     * <ul>
-     * <li>
-     * <p>
-     * If Easy DKIM signing is enabled for a domain name identity (such as
-     * <code>example.com</code>), then Amazon SES will DKIM-sign all email sent
-     * by addresses under that domain name (for example,
-     * <code>user@example.com</code>).
+     * Enables or disables Easy DKIM signing of email sent from an identity. If
+     * Easy DKIM signing is enabled for a domain, then Amazon SES uses DKIM to
+     * sign all email that it sends from addresses on that domain. If Easy DKIM
+     * signing is enabled for an email address, then Amazon SES uses DKIM to
+     * sign all email it sends from that address.
      * </p>
-     * </li>
-     * <li>
+     * <note>
      * <p>
-     * If Easy DKIM signing is enabled for an email address, then Amazon SES
-     * will DKIM-sign all email sent by that email address.
+     * For email addresses (for example, <code>user@example.com</code>), you can
+     * only enable DKIM signing if the corresponding domain (in this case,
+     * <code>example.com</code>) has been set up to use Easy DKIM.
      * </p>
-     * </li>
-     * </ul>
+     * </note>
      * <p>
-     * For email addresses (for example, <code>user@example.com</code>), you can
-     * only enable Easy DKIM signing if the corresponding domain (in this case,
-     * <code>example.com</code>) has been set up for Easy DKIM using the AWS
-     * Console or the <code>VerifyDomainDkim</code> operation.
+     * You can enable DKIM signing for an identity at any time after you start
+     * the verification process for the identity, even if the verification
+     * process isn't complete.
      * </p>
      * <p>
      * You can execute this operation no more than once per second.
@@ -4857,29 +4849,23 @@
 
     /**
      * <p>
-     * Enables or disables Easy DKIM signing of email sent from an identity:
+     * Enables or disables Easy DKIM signing of email sent from an identity. If
+     * Easy DKIM signing is enabled for a domain, then Amazon SES uses DKIM to
+     * sign all email that it sends from addresses on that domain. If Easy DKIM
+     * signing is enabled for an email address, then Amazon SES uses DKIM to
+     * sign all email it sends from that address.
      * </p>
-     * <ul>
-     * <li>
-     * <p>
-     * If Easy DKIM signing is enabled for a domain name identity (such as
-     * <code>example.com</code>), then Amazon SES will DKIM-sign all email sent
-     * by addresses under that domain name (for example,
-     * <code>user@example.com</code>).
-     * </p>
-     * </li>
-     * <li>
+     * <note>
      * <p>
-     * If Easy DKIM signing is enabled for an email address, then Amazon SES
-     * will DKIM-sign all email sent by that email address.
+     * For email addresses (for example, <code>user@example.com</code>), you can
+     * only enable DKIM signing if the corresponding domain (in this case,
+     * <code>example.com</code>) has been set up to use Easy DKIM.
      * </p>
-     * </li>
-     * </ul>
+     * </note>
      * <p>
-     * For email addresses (for example, <code>user@example.com</code>), you can
-     * only enable Easy DKIM signing if the corresponding domain (in this case,
-     * <code>example.com</code>) has been set up for Easy DKIM using the AWS
-     * Console or the <code>VerifyDomainDkim</code> operation.
+     * You can enable DKIM signing for an identity at any time after you start
+     * the verification process for the identity, even if the verification
+     * process isn't complete.
      * </p>
      * <p>
      * You can execute this operation no more than once per second.
diff --git a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailServiceAsyncClient.java b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailServiceAsyncClient.java
index 881ece8fb1..8578ac61ed 100644
--- a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailServiceAsyncClient.java
+++ b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailServiceAsyncClient.java
@@ -3661,7 +3661,7 @@ public ListConfigurationSetsResult call() throws Exception {
      *            <p>
      *            For more information about custom verification email
      *            templates, see <a href=
-     *            "ses/latest/DeveloperGuide/custom-verification-emails.html"
+     *            "https://docs.aws.amazon.com/ses/latest/DeveloperGuide/custom-verification-emails.html"
      *            >Using Custom Verification Email Templates</a> in the
      *            <i>Amazon SES Developer Guide</i>.
      *            </p>
@@ -3709,7 +3709,7 @@ public ListCustomVerificationEmailTemplatesResult call() throws Exception {
      *            <p>
      *            For more information about custom verification email
      *            templates, see <a href=
-     *            "ses/latest/DeveloperGuide/custom-verification-emails.html"
+     *            "https://docs.aws.amazon.com/ses/latest/DeveloperGuide/custom-verification-emails.html"
      *            >Using Custom Verification Email Templates</a> in the
      *            <i>Amazon SES Developer Guide</i>.
      *            </p>
@@ -5290,18 +5290,17 @@ public SendEmailResult call() throws Exception {
      * </ul>
      * <important>
      * <p>
-     * Do not include these X-headers in the DKIM signature; Amazon SES will
-     * remove them before sending the email.
+     * Don't include these X-headers in the DKIM signature. Amazon SES removes
+     * these before it sends the email.
      * </p>
      * </important>
      * <p>
-     * For most common sending authorization scenarios, we recommend that you
-     * specify the <code>SourceIdentityArn</code> parameter and not the
-     * <code>FromIdentityArn</code> or <code>ReturnPathIdentityArn</code>
-     * parameters. If you only specify the <code>SourceIdentityArn</code>
-     * parameter, Amazon SES will set the From and Return Path addresses to the
-     * identity specified in <code>SourceIdentityArn</code>. For more
-     * information about sending authorization, see the <a href=
+     * If you only specify the <code>SourceIdentityArn</code> parameter, Amazon
+     * SES sets the From and Return-Path addresses to the same identity that you
+     * specified.
+     * </p>
+     * <p>
+     * For more information about sending authorization, see the <a href=
      * "https://docs.aws.amazon.com/ses/latest/DeveloperGuide/sending-authorization.html"
      * >Using Sending Authorization with Amazon SES</a> in the <i>Amazon SES
      * Developer Guide.</i>
@@ -5471,18 +5470,17 @@ public SendRawEmailResult call() throws Exception {
      * </ul>
      * <important>
      * <p>
-     * Do not include these X-headers in the DKIM signature; Amazon SES will
-     * remove them before sending the email.
+     * Don't include these X-headers in the DKIM signature. Amazon SES removes
+     * these before it sends the email.
      * </p>
      * </important>
      * <p>
-     * For most common sending authorization scenarios, we recommend that you
-     * specify the <code>SourceIdentityArn</code> parameter and not the
-     * <code>FromIdentityArn</code> or <code>ReturnPathIdentityArn</code>
-     * parameters. If you only specify the <code>SourceIdentityArn</code>
-     * parameter, Amazon SES will set the From and Return Path addresses to the
-     * identity specified in <code>SourceIdentityArn</code>. For more
-     * information about sending authorization, see the <a href=
+     * If you only specify the <code>SourceIdentityArn</code> parameter, Amazon
+     * SES sets the From and Return-Path addresses to the same identity that you
+     * specified.
+     * </p>
+     * <p>
+     * For more information about sending authorization, see the <a href=
      * "https://docs.aws.amazon.com/ses/latest/DeveloperGuide/sending-authorization.html"
      * >Using Sending Authorization with Amazon SES</a> in the <i>Amazon SES
      * Developer Guide.</i>
@@ -5877,29 +5875,23 @@ public SetActiveReceiptRuleSetResult call() throws Exception {
 
     /**
      * <p>
-     * Enables or disables Easy DKIM signing of email sent from an identity:
-     * </p>
-     * <ul>
-     * <li>
-     * <p>
-     * If Easy DKIM signing is enabled for a domain name identity (such as
-     * <code>example.com</code>), then Amazon SES will DKIM-sign all email sent
-     * by addresses under that domain name (for example,
-     * <code>user@example.com</code>).
+     * Enables or disables Easy DKIM signing of email sent from an identity. If
+     * Easy DKIM signing is enabled for a domain, then Amazon SES uses DKIM to
+     * sign all email that it sends from addresses on that domain. If Easy DKIM
+     * signing is enabled for an email address, then Amazon SES uses DKIM to
+     * sign all email it sends from that address.
      * </p>
-     * </li>
-     * <li>
+     * <note>
      * <p>
-     * If Easy DKIM signing is enabled for an email address, then Amazon SES
-     * will DKIM-sign all email sent by that email address.
+     * For email addresses (for example, <code>user@example.com</code>), you can
+     * only enable DKIM signing if the corresponding domain (in this case,
+     * <code>example.com</code>) has been set up to use Easy DKIM.
      * </p>
-     * </li>
-     * </ul>
+     * </note>
      * <p>
-     * For email addresses (for example, <code>user@example.com</code>), you can
-     * only enable Easy DKIM signing if the corresponding domain (in this case,
-     * <code>example.com</code>) has been set up for Easy DKIM using the AWS
-     * Console or the <code>VerifyDomainDkim</code> operation.
+     * You can enable DKIM signing for an identity at any time after you start
+     * the verification process for the identity, even if the verification
+     * process isn't complete.
      * </p>
      * <p>
      * You can execute this operation no more than once per second.
@@ -5940,29 +5932,23 @@ public SetIdentityDkimEnabledResult call() throws Exception {
 
     /**
      * <p>
-     * Enables or disables Easy DKIM signing of email sent from an identity:
+     * Enables or disables Easy DKIM signing of email sent from an identity. If
+     * Easy DKIM signing is enabled for a domain, then Amazon SES uses DKIM to
+     * sign all email that it sends from addresses on that domain. If Easy DKIM
+     * signing is enabled for an email address, then Amazon SES uses DKIM to
+     * sign all email it sends from that address.
      * </p>
-     * <ul>
-     * <li>
-     * <p>
-     * If Easy DKIM signing is enabled for a domain name identity (such as
-     * <code>example.com</code>), then Amazon SES will DKIM-sign all email sent
-     * by addresses under that domain name (for example,
-     * <code>user@example.com</code>).
-     * </p>
-     * </li>
-     * <li>
+     * <note>
      * <p>
-     * If Easy DKIM signing is enabled for an email address, then Amazon SES
-     * will DKIM-sign all email sent by that email address.
+     * For email addresses (for example, <code>user@example.com</code>), you can
+     * only enable DKIM signing if the corresponding domain (in this case,
+     * <code>example.com</code>) has been set up to use Easy DKIM.
      * </p>
-     * </li>
-     * </ul>
+     * </note>
      * <p>
-     * For email addresses (for example, <code>user@example.com</code>), you can
-     * only enable Easy DKIM signing if the corresponding domain (in this case,
-     * <code>example.com</code>) has been set up for Easy DKIM using the AWS
-     * Console or the <code>VerifyDomainDkim</code> operation.
+     * You can enable DKIM signing for an identity at any time after you start
+     * the verification process for the identity, even if the verification
+     * process isn't complete.
      * </p>
      * <p>
      * You can execute this operation no more than once per second.
diff --git a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailServiceClient.java b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailServiceClient.java
index f13d532ce3..d27983d785 100644
--- a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailServiceClient.java
+++ b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/AmazonSimpleEmailServiceClient.java
@@ -2339,7 +2339,7 @@ public ListConfigurationSetsResult listConfigurationSets(
      *            <p>
      *            For more information about custom verification email
      *            templates, see <a href=
-     *            "ses/latest/DeveloperGuide/custom-verification-emails.html"
+     *            "https://docs.aws.amazon.com/ses/latest/DeveloperGuide/custom-verification-emails.html"
      *            >Using Custom Verification Email Templates</a> in the
      *            <i>Amazon SES Developer Guide</i>.
      *            </p>
@@ -3292,18 +3292,17 @@ public SendEmailResult sendEmail(SendEmailRequest sendEmailRequest)
      * </ul>
      * <important>
      * <p>
-     * Do not include these X-headers in the DKIM signature; Amazon SES will
-     * remove them before sending the email.
+     * Don't include these X-headers in the DKIM signature. Amazon SES removes
+     * these before it sends the email.
      * </p>
      * </important>
      * <p>
-     * For most common sending authorization scenarios, we recommend that you
-     * specify the <code>SourceIdentityArn</code> parameter and not the
-     * <code>FromIdentityArn</code> or <code>ReturnPathIdentityArn</code>
-     * parameters. If you only specify the <code>SourceIdentityArn</code>
-     * parameter, Amazon SES will set the From and Return Path addresses to the
-     * identity specified in <code>SourceIdentityArn</code>. For more
-     * information about sending authorization, see the <a href=
+     * If you only specify the <code>SourceIdentityArn</code> parameter, Amazon
+     * SES sets the From and Return-Path addresses to the same identity that you
+     * specified.
+     * </p>
+     * <p>
+     * For more information about sending authorization, see the <a href=
      * "https://docs.aws.amazon.com/ses/latest/DeveloperGuide/sending-authorization.html"
      * >Using Sending Authorization with Amazon SES</a> in the <i>Amazon SES
      * Developer Guide.</i>
@@ -3544,29 +3543,23 @@ public SetActiveReceiptRuleSetResult setActiveReceiptRuleSet(
 
     /**
      * <p>
-     * Enables or disables Easy DKIM signing of email sent from an identity:
+     * Enables or disables Easy DKIM signing of email sent from an identity. If
+     * Easy DKIM signing is enabled for a domain, then Amazon SES uses DKIM to
+     * sign all email that it sends from addresses on that domain. If Easy DKIM
+     * signing is enabled for an email address, then Amazon SES uses DKIM to
+     * sign all email it sends from that address.
      * </p>
-     * <ul>
-     * <li>
-     * <p>
-     * If Easy DKIM signing is enabled for a domain name identity (such as
-     * <code>example.com</code>), then Amazon SES will DKIM-sign all email sent
-     * by addresses under that domain name (for example,
-     * <code>user@example.com</code>).
-     * </p>
-     * </li>
-     * <li>
+     * <note>
      * <p>
-     * If Easy DKIM signing is enabled for an email address, then Amazon SES
-     * will DKIM-sign all email sent by that email address.
+     * For email addresses (for example, <code>user@example.com</code>), you can
+     * only enable DKIM signing if the corresponding domain (in this case,
+     * <code>example.com</code>) has been set up to use Easy DKIM.
      * </p>
-     * </li>
-     * </ul>
+     * </note>
      * <p>
-     * For email addresses (for example, <code>user@example.com</code>), you can
-     * only enable Easy DKIM signing if the corresponding domain (in this case,
-     * <code>example.com</code>) has been set up for Easy DKIM using the AWS
-     * Console or the <code>VerifyDomainDkim</code> operation.
+     * You can enable DKIM signing for an identity at any time after you start
+     * the verification process for the identity, even if the verification
+     * process isn't complete.
      * </p>
      * <p>
      * You can execute this operation no more than once per second.
diff --git a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/Destination.java b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/Destination.java
index 7b3c2cb3a3..84116dba6d 100644
--- a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/Destination.java
+++ b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/Destination.java
@@ -38,21 +38,21 @@
 public class Destination implements Serializable {
     /**
      * <p>
-     * The To: field(s) of the message.
+     * The recipients to place on the To: line of the message.
      * </p>
      */
     private java.util.List<String> toAddresses = new java.util.ArrayList<String>();
 
     /**
      * <p>
-     * The CC: field(s) of the message.
+     * The recipients to place on the CC: line of the message.
      * </p>
      */
     private java.util.List<String> ccAddresses = new java.util.ArrayList<String>();
 
     /**
      * <p>
-     * The BCC: field(s) of the message.
+     * The recipients to place on the BCC: line of the message.
      * </p>
      */
     private java.util.List<String> bccAddresses = new java.util.ArrayList<String>();
@@ -71,7 +71,7 @@ public Destination() {
      * members.
      * 
      * @param toAddresses <p>
-     *            The To: field(s) of the message.
+     *            The recipients to place on the To: line of the message.
      *            </p>
      */
     public Destination(java.util.List<String> toAddresses) {
@@ -80,11 +80,11 @@ public Destination(java.util.List<String> toAddresses) {
 
     /**
      * <p>
-     * The To: field(s) of the message.
+     * The recipients to place on the To: line of the message.
      * </p>
      *
      * @return <p>
-     *         The To: field(s) of the message.
+     *         The recipients to place on the To: line of the message.
      *         </p>
      */
     public java.util.List<String> getToAddresses() {
@@ -93,11 +93,11 @@ public Destination(java.util.List<String> toAddresses) {
 
     /**
      * <p>
-     * The To: field(s) of the message.
+     * The recipients to place on the To: line of the message.
      * </p>
      *
      * @param toAddresses <p>
-     *            The To: field(s) of the message.
+     *            The recipients to place on the To: line of the message.
      *            </p>
      */
     public void setToAddresses(java.util.Collection<String> toAddresses) {
@@ -111,14 +111,14 @@ public void setToAddresses(java.util.Collection<String> toAddresses) {
 
     /**
      * <p>
-     * The To: field(s) of the message.
+     * The recipients to place on the To: line of the message.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param toAddresses <p>
-     *            The To: field(s) of the message.
+     *            The recipients to place on the To: line of the message.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -135,14 +135,14 @@ public Destination withToAddresses(String... toAddresses) {
 
     /**
      * <p>
-     * The To: field(s) of the message.
+     * The recipients to place on the To: line of the message.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param toAddresses <p>
-     *            The To: field(s) of the message.
+     *            The recipients to place on the To: line of the message.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -154,11 +154,11 @@ public Destination withToAddresses(java.util.Collection<String> toAddresses) {
 
     /**
      * <p>
-     * The CC: field(s) of the message.
+     * The recipients to place on the CC: line of the message.
      * </p>
      *
      * @return <p>
-     *         The CC: field(s) of the message.
+     *         The recipients to place on the CC: line of the message.
      *         </p>
      */
     public java.util.List<String> getCcAddresses() {
@@ -167,11 +167,11 @@ public Destination withToAddresses(java.util.Collection<String> toAddresses) {
 
     /**
      * <p>
-     * The CC: field(s) of the message.
+     * The recipients to place on the CC: line of the message.
      * </p>
      *
      * @param ccAddresses <p>
-     *            The CC: field(s) of the message.
+     *            The recipients to place on the CC: line of the message.
      *            </p>
      */
     public void setCcAddresses(java.util.Collection<String> ccAddresses) {
@@ -185,14 +185,14 @@ public void setCcAddresses(java.util.Collection<String> ccAddresses) {
 
     /**
      * <p>
-     * The CC: field(s) of the message.
+     * The recipients to place on the CC: line of the message.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param ccAddresses <p>
-     *            The CC: field(s) of the message.
+     *            The recipients to place on the CC: line of the message.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -209,14 +209,14 @@ public Destination withCcAddresses(String... ccAddresses) {
 
     /**
      * <p>
-     * The CC: field(s) of the message.
+     * The recipients to place on the CC: line of the message.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param ccAddresses <p>
-     *            The CC: field(s) of the message.
+     *            The recipients to place on the CC: line of the message.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -228,11 +228,11 @@ public Destination withCcAddresses(java.util.Collection<String> ccAddresses) {
 
     /**
      * <p>
-     * The BCC: field(s) of the message.
+     * The recipients to place on the BCC: line of the message.
      * </p>
      *
      * @return <p>
-     *         The BCC: field(s) of the message.
+     *         The recipients to place on the BCC: line of the message.
      *         </p>
      */
     public java.util.List<String> getBccAddresses() {
@@ -241,11 +241,11 @@ public Destination withCcAddresses(java.util.Collection<String> ccAddresses) {
 
     /**
      * <p>
-     * The BCC: field(s) of the message.
+     * The recipients to place on the BCC: line of the message.
      * </p>
      *
      * @param bccAddresses <p>
-     *            The BCC: field(s) of the message.
+     *            The recipients to place on the BCC: line of the message.
      *            </p>
      */
     public void setBccAddresses(java.util.Collection<String> bccAddresses) {
@@ -259,14 +259,14 @@ public void setBccAddresses(java.util.Collection<String> bccAddresses) {
 
     /**
      * <p>
-     * The BCC: field(s) of the message.
+     * The recipients to place on the BCC: line of the message.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param bccAddresses <p>
-     *            The BCC: field(s) of the message.
+     *            The recipients to place on the BCC: line of the message.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -283,14 +283,14 @@ public Destination withBccAddresses(String... bccAddresses) {
 
     /**
      * <p>
-     * The BCC: field(s) of the message.
+     * The recipients to place on the BCC: line of the message.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param bccAddresses <p>
-     *            The BCC: field(s) of the message.
+     *            The recipients to place on the BCC: line of the message.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/SendRawEmailRequest.java b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/SendRawEmailRequest.java
index 60b694d999..8cac075b08 100644
--- a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/SendRawEmailRequest.java
+++ b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/SendRawEmailRequest.java
@@ -135,18 +135,17 @@
  * </ul>
  * <important>
  * <p>
- * Do not include these X-headers in the DKIM signature; Amazon SES will remove
- * them before sending the email.
+ * Don't include these X-headers in the DKIM signature. Amazon SES removes these
+ * before it sends the email.
  * </p>
  * </important>
  * <p>
- * For most common sending authorization scenarios, we recommend that you
- * specify the <code>SourceIdentityArn</code> parameter and not the
- * <code>FromIdentityArn</code> or <code>ReturnPathIdentityArn</code>
- * parameters. If you only specify the <code>SourceIdentityArn</code> parameter,
- * Amazon SES will set the From and Return Path addresses to the identity
- * specified in <code>SourceIdentityArn</code>. For more information about
- * sending authorization, see the <a href=
+ * If you only specify the <code>SourceIdentityArn</code> parameter, Amazon SES
+ * sets the From and Return-Path addresses to the same identity that you
+ * specified.
+ * </p>
+ * <p>
+ * For more information about sending authorization, see the <a href=
  * "https://docs.aws.amazon.com/ses/latest/DeveloperGuide/sending-authorization.html"
  * >Using Sending Authorization with Amazon SES</a> in the <i>Amazon SES
  * Developer Guide.</i>
diff --git a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/SetIdentityDkimEnabledRequest.java b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/SetIdentityDkimEnabledRequest.java
index 0d08b0075e..c5f8cef5e6 100644
--- a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/SetIdentityDkimEnabledRequest.java
+++ b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/SetIdentityDkimEnabledRequest.java
@@ -21,29 +21,23 @@
 
 /**
  * <p>
- * Enables or disables Easy DKIM signing of email sent from an identity:
+ * Enables or disables Easy DKIM signing of email sent from an identity. If Easy
+ * DKIM signing is enabled for a domain, then Amazon SES uses DKIM to sign all
+ * email that it sends from addresses on that domain. If Easy DKIM signing is
+ * enabled for an email address, then Amazon SES uses DKIM to sign all email it
+ * sends from that address.
  * </p>
- * <ul>
- * <li>
+ * <note>
  * <p>
- * If Easy DKIM signing is enabled for a domain name identity (such as
- * <code>example.com</code>), then Amazon SES will DKIM-sign all email sent by
- * addresses under that domain name (for example, <code>user@example.com</code>
- * ).
- * </p>
- * </li>
- * <li>
- * <p>
- * If Easy DKIM signing is enabled for an email address, then Amazon SES will
- * DKIM-sign all email sent by that email address.
+ * For email addresses (for example, <code>user@example.com</code>), you can
+ * only enable DKIM signing if the corresponding domain (in this case,
+ * <code>example.com</code>) has been set up to use Easy DKIM.
  * </p>
- * </li>
- * </ul>
+ * </note>
  * <p>
- * For email addresses (for example, <code>user@example.com</code>), you can
- * only enable Easy DKIM signing if the corresponding domain (in this case,
- * <code>example.com</code>) has been set up for Easy DKIM using the AWS Console
- * or the <code>VerifyDomainDkim</code> operation.
+ * You can enable DKIM signing for an identity at any time after you start the
+ * verification process for the identity, even if the verification process isn't
+ * complete.
  * </p>
  * <p>
  * You can execute this operation no more than once per second.
diff --git a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/StopAction.java b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/StopAction.java
index 4aa777a6b2..4e53f3689b 100644
--- a/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/StopAction.java
+++ b/aws-android-sdk-ses/src/main/java/com/amazonaws/services/simpleemail/model/StopAction.java
@@ -33,7 +33,8 @@
 public class StopAction implements Serializable {
     /**
      * <p>
-     * The name of the RuleSet that is being stopped.
+     * The scope of the StopAction. The only acceptable value is
+     * <code>RuleSet</code>.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -55,14 +56,16 @@
 
     /**
      * <p>
-     * The name of the RuleSet that is being stopped.
+     * The scope of the StopAction. The only acceptable value is
+     * <code>RuleSet</code>.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
      * <b>Allowed Values: </b>RuleSet
      *
      * @return <p>
-     *         The name of the RuleSet that is being stopped.
+     *         The scope of the StopAction. The only acceptable value is
+     *         <code>RuleSet</code>.
      *         </p>
      * @see StopScope
      */
@@ -72,14 +75,16 @@ public String getScope() {
 
     /**
      * <p>
-     * The name of the RuleSet that is being stopped.
+     * The scope of the StopAction. The only acceptable value is
+     * <code>RuleSet</code>.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
      * <b>Allowed Values: </b>RuleSet
      *
      * @param scope <p>
-     *            The name of the RuleSet that is being stopped.
+     *            The scope of the StopAction. The only acceptable value is
+     *            <code>RuleSet</code>.
      *            </p>
      * @see StopScope
      */
@@ -89,7 +94,8 @@ public void setScope(String scope) {
 
     /**
      * <p>
-     * The name of the RuleSet that is being stopped.
+     * The scope of the StopAction. The only acceptable value is
+     * <code>RuleSet</code>.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -99,7 +105,8 @@ public void setScope(String scope) {
      * <b>Allowed Values: </b>RuleSet
      *
      * @param scope <p>
-     *            The name of the RuleSet that is being stopped.
+     *            The scope of the StopAction. The only acceptable value is
+     *            <code>RuleSet</code>.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -112,14 +119,16 @@ public StopAction withScope(String scope) {
 
     /**
      * <p>
-     * The name of the RuleSet that is being stopped.
+     * The scope of the StopAction. The only acceptable value is
+     * <code>RuleSet</code>.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
      * <b>Allowed Values: </b>RuleSet
      *
      * @param scope <p>
-     *            The name of the RuleSet that is being stopped.
+     *            The scope of the StopAction. The only acceptable value is
+     *            <code>RuleSet</code>.
      *            </p>
      * @see StopScope
      */
@@ -129,7 +138,8 @@ public void setScope(StopScope scope) {
 
     /**
      * <p>
-     * The name of the RuleSet that is being stopped.
+     * The scope of the StopAction. The only acceptable value is
+     * <code>RuleSet</code>.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -139,7 +149,8 @@ public void setScope(StopScope scope) {
      * <b>Allowed Values: </b>RuleSet
      *
      * @param scope <p>
-     *            The name of the RuleSet that is being stopped.
+     *            The scope of the StopAction. The only acceptable value is
+     *            <code>RuleSet</code>.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
