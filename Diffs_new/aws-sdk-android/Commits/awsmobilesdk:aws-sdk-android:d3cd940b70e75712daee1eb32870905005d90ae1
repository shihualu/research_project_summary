diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/AmazonRekognition.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/AmazonRekognition.java
index ef54d2ed7d..581faddb04 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/AmazonRekognition.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/AmazonRekognition.java
@@ -601,10 +601,10 @@ DetectLabelsResult detectLabels(DetectLabelsRequest detectLabelsRequest)
 
     /**
      * <p>
-     * Detects explicit or suggestive adult content in a specified JPEG or PNG
-     * format image. Use <code>DetectModerationLabels</code> to moderate images
-     * depending on your requirements. For example, you might want to filter
-     * images that contain nudity, but not images containing suggestive content.
+     * Detects unsafe content in a specified JPEG or PNG format image. Use
+     * <code>DetectModerationLabels</code> to moderate images depending on your
+     * requirements. For example, you might want to filter images that contain
+     * nudity, but not images containing suggestive content.
      * </p>
      * <p>
      * To filter images, use the labels returned by
@@ -842,17 +842,17 @@ GetCelebrityRecognitionResult getCelebrityRecognition(
 
     /**
      * <p>
-     * Gets the content moderation analysis results for a Amazon Rekognition
-     * Video analysis started by <a>StartContentModeration</a>.
+     * Gets the unsafe content analysis results for a Amazon Rekognition Video
+     * analysis started by <a>StartContentModeration</a>.
      * </p>
      * <p>
-     * Content moderation analysis of a video is an asynchronous operation. You
+     * Unsafe content analysis of a video is an asynchronous operation. You
      * start analysis by calling <a>StartContentModeration</a> which returns a
      * job identifier (<code>JobId</code>). When analysis finishes, Amazon
      * Rekognition Video publishes a completion status to the Amazon Simple
      * Notification Service topic registered in the initial call to
-     * <code>StartContentModeration</code>. To get the results of the content
-     * moderation analysis, first check that the status value published to the
+     * <code>StartContentModeration</code>. To get the results of the unsafe
+     * content analysis, first check that the status value published to the
      * Amazon SNS topic is <code>SUCCEEDED</code>. If so, call
      * <code>GetContentModeration</code> and pass the job identifier (
      * <code>JobId</code>) from the initial call to
@@ -863,8 +863,8 @@ GetCelebrityRecognitionResult getCelebrityRecognition(
      * Rekognition Devlopers Guide.
      * </p>
      * <p>
-     * <code>GetContentModeration</code> returns detected content moderation
-     * labels, and the time they are detected, in an array,
+     * <code>GetContentModeration</code> returns detected unsafe content labels,
+     * and the time they are detected, in an array,
      * <code>ModerationLabels</code>, of <a>ContentModerationDetection</a>
      * objects.
      * </p>
@@ -1686,24 +1686,22 @@ StartCelebrityRecognitionResult startCelebrityRecognition(
 
     /**
      * <p>
-     * Starts asynchronous detection of explicit or suggestive adult content in
-     * a stored video.
+     * Starts asynchronous detection of unsafe content in a stored video.
      * </p>
      * <p>
      * Amazon Rekognition Video can moderate content in a video stored in an
      * Amazon S3 bucket. Use <a>Video</a> to specify the bucket name and the
      * filename of the video. <code>StartContentModeration</code> returns a job
      * identifier (<code>JobId</code>) which you use to get the results of the
-     * analysis. When content moderation analysis is finished, Amazon
-     * Rekognition Video publishes a completion status to the Amazon Simple
-     * Notification Service topic that you specify in
-     * <code>NotificationChannel</code>.
+     * analysis. When unsafe content analysis is finished, Amazon Rekognition
+     * Video publishes a completion status to the Amazon Simple Notification
+     * Service topic that you specify in <code>NotificationChannel</code>.
      * </p>
      * <p>
-     * To get the results of the content moderation analysis, first check that
-     * the status value published to the Amazon SNS topic is
-     * <code>SUCCEEDED</code>. If so, call <a>GetContentModeration</a> and pass
-     * the job identifier (<code>JobId</code>) from the initial call to
+     * To get the results of the unsafe content analysis, first check that the
+     * status value published to the Amazon SNS topic is <code>SUCCEEDED</code>.
+     * If so, call <a>GetContentModeration</a> and pass the job identifier (
+     * <code>JobId</code>) from the initial call to
      * <code>StartContentModeration</code>.
      * </p>
      * <p>
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/AmazonRekognitionClient.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/AmazonRekognitionClient.java
index 91f467f89e..6de2fe7011 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/AmazonRekognitionClient.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/AmazonRekognitionClient.java
@@ -1139,10 +1139,10 @@ public DetectLabelsResult detectLabels(DetectLabelsRequest detectLabelsRequest)
 
     /**
      * <p>
-     * Detects explicit or suggestive adult content in a specified JPEG or PNG
-     * format image. Use <code>DetectModerationLabels</code> to moderate images
-     * depending on your requirements. For example, you might want to filter
-     * images that contain nudity, but not images containing suggestive content.
+     * Detects unsafe content in a specified JPEG or PNG format image. Use
+     * <code>DetectModerationLabels</code> to moderate images depending on your
+     * requirements. For example, you might want to filter images that contain
+     * nudity, but not images containing suggestive content.
      * </p>
      * <p>
      * To filter images, use the labels returned by
@@ -1486,17 +1486,17 @@ public GetCelebrityRecognitionResult getCelebrityRecognition(
 
     /**
      * <p>
-     * Gets the content moderation analysis results for a Amazon Rekognition
-     * Video analysis started by <a>StartContentModeration</a>.
+     * Gets the unsafe content analysis results for a Amazon Rekognition Video
+     * analysis started by <a>StartContentModeration</a>.
      * </p>
      * <p>
-     * Content moderation analysis of a video is an asynchronous operation. You
+     * Unsafe content analysis of a video is an asynchronous operation. You
      * start analysis by calling <a>StartContentModeration</a> which returns a
      * job identifier (<code>JobId</code>). When analysis finishes, Amazon
      * Rekognition Video publishes a completion status to the Amazon Simple
      * Notification Service topic registered in the initial call to
-     * <code>StartContentModeration</code>. To get the results of the content
-     * moderation analysis, first check that the status value published to the
+     * <code>StartContentModeration</code>. To get the results of the unsafe
+     * content analysis, first check that the status value published to the
      * Amazon SNS topic is <code>SUCCEEDED</code>. If so, call
      * <code>GetContentModeration</code> and pass the job identifier (
      * <code>JobId</code>) from the initial call to
@@ -1507,8 +1507,8 @@ public GetCelebrityRecognitionResult getCelebrityRecognition(
      * Rekognition Devlopers Guide.
      * </p>
      * <p>
-     * <code>GetContentModeration</code> returns detected content moderation
-     * labels, and the time they are detected, in an array,
+     * <code>GetContentModeration</code> returns detected unsafe content labels,
+     * and the time they are detected, in an array,
      * <code>ModerationLabels</code>, of <a>ContentModerationDetection</a>
      * objects.
      * </p>
@@ -2678,24 +2678,22 @@ public StartCelebrityRecognitionResult startCelebrityRecognition(
 
     /**
      * <p>
-     * Starts asynchronous detection of explicit or suggestive adult content in
-     * a stored video.
+     * Starts asynchronous detection of unsafe content in a stored video.
      * </p>
      * <p>
      * Amazon Rekognition Video can moderate content in a video stored in an
      * Amazon S3 bucket. Use <a>Video</a> to specify the bucket name and the
      * filename of the video. <code>StartContentModeration</code> returns a job
      * identifier (<code>JobId</code>) which you use to get the results of the
-     * analysis. When content moderation analysis is finished, Amazon
-     * Rekognition Video publishes a completion status to the Amazon Simple
-     * Notification Service topic that you specify in
-     * <code>NotificationChannel</code>.
+     * analysis. When unsafe content analysis is finished, Amazon Rekognition
+     * Video publishes a completion status to the Amazon Simple Notification
+     * Service topic that you specify in <code>NotificationChannel</code>.
      * </p>
      * <p>
-     * To get the results of the content moderation analysis, first check that
-     * the status value published to the Amazon SNS topic is
-     * <code>SUCCEEDED</code>. If so, call <a>GetContentModeration</a> and pass
-     * the job identifier (<code>JobId</code>) from the initial call to
+     * To get the results of the unsafe content analysis, first check that the
+     * status value published to the Amazon SNS topic is <code>SUCCEEDED</code>.
+     * If so, call <a>GetContentModeration</a> and pass the job identifier (
+     * <code>JobId</code>) from the initial call to
      * <code>StartContentModeration</code>.
      * </p>
      * <p>
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/ContentModerationDetection.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/ContentModerationDetection.java
index b963585c8a..1d951651e6 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/ContentModerationDetection.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/ContentModerationDetection.java
@@ -19,34 +19,34 @@
 
 /**
  * <p>
- * Information about a moderation label detection in a stored video.
+ * Information about an unsafe content label detection in a stored video.
  * </p>
  */
 public class ContentModerationDetection implements Serializable {
     /**
      * <p>
-     * Time, in milliseconds from the beginning of the video, that the
-     * moderation label was detected.
+     * Time, in milliseconds from the beginning of the video, that the unsafe
+     * content label was detected.
      * </p>
      */
     private Long timestamp;
 
     /**
      * <p>
-     * The moderation label detected by in the stored video.
+     * The unsafe content label detected by in the stored video.
      * </p>
      */
     private ModerationLabel moderationLabel;
 
     /**
      * <p>
-     * Time, in milliseconds from the beginning of the video, that the
-     * moderation label was detected.
+     * Time, in milliseconds from the beginning of the video, that the unsafe
+     * content label was detected.
      * </p>
      *
      * @return <p>
      *         Time, in milliseconds from the beginning of the video, that the
-     *         moderation label was detected.
+     *         unsafe content label was detected.
      *         </p>
      */
     public Long getTimestamp() {
@@ -55,13 +55,13 @@ public Long getTimestamp() {
 
     /**
      * <p>
-     * Time, in milliseconds from the beginning of the video, that the
-     * moderation label was detected.
+     * Time, in milliseconds from the beginning of the video, that the unsafe
+     * content label was detected.
      * </p>
      *
      * @param timestamp <p>
      *            Time, in milliseconds from the beginning of the video, that
-     *            the moderation label was detected.
+     *            the unsafe content label was detected.
      *            </p>
      */
     public void setTimestamp(Long timestamp) {
@@ -70,8 +70,8 @@ public void setTimestamp(Long timestamp) {
 
     /**
      * <p>
-     * Time, in milliseconds from the beginning of the video, that the
-     * moderation label was detected.
+     * Time, in milliseconds from the beginning of the video, that the unsafe
+     * content label was detected.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -79,7 +79,7 @@ public void setTimestamp(Long timestamp) {
      *
      * @param timestamp <p>
      *            Time, in milliseconds from the beginning of the video, that
-     *            the moderation label was detected.
+     *            the unsafe content label was detected.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -91,11 +91,11 @@ public ContentModerationDetection withTimestamp(Long timestamp) {
 
     /**
      * <p>
-     * The moderation label detected by in the stored video.
+     * The unsafe content label detected by in the stored video.
      * </p>
      *
      * @return <p>
-     *         The moderation label detected by in the stored video.
+     *         The unsafe content label detected by in the stored video.
      *         </p>
      */
     public ModerationLabel getModerationLabel() {
@@ -104,11 +104,11 @@ public ModerationLabel getModerationLabel() {
 
     /**
      * <p>
-     * The moderation label detected by in the stored video.
+     * The unsafe content label detected by in the stored video.
      * </p>
      *
      * @param moderationLabel <p>
-     *            The moderation label detected by in the stored video.
+     *            The unsafe content label detected by in the stored video.
      *            </p>
      */
     public void setModerationLabel(ModerationLabel moderationLabel) {
@@ -117,14 +117,14 @@ public void setModerationLabel(ModerationLabel moderationLabel) {
 
     /**
      * <p>
-     * The moderation label detected by in the stored video.
+     * The unsafe content label detected by in the stored video.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param moderationLabel <p>
-     *            The moderation label detected by in the stored video.
+     *            The unsafe content label detected by in the stored video.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/DetectModerationLabelsRequest.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/DetectModerationLabelsRequest.java
index ec5601a657..c425b0af31 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/DetectModerationLabelsRequest.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/DetectModerationLabelsRequest.java
@@ -21,10 +21,10 @@
 
 /**
  * <p>
- * Detects explicit or suggestive adult content in a specified JPEG or PNG
- * format image. Use <code>DetectModerationLabels</code> to moderate images
- * depending on your requirements. For example, you might want to filter images
- * that contain nudity, but not images containing suggestive content.
+ * Detects unsafe content in a specified JPEG or PNG format image. Use
+ * <code>DetectModerationLabels</code> to moderate images depending on your
+ * requirements. For example, you might want to filter images that contain
+ * nudity, but not images containing suggestive content.
  * </p>
  * <p>
  * To filter images, use the labels returned by
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/DetectModerationLabelsResult.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/DetectModerationLabelsResult.java
index 79bee05888..8fa6632816 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/DetectModerationLabelsResult.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/DetectModerationLabelsResult.java
@@ -20,8 +20,8 @@
 public class DetectModerationLabelsResult implements Serializable {
     /**
      * <p>
-     * Array of detected Moderation labels and the time, in millseconds from the
-     * start of the video, they were detected.
+     * Array of detected Moderation labels and the time, in milliseconds from
+     * the start of the video, they were detected.
      * </p>
      */
     private java.util.List<ModerationLabel> moderationLabels;
@@ -36,12 +36,12 @@
 
     /**
      * <p>
-     * Array of detected Moderation labels and the time, in millseconds from the
-     * start of the video, they were detected.
+     * Array of detected Moderation labels and the time, in milliseconds from
+     * the start of the video, they were detected.
      * </p>
      *
      * @return <p>
-     *         Array of detected Moderation labels and the time, in millseconds
+     *         Array of detected Moderation labels and the time, in milliseconds
      *         from the start of the video, they were detected.
      *         </p>
      */
@@ -51,13 +51,13 @@
 
     /**
      * <p>
-     * Array of detected Moderation labels and the time, in millseconds from the
-     * start of the video, they were detected.
+     * Array of detected Moderation labels and the time, in milliseconds from
+     * the start of the video, they were detected.
      * </p>
      *
      * @param moderationLabels <p>
      *            Array of detected Moderation labels and the time, in
-     *            millseconds from the start of the video, they were detected.
+     *            milliseconds from the start of the video, they were detected.
      *            </p>
      */
     public void setModerationLabels(java.util.Collection<ModerationLabel> moderationLabels) {
@@ -71,8 +71,8 @@ public void setModerationLabels(java.util.Collection<ModerationLabel> moderation
 
     /**
      * <p>
-     * Array of detected Moderation labels and the time, in millseconds from the
-     * start of the video, they were detected.
+     * Array of detected Moderation labels and the time, in milliseconds from
+     * the start of the video, they were detected.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -80,7 +80,7 @@ public void setModerationLabels(java.util.Collection<ModerationLabel> moderation
      *
      * @param moderationLabels <p>
      *            Array of detected Moderation labels and the time, in
-     *            millseconds from the start of the video, they were detected.
+     *            milliseconds from the start of the video, they were detected.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -98,8 +98,8 @@ public DetectModerationLabelsResult withModerationLabels(ModerationLabel... mode
 
     /**
      * <p>
-     * Array of detected Moderation labels and the time, in millseconds from the
-     * start of the video, they were detected.
+     * Array of detected Moderation labels and the time, in milliseconds from
+     * the start of the video, they were detected.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -107,7 +107,7 @@ public DetectModerationLabelsResult withModerationLabels(ModerationLabel... mode
      *
      * @param moderationLabels <p>
      *            Array of detected Moderation labels and the time, in
-     *            millseconds from the start of the video, they were detected.
+     *            milliseconds from the start of the video, they were detected.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/Emotion.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/Emotion.java
index 1ec4ef9554..62b63b729c 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/Emotion.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/Emotion.java
@@ -19,8 +19,11 @@
 
 /**
  * <p>
- * The emotions detected on the face, and the confidence level in the
- * determination. For example, HAPPY, SAD, and ANGRY.
+ * The emotions that appear to be expressed on the face, and the confidence
+ * level in the determination. The API is only making a determination of the
+ * physical appearance of a person's face. It is not a determination of the
+ * person’s internal emotional state and should not be used in such a way. For
+ * example, a person pretending to have a sad face might not be sad emotionally.
  * </p>
  */
 public class Emotion implements Serializable {
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/FaceDetail.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/FaceDetail.java
index a333ad2da8..ed3d64b7f9 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/FaceDetail.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/FaceDetail.java
@@ -142,8 +142,12 @@
 
     /**
      * <p>
-     * The emotions detected on the face, and the confidence level in the
-     * determination. For example, HAPPY, SAD, and ANGRY.
+     * The emotions that appear to be expressed on the face, and the confidence
+     * level in the determination. The API is only making a determination of the
+     * physical appearance of a person's face. It is not a determination of the
+     * person’s internal emotional state and should not be used in such a way.
+     * For example, a person pretending to have a sad face might not be sad
+     * emotionally.
      * </p>
      */
     private java.util.List<Emotion> emotions;
@@ -686,13 +690,21 @@ public FaceDetail withMouthOpen(MouthOpen mouthOpen) {
 
     /**
      * <p>
-     * The emotions detected on the face, and the confidence level in the
-     * determination. For example, HAPPY, SAD, and ANGRY.
+     * The emotions that appear to be expressed on the face, and the confidence
+     * level in the determination. The API is only making a determination of the
+     * physical appearance of a person's face. It is not a determination of the
+     * person’s internal emotional state and should not be used in such a way.
+     * For example, a person pretending to have a sad face might not be sad
+     * emotionally.
      * </p>
      *
      * @return <p>
-     *         The emotions detected on the face, and the confidence level in
-     *         the determination. For example, HAPPY, SAD, and ANGRY.
+     *         The emotions that appear to be expressed on the face, and the
+     *         confidence level in the determination. The API is only making a
+     *         determination of the physical appearance of a person's face. It
+     *         is not a determination of the person’s internal emotional state
+     *         and should not be used in such a way. For example, a person
+     *         pretending to have a sad face might not be sad emotionally.
      *         </p>
      */
     public java.util.List<Emotion> getEmotions() {
@@ -701,13 +713,22 @@ public FaceDetail withMouthOpen(MouthOpen mouthOpen) {
 
     /**
      * <p>
-     * The emotions detected on the face, and the confidence level in the
-     * determination. For example, HAPPY, SAD, and ANGRY.
+     * The emotions that appear to be expressed on the face, and the confidence
+     * level in the determination. The API is only making a determination of the
+     * physical appearance of a person's face. It is not a determination of the
+     * person’s internal emotional state and should not be used in such a way.
+     * For example, a person pretending to have a sad face might not be sad
+     * emotionally.
      * </p>
      *
      * @param emotions <p>
-     *            The emotions detected on the face, and the confidence level in
-     *            the determination. For example, HAPPY, SAD, and ANGRY.
+     *            The emotions that appear to be expressed on the face, and the
+     *            confidence level in the determination. The API is only making
+     *            a determination of the physical appearance of a person's face.
+     *            It is not a determination of the person’s internal emotional
+     *            state and should not be used in such a way. For example, a
+     *            person pretending to have a sad face might not be sad
+     *            emotionally.
      *            </p>
      */
     public void setEmotions(java.util.Collection<Emotion> emotions) {
@@ -721,16 +742,25 @@ public void setEmotions(java.util.Collection<Emotion> emotions) {
 
     /**
      * <p>
-     * The emotions detected on the face, and the confidence level in the
-     * determination. For example, HAPPY, SAD, and ANGRY.
+     * The emotions that appear to be expressed on the face, and the confidence
+     * level in the determination. The API is only making a determination of the
+     * physical appearance of a person's face. It is not a determination of the
+     * person’s internal emotional state and should not be used in such a way.
+     * For example, a person pretending to have a sad face might not be sad
+     * emotionally.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param emotions <p>
-     *            The emotions detected on the face, and the confidence level in
-     *            the determination. For example, HAPPY, SAD, and ANGRY.
+     *            The emotions that appear to be expressed on the face, and the
+     *            confidence level in the determination. The API is only making
+     *            a determination of the physical appearance of a person's face.
+     *            It is not a determination of the person’s internal emotional
+     *            state and should not be used in such a way. For example, a
+     *            person pretending to have a sad face might not be sad
+     *            emotionally.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -747,16 +777,25 @@ public FaceDetail withEmotions(Emotion... emotions) {
 
     /**
      * <p>
-     * The emotions detected on the face, and the confidence level in the
-     * determination. For example, HAPPY, SAD, and ANGRY.
+     * The emotions that appear to be expressed on the face, and the confidence
+     * level in the determination. The API is only making a determination of the
+     * physical appearance of a person's face. It is not a determination of the
+     * person’s internal emotional state and should not be used in such a way.
+     * For example, a person pretending to have a sad face might not be sad
+     * emotionally.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param emotions <p>
-     *            The emotions detected on the face, and the confidence level in
-     *            the determination. For example, HAPPY, SAD, and ANGRY.
+     *            The emotions that appear to be expressed on the face, and the
+     *            confidence level in the determination. The API is only making
+     *            a determination of the physical appearance of a person's face.
+     *            It is not a determination of the person’s internal emotional
+     *            state and should not be used in such a way. For example, a
+     *            person pretending to have a sad face might not be sad
+     *            emotionally.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/GetContentModerationRequest.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/GetContentModerationRequest.java
index 2e39cdec5b..b17b6debad 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/GetContentModerationRequest.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/GetContentModerationRequest.java
@@ -21,16 +21,16 @@
 
 /**
  * <p>
- * Gets the content moderation analysis results for a Amazon Rekognition Video
+ * Gets the unsafe content analysis results for a Amazon Rekognition Video
  * analysis started by <a>StartContentModeration</a>.
  * </p>
  * <p>
- * Content moderation analysis of a video is an asynchronous operation. You
- * start analysis by calling <a>StartContentModeration</a> which returns a job
+ * Unsafe content analysis of a video is an asynchronous operation. You start
+ * analysis by calling <a>StartContentModeration</a> which returns a job
  * identifier (<code>JobId</code>). When analysis finishes, Amazon Rekognition
  * Video publishes a completion status to the Amazon Simple Notification Service
  * topic registered in the initial call to <code>StartContentModeration</code>.
- * To get the results of the content moderation analysis, first check that the
+ * To get the results of the unsafe content analysis, first check that the
  * status value published to the Amazon SNS topic is <code>SUCCEEDED</code>. If
  * so, call <code>GetContentModeration</code> and pass the job identifier (
  * <code>JobId</code>) from the initial call to
@@ -41,9 +41,9 @@
  * Rekognition Devlopers Guide.
  * </p>
  * <p>
- * <code>GetContentModeration</code> returns detected content moderation labels,
- * and the time they are detected, in an array, <code>ModerationLabels</code>,
- * of <a>ContentModerationDetection</a> objects.
+ * <code>GetContentModeration</code> returns detected unsafe content labels, and
+ * the time they are detected, in an array, <code>ModerationLabels</code>, of
+ * <a>ContentModerationDetection</a> objects.
  * </p>
  * <p>
  * By default, the moderated labels are returned sorted by time, in milliseconds
@@ -69,7 +69,7 @@
 public class GetContentModerationRequest extends AmazonWebServiceRequest implements Serializable {
     /**
      * <p>
-     * The identifier for the content moderation job. Use <code>JobId</code> to
+     * The identifier for the unsafe content job. Use <code>JobId</code> to
      * identify the job in a subsequent call to
      * <code>GetContentModeration</code>.
      * </p>
@@ -96,8 +96,8 @@
      * <p>
      * If the previous response was incomplete (because there is more data to
      * retrieve), Amazon Rekognition returns a pagination token in the response.
-     * You can use this pagination token to retrieve the next set of content
-     * moderation labels.
+     * You can use this pagination token to retrieve the next set of unsafe
+     * content labels.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -122,7 +122,7 @@
 
     /**
      * <p>
-     * The identifier for the content moderation job. Use <code>JobId</code> to
+     * The identifier for the unsafe content job. Use <code>JobId</code> to
      * identify the job in a subsequent call to
      * <code>GetContentModeration</code>.
      * </p>
@@ -132,8 +132,8 @@
      * <b>Pattern: </b>^[a-zA-Z0-9-_]+$<br/>
      *
      * @return <p>
-     *         The identifier for the content moderation job. Use
-     *         <code>JobId</code> to identify the job in a subsequent call to
+     *         The identifier for the unsafe content job. Use <code>JobId</code>
+     *         to identify the job in a subsequent call to
      *         <code>GetContentModeration</code>.
      *         </p>
      */
@@ -143,7 +143,7 @@ public String getJobId() {
 
     /**
      * <p>
-     * The identifier for the content moderation job. Use <code>JobId</code> to
+     * The identifier for the unsafe content job. Use <code>JobId</code> to
      * identify the job in a subsequent call to
      * <code>GetContentModeration</code>.
      * </p>
@@ -153,7 +153,7 @@ public String getJobId() {
      * <b>Pattern: </b>^[a-zA-Z0-9-_]+$<br/>
      *
      * @param jobId <p>
-     *            The identifier for the content moderation job. Use
+     *            The identifier for the unsafe content job. Use
      *            <code>JobId</code> to identify the job in a subsequent call to
      *            <code>GetContentModeration</code>.
      *            </p>
@@ -164,7 +164,7 @@ public void setJobId(String jobId) {
 
     /**
      * <p>
-     * The identifier for the content moderation job. Use <code>JobId</code> to
+     * The identifier for the unsafe content job. Use <code>JobId</code> to
      * identify the job in a subsequent call to
      * <code>GetContentModeration</code>.
      * </p>
@@ -177,7 +177,7 @@ public void setJobId(String jobId) {
      * <b>Pattern: </b>^[a-zA-Z0-9-_]+$<br/>
      *
      * @param jobId <p>
-     *            The identifier for the content moderation job. Use
+     *            The identifier for the unsafe content job. Use
      *            <code>JobId</code> to identify the job in a subsequent call to
      *            <code>GetContentModeration</code>.
      *            </p>
@@ -262,8 +262,8 @@ public GetContentModerationRequest withMaxResults(Integer maxResults) {
      * <p>
      * If the previous response was incomplete (because there is more data to
      * retrieve), Amazon Rekognition returns a pagination token in the response.
-     * You can use this pagination token to retrieve the next set of content
-     * moderation labels.
+     * You can use this pagination token to retrieve the next set of unsafe
+     * content labels.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -273,7 +273,7 @@ public GetContentModerationRequest withMaxResults(Integer maxResults) {
      *         If the previous response was incomplete (because there is more
      *         data to retrieve), Amazon Rekognition returns a pagination token
      *         in the response. You can use this pagination token to retrieve
-     *         the next set of content moderation labels.
+     *         the next set of unsafe content labels.
      *         </p>
      */
     public String getNextToken() {
@@ -284,8 +284,8 @@ public String getNextToken() {
      * <p>
      * If the previous response was incomplete (because there is more data to
      * retrieve), Amazon Rekognition returns a pagination token in the response.
-     * You can use this pagination token to retrieve the next set of content
-     * moderation labels.
+     * You can use this pagination token to retrieve the next set of unsafe
+     * content labels.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -295,7 +295,7 @@ public String getNextToken() {
      *            If the previous response was incomplete (because there is more
      *            data to retrieve), Amazon Rekognition returns a pagination
      *            token in the response. You can use this pagination token to
-     *            retrieve the next set of content moderation labels.
+     *            retrieve the next set of unsafe content labels.
      *            </p>
      */
     public void setNextToken(String nextToken) {
@@ -306,8 +306,8 @@ public void setNextToken(String nextToken) {
      * <p>
      * If the previous response was incomplete (because there is more data to
      * retrieve), Amazon Rekognition returns a pagination token in the response.
-     * You can use this pagination token to retrieve the next set of content
-     * moderation labels.
+     * You can use this pagination token to retrieve the next set of unsafe
+     * content labels.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -320,7 +320,7 @@ public void setNextToken(String nextToken) {
      *            If the previous response was incomplete (because there is more
      *            data to retrieve), Amazon Rekognition returns a pagination
      *            token in the response. You can use this pagination token to
-     *            retrieve the next set of content moderation labels.
+     *            retrieve the next set of unsafe content labels.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/GetContentModerationResult.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/GetContentModerationResult.java
index 97235e8368..2c05de687d 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/GetContentModerationResult.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/GetContentModerationResult.java
@@ -20,7 +20,7 @@
 public class GetContentModerationResult implements Serializable {
     /**
      * <p>
-     * The current status of the content moderation job.
+     * The current status of the unsafe content analysis job.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -47,7 +47,7 @@
 
     /**
      * <p>
-     * The detected moderation labels and the time(s) they were detected.
+     * The detected unsafe content labels and the time(s) they were detected.
      * </p>
      */
     private java.util.List<ContentModerationDetection> moderationLabels;
@@ -56,7 +56,7 @@
      * <p>
      * If the response is truncated, Amazon Rekognition Video returns this token
      * that you can use in the subsequent request to retrieve the next set of
-     * moderation labels.
+     * unsafe content labels.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -74,14 +74,14 @@
 
     /**
      * <p>
-     * The current status of the content moderation job.
+     * The current status of the unsafe content analysis job.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
      * <b>Allowed Values: </b>IN_PROGRESS, SUCCEEDED, FAILED
      *
      * @return <p>
-     *         The current status of the content moderation job.
+     *         The current status of the unsafe content analysis job.
      *         </p>
      * @see VideoJobStatus
      */
@@ -91,14 +91,14 @@ public String getJobStatus() {
 
     /**
      * <p>
-     * The current status of the content moderation job.
+     * The current status of the unsafe content analysis job.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
      * <b>Allowed Values: </b>IN_PROGRESS, SUCCEEDED, FAILED
      *
      * @param jobStatus <p>
-     *            The current status of the content moderation job.
+     *            The current status of the unsafe content analysis job.
      *            </p>
      * @see VideoJobStatus
      */
@@ -108,7 +108,7 @@ public void setJobStatus(String jobStatus) {
 
     /**
      * <p>
-     * The current status of the content moderation job.
+     * The current status of the unsafe content analysis job.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -118,7 +118,7 @@ public void setJobStatus(String jobStatus) {
      * <b>Allowed Values: </b>IN_PROGRESS, SUCCEEDED, FAILED
      *
      * @param jobStatus <p>
-     *            The current status of the content moderation job.
+     *            The current status of the unsafe content analysis job.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -131,14 +131,14 @@ public GetContentModerationResult withJobStatus(String jobStatus) {
 
     /**
      * <p>
-     * The current status of the content moderation job.
+     * The current status of the unsafe content analysis job.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
      * <b>Allowed Values: </b>IN_PROGRESS, SUCCEEDED, FAILED
      *
      * @param jobStatus <p>
-     *            The current status of the content moderation job.
+     *            The current status of the unsafe content analysis job.
      *            </p>
      * @see VideoJobStatus
      */
@@ -148,7 +148,7 @@ public void setJobStatus(VideoJobStatus jobStatus) {
 
     /**
      * <p>
-     * The current status of the content moderation job.
+     * The current status of the unsafe content analysis job.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -158,7 +158,7 @@ public void setJobStatus(VideoJobStatus jobStatus) {
      * <b>Allowed Values: </b>IN_PROGRESS, SUCCEEDED, FAILED
      *
      * @param jobStatus <p>
-     *            The current status of the content moderation job.
+     *            The current status of the unsafe content analysis job.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -279,11 +279,11 @@ public GetContentModerationResult withVideoMetadata(VideoMetadata videoMetadata)
 
     /**
      * <p>
-     * The detected moderation labels and the time(s) they were detected.
+     * The detected unsafe content labels and the time(s) they were detected.
      * </p>
      *
      * @return <p>
-     *         The detected moderation labels and the time(s) they were
+     *         The detected unsafe content labels and the time(s) they were
      *         detected.
      *         </p>
      */
@@ -293,11 +293,11 @@ public GetContentModerationResult withVideoMetadata(VideoMetadata videoMetadata)
 
     /**
      * <p>
-     * The detected moderation labels and the time(s) they were detected.
+     * The detected unsafe content labels and the time(s) they were detected.
      * </p>
      *
      * @param moderationLabels <p>
-     *            The detected moderation labels and the time(s) they were
+     *            The detected unsafe content labels and the time(s) they were
      *            detected.
      *            </p>
      */
@@ -314,14 +314,14 @@ public void setModerationLabels(
 
     /**
      * <p>
-     * The detected moderation labels and the time(s) they were detected.
+     * The detected unsafe content labels and the time(s) they were detected.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param moderationLabels <p>
-     *            The detected moderation labels and the time(s) they were
+     *            The detected unsafe content labels and the time(s) they were
      *            detected.
      *            </p>
      * @return A reference to this updated object so that method calls can be
@@ -341,14 +341,14 @@ public GetContentModerationResult withModerationLabels(
 
     /**
      * <p>
-     * The detected moderation labels and the time(s) they were detected.
+     * The detected unsafe content labels and the time(s) they were detected.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param moderationLabels <p>
-     *            The detected moderation labels and the time(s) they were
+     *            The detected unsafe content labels and the time(s) they were
      *            detected.
      *            </p>
      * @return A reference to this updated object so that method calls can be
@@ -364,7 +364,7 @@ public GetContentModerationResult withModerationLabels(
      * <p>
      * If the response is truncated, Amazon Rekognition Video returns this token
      * that you can use in the subsequent request to retrieve the next set of
-     * moderation labels.
+     * unsafe content labels.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -373,7 +373,7 @@ public GetContentModerationResult withModerationLabels(
      * @return <p>
      *         If the response is truncated, Amazon Rekognition Video returns
      *         this token that you can use in the subsequent request to retrieve
-     *         the next set of moderation labels.
+     *         the next set of unsafe content labels.
      *         </p>
      */
     public String getNextToken() {
@@ -384,7 +384,7 @@ public String getNextToken() {
      * <p>
      * If the response is truncated, Amazon Rekognition Video returns this token
      * that you can use in the subsequent request to retrieve the next set of
-     * moderation labels.
+     * unsafe content labels.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -393,7 +393,7 @@ public String getNextToken() {
      * @param nextToken <p>
      *            If the response is truncated, Amazon Rekognition Video returns
      *            this token that you can use in the subsequent request to
-     *            retrieve the next set of moderation labels.
+     *            retrieve the next set of unsafe content labels.
      *            </p>
      */
     public void setNextToken(String nextToken) {
@@ -404,7 +404,7 @@ public void setNextToken(String nextToken) {
      * <p>
      * If the response is truncated, Amazon Rekognition Video returns this token
      * that you can use in the subsequent request to retrieve the next set of
-     * moderation labels.
+     * unsafe content labels.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -416,7 +416,7 @@ public void setNextToken(String nextToken) {
      * @param nextToken <p>
      *            If the response is truncated, Amazon Rekognition Video returns
      *            this token that you can use in the subsequent request to
-     *            retrieve the next set of moderation labels.
+     *            retrieve the next set of unsafe content labels.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/ModerationLabel.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/ModerationLabel.java
index 01a4e30bec..f760398fe1 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/ModerationLabel.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/ModerationLabel.java
@@ -19,10 +19,10 @@
 
 /**
  * <p>
- * Provides information about a single type of moderated content found in an
- * image or video. Each type of moderated content has a label within a
- * hierarchical taxonomy. For more information, see Detecting Unsafe Content in
- * the Amazon Rekognition Developer Guide.
+ * Provides information about a single type of unsafe content found in an image
+ * or video. Each type of moderated content has a label within a hierarchical
+ * taxonomy. For more information, see Detecting Unsafe Content in the Amazon
+ * Rekognition Developer Guide.
  * </p>
  */
 public class ModerationLabel implements Serializable {
@@ -44,7 +44,7 @@
 
     /**
      * <p>
-     * The label name for the type of content detected in the image.
+     * The label name for the type of unsafe content detected in the image.
      * </p>
      */
     private String name;
@@ -152,11 +152,12 @@ public ModerationLabel withConfidence(Float confidence) {
 
     /**
      * <p>
-     * The label name for the type of content detected in the image.
+     * The label name for the type of unsafe content detected in the image.
      * </p>
      *
      * @return <p>
-     *         The label name for the type of content detected in the image.
+     *         The label name for the type of unsafe content detected in the
+     *         image.
      *         </p>
      */
     public String getName() {
@@ -165,11 +166,12 @@ public String getName() {
 
     /**
      * <p>
-     * The label name for the type of content detected in the image.
+     * The label name for the type of unsafe content detected in the image.
      * </p>
      *
      * @param name <p>
-     *            The label name for the type of content detected in the image.
+     *            The label name for the type of unsafe content detected in the
+     *            image.
      *            </p>
      */
     public void setName(String name) {
@@ -178,14 +180,15 @@ public void setName(String name) {
 
     /**
      * <p>
-     * The label name for the type of content detected in the image.
+     * The label name for the type of unsafe content detected in the image.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param name <p>
-     *            The label name for the type of content detected in the image.
+     *            The label name for the type of unsafe content detected in the
+     *            image.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/SearchFacesByImageRequest.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/SearchFacesByImageRequest.java
index c652e241a2..717aeb1d6e 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/SearchFacesByImageRequest.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/SearchFacesByImageRequest.java
@@ -104,7 +104,7 @@
      * <p>
      * (Optional) Specifies the minimum confidence in the face match to return.
      * For example, don't return any matches where confidence in matches is less
-     * than 70%.
+     * than 70%. The default value is 80%.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -361,7 +361,7 @@ public SearchFacesByImageRequest withMaxFaces(Integer maxFaces) {
      * <p>
      * (Optional) Specifies the minimum confidence in the face match to return.
      * For example, don't return any matches where confidence in matches is less
-     * than 70%.
+     * than 70%. The default value is 80%.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -370,7 +370,7 @@ public SearchFacesByImageRequest withMaxFaces(Integer maxFaces) {
      * @return <p>
      *         (Optional) Specifies the minimum confidence in the face match to
      *         return. For example, don't return any matches where confidence in
-     *         matches is less than 70%.
+     *         matches is less than 70%. The default value is 80%.
      *         </p>
      */
     public Float getFaceMatchThreshold() {
@@ -381,7 +381,7 @@ public Float getFaceMatchThreshold() {
      * <p>
      * (Optional) Specifies the minimum confidence in the face match to return.
      * For example, don't return any matches where confidence in matches is less
-     * than 70%.
+     * than 70%. The default value is 80%.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -390,7 +390,8 @@ public Float getFaceMatchThreshold() {
      * @param faceMatchThreshold <p>
      *            (Optional) Specifies the minimum confidence in the face match
      *            to return. For example, don't return any matches where
-     *            confidence in matches is less than 70%.
+     *            confidence in matches is less than 70%. The default value is
+     *            80%.
      *            </p>
      */
     public void setFaceMatchThreshold(Float faceMatchThreshold) {
@@ -401,7 +402,7 @@ public void setFaceMatchThreshold(Float faceMatchThreshold) {
      * <p>
      * (Optional) Specifies the minimum confidence in the face match to return.
      * For example, don't return any matches where confidence in matches is less
-     * than 70%.
+     * than 70%. The default value is 80%.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -413,7 +414,8 @@ public void setFaceMatchThreshold(Float faceMatchThreshold) {
      * @param faceMatchThreshold <p>
      *            (Optional) Specifies the minimum confidence in the face match
      *            to return. For example, don't return any matches where
-     *            confidence in matches is less than 70%.
+     *            confidence in matches is less than 70%. The default value is
+     *            80%.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/SearchFacesRequest.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/SearchFacesRequest.java
index e6753dafe7..b8557b02af 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/SearchFacesRequest.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/SearchFacesRequest.java
@@ -87,7 +87,7 @@
      * <p>
      * Optional value specifying the minimum confidence in the face match to
      * return. For example, don't return any matches where confidence in matches
-     * is less than 70%.
+     * is less than 70%. The default value is 80%.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -300,7 +300,7 @@ public SearchFacesRequest withMaxFaces(Integer maxFaces) {
      * <p>
      * Optional value specifying the minimum confidence in the face match to
      * return. For example, don't return any matches where confidence in matches
-     * is less than 70%.
+     * is less than 70%. The default value is 80%.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -309,7 +309,7 @@ public SearchFacesRequest withMaxFaces(Integer maxFaces) {
      * @return <p>
      *         Optional value specifying the minimum confidence in the face
      *         match to return. For example, don't return any matches where
-     *         confidence in matches is less than 70%.
+     *         confidence in matches is less than 70%. The default value is 80%.
      *         </p>
      */
     public Float getFaceMatchThreshold() {
@@ -320,7 +320,7 @@ public Float getFaceMatchThreshold() {
      * <p>
      * Optional value specifying the minimum confidence in the face match to
      * return. For example, don't return any matches where confidence in matches
-     * is less than 70%.
+     * is less than 70%. The default value is 80%.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -329,7 +329,8 @@ public Float getFaceMatchThreshold() {
      * @param faceMatchThreshold <p>
      *            Optional value specifying the minimum confidence in the face
      *            match to return. For example, don't return any matches where
-     *            confidence in matches is less than 70%.
+     *            confidence in matches is less than 70%. The default value is
+     *            80%.
      *            </p>
      */
     public void setFaceMatchThreshold(Float faceMatchThreshold) {
@@ -340,7 +341,7 @@ public void setFaceMatchThreshold(Float faceMatchThreshold) {
      * <p>
      * Optional value specifying the minimum confidence in the face match to
      * return. For example, don't return any matches where confidence in matches
-     * is less than 70%.
+     * is less than 70%. The default value is 80%.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -352,7 +353,8 @@ public void setFaceMatchThreshold(Float faceMatchThreshold) {
      * @param faceMatchThreshold <p>
      *            Optional value specifying the minimum confidence in the face
      *            match to return. For example, don't return any matches where
-     *            confidence in matches is less than 70%.
+     *            confidence in matches is less than 70%. The default value is
+     *            80%.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartCelebrityRecognitionRequest.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartCelebrityRecognitionRequest.java
index 7a3d25e755..b23806fafc 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartCelebrityRecognitionRequest.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartCelebrityRecognitionRequest.java
@@ -76,8 +76,10 @@
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -275,8 +277,10 @@ public StartCelebrityRecognitionRequest withNotificationChannel(
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -284,9 +288,11 @@ public StartCelebrityRecognitionRequest withNotificationChannel(
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @return <p>
-     *         Unique identifier you specify to identify the job in the
-     *         completion status published to the Amazon Simple Notification
-     *         Service topic.
+     *         An identifier you specify that's returned in the completion
+     *         notification that's published to your Amazon Simple Notification
+     *         Service topic. For example, you can use <code>JobTag</code> to
+     *         group related jobs and identify them in the completion
+     *         notification.
      *         </p>
      */
     public String getJobTag() {
@@ -295,8 +301,10 @@ public String getJobTag() {
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -304,9 +312,11 @@ public String getJobTag() {
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @param jobTag <p>
-     *            Unique identifier you specify to identify the job in the
-     *            completion status published to the Amazon Simple Notification
-     *            Service topic.
+     *            An identifier you specify that's returned in the completion
+     *            notification that's published to your Amazon Simple
+     *            Notification Service topic. For example, you can use
+     *            <code>JobTag</code> to group related jobs and identify them in
+     *            the completion notification.
      *            </p>
      */
     public void setJobTag(String jobTag) {
@@ -315,8 +325,10 @@ public void setJobTag(String jobTag) {
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -327,9 +339,11 @@ public void setJobTag(String jobTag) {
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @param jobTag <p>
-     *            Unique identifier you specify to identify the job in the
-     *            completion status published to the Amazon Simple Notification
-     *            Service topic.
+     *            An identifier you specify that's returned in the completion
+     *            notification that's published to your Amazon Simple
+     *            Notification Service topic. For example, you can use
+     *            <code>JobTag</code> to group related jobs and identify them in
+     *            the completion notification.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartContentModerationRequest.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartContentModerationRequest.java
index 3e285cb666..daba7ff2b5 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartContentModerationRequest.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartContentModerationRequest.java
@@ -21,20 +21,19 @@
 
 /**
  * <p>
- * Starts asynchronous detection of explicit or suggestive adult content in a
- * stored video.
+ * Starts asynchronous detection of unsafe content in a stored video.
  * </p>
  * <p>
  * Amazon Rekognition Video can moderate content in a video stored in an Amazon
  * S3 bucket. Use <a>Video</a> to specify the bucket name and the filename of
  * the video. <code>StartContentModeration</code> returns a job identifier (
  * <code>JobId</code>) which you use to get the results of the analysis. When
- * content moderation analysis is finished, Amazon Rekognition Video publishes a
+ * unsafe content analysis is finished, Amazon Rekognition Video publishes a
  * completion status to the Amazon Simple Notification Service topic that you
  * specify in <code>NotificationChannel</code>.
  * </p>
  * <p>
- * To get the results of the content moderation analysis, first check that the
+ * To get the results of the unsafe content analysis, first check that the
  * status value published to the Amazon SNS topic is <code>SUCCEEDED</code>. If
  * so, call <a>GetContentModeration</a> and pass the job identifier (
  * <code>JobId</code>) from the initial call to
@@ -48,8 +47,8 @@
 public class StartContentModerationRequest extends AmazonWebServiceRequest implements Serializable {
     /**
      * <p>
-     * The video in which you want to moderate content. The video must be stored
-     * in an Amazon S3 bucket.
+     * The video in which you want to detect unsafe content. The video must be
+     * stored in an Amazon S3 bucket.
      * </p>
      */
     private Video video;
@@ -88,15 +87,17 @@
     /**
      * <p>
      * The Amazon SNS topic ARN that you want Amazon Rekognition Video to
-     * publish the completion status of the content moderation analysis to.
+     * publish the completion status of the unsafe content analysis to.
      * </p>
      */
     private NotificationChannel notificationChannel;
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -107,13 +108,13 @@
 
     /**
      * <p>
-     * The video in which you want to moderate content. The video must be stored
-     * in an Amazon S3 bucket.
+     * The video in which you want to detect unsafe content. The video must be
+     * stored in an Amazon S3 bucket.
      * </p>
      *
      * @return <p>
-     *         The video in which you want to moderate content. The video must
-     *         be stored in an Amazon S3 bucket.
+     *         The video in which you want to detect unsafe content. The video
+     *         must be stored in an Amazon S3 bucket.
      *         </p>
      */
     public Video getVideo() {
@@ -122,13 +123,13 @@ public Video getVideo() {
 
     /**
      * <p>
-     * The video in which you want to moderate content. The video must be stored
-     * in an Amazon S3 bucket.
+     * The video in which you want to detect unsafe content. The video must be
+     * stored in an Amazon S3 bucket.
      * </p>
      *
      * @param video <p>
-     *            The video in which you want to moderate content. The video
-     *            must be stored in an Amazon S3 bucket.
+     *            The video in which you want to detect unsafe content. The
+     *            video must be stored in an Amazon S3 bucket.
      *            </p>
      */
     public void setVideo(Video video) {
@@ -137,16 +138,16 @@ public void setVideo(Video video) {
 
     /**
      * <p>
-     * The video in which you want to moderate content. The video must be stored
-     * in an Amazon S3 bucket.
+     * The video in which you want to detect unsafe content. The video must be
+     * stored in an Amazon S3 bucket.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
      * together.
      *
      * @param video <p>
-     *            The video in which you want to moderate content. The video
-     *            must be stored in an Amazon S3 bucket.
+     *            The video in which you want to detect unsafe content. The
+     *            video must be stored in an Amazon S3 bucket.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -340,13 +341,13 @@ public StartContentModerationRequest withClientRequestToken(String clientRequest
     /**
      * <p>
      * The Amazon SNS topic ARN that you want Amazon Rekognition Video to
-     * publish the completion status of the content moderation analysis to.
+     * publish the completion status of the unsafe content analysis to.
      * </p>
      *
      * @return <p>
      *         The Amazon SNS topic ARN that you want Amazon Rekognition Video
-     *         to publish the completion status of the content moderation
-     *         analysis to.
+     *         to publish the completion status of the unsafe content analysis
+     *         to.
      *         </p>
      */
     public NotificationChannel getNotificationChannel() {
@@ -356,13 +357,13 @@ public NotificationChannel getNotificationChannel() {
     /**
      * <p>
      * The Amazon SNS topic ARN that you want Amazon Rekognition Video to
-     * publish the completion status of the content moderation analysis to.
+     * publish the completion status of the unsafe content analysis to.
      * </p>
      *
      * @param notificationChannel <p>
      *            The Amazon SNS topic ARN that you want Amazon Rekognition
-     *            Video to publish the completion status of the content
-     *            moderation analysis to.
+     *            Video to publish the completion status of the unsafe content
+     *            analysis to.
      *            </p>
      */
     public void setNotificationChannel(NotificationChannel notificationChannel) {
@@ -372,7 +373,7 @@ public void setNotificationChannel(NotificationChannel notificationChannel) {
     /**
      * <p>
      * The Amazon SNS topic ARN that you want Amazon Rekognition Video to
-     * publish the completion status of the content moderation analysis to.
+     * publish the completion status of the unsafe content analysis to.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -380,8 +381,8 @@ public void setNotificationChannel(NotificationChannel notificationChannel) {
      *
      * @param notificationChannel <p>
      *            The Amazon SNS topic ARN that you want Amazon Rekognition
-     *            Video to publish the completion status of the content
-     *            moderation analysis to.
+     *            Video to publish the completion status of the unsafe content
+     *            analysis to.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -394,8 +395,10 @@ public StartContentModerationRequest withNotificationChannel(
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -403,9 +406,11 @@ public StartContentModerationRequest withNotificationChannel(
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @return <p>
-     *         Unique identifier you specify to identify the job in the
-     *         completion status published to the Amazon Simple Notification
-     *         Service topic.
+     *         An identifier you specify that's returned in the completion
+     *         notification that's published to your Amazon Simple Notification
+     *         Service topic. For example, you can use <code>JobTag</code> to
+     *         group related jobs and identify them in the completion
+     *         notification.
      *         </p>
      */
     public String getJobTag() {
@@ -414,8 +419,10 @@ public String getJobTag() {
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -423,9 +430,11 @@ public String getJobTag() {
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @param jobTag <p>
-     *            Unique identifier you specify to identify the job in the
-     *            completion status published to the Amazon Simple Notification
-     *            Service topic.
+     *            An identifier you specify that's returned in the completion
+     *            notification that's published to your Amazon Simple
+     *            Notification Service topic. For example, you can use
+     *            <code>JobTag</code> to group related jobs and identify them in
+     *            the completion notification.
      *            </p>
      */
     public void setJobTag(String jobTag) {
@@ -434,8 +443,10 @@ public void setJobTag(String jobTag) {
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -446,9 +457,11 @@ public void setJobTag(String jobTag) {
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @param jobTag <p>
-     *            Unique identifier you specify to identify the job in the
-     *            completion status published to the Amazon Simple Notification
-     *            Service topic.
+     *            An identifier you specify that's returned in the completion
+     *            notification that's published to your Amazon Simple
+     *            Notification Service topic. For example, you can use
+     *            <code>JobTag</code> to group related jobs and identify them in
+     *            the completion notification.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartContentModerationResult.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartContentModerationResult.java
index 0f7928c190..2acea909e9 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartContentModerationResult.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartContentModerationResult.java
@@ -20,7 +20,7 @@
 public class StartContentModerationResult implements Serializable {
     /**
      * <p>
-     * The identifier for the content moderation analysis job. Use
+     * The identifier for the unsafe content analysis job. Use
      * <code>JobId</code> to identify the job in a subsequent call to
      * <code>GetContentModeration</code>.
      * </p>
@@ -33,7 +33,7 @@
 
     /**
      * <p>
-     * The identifier for the content moderation analysis job. Use
+     * The identifier for the unsafe content analysis job. Use
      * <code>JobId</code> to identify the job in a subsequent call to
      * <code>GetContentModeration</code>.
      * </p>
@@ -43,7 +43,7 @@
      * <b>Pattern: </b>^[a-zA-Z0-9-_]+$<br/>
      *
      * @return <p>
-     *         The identifier for the content moderation analysis job. Use
+     *         The identifier for the unsafe content analysis job. Use
      *         <code>JobId</code> to identify the job in a subsequent call to
      *         <code>GetContentModeration</code>.
      *         </p>
@@ -54,7 +54,7 @@ public String getJobId() {
 
     /**
      * <p>
-     * The identifier for the content moderation analysis job. Use
+     * The identifier for the unsafe content analysis job. Use
      * <code>JobId</code> to identify the job in a subsequent call to
      * <code>GetContentModeration</code>.
      * </p>
@@ -64,7 +64,7 @@ public String getJobId() {
      * <b>Pattern: </b>^[a-zA-Z0-9-_]+$<br/>
      *
      * @param jobId <p>
-     *            The identifier for the content moderation analysis job. Use
+     *            The identifier for the unsafe content analysis job. Use
      *            <code>JobId</code> to identify the job in a subsequent call to
      *            <code>GetContentModeration</code>.
      *            </p>
@@ -75,7 +75,7 @@ public void setJobId(String jobId) {
 
     /**
      * <p>
-     * The identifier for the content moderation analysis job. Use
+     * The identifier for the unsafe content analysis job. Use
      * <code>JobId</code> to identify the job in a subsequent call to
      * <code>GetContentModeration</code>.
      * </p>
@@ -88,7 +88,7 @@ public void setJobId(String jobId) {
      * <b>Pattern: </b>^[a-zA-Z0-9-_]+$<br/>
      *
      * @param jobId <p>
-     *            The identifier for the content moderation analysis job. Use
+     *            The identifier for the unsafe content analysis job. Use
      *            <code>JobId</code> to identify the job in a subsequent call to
      *            <code>GetContentModeration</code>.
      *            </p>
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartFaceDetectionRequest.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartFaceDetectionRequest.java
index cd982e5ae0..894572ad03 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartFaceDetectionRequest.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartFaceDetectionRequest.java
@@ -91,8 +91,10 @@
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -460,8 +462,10 @@ public StartFaceDetectionRequest withFaceAttributes(FaceAttributes faceAttribute
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -469,9 +473,11 @@ public StartFaceDetectionRequest withFaceAttributes(FaceAttributes faceAttribute
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @return <p>
-     *         Unique identifier you specify to identify the job in the
-     *         completion status published to the Amazon Simple Notification
-     *         Service topic.
+     *         An identifier you specify that's returned in the completion
+     *         notification that's published to your Amazon Simple Notification
+     *         Service topic. For example, you can use <code>JobTag</code> to
+     *         group related jobs and identify them in the completion
+     *         notification.
      *         </p>
      */
     public String getJobTag() {
@@ -480,8 +486,10 @@ public String getJobTag() {
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -489,9 +497,11 @@ public String getJobTag() {
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @param jobTag <p>
-     *            Unique identifier you specify to identify the job in the
-     *            completion status published to the Amazon Simple Notification
-     *            Service topic.
+     *            An identifier you specify that's returned in the completion
+     *            notification that's published to your Amazon Simple
+     *            Notification Service topic. For example, you can use
+     *            <code>JobTag</code> to group related jobs and identify them in
+     *            the completion notification.
      *            </p>
      */
     public void setJobTag(String jobTag) {
@@ -500,8 +510,10 @@ public void setJobTag(String jobTag) {
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -512,9 +524,11 @@ public void setJobTag(String jobTag) {
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @param jobTag <p>
-     *            Unique identifier you specify to identify the job in the
-     *            completion status published to the Amazon Simple Notification
-     *            Service topic.
+     *            An identifier you specify that's returned in the completion
+     *            notification that's published to your Amazon Simple
+     *            Notification Service topic. For example, you can use
+     *            <code>JobTag</code> to group related jobs and identify them in
+     *            the completion notification.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartFaceSearchRequest.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartFaceSearchRequest.java
index d5e803d4eb..251b28a96d 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartFaceSearchRequest.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartFaceSearchRequest.java
@@ -64,7 +64,8 @@
     /**
      * <p>
      * The minimum confidence in the person match to return. For example, don't
-     * return any matches where confidence in matches is less than 70%.
+     * return any matches where confidence in matches is less than 70%. The
+     * default value is 80%.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -93,8 +94,10 @@
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -235,7 +238,8 @@ public StartFaceSearchRequest withClientRequestToken(String clientRequestToken)
     /**
      * <p>
      * The minimum confidence in the person match to return. For example, don't
-     * return any matches where confidence in matches is less than 70%.
+     * return any matches where confidence in matches is less than 70%. The
+     * default value is 80%.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -244,7 +248,7 @@ public StartFaceSearchRequest withClientRequestToken(String clientRequestToken)
      * @return <p>
      *         The minimum confidence in the person match to return. For
      *         example, don't return any matches where confidence in matches is
-     *         less than 70%.
+     *         less than 70%. The default value is 80%.
      *         </p>
      */
     public Float getFaceMatchThreshold() {
@@ -254,7 +258,8 @@ public Float getFaceMatchThreshold() {
     /**
      * <p>
      * The minimum confidence in the person match to return. For example, don't
-     * return any matches where confidence in matches is less than 70%.
+     * return any matches where confidence in matches is less than 70%. The
+     * default value is 80%.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -263,7 +268,7 @@ public Float getFaceMatchThreshold() {
      * @param faceMatchThreshold <p>
      *            The minimum confidence in the person match to return. For
      *            example, don't return any matches where confidence in matches
-     *            is less than 70%.
+     *            is less than 70%. The default value is 80%.
      *            </p>
      */
     public void setFaceMatchThreshold(Float faceMatchThreshold) {
@@ -273,7 +278,8 @@ public void setFaceMatchThreshold(Float faceMatchThreshold) {
     /**
      * <p>
      * The minimum confidence in the person match to return. For example, don't
-     * return any matches where confidence in matches is less than 70%.
+     * return any matches where confidence in matches is less than 70%. The
+     * default value is 80%.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -285,7 +291,7 @@ public void setFaceMatchThreshold(Float faceMatchThreshold) {
      * @param faceMatchThreshold <p>
      *            The minimum confidence in the person match to return. For
      *            example, don't return any matches where confidence in matches
-     *            is less than 70%.
+     *            is less than 70%. The default value is 80%.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
@@ -410,8 +416,10 @@ public StartFaceSearchRequest withNotificationChannel(NotificationChannel notifi
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -419,9 +427,11 @@ public StartFaceSearchRequest withNotificationChannel(NotificationChannel notifi
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @return <p>
-     *         Unique identifier you specify to identify the job in the
-     *         completion status published to the Amazon Simple Notification
-     *         Service topic.
+     *         An identifier you specify that's returned in the completion
+     *         notification that's published to your Amazon Simple Notification
+     *         Service topic. For example, you can use <code>JobTag</code> to
+     *         group related jobs and identify them in the completion
+     *         notification.
      *         </p>
      */
     public String getJobTag() {
@@ -430,8 +440,10 @@ public String getJobTag() {
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -439,9 +451,11 @@ public String getJobTag() {
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @param jobTag <p>
-     *            Unique identifier you specify to identify the job in the
-     *            completion status published to the Amazon Simple Notification
-     *            Service topic.
+     *            An identifier you specify that's returned in the completion
+     *            notification that's published to your Amazon Simple
+     *            Notification Service topic. For example, you can use
+     *            <code>JobTag</code> to group related jobs and identify them in
+     *            the completion notification.
      *            </p>
      */
     public void setJobTag(String jobTag) {
@@ -450,8 +464,10 @@ public void setJobTag(String jobTag) {
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -462,9 +478,11 @@ public void setJobTag(String jobTag) {
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @param jobTag <p>
-     *            Unique identifier you specify to identify the job in the
-     *            completion status published to the Amazon Simple Notification
-     *            Service topic.
+     *            An identifier you specify that's returned in the completion
+     *            notification that's published to your Amazon Simple
+     *            Notification Service topic. For example, you can use
+     *            <code>JobTag</code> to group related jobs and identify them in
+     *            the completion notification.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartLabelDetectionRequest.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartLabelDetectionRequest.java
index d0b3efab77..f5c1718c10 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartLabelDetectionRequest.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartLabelDetectionRequest.java
@@ -100,8 +100,10 @@
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -411,8 +413,10 @@ public StartLabelDetectionRequest withNotificationChannel(
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -420,9 +424,11 @@ public StartLabelDetectionRequest withNotificationChannel(
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @return <p>
-     *         Unique identifier you specify to identify the job in the
-     *         completion status published to the Amazon Simple Notification
-     *         Service topic.
+     *         An identifier you specify that's returned in the completion
+     *         notification that's published to your Amazon Simple Notification
+     *         Service topic. For example, you can use <code>JobTag</code> to
+     *         group related jobs and identify them in the completion
+     *         notification.
      *         </p>
      */
     public String getJobTag() {
@@ -431,8 +437,10 @@ public String getJobTag() {
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -440,9 +448,11 @@ public String getJobTag() {
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @param jobTag <p>
-     *            Unique identifier you specify to identify the job in the
-     *            completion status published to the Amazon Simple Notification
-     *            Service topic.
+     *            An identifier you specify that's returned in the completion
+     *            notification that's published to your Amazon Simple
+     *            Notification Service topic. For example, you can use
+     *            <code>JobTag</code> to group related jobs and identify them in
+     *            the completion notification.
      *            </p>
      */
     public void setJobTag(String jobTag) {
@@ -451,8 +461,10 @@ public void setJobTag(String jobTag) {
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -463,9 +475,11 @@ public void setJobTag(String jobTag) {
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @param jobTag <p>
-     *            Unique identifier you specify to identify the job in the
-     *            completion status published to the Amazon Simple Notification
-     *            Service topic.
+     *            An identifier you specify that's returned in the completion
+     *            notification that's published to your Amazon Simple
+     *            Notification Service topic. For example, you can use
+     *            <code>JobTag</code> to group related jobs and identify them in
+     *            the completion notification.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
diff --git a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartPersonTrackingRequest.java b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartPersonTrackingRequest.java
index beb11aa2a6..3fdfbc9802 100644
--- a/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartPersonTrackingRequest.java
+++ b/aws-android-sdk-rekognition/src/main/java/com/amazonaws/services/rekognition/model/StartPersonTrackingRequest.java
@@ -73,8 +73,10 @@
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -271,8 +273,10 @@ public StartPersonTrackingRequest withNotificationChannel(
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -280,9 +284,11 @@ public StartPersonTrackingRequest withNotificationChannel(
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @return <p>
-     *         Unique identifier you specify to identify the job in the
-     *         completion status published to the Amazon Simple Notification
-     *         Service topic.
+     *         An identifier you specify that's returned in the completion
+     *         notification that's published to your Amazon Simple Notification
+     *         Service topic. For example, you can use <code>JobTag</code> to
+     *         group related jobs and identify them in the completion
+     *         notification.
      *         </p>
      */
     public String getJobTag() {
@@ -291,8 +297,10 @@ public String getJobTag() {
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * <b>Constraints:</b><br/>
@@ -300,9 +308,11 @@ public String getJobTag() {
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @param jobTag <p>
-     *            Unique identifier you specify to identify the job in the
-     *            completion status published to the Amazon Simple Notification
-     *            Service topic.
+     *            An identifier you specify that's returned in the completion
+     *            notification that's published to your Amazon Simple
+     *            Notification Service topic. For example, you can use
+     *            <code>JobTag</code> to group related jobs and identify them in
+     *            the completion notification.
      *            </p>
      */
     public void setJobTag(String jobTag) {
@@ -311,8 +321,10 @@ public void setJobTag(String jobTag) {
 
     /**
      * <p>
-     * Unique identifier you specify to identify the job in the completion
-     * status published to the Amazon Simple Notification Service topic.
+     * An identifier you specify that's returned in the completion notification
+     * that's published to your Amazon Simple Notification Service topic. For
+     * example, you can use <code>JobTag</code> to group related jobs and
+     * identify them in the completion notification.
      * </p>
      * <p>
      * Returns a reference to this object so that method calls can be chained
@@ -323,9 +335,11 @@ public void setJobTag(String jobTag) {
      * <b>Pattern: </b>[a-zA-Z0-9_.\-:]+<br/>
      *
      * @param jobTag <p>
-     *            Unique identifier you specify to identify the job in the
-     *            completion status published to the Amazon Simple Notification
-     *            Service topic.
+     *            An identifier you specify that's returned in the completion
+     *            notification that's published to your Amazon Simple
+     *            Notification Service topic. For example, you can use
+     *            <code>JobTag</code> to group related jobs and identify them in
+     *            the completion notification.
      *            </p>
      * @return A reference to this updated object so that method calls can be
      *         chained together.
