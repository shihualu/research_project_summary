diff --git a/roboguice/src/main/java/roboguice/config/DefaultRoboModule.java b/roboguice/src/main/java/roboguice/config/DefaultRoboModule.java
index 716a7d475..8117a7c55 100644
--- a/roboguice/src/main/java/roboguice/config/DefaultRoboModule.java
+++ b/roboguice/src/main/java/roboguice/config/DefaultRoboModule.java
@@ -174,7 +174,6 @@ protected void configure() {
 
         requestStaticInjection(Ln.class);
 
-        System.out.println("hasSupport = "+FragmentUtil.hasSupport+ " hasNative = " +FragmentUtil.hasNative);
         // Compatibility library bindings
         if(FragmentUtil.hasSupport) {
             bind(FragmentUtil.supportFrag.fragmentManagerType()).toProvider(FragmentUtil.supportFrag.fragmentManagerProviderType());
@@ -183,7 +182,6 @@ protected void configure() {
         	bind(FragmentUtil.nativeFrag.fragmentManagerType()).toProvider(FragmentUtil.nativeFrag.fragmentManagerProviderType());
         }
 
-
         // 2.0 Eclair
         if( VERSION.SDK_INT>=5 ) {
             bind(accountManagerClass).toProvider(AccountManagerProvider.class);
diff --git a/roboguice/src/test/java/roboguice/activity/ActivityInjectionTest.java b/roboguice/src/test/java/roboguice/activity/ActivityInjectionTest.java
index e425320e6..7dea2bc92 100644
--- a/roboguice/src/test/java/roboguice/activity/ActivityInjectionTest.java
+++ b/roboguice/src/test/java/roboguice/activity/ActivityInjectionTest.java
@@ -264,25 +264,10 @@ public void configure() {
 
         public static class D extends RoboActivity{
             @InjectExtra("xxx") static String s;
-
-            @Override
-            protected void onCreate(Bundle savedInstanceState) {
-                super.onCreate(savedInstanceState);
-            }
         }
     }
 
-    public static class F extends RoboActivity {
-        @Override
-        protected void onCreate(Bundle savedInstanceState) {
-            super.onCreate(savedInstanceState);
-        }
-
-        @Override
-        protected void onDestroy() {
-            super.onDestroy();
-        }
-    }
+    public static class F extends RoboActivity {}
 
     public static class PojoA {
         @InjectView(100) View v;
@@ -291,10 +276,5 @@ protected void onDestroy() {
 
     public static class G extends RoboActivity {
         @Inject Application application;
-
-        @Override
-        protected void onCreate(Bundle savedInstanceState) {
-            super.onCreate(savedInstanceState);
-        }
     }
 }
diff --git a/roboguice/src/test/java/roboguice/fragment/FragmentInjectionTest.java b/roboguice/src/test/java/roboguice/fragment/FragmentInjectionTest.java
index 95a918b14..c0654ad6b 100644
--- a/roboguice/src/test/java/roboguice/fragment/FragmentInjectionTest.java
+++ b/roboguice/src/test/java/roboguice/fragment/FragmentInjectionTest.java
@@ -111,12 +111,6 @@ public View onCreateView(LayoutInflater inflater, ViewGroup container, Bundle sa
                 ref.setId(101);
                 return ref;
             }
-
-            @Override
-            public void onCreate(Bundle savedInstanceState) {
-                super.onCreate(savedInstanceState);
-
-            }
         }
 
     }
@@ -153,12 +147,6 @@ public View onCreateView(LayoutInflater inflater, ViewGroup container, Bundle sa
                 viewRef.setId(101);
                 return viewRef;
             }
-
-            @Override
-            public void onCreate(Bundle savedInstanceState) {
-                super.onCreate(savedInstanceState);
-
-            }
         }
 
     }
@@ -223,16 +211,6 @@ protected void onResume() {
             super.onResume();
         }
 
-        @Override
-        protected void onStop() {
-            super.onStop();
-        }
-
-        @Override
-        protected void onDestroy() {
-            super.onDestroy();
-        }
-
         public static class FragmentD extends RoboFragment {
             @InjectView(101) View v;
 
@@ -248,7 +226,6 @@ public View onCreateView(LayoutInflater inflater, ViewGroup container, Bundle sa
             @Override
             public void onCreate(Bundle savedInstanceState) {
                 super.onCreate(savedInstanceState);
-
             }
         }
 
