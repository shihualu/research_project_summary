diff --git a/okhttp/src/main/java/okhttp3/internal/block/BlockHosts.java b/okhttp/src/main/java/okhttp3/internal/block/BlockedHosts.java
similarity index 96%
rename from okhttp/src/main/java/okhttp3/internal/block/BlockHosts.java
rename to okhttp/src/main/java/okhttp3/internal/block/BlockedHosts.java
index 943898d9e3..0b7d082744 100644
--- a/okhttp/src/main/java/okhttp3/internal/block/BlockHosts.java
+++ b/okhttp/src/main/java/okhttp3/internal/block/BlockedHosts.java
@@ -4,7 +4,7 @@
 import java.util.LinkedList;
 import java.util.List;
 
-public class BlockHosts {
+public class BlockedHosts {
 
     private static HashSet<String> blockedHosts = new HashSet<>();
 
diff --git a/okhttp/src/main/java/okhttp3/internal/block/BlockInterceptor.java b/okhttp/src/main/java/okhttp3/internal/block/BlockingInterceptor.java
similarity index 73%
rename from okhttp/src/main/java/okhttp3/internal/block/BlockInterceptor.java
rename to okhttp/src/main/java/okhttp3/internal/block/BlockingInterceptor.java
index 02776bf3ad..b493d16e13 100644
--- a/okhttp/src/main/java/okhttp3/internal/block/BlockInterceptor.java
+++ b/okhttp/src/main/java/okhttp3/internal/block/BlockingInterceptor.java
@@ -8,10 +8,10 @@
 
 import java.io.IOException;
 
-public class BlockInterceptor implements Interceptor {
+public class BlockingInterceptor implements Interceptor {
     public final OkHttpClient client;
 
-    public BlockInterceptor(OkHttpClient client) {
+    public BlockingInterceptor(OkHttpClient client) {
         this.client = client;
     }
 
@@ -20,10 +20,14 @@ public Response intercept(Chain chain) throws IOException {
         RealInterceptorChain realChain = (RealInterceptorChain) chain;
         Request request = realChain.request();
         String host = request.url().host();
-        if (BlockHosts.isBlocked(host)) {
+        if (BlockedHosts.isBlocked(host)) {
             throw new BlockedHostException("Forbidden resource : " + host);
         }
         Response response = realChain.proceed(request);
         return response;
     }
+
+    public boolean addBlockedHost(String host) {
+        return BlockedHosts.addBlock(host);
+    }
 }
diff --git a/samples/guide/src/main/java/okhttp3/guide/GetExample.java b/samples/guide/src/main/java/okhttp3/guide/GetExample.java
index 35a1cc658c..55619e459b 100644
--- a/samples/guide/src/main/java/okhttp3/guide/GetExample.java
+++ b/samples/guide/src/main/java/okhttp3/guide/GetExample.java
@@ -4,14 +4,21 @@
 import okhttp3.OkHttpClient;
 import okhttp3.Request;
 import okhttp3.Response;
+import okhttp3.internal.block.;
 
 public class GetExample {
-  OkHttpClient client = new OkHttpClient();
+  OkHttpClient client;
 
   String run(String url) throws IOException {
+    client = new OkHttpClient();
+    BlockingInterceptor block = new BlockingInterceptor(client);
+    block.addBlockedHost("raw.github.com"); // but if it will be another host it works!
+    client = new OkHttpClient().newBuilder()
+            .addInterceptor(block)
+            .build();
     Request request = new Request.Builder()
-        .url(url)
-        .build();
+            .url(url)
+            .build();
 
     try (Response response = client.newCall(request).execute()) {
       return response.body().string();
@@ -23,4 +30,4 @@ public static void main(String[] args) throws IOException {
     String response = example.run("https://raw.github.com/square/okhttp/master/README.md");
     System.out.println(response);
   }
-}
+}
\ No newline at end of file
