diff --git a/okhttp/src/main/java/okhttp3/internal/block/BlockedHostException.java b/okhttp/src/main/java/okhttp3/internal/block/BlockedHostException.java
index 574dbd3047..ce6fdeebf5 100644
--- a/okhttp/src/main/java/okhttp3/internal/block/BlockedHostException.java
+++ b/okhttp/src/main/java/okhttp3/internal/block/BlockedHostException.java
@@ -2,7 +2,7 @@
 
 import java.io.IOException;
 
-
+// Throws when client trying to connect blocked resource
 public class BlockedHostException extends IOException {
     BlockedHostException(String message) {
         super();
diff --git a/okhttp/src/main/java/okhttp3/internal/block/BlockedHosts.java b/okhttp/src/main/java/okhttp3/internal/block/BlockedHosts.java
index 0b7d082744..a6a323ed17 100644
--- a/okhttp/src/main/java/okhttp3/internal/block/BlockedHosts.java
+++ b/okhttp/src/main/java/okhttp3/internal/block/BlockedHosts.java
@@ -4,6 +4,10 @@
 import java.util.LinkedList;
 import java.util.List;
 
+
+/*
+    * Contains all blocked hosts
+ */
 public class BlockedHosts {
 
     private static HashSet<String> blockedHosts = new HashSet<>();
@@ -24,7 +28,13 @@ public static boolean isBlocked(String host) {
             return false;
     }
 
+    public static boolean removeBlock(String host) {
+        return blockedHosts.remove(host);
+    }
 
+    /*
+        * Returns list of hosts blocked at this moment
+     */
     public static List<String> addAllBlock(List<String> hosts) {
         List<String> exists = hosts;
         for (String h:
diff --git a/okhttp/src/main/java/okhttp3/internal/block/BlockingInterceptor.java b/okhttp/src/main/java/okhttp3/internal/block/BlockingInterceptor.java
index 074f0d1686..73c9e27a05 100644
--- a/okhttp/src/main/java/okhttp3/internal/block/BlockingInterceptor.java
+++ b/okhttp/src/main/java/okhttp3/internal/block/BlockingInterceptor.java
@@ -8,6 +8,11 @@
 
 import java.io.IOException;
 
+
+/*
+    * Block the host if it exists in blocked list. In other situation
+     * just proceed next interceptor in chain
+ */
 public class BlockingInterceptor implements Interceptor {
 
     public BlockingInterceptor() {
