diff --git a/okhttp/src/main/java/okhttp3/RealCall.java b/okhttp/src/main/java/okhttp3/RealCall.java
index 168d2841b6..88d0aa52aa 100644
--- a/okhttp/src/main/java/okhttp3/RealCall.java
+++ b/okhttp/src/main/java/okhttp3/RealCall.java
@@ -19,7 +19,6 @@
 import java.util.ArrayList;
 import java.util.List;
 import okhttp3.internal.NamedRunnable;
-import okhttp3.internal.block.BlockingInterceptor;
 import okhttp3.internal.connection.ConnectInterceptor;
 import okhttp3.internal.connection.StreamAllocation;
 import okhttp3.internal.http.*;
diff --git a/okhttp/src/main/java/okhttp3/internal/block/BlockedHosts.java b/okhttp/src/main/java/okhttp3/internal/block/BlockHosts.java
similarity index 96%
rename from okhttp/src/main/java/okhttp3/internal/block/BlockedHosts.java
rename to okhttp/src/main/java/okhttp3/internal/block/BlockHosts.java
index 0b7d082744..943898d9e3 100644
--- a/okhttp/src/main/java/okhttp3/internal/block/BlockedHosts.java
+++ b/okhttp/src/main/java/okhttp3/internal/block/BlockHosts.java
@@ -4,7 +4,7 @@
 import java.util.LinkedList;
 import java.util.List;
 
-public class BlockedHosts {
+public class BlockHosts {
 
     private static HashSet<String> blockedHosts = new HashSet<>();
 
diff --git a/okhttp/src/main/java/okhttp3/internal/block/BlockingInterceptor.java b/okhttp/src/main/java/okhttp3/internal/block/BlockInterceptor.java
similarity index 73%
rename from okhttp/src/main/java/okhttp3/internal/block/BlockingInterceptor.java
rename to okhttp/src/main/java/okhttp3/internal/block/BlockInterceptor.java
index 3e53f70eae..02776bf3ad 100644
--- a/okhttp/src/main/java/okhttp3/internal/block/BlockingInterceptor.java
+++ b/okhttp/src/main/java/okhttp3/internal/block/BlockInterceptor.java
@@ -8,26 +8,19 @@
 
 import java.io.IOException;
 
-public class BlockingInterceptor implements Interceptor {
+public class BlockInterceptor implements Interceptor {
     public final OkHttpClient client;
 
-
-
-    public BlockingInterceptor(OkHttpClient client) {
+    public BlockInterceptor(OkHttpClient client) {
         this.client = client;
     }
 
-    public boolean addBlockedHost(String host) {
-        return BlockedHosts.addBlock(host);
-    }
-
-
     @Override
     public Response intercept(Chain chain) throws IOException {
         RealInterceptorChain realChain = (RealInterceptorChain) chain;
         Request request = realChain.request();
         String host = request.url().host();
-        if (BlockedHosts.isBlocked(host)) {
+        if (BlockHosts.isBlocked(host)) {
             throw new BlockedHostException("Forbidden resource : " + host);
         }
         Response response = realChain.proceed(request);
diff --git a/samples/guide/src/main/java/okhttp3/guide/GetExample.java b/samples/guide/src/main/java/okhttp3/guide/GetExample.java
index c7ac6b5988..35a1cc658c 100644
--- a/samples/guide/src/main/java/okhttp3/guide/GetExample.java
+++ b/samples/guide/src/main/java/okhttp3/guide/GetExample.java
@@ -4,18 +4,11 @@
 import okhttp3.OkHttpClient;
 import okhttp3.Request;
 import okhttp3.Response;
-import okhttp3.internal.block.BlockingInterceptor;
 
 public class GetExample {
-  OkHttpClient client;
+  OkHttpClient client = new OkHttpClient();
 
   String run(String url) throws IOException {
-    client = new OkHttpClient();
-    BlockingInterceptor block = new BlockingInterceptor(client);
-    block.addBlockedHost("raw.github.com"); // but if it will be another host it works!
-    client = new OkHttpClient().newBuilder()
-            .addInterceptor(block)
-            .build();
     Request request = new Request.Builder()
         .url(url)
         .build();
