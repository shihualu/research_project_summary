diff --git a/okhttp/src/main/java/okhttp3/ConnectionPool.java b/okhttp/src/main/java/okhttp3/ConnectionPool.java
index 605f275661..b5a7d647e1 100644
--- a/okhttp/src/main/java/okhttp3/ConnectionPool.java
+++ b/okhttp/src/main/java/okhttp3/ConnectionPool.java
@@ -115,6 +115,11 @@ public synchronized int connectionCount() {
     return connections.size();
   }
 
+  /** Returns connections to get sub RealConnection info. */
+  public final synchronized Deque<RealConnection> connections() {
+    return new ArrayDeque<RealConnection>(connections);
+  }
+
   /**
    * Returns a recycled connection to {@code address}, or null if no such connection exists. The
    * route is null if the address has not yet been routed.
