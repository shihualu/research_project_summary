diff --git a/okhttp/src/main/java/okhttp3/Cookie.java b/okhttp/src/main/java/okhttp3/Cookie.java
index 74e341de4d..585c4f1888 100644
--- a/okhttp/src/main/java/okhttp3/Cookie.java
+++ b/okhttp/src/main/java/okhttp3/Cookie.java
@@ -417,9 +417,10 @@ private static String parseDomain(String s) {
     if (s.endsWith(".")) {
       throw new IllegalArgumentException();
     }
-    if (s.startsWith(".")) {
-      s = s.substring(1);
-    }
+    // why delete this first dot. it will cause CookieHandler can't find a cookie from domain
+    //if (s.startsWith(".")) {
+    //  s = s.substring(1);
+    //}
     String canonicalDomain = domainToAscii(s);
     if (canonicalDomain == null) {
       throw new IllegalArgumentException();
