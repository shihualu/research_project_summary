diff --git a/extras/src/com/mopub/mobileads/GoogleAdMobBanner.java b/extras/src/com/mopub/mobileads/GoogleAdMobBanner.java
index 3c1e6539..c0db86d7 100644
--- a/extras/src/com/mopub/mobileads/GoogleAdMobBanner.java
+++ b/extras/src/com/mopub/mobileads/GoogleAdMobBanner.java
@@ -53,11 +53,12 @@
  * Compatible with version 6.4.1 of the Google AdMob Ads SDK.
  */
 
-class GoogleAdMobBanner extends CustomEventBanner implements AdListener {
+public class GoogleAdMobBanner extends CustomEventBanner implements AdListener {
     public static final String AD_UNIT_ID_KEY = "adUnitID";
     public static final String AD_WIDTH_KEY = "adWidth";
     public static final String AD_HEIGHT_KEY = "adHeight";
     public static final String LOCATION_KEY = "location";
+    public static final String KEYWORDS_KEY = "adKeywords";
 
     private AdView mAdMobView;
     private CustomEventBannerListener mBannerListener;
@@ -98,9 +99,13 @@ protected void loadBanner(Context context,
         mAdMobView.setAdListener(this);
 
         AdRequest request = new AdRequest();
+
         Location location = extractLocation(localExtras);
         if (location != null) request.setLocation(location);
 
+        Set<String> keywords = extractKeywords(serverExtras, localExtras);
+        if (keywords != null && keywords.size() > 0) request.setKeywords(keywords);
+
         mAdMobView.loadAd(request);
     }
 
@@ -118,6 +123,25 @@ private Location extractLocation(Map<String, Object> localExtras) {
         return null;
     }
 
+    private Set<String> extractKeywords(Map<String, String> serverExtras, Map<String, Object> localExtras) {
+        // Try to get keywords from serverExtras
+        String serverKeywords = serverExtras.get(KEYWORDS_KEY);
+        if (serverKeywords != null) {
+            Set<String> keywordSet = new HashSet<String>();
+            keywordSet.addAll(Arrays.<String>asList(TextUtils.split(serverKeywords, ",")));
+            return keywordSet;
+        }
+        // Try to get keywords from localExtras as serverExtras didn't have any
+        Object localKeywords = localExtras.get(KEYWORDS_KEY);
+        if (localKeywords instanceof String[]) {
+            Set<String> keywordSet = new HashSet<String>();
+            keywordSet.addAll(Arrays.<String>asList((String[]) localKeywords));
+            return keywordSet;
+        }
+        // No keywords found so return null
+        return null;
+    }
+
     private AdSize calculateAdSize(int width, int height) {
         // Use the smallest AdMob AdSize that will properly contain the adView
         if (width <= BANNER.getWidth() && height <= BANNER.getHeight()) {
diff --git a/extras/src/com/mopub/mobileads/GoogleAdMobInterstitial.java b/extras/src/com/mopub/mobileads/GoogleAdMobInterstitial.java
index ba691329..7d4a672b 100644
--- a/extras/src/com/mopub/mobileads/GoogleAdMobInterstitial.java
+++ b/extras/src/com/mopub/mobileads/GoogleAdMobInterstitial.java
@@ -51,9 +51,10 @@
  * Compatible with version 6.4.1 of the Google AdMob Ads SDK.
  */
 
-class GoogleAdMobInterstitial extends CustomEventInterstitial implements AdListener {
+public class GoogleAdMobInterstitial extends CustomEventInterstitial implements AdListener {
     public static final String AD_UNIT_ID_KEY = "adUnitID";
     public static final String LOCATION_KEY = "location";
+    public static final String KEYWORDS_KEY = "adKeywords";
 
     private InterstitialAd mInterstitialAd;
     private boolean mHasAlreadyRegisteredClick;
@@ -82,10 +83,15 @@ protected void loadInterstitial(Context context,
         mInterstitialAd = new InterstitialAd((Activity) context, pubId);
         mInterstitialAd.setAdListener(this);
 
-        AdRequest adRequest = new AdRequest();
+        AdRequest request = new AdRequest();
+
         Location location = extractLocation(localExtras);
-        if (location != null) adRequest.setLocation(location);
-        mInterstitialAd.loadAd(adRequest);
+        if (location != null) request.setLocation(location);
+
+        Set<String> keywords = extractKeywords(serverExtras, localExtras);
+        if (keywords != null && keywords.size() > 0) request.setKeywords(keywords);
+
+        mInterstitialAd.loadAd(request);
     }
 
     @Override
@@ -112,6 +118,22 @@ private Location extractLocation(Map<String, Object> localExtras) {
         return null;
     }
 
+    private Set<String> extractKeywords(Map<String, String> serverExtras, Map<String, Object> localExtras) {
+        String serverKeywords = serverExtras.get(KEYWORDS_KEY);
+        if (serverKeywords != null) {
+            Set<String> keywordSet = new HashSet<String>();
+            keywordSet.addAll(Arrays.<String>asList(TextUtils.split(serverKeywords, ",")));
+            return keywordSet;
+        }
+        Object localKeywords = localExtras.get(KEYWORDS_KEY);
+        if (localKeywords instanceof String[]) {
+            Set<String> keywordSet = new HashSet<String>();
+            keywordSet.addAll(Arrays.<String>asList((String[]) localKeywords));
+            return keywordSet;
+        }
+        return null;
+    }
+
     private boolean extrasAreValid(Map<String, String> serverExtras) {
         return serverExtras.containsKey(AD_UNIT_ID_KEY);
     }
