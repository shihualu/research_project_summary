diff --git a/mopub-sdk/src/main/java/com/mopub/ads/Interstitial.java b/mopub-sdk/src/main/java/com/mopub/ads/Interstitial.java
index 94259c89..1411c855 100644
--- a/mopub-sdk/src/main/java/com/mopub/ads/Interstitial.java
+++ b/mopub-sdk/src/main/java/com/mopub/ads/Interstitial.java
@@ -212,8 +212,8 @@ public void showFastDelayed(int mills) {
         mainHandler.postDelayed(new Runnable() {
             @Override
             public void run() {
-                if(mopubInterstitial != null){
-                    show();
+                if(mopubInterstitial != null && fastAd.showed){
+                    mopubInterstitial.show();
                 }else if (lock.isLocked() || fastAd == null || !fastAd.show()) {
                     _initDelayed();
                 }
@@ -368,7 +368,14 @@ private void loadAfterDelay(long delay) {
         private boolean game;
 
         public boolean isLocked() {
-            Helper.wtf("I","isLocked: " +
+            Helper.wtf("isLocked: " +
+                    "multiplayer [" + multiplayer + "]" + " " +
+                    "internet [" + internet + "]" + " " +
+                    "gap [" + gap + "]" + " " +
+                    "stop [" + stop + "] "+
+                    "game [" + game + "]");
+
+            Log.e(TAG,"isLocked: " +
                     "multiplayer [" + multiplayer + "]" + " " +
                     "internet [" + internet + "]" + " " +
                     "gap [" + gap + "]" + " " +
