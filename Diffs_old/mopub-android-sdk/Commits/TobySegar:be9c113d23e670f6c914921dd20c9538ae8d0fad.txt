diff --git a/mopub-sdk/mopub-sdk-base/src/main/java/com/mopub/common/util/DeviceUtils.java b/mopub-sdk/mopub-sdk-base/src/main/java/com/mopub/common/util/DeviceUtils.java
index 72cd3900..ed4142da 100644
--- a/mopub-sdk/mopub-sdk-base/src/main/java/com/mopub/common/util/DeviceUtils.java
+++ b/mopub-sdk/mopub-sdk-base/src/main/java/com/mopub/common/util/DeviceUtils.java
@@ -15,6 +15,7 @@
 import android.os.StatFs;
 import android.support.annotation.NonNull;
 import android.support.annotation.Nullable;
+import android.support.v4.content.ContextCompat;
 import android.util.DisplayMetrics;
 import android.view.Display;
 import android.view.Surface;
@@ -253,10 +254,8 @@ public static boolean isPermissionGranted(@NonNull final Context context,
         Preconditions.checkNotNull(context);
         Preconditions.checkNotNull(permission);
 
-        return true;
-        //api 23
-        //return ContextCompat.checkSelfPermission(context, permission) ==
-        //        PackageManager.PERMISSION_GRANTED;
+        return ContextCompat.checkSelfPermission(context, permission) ==
+                PackageManager.PERMISSION_GRANTED;
     }
 
     /**
diff --git a/mopub-sdk/proguard.txt b/mopub-sdk/proguard.txt
index 53ea4672..45db3593 100644
--- a/mopub-sdk/proguard.txt
+++ b/mopub-sdk/proguard.txt
@@ -2,24 +2,9 @@
 # NOTE: You should also include the Android Proguard config found with the build tools:
 # $ANDROID_HOME/tools/proguard/proguard-android.txt
 
-# Mopub-Keep public classes and methods.
--keepclassmembers class com.mopub.common.** { public *; }
--keepclassmembers class com.mopub.exceptions.** { public *; }
--keepclassmembers class com.mopub.mobileads.** { public *; }
--keepclassmembers class com.mopub.mraid.** { public *; }
--keepclassmembers class com.mopub.nativeads.** { public *; }
--keepclassmembers class com.mopub.network.** { public *; }
--keepclassmembers class com.mopub.volley.** { public *; }
--keep public class com.mopub.common.**
--keep public class com.mopub.exceptions.**
--keep public class com.mopub.mobileads.**
--keep public class com.mopub.mraid.**
--keep public class com.mopub.nativeads.**
--keep public class com.mopub.network.**
--keep public class com.mopub.volley.**
-#-keep public class com.mopub.ads.Interstitial {
-#  public static boolean showFromNative();
-#}
+# Keep public classes and methods.
+-keepclassmembers class com.mopub.** { public *; }
+-keep public class com.mopub.**
 
 # Mopub-Explicitly keep any custom event classes in any package.
 -keep class * extends com.mopub.mobileads.CustomEventBanner {}
