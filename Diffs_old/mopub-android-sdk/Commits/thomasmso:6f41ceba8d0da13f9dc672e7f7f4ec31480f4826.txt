diff --git a/extras/src/com/mopub/mobileads/AppLovinBanner.java b/extras/src/com/mopub/mobileads/AppLovinBanner.java
index ca2d971b..90c05f7f 100644
--- a/extras/src/com/mopub/mobileads/AppLovinBanner.java
+++ b/extras/src/com/mopub/mobileads/AppLovinBanner.java
@@ -102,8 +102,7 @@ public void adClicked(final AppLovinAd ad)
                 {
                     log( DEBUG, "Banner clicked" );
 
-                    customEventBannerListener.onBannerClicked();
-                    customEventBannerListener.onLeaveApplication();
+                    customEventBannerListener.onBannerClicked(); // No need to call onLeaveApplication() since it's an alias for onBannerClicked()
                 }
             } );
 
