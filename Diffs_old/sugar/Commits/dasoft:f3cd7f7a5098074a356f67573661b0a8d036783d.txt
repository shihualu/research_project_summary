diff --git a/library/src/com/orm/SugarDb.java b/library/src/com/orm/SugarDb.java
index ceab2243..522ad64d 100644
--- a/library/src/com/orm/SugarDb.java
+++ b/library/src/com/orm/SugarDb.java
@@ -28,6 +28,7 @@
 import com.orm.dsl.Column;
 import com.orm.dsl.NotNull;
 import com.orm.dsl.Unique;
+import com.orm.dsl.References;
 
 import dalvik.system.DexFile;
 
@@ -207,6 +208,14 @@ public void onCreate(SQLiteDatabase sqLiteDatabase) {
                         sb.append(" UNIQUE");
                     }
                 }
+                
+                if (column.isAnnotationPresent(References.class)) {
+                    References columnAnnotation = column.getAnnotation(References.class);
+                    sb.append(" REFERENCES ").append(columnAnnotation.foreignTable())
+                            .append(" (").append(columnAnnotation.foreignColumn()).append(") ")
+                            .append(" ON DELETE ").append(columnAnnotation.onDelete())
+                            .append(" ON UPDATE ").append(columnAnnotation.onUpdate());
+                }
             }
         }
         sb.append(" ) ");
diff --git a/library/src/com/orm/dsl/References.java b/library/src/com/orm/dsl/References.java
new file mode 100644
index 00000000..ba5fba3b
--- /dev/null
+++ b/library/src/com/orm/dsl/References.java
@@ -0,0 +1,13 @@
+package com.orm.dsl;
+
+import java.lang.annotation.Retention;
+import java.lang.annotation.RetentionPolicy;
+
+@Retention(RetentionPolicy.RUNTIME)
+public @interface References
+{
+    String foreignTable();
+    String foreignColumn() default "ID";
+    String onDelete() default "NO ACTION";
+    String onUpdate() default "NO ACTION";
+}
