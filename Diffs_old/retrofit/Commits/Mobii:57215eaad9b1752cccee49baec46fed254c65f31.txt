diff --git a/retrofit/src/main/java/retrofit/RestAdapter.java b/retrofit/src/main/java/retrofit/RestAdapter.java
index 19e05298d..16770f0cd 100644
--- a/retrofit/src/main/java/retrofit/RestAdapter.java
+++ b/retrofit/src/main/java/retrofit/RestAdapter.java
@@ -26,7 +26,9 @@
 import java.util.Map;
 import java.util.concurrent.Executor;
 import java.util.concurrent.TimeUnit;
+
 import retrofit.Profiler.RequestInformation;
+import retrofit.RestMethodInfo.ParamUsage;
 import retrofit.client.Client;
 import retrofit.client.Header;
 import retrofit.client.Request;
@@ -359,6 +361,14 @@ private Object invokeRequest(RequestInterceptor requestInterceptor, RestMethodIn
 
           ExceptionCatchingTypedInput wrapped = new ExceptionCatchingTypedInput(body);
           try {
+        	// Try to find type from PathFromClass annotation
+	    	for(int i = 0; i < methodInfo.requestParamUsage.length; i++){
+	    	  if(methodInfo.requestParamUsage[i].equals(ParamUsage.PATH_FROM_CLASS)){
+	    	    type = (Type) args[0];
+	    	    break;
+	    	  }
+	    	}
+        	  
             Object convert = converter.fromBody(wrapped, type);
             if (methodInfo.isSynchronous) {
               return convert;
