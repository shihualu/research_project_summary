diff --git a/README.md b/README.md
index 69c8b6264..533308473 100644
--- a/README.md
+++ b/README.md
@@ -10,16 +10,26 @@ Download
 --------
 
 Download [the latest JAR][2] or grab via Maven:
+1. Add the JitPack repository to your build file
 ```xml
-<dependency>
-  <groupId>com.squareup.retrofit2</groupId>
-  <artifactId>retrofit</artifactId>
-  <version>2.2.0</version>
-</dependency>
+	<repositories>
+		<repository>
+		    <id>jitpack.io</id>
+		    <url>https://jitpack.io</url>
+		</repository>
+	</repositories>
+```
+2. Add the dependency
+```xml
+        <dependency>
+	    <groupId>com.github.ThorChiam.retrofit</groupId>
+	    <artifactId>adapter-guava</artifactId>
+	    <version>0.6.2-rc1</version>
+        </dependency>
 ```
 or Gradle:
-
-```java
+1. Add it in your root build.gradle at the end of repositories
+```groovy
 allprojects {
 		repositories {
 			...
@@ -27,9 +37,11 @@ allprojects {
 		}
 	}
 ```
-
+2. Add the dependency
 ```groovy
-compile 'com.github.ThorChiam.retrofit:adapter-guava:0.6.2-rc1'
+dependencies {
+        compile 'com.github.ThorChiam.retrofit:adapter-guava:0.6.2-rc1'
+	}
 ```
 
 Snapshots of the development version are available in [Sonatype's `snapshots` repository][snap].
