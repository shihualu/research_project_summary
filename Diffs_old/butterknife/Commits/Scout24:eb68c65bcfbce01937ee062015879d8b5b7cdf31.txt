diff --git a/gradle.properties b/gradle.properties
index a4db5d39..94368128 100644
--- a/gradle.properties
+++ b/gradle.properties
@@ -1,5 +1,5 @@
 GROUP=com.jakewharton
-VERSION_NAME=9.0.0-SNAPSHOT
+VERSION_NAME=1.0.0-autoscout
 
 POM_DESCRIPTION=Field and method binding for Android views.
 
diff --git a/gradle/gradle-mvn-push.gradle b/gradle/gradle-mvn-push.gradle
index ea3f6bbb..93e5f99d 100644
--- a/gradle/gradle-mvn-push.gradle
+++ b/gradle/gradle-mvn-push.gradle
@@ -25,8 +25,7 @@ def isReleaseBuild() {
 }
 
 def getReleaseRepositoryUrl() {
-  return hasProperty('RELEASE_REPOSITORY_URL') ? RELEASE_REPOSITORY_URL
-      : "https://oss.sonatype.org/service/local/staging/deploy/maven2/"
+  return hasProperty('NEXUS_URL') ? NEXUS_URL : ""
 }
 
 def getSnapshotRepositoryUrl() {
@@ -35,11 +34,11 @@ def getSnapshotRepositoryUrl() {
 }
 
 def getRepositoryUsername() {
-  return hasProperty('SONATYPE_NEXUS_USERNAME') ? SONATYPE_NEXUS_USERNAME : ""
+  return hasProperty('NEXUS_USER') ? NEXUS_USER :""
 }
 
 def getRepositoryPassword() {
-  return hasProperty('SONATYPE_NEXUS_PASSWORD') ? SONATYPE_NEXUS_PASSWORD : ""
+  return hasProperty('NEXUS_PASS') ? NEXUS_PASS :""
 }
 
 afterEvaluate { project ->
@@ -91,7 +90,7 @@ afterEvaluate { project ->
   }
 
   signing {
-    required { isReleaseBuild() && gradle.taskGraph.hasTask("uploadArchives") }
+    required false
     sign configurations.archives
   }
 
