diff --git a/lib/src/main/java/org/altbeacon/beacon/service/StatUtils b/lib/src/main/java/org/altbeacon/beacon/service/StatUtils
new file mode 100644
index 00000000..1d235c66
--- /dev/null
+++ b/lib/src/main/java/org/altbeacon/beacon/service/StatUtils
@@ -0,0 +1,27 @@
+package org.altbeacon.beacon.service;
+
+import java.util.ArrayList;
+
+public class StatUtils {
+
+    static Double variance (ArrayList<Double> values){
+        Double sum = 0.0;
+        Double mean = mean(values);
+        for(double num : values){
+            sum += Math.pow(num - mean , 2);
+        }
+        return sum/(values.size()-1);
+    }
+
+    static Double mean (ArrayList<Double> values){
+        return sum(values)/values.size();
+    }
+
+    private static Double sum (ArrayList<Double> values){
+        Double sum = 0.0;
+        for (Double num : values){
+            sum+=num;
+        }
+        return sum;
+    }
+}
