diff --git a/DaoCore/build.gradle b/DaoCore/build.gradle
index 184b20b2..38db9926 100644
--- a/DaoCore/build.gradle
+++ b/DaoCore/build.gradle
@@ -1,6 +1,5 @@
 apply plugin: 'java'
 apply plugin: 'maven'
-apply plugin: 'signing'
 
 group = 'de.greenrobot'
 version = '1.3.7-java'
@@ -81,24 +80,15 @@ artifacts {
     archives sourcesJar
 }
 
-signing {
-    if(project.hasProperty('signing.keyId') && project.hasProperty('signing.password') &&
-    project.hasProperty('signing.secretKeyRingFile')) {
-        sign configurations.archives
-    } else {
-        println "Signing information missing/incomplete for ${project.name}"
-    }
-}
-
 uploadArchives {
     repositories {
         mavenDeployer {
             repository(url: preferedRepo, id: preferedRepoId) {
               authentication(userName: username, password: password)
             }
-            pom.groupId = '${group}'
-            pom.artifactId = 'greendao'
-            pom.version = '${version}'
+            pom.groupId = "${group}"
+            pom.artifactId = "${rootProject.name}"
+            pom.version = "${version}"
             pom.project {
                name 'greenDAO'
                packaging 'jar'
diff --git a/DaoGenerator/build.gradle b/DaoGenerator/build.gradle
index 7fd051bf..53761e21 100644
--- a/DaoGenerator/build.gradle
+++ b/DaoGenerator/build.gradle
@@ -1,6 +1,5 @@
 apply plugin: 'java'
 apply plugin: 'maven'
-apply plugin: 'signing'
 
 group = 'de.greenrobot'
 version = '1.3.7-SNAPSHOT'
@@ -93,24 +92,15 @@ artifacts {
     archives sourcesJar
 }
 
-signing {
-    if(project.hasProperty('signing.keyId') && project.hasProperty('signing.password') &&
-    project.hasProperty('signing.secretKeyRingFile')) {
-        sign configurations.archives
-    } else {
-        println "Signing information missing/incomplete for ${project.name}"
-    }
-}
-
 uploadArchives {
     repositories {
         mavenDeployer {
             repository(url: preferedRepo, id: preferedRepoId) {
               authentication(userName: username, password: password)
             }
-            pom.groupId = '${group}'
-            pom.artifactId = 'greendao-generator'
-            pom.version = '${version}'
+            pom.groupId = "${group}"
+            pom.artifactId = "${rootProject.name}"
+            pom.version = "${version}"
             
             pom.project {
                name 'greenDAO Generator'
