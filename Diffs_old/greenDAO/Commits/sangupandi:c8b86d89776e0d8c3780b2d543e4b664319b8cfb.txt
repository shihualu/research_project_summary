diff --git a/DaoCore/src/de/greenrobot/dao/internal/DaoConfig.java b/DaoCore/src/de/greenrobot/dao/internal/DaoConfig.java
index 96473029..1c70f1aa 100644
--- a/DaoCore/src/de/greenrobot/dao/internal/DaoConfig.java
+++ b/DaoCore/src/de/greenrobot/dao/internal/DaoConfig.java
@@ -97,7 +97,13 @@ public DaoConfig(SQLiteDatabase db, Class<? extends AbstractDao<?, ?>> daoClass)
 
     private static Property[] reflectProperties(Class<? extends AbstractDao<?, ?>> daoClass)
             throws ClassNotFoundException, IllegalArgumentException, IllegalAccessException {
-        Class<?> propertiesClass = Class.forName(daoClass.getName() + "$Properties");
+        Class<?> propertiesClass;
+        try {
+          propertiesClass = Class.forName(daoClass.getName() + "$Properties");
+        } catch(ClassNotFoundException ignored) {
+          // This is for user-specified DAO class.
+          propertiesClass = Class.forName(daoClass.getSuperclass().getName() + "$Properties");
+        }
         Field[] fields = propertiesClass.getDeclaredFields();
 
         ArrayList<Property> propertyList = new ArrayList<Property>();
diff --git a/DaoGenerator/src-template/dao-user.ftl b/DaoGenerator/src-template/dao-user.ftl
index ad55aaa2..d140d15d 100644
--- a/DaoGenerator/src-template/dao-user.ftl
+++ b/DaoGenerator/src-template/dao-user.ftl
@@ -31,16 +31,6 @@ import ${entity.javaPackage}.${entity.classNameDao};
 */
 public class ${entity.classNameUserDao} extends ${entity.classNameDao} {
 
-    // KEEP THIS PROPERTY ALWAYS.
-    public static final String TABLENAME = ${entity.classNameDao}.TABLENAME;
-
-    /**
-     * Properties of entity ${entity.className} -- needed for DaoCore.<br/>
-     * KEEP THIS CLASS ALWAYS.
-    */
-    public static class Properties extends ${entity.classNameDao}.Properties {
-    };
-
     public ${entity.classNameUserDao}(DaoConfig config) {
         super(config);
     }
