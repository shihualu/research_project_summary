diff --git a/facebook-core/src/main/java/com/facebook/FacebookSdk.java b/facebook-core/src/main/java/com/facebook/FacebookSdk.java
index c223c0a32..3e09c98c6 100644
--- a/facebook-core/src/main/java/com/facebook/FacebookSdk.java
+++ b/facebook-core/src/main/java/com/facebook/FacebookSdk.java
@@ -75,6 +75,7 @@
     private static volatile String appClientToken;
     private static volatile Boolean autoLogAppEventsEnabled;
     private static volatile Boolean codelessDebugLogEnabled;
+    private static volatile Boolean advertiserIDCollectionEnabled;
     private static final String FACEBOOK_COM = "facebook.com";
     private static volatile String facebookDomain = FACEBOOK_COM;
     private static AtomicLong onProgressThreshold = new AtomicLong(65536);
@@ -145,6 +146,12 @@ public Thread newThread(Runnable runnable) {
     public static final String CODELESS_DEBUG_LOG_ENABLED_PROPERTY =
             "com.facebook.sdk.CodelessDebugLogEnabled";
 
+    /**
+     * The key for the advertiserID collection in the Android manifest.
+     */
+    public static final String ADVERTISER_ID_COLLECTION_ENABLED_PROPERTY =
+            "com.facebook.sdk.AdvertiserIDCollectionEnabled";
+
     /**
      * The key for the callback off set in the Android manifest.
      */
@@ -733,6 +740,13 @@ static void loadDefaultsFromMetadata(Context context) {
                     CODELESS_DEBUG_LOG_ENABLED_PROPERTY,
                     false);
         }
+
+        if (advertiserIDCollectionEnabled == null) {
+            advertiserIDCollectionEnabled = ai.metaData.getBoolean(
+              ADVERTISER_ID_COLLECTION_ENABLED_PROPERTY,
+              true
+            );
+        }
     }
 
     /**
@@ -856,6 +870,22 @@ public static boolean getCodelessDebugLogEnabled() {
         return codelessDebugLogEnabled;
     }
 
+    /**
+     * Sets the advertiserID collection flag for the application
+     * @param flag true or false
+     */
+    public static void setAdvertiserIDCollectionEnabled(boolean flag) {
+        advertiserIDCollectionEnabled = flag;
+    }
+
+    /**
+     * @return the advertiserID collection flag for the application
+     */
+    public static boolean getAdvertiserIDCollectionEnabled() {
+        Validate.sdkInitialized();
+        return advertiserIDCollectionEnabled;
+    }
+
     /**
      * Sets the codeless debug flag for the application
      * @param flag true or false
diff --git a/facebook-core/src/main/java/com/facebook/internal/AttributionIdentifiers.java b/facebook-core/src/main/java/com/facebook/internal/AttributionIdentifiers.java
index a0c526eb2..dc1759fdf 100644
--- a/facebook-core/src/main/java/com/facebook/internal/AttributionIdentifiers.java
+++ b/facebook-core/src/main/java/com/facebook/internal/AttributionIdentifiers.java
@@ -136,10 +136,6 @@ private static AttributionIdentifiers getAndroidIdViaReflection(Context context)
                     advertisingInfo,
                     isLimitAdTrackingEnabled);
 
-            if (identifiers.limitTracking || !FacebookSdk.getAutoLogAppEventsEnabled()) {
-                return null;
-            }
-
             return identifiers;
         } catch (Exception e) {
             Utility.logd("android_id", e);
@@ -248,7 +244,11 @@ public String getAttributionId() {
     }
 
     public String getAndroidAdvertiserId() {
-        return androidAdvertiserId;
+        if (FacebookSdk.isInitialized() && FacebookSdk.getAdvertiserIDCollectionEnabled()) {
+            return androidAdvertiserId;
+        } else {
+            return null;
+        }
     }
 
     public String getAndroidInstallerPackage() {
