diff --git a/facebook/src/com/facebook/widget/WebDialog.java b/facebook/src/com/facebook/widget/WebDialog.java
index 7aea576c7..3d97295ad 100644
--- a/facebook/src/com/facebook/widget/WebDialog.java
+++ b/facebook/src/com/facebook/widget/WebDialog.java
@@ -353,8 +353,8 @@ public void onReceivedSslError(WebView view, SslErrorHandler handler, SslError e
         public void onPageStarted(WebView view, String url, Bitmap favicon) {
             Utility.logd(LOG_TAG, "Webview loading URL: " + url);
             super.onPageStarted(view, url, favicon);
-            if (!isDetached) {
-                spinner.show();
+            if (!isDetached && !WebDialog.this.isShowing()) {
+                    spinner.show();
             }
         }
 
