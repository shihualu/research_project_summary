diff --git a/mobicomkitui/src/main/java/com/applozic/mobicomkit/uiwidgets/conversation/activity/ConversationActivity.java b/mobicomkitui/src/main/java/com/applozic/mobicomkit/uiwidgets/conversation/activity/ConversationActivity.java
index 2df84475..55f112fc 100755
--- a/mobicomkitui/src/main/java/com/applozic/mobicomkit/uiwidgets/conversation/activity/ConversationActivity.java
+++ b/mobicomkitui/src/main/java/com/applozic/mobicomkit/uiwidgets/conversation/activity/ConversationActivity.java
@@ -42,6 +42,7 @@
 import android.view.MenuItem;
 import android.view.View;
 import android.view.ViewGroup;
+import android.widget.FrameLayout;
 import android.widget.ImageView;
 import android.widget.LinearLayout;
 import android.widget.RelativeLayout;
@@ -176,6 +177,7 @@ public ConversationActivity() {
 
     }
 
+    //manages toolbar visibility here.. hide show
     public static void showToolbar() {
         llToolbar.setVisibility(View.VISIBLE);
     }
@@ -350,6 +352,8 @@ protected void onCreate(Bundle savedInstanceState) {
 
         llToolbar = findViewById(R.id.toolbarConversation);
 
+        handleToolbarClickEvents();
+
         /*  Toolbar myToolbar = (Toolbar) findViewById(R.id.my_toolbar);
         setSupportActionBar(myToolbar);*/
         baseContactService = new AppContactService(this);
@@ -466,6 +470,54 @@ protected void onCreate(Bundle savedInstanceState) {
         setClickForToolbar();
     }
 
+    private void handleToolbarClickEvents() {
+
+        ImageView image_menu = findViewById(R.id.image_menu);
+        image_menu.setImageResource(R.drawable.ic_arrow_back);
+        image_menu.setOnClickListener(new View.OnClickListener() {
+            @Override
+            public void onClick(View view) {
+                finish();
+            }
+        });
+
+        FrameLayout frame_user = findViewById(R.id.frame_user);
+        //((ImageView) frame_user.getChildAt(0)).setImageResource(R.drawable.ic_add_round);
+        frame_user.setOnClickListener(new View.OnClickListener() {
+            @Override
+            public void onClick(View view) {
+                conversationUIService.startContactActivityForResult();
+//                try {
+//                    Intent myIntent = new Intent(ConversationActivity.this,
+//                            Class.forName("com.proteenlife.ui.MainActivity"));
+//                    startActivity(myIntent);
+//                    finish();
+//                } catch (ClassNotFoundException e) {
+//                    e.printStackTrace();
+//                }
+            }
+        });
+
+         /*
+        * This is important on click of logo we need to remove every back stack and launch fresh
+        * Main activity that loads home page by default
+        * */
+        ImageView ivProteenLogo = findViewById(R.id.proteenChatLogo);
+        ivProteenLogo.setOnClickListener(new View.OnClickListener() {
+            @Override
+            public void onClick(View v) {
+                try {
+                    Intent myIntent = new Intent(ConversationActivity.this, Class.forName("com.proteenlife.ui.MainActivity"));
+                    ActivityCompat.finishAffinity(ConversationActivity.this);
+                    startActivity(myIntent);
+                } catch (ClassNotFoundException e) {
+                    e.printStackTrace();
+                }
+            }
+        });
+
+    }
+
 
     private void setClickForToolbar() {
         //click of back arrow
