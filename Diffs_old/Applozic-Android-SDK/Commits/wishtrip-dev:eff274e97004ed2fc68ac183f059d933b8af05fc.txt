diff --git a/mobicomkit/build.gradle b/mobicomkit/build.gradle
index 36f6c4fc..90e76df0 100755
--- a/mobicomkit/build.gradle
+++ b/mobicomkit/build.gradle
@@ -16,9 +16,6 @@ android {
             minifyEnabled false
             proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
         }
-        ci {
-            initWith(release)
-        }
 
         staging {
             initWith(release)
@@ -32,6 +29,10 @@ android {
             initWith(release)
         }
 
+        ci{
+            initWith(release)
+        }
+
         ci_production {
             initWith(release)
         }
diff --git a/mobicomkitui/build.gradle b/mobicomkitui/build.gradle
index efd1b6d9..4bfdba43 100755
--- a/mobicomkitui/build.gradle
+++ b/mobicomkitui/build.gradle
@@ -16,10 +16,6 @@ android {
             proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
         }
 
-        ci {
-            initWith(release)
-        }
-
         staging {
             initWith(release)
         }
@@ -35,6 +31,10 @@ android {
         ci_production {
             initWith(release)
         }
+
+        ci {
+            initWith(release)
+        }
     }
     lintOptions {
 
diff --git a/mobicommons/build.gradle b/mobicommons/build.gradle
index e09a8a02..86e69099 100755
--- a/mobicommons/build.gradle
+++ b/mobicommons/build.gradle
@@ -16,10 +16,6 @@ android {
             proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
         }
 
-        ci {
-            initWith(release)
-        }
-
         staging {
             initWith(release)
         }
@@ -35,6 +31,10 @@ android {
         ci_production {
             initWith(release)
         }
+
+        ci {
+            initWith(release)
+        }
     }
     lintOptions {
 
