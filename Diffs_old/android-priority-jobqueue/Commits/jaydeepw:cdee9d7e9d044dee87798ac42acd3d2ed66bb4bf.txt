diff --git a/EnhancedPriorityJobQueue/app/src/main/java/com/path/android/jobqueue/JobManager.java b/EnhancedPriorityJobQueue/app/src/main/java/com/path/android/jobqueue/JobManager.java
index 1ac6968..40a9826 100644
--- a/EnhancedPriorityJobQueue/app/src/main/java/com/path/android/jobqueue/JobManager.java
+++ b/EnhancedPriorityJobQueue/app/src/main/java/com/path/android/jobqueue/JobManager.java
@@ -125,6 +125,20 @@ public int count() {
         return cnt;
     }
 
+    public JobHolder findJobById(Long id, boolean isPersistent) {
+        JobHolder holder;
+        if(isPersistent) {
+            synchronized (persistentJobQueue) {
+                holder = persistentJobQueue.findJobById(id);
+            }
+        } else {
+            synchronized (nonPersistentJobQueue) {
+                holder = nonPersistentJobQueue.findJobById(id);
+            }
+        }
+        return holder;
+    }
+
     private int countReadyJobs(boolean hasNetwork) {
         //TODO we can cache this
         int total = 0;
