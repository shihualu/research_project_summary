diff --git a/.gitignore b/.gitignore
index 796b96d..06ba198 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1 +1,11 @@
-/build
+bin/
+gen/
+obj/
+libs/*/*.so
+app/build/*
+.gradle/
+build/
+local.properties
+*~
+~*
+*#*#
diff --git a/src/main/java/uk/co/chrisjenx/calligraphy/CalligraphyLayoutInflater.java b/src/main/java/uk/co/chrisjenx/calligraphy/CalligraphyLayoutInflater.java
index 6f9cfc1..2bdfa3e 100644
--- a/src/main/java/uk/co/chrisjenx/calligraphy/CalligraphyLayoutInflater.java
+++ b/src/main/java/uk/co/chrisjenx/calligraphy/CalligraphyLayoutInflater.java
@@ -66,6 +66,7 @@ public View inflate(XmlPullParser parser, ViewGroup root, boolean attachToRoot)
      */
     private void setUpLayoutFactories(boolean cloned) {
         if (cloned) return;
+        /*
         // If we are HC+ we get and set Factory2 otherwise we just wrap Factory1
         if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.HONEYCOMB) {
             if (getFactory2() != null && !(getFactory2() instanceof WrapperFactory2)) {
@@ -77,6 +78,7 @@ private void setUpLayoutFactories(boolean cloned) {
         if (getFactory() != null && !(getFactory() instanceof WrapperFactory)) {
             setFactory(getFactory());
         }
+        */
     }
 
     @Override
