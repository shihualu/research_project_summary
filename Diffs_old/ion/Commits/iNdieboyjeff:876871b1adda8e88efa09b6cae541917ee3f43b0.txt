diff --git a/README.md b/README.md
index 83b48f50..23e803a4 100644
--- a/README.md
+++ b/README.md
@@ -375,14 +375,14 @@ Ion.with(getContext())
 <dependency>
    <groupId>com.koushikdutta.ion</groupId>
    <artifactId>ion</artifactId>
-   <version>1.2.4</version>
+   <version>1,</version>
 </dependency>
 ```
 
 ##### Gradle
 ```groovy
 dependencies {
-   compile 'com.koushikdutta.ion:ion:1.2.4'
+   compile 'com.koushikdutta.ion:ion:1.+'
 }
 ````
 
diff --git a/ion/AndroidManifest.xml b/ion/AndroidManifest.xml
index cb2394f3..e5e3361c 100644
--- a/ion/AndroidManifest.xml
+++ b/ion/AndroidManifest.xml
@@ -2,8 +2,8 @@
 <manifest xmlns:android="http://schemas.android.com/apk/res/android"
           xmlns:tools="http://schemas.android.com/tools"
           package="com.koushikdutta.ion"
-          android:versionCode="127"
-          android:versionName="1.2.7">
+          android:versionCode="129"
+          android:versionName="1.2.9">
     <uses-sdk
             tools:node="replace" />
     <application />
diff --git a/ion/build.gradle b/ion/build.gradle
index 2f8ac7fb..d9dab386 100644
--- a/ion/build.gradle
+++ b/ion/build.gradle
@@ -46,6 +46,6 @@ android {
 }
 
 // upload to maven task
-//if (System.getenv().I_AM_KOUSH == 'true') {
-//  apply from: 'https://raw.github.com/koush/mvn-repo/master/maven.gradle'
-//}
+if (System.getenv().I_AM_KOUSH == 'true') {
+  apply from: 'https://raw.github.com/koush/mvn-repo/master/maven.gradle'
+}
diff --git a/ion/src/com/koushikdutta/ion/IonDrawable.java b/ion/src/com/koushikdutta/ion/IonDrawable.java
index 5d75fe23..7b2411b6 100644
--- a/ion/src/com/koushikdutta/ion/IonDrawable.java
+++ b/ion/src/com/koushikdutta/ion/IonDrawable.java
@@ -152,9 +152,6 @@ public void onCompleted(Exception e, BitmapInfo result) {
     }
 
     public void cancel() {
-//        if (callback.bitmapKey == null)
-//            return;
-//        ion.bitmapsPending.removeItem(callback.bitmapKey, callback);
         unregister(ion, callback.bitmapKey, callback);
         callback.bitmapKey = null;
     }
@@ -179,6 +176,9 @@ private static void unregister(Ion ion, String key, IonDrawableCallback callback
                 if (ion.bitmapsPending.removeItem(info.downloadKey, info))
                     owner = ion.bitmapsPending.tag(info.downloadKey);
             }
+            // only cancel deferred loads... LoadBitmap means a download is already in progress.
+            // due to view recycling, cancelling that may be bad, as it may be rerequested again
+            // during the recycle process.
             if (owner instanceof DeferredLoadBitmap) {
                 DeferredLoadBitmap defer = (DeferredLoadBitmap)owner;
                 ion.bitmapsPending.remove(defer.key);
