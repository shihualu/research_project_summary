diff --git a/codedeploy/appspec.yml b/codedeploy/appspec.yml
new file mode 100644
index 0000000000..25248e79df
--- /dev/null
+++ b/codedeploy/appspec.yml
@@ -0,0 +1,29 @@
+version: 0.0
+os: linux
+files:
+  - source: /
+    destination: /home/ubuntu/graphhopper
+
+permissions:
+  - object: /home/ubuntu/graphhopper
+    pattern: "**"
+    owner: ubuntu
+    mode: 777
+    type:
+      - file
+hooks:
+  BeforeInstall:
+    - location: cleanup.sh
+      runas: ubuntu
+#  ApplicationStop:
+#    - location: stop_server.sh
+#      runas: ubuntu
+  ApplicationStart:
+    - location: start_server.sh
+      timeout: 3600
+      runas: ubuntu
+  ValidateService:
+    - location: validate_service.sh
+      timeout: 120
+      runas: ubuntu
+
diff --git a/codedeploy/cleanup.sh b/codedeploy/cleanup.sh
new file mode 100644
index 0000000000..1f7afcb188
--- /dev/null
+++ b/codedeploy/cleanup.sh
@@ -0,0 +1,4 @@
+#true is given so that even if the container doesn't exists, the command should not fail, because if the command fails, it will fail the CodeDeploy process also.
+sudo docker stop graphhopper || true
+sudo docker rm graphhopper || true
+sudo rm -rf /home/ubuntu/graphhopper
\ No newline at end of file
diff --git a/codedeploy/start_server.sh b/codedeploy/start_server.sh
new file mode 100644
index 0000000000..c1b75d7e18
--- /dev/null
+++ b/codedeploy/start_server.sh
@@ -0,0 +1,18 @@
+#!/bin/bash
+
+# Build Docker Image from Dockerfile
+# sudo docker build -t graphhopper-curefit:latest /home/ubuntu/graphhopper/
+
+ENVIRONMENT=`echo $DEPLOYMENT_GROUP_NAME`
+
+echo "ENVIRONMENT = $ENVIRONMENT"
+if [[ $DEPLOYMENT_GROUP_NAME == "osrm" ]]
+then
+    echo "Will run Docker container for graphhopper."
+
+    sudo docker run --name graphhopper --memory="2048m" -e "ENVIRONMENT=stage" -v /home/ubuntu/graphhopper:/app -d -p 5200:8989 graphhopper-curefit:latest
+
+
+sleep 60   # sleeping 60 seconds so the code-deploy waits here before picking up the next box. This 60 seconds ensure that the service inside the container is up and running. (if no errors)
+
+echo "Docker container started..."
\ No newline at end of file
diff --git a/codedeploy/stop_server.sh b/codedeploy/stop_server.sh
new file mode 100644
index 0000000000..7de4284802
--- /dev/null
+++ b/codedeploy/stop_server.sh
@@ -0,0 +1,4 @@
+#!/bin/bash
+
+ENVIRONMENT=`echo $DEPLOYMENT_GROUP_NAME`
+echo "ENVIRONMENT = $ENVIRONMENT"
diff --git a/codedeploy/validate_service.sh b/codedeploy/validate_service.sh
new file mode 100644
index 0000000000..d4aabdb9f4
--- /dev/null
+++ b/codedeploy/validate_service.sh
@@ -0,0 +1,21 @@
+#!/usr/bin/env bash
+PORT=80
+
+if [ "$DEPLOYMENT_GROUP_NAME" == "osrm" ]
+then
+  PORT=5200
+else
+  PORT=5200
+fi
+echo ${PORT}
+
+while true
+do
+  result=$(curl -s -o /dev/null -w "%{http_code}" "http://localhost:5200/route?point=12.980269%2C77.694232&point=12.979178%2C77.69725&type=json&locale=en-GB&vehicle=car&weighting=fastest&elevation=false")
+  if [ "$result" == "200" ]; then
+    break
+  fi
+  sleep 5
+done
+
+
