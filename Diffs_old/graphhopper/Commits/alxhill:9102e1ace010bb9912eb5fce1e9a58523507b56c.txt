diff --git a/marmoset/src/main/webapp/main.js b/marmoset/src/main/webapp/main.js
index ef5a27d7fe..12844338b8 100644
--- a/marmoset/src/main/webapp/main.js
+++ b/marmoset/src/main/webapp/main.js
@@ -66,7 +66,8 @@ var carSet = {
             window.setTimeout(function () {
                 // send the 'next' command to tell the server that it's ready to
                 // received more data from the back end
-                this.ws.send("next");
+                if (!this.paused)
+                    this.ws.send("next");
             }.bind(this));
         }.bind(this);
         this.ws.onopen = function (e) {
@@ -79,10 +80,9 @@ var carSet = {
         if (this.ws == null)
             return;
         if (this.paused) {
-            this.ws.send("start|0");
+            this.ws.send("next");
             e.target.value = "Pause Simulation";
         } else {
-            this.ws.send("pause");
             e.target.value = "Resume Simulation";
         }
         this.paused = !this.paused;
