diff --git a/codedeploy/start_server.sh b/codedeploy/start_server.sh
index d76de28cc8..9fcbef081a 100644
--- a/codedeploy/start_server.sh
+++ b/codedeploy/start_server.sh
@@ -1,13 +1,11 @@
 #!/bin/bash
 
 # Build Docker Image from Dockerfile
-# sudo docker build -t graphhopper-curefit:latest /home/ubuntu/graphhopper/
+sudo docker build -t graphhopper-curefit:latest /home/ubuntu/graphhopper/
 
-ENVIRONMENT=`echo $DEPLOYMENT_GROUP_NAME`
 
-echo "ENVIRONMENT = $ENVIRONMENT"
-echo "Will run Docker container for graphhopper."
-sudo docker run --name graphhopper --memory="2048m" --log-driver=awslogs --log-opt awslogs-group=eatfit/graphhopper -e "ENVIRONMENT=stage" -v /home/ubuntu/graphhopper:/app -d -p 5200:8989 graphhopper-curefit:latest
+echo "Will run Docker container for graphhopper"
+sudo docker run --name graphhopper --memory="2048m" --log-driver=awslogs --log-opt awslogs-group=eatfit/graphhopper -v /home/ubuntu/graphhopper:/app -d -p 5200:8989 graphhopper-curefit:latest
 
 
 sleep 60   # sleeping 60 seconds so the code-deploy waits here before picking up the next box. This 60 seconds ensure that the service inside the container is up and running. (if no errors)
diff --git a/docker/entrypoint.sh b/docker/entrypoint.sh
index 1cee925aba..30c50aa085 100644
--- a/docker/entrypoint.sh
+++ b/docker/entrypoint.sh
@@ -3,6 +3,4 @@
 # PROD Setting : The G1GC Garbage collector is used initially, but based on the metrics and performance we can try -XX:+UseConcMarkSweepGC and keep whichever workds better.
 
 
-echo "GRAPHHOPPER will start in ENVIRONMENT : $ENVIRONMENT"
-
 ./graphhopper.sh -a web -i ./osrm_location.osm.pbf
