diff --git a/zxing-sample/src/main/java/me/dm7/barcodescanner/zxing/sample/FullScannerActivity.kt b/zxing-sample/src/main/java/me/dm7/barcodescanner/zxing/sample/FullScannerActivity.kt
index 655be21..0e9a0bc 100644
--- a/zxing-sample/src/main/java/me/dm7/barcodescanner/zxing/sample/FullScannerActivity.kt
+++ b/zxing-sample/src/main/java/me/dm7/barcodescanner/zxing/sample/FullScannerActivity.kt
@@ -8,6 +8,7 @@ import android.media.Ringtone
 import android.media.RingtoneManager
 import android.net.Uri
 import android.os.Bundle
+import android.os.CountDownTimer
 import android.os.Handler
 import android.support.v4.app.DialogFragment
 import android.support.v4.app.FragmentManager
@@ -196,14 +197,18 @@ class FullScannerActivity : BaseScannerActivity(), MessageDialogFragment.Message
     }
 
     fun showMessageDialog(message: String) {
+        // toast for specific time
         handler!!.post({
-            Toast.makeText(this, message, Toast.LENGTH_SHORT).show()
+            val toast = Toast.makeText(this, message, Toast.LENGTH_LONG)
+            toast.show()
+            handler!!.postDelayed({
+                toast.cancel()
+            }, 350)
         })
 
         handler!!.postDelayed({
             mScannerView!!.resumeCameraPreview(this)
-        }, 1000)
-
+        }, 500)
 //        val fragment = MessageDialogFragment.newInstance("Scan Results", message, this)
 //        fragment.show(supportFragmentManager, "scan_results")
     }
@@ -242,13 +247,6 @@ class FullScannerActivity : BaseScannerActivity(), MessageDialogFragment.Message
     fun setupFormats() {
         val formats = ArrayList<BarcodeFormat>()
 
-//        if (mSelectedIndices == null || mSelectedIndices!!.isEmpty()) {
-//            mSelectedIndices = ArrayList<Int>()
-//            for (i in ZXingScannerView.ALL_FORMATS.indices) {
-//                mSelectedIndices!!.add(i)
-//            }
-//        }
-
         // force using only 1: CODBRA
         for (index in listOf(1)) {
             formats.add(ZXingScannerView.ALL_FORMATS[index])
@@ -261,8 +259,10 @@ class FullScannerActivity : BaseScannerActivity(), MessageDialogFragment.Message
     public override fun onPause() {
         super.onPause()
         mScannerView!!.stopCamera()
-        closeMessageDialog()
-        closeFormatsDialog()
+
+        // causing crashes on pause
+//        closeMessageDialog()
+//        closeFormatsDialog()
     }
 
     companion object {
