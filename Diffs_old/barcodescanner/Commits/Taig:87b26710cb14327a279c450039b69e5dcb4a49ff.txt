diff --git a/src/main/java/com/taig/android/barcode/scanner/BarcodeScannerView.java b/src/main/java/com/taig/android/barcode/scanner/BarcodeScannerView.java
index 801c596..facfb6d 100644
--- a/src/main/java/com/taig/android/barcode/scanner/BarcodeScannerView.java
+++ b/src/main/java/com/taig/android/barcode/scanner/BarcodeScannerView.java
@@ -11,7 +11,7 @@
 {
 	private Camera mCamera;
 	private CameraPreview mPreview;
-	private ViewFinderView mViewFinderView;
+	private Hud mHud;
 	private Rect mFramingRectInPreview;
 
 	public BarcodeScannerView( Context context )
@@ -29,9 +29,9 @@ public BarcodeScannerView( Context context, AttributeSet attributeSet )
 	public void setupLayout()
 	{
 		mPreview = new CameraPreview( getContext() );
-		mViewFinderView = new ViewFinderView( getContext() );
+		mHud = new Hud( getContext() );
 		addView( mPreview );
-		addView( mViewFinderView );
+		addView( mHud );
 	}
 
 	public void startCamera()
@@ -39,7 +39,7 @@ public void startCamera()
 		mCamera = CameraUtils.getCameraInstance();
 		if( mCamera != null )
 		{
-			mViewFinderView.setupViewFinder();
+			mHud.setupViewFinder();
 			mPreview.setCamera( mCamera, this );
 			mPreview.initCameraPreview();
 		}
@@ -60,7 +60,7 @@ public synchronized Rect getFramingRectInPreview( int width, int height )
 	{
 		if( mFramingRectInPreview == null )
 		{
-			Rect framingRect = mViewFinderView.getFramingRect();
+			Rect framingRect = mHud.getFramingRect();
 			if( framingRect == null )
 			{
 				return null;
diff --git a/src/main/java/com/taig/android/barcode/scanner/ViewFinderView.java b/src/main/java/com/taig/android/barcode/scanner/Hud.java
similarity index 97%
rename from src/main/java/com/taig/android/barcode/scanner/ViewFinderView.java
rename to src/main/java/com/taig/android/barcode/scanner/Hud.java
index 53fb7bb..8fe1243 100644
--- a/src/main/java/com/taig/android/barcode/scanner/ViewFinderView.java
+++ b/src/main/java/com/taig/android/barcode/scanner/Hud.java
@@ -10,7 +10,7 @@
 import android.util.AttributeSet;
 import android.view.View;
 
-public class ViewFinderView extends View
+public class Hud extends View
 {
 	private static final String TAG = "ViewFinderView";
 
@@ -34,12 +34,12 @@
 	private static final int POINT_SIZE = 10;
 	private static final long ANIMATION_DELAY = 80l;
 
-	public ViewFinderView( Context context )
+	public Hud( Context context )
 	{
 		super( context );
 	}
 
-	public ViewFinderView( Context context, AttributeSet attrs )
+	public Hud( Context context, AttributeSet attrs )
 	{
 		super( context, attrs );
 	}
