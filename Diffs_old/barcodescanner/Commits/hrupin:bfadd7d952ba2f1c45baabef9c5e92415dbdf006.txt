diff --git a/build.gradle b/build.gradle
index 31235da..2810ab5 100644
--- a/build.gradle
+++ b/build.gradle
@@ -7,13 +7,13 @@ buildscript {
     }
 
     dependencies {
-        classpath 'com.android.tools.build:gradle:2.3.3'
+        classpath 'com.android.tools.build:gradle:3.2.0'
     }
 }
 
 allprojects {
     group = 'me.dm7.barcodescanner'
-    version = '1.9.8'
+    version = '1.9.12'
 
     repositories {
         mavenCentral()
@@ -44,8 +44,8 @@ subprojects {
                 defaultConfig {
                     minSdkVersion versions.min_sdk
                     targetSdkVersion versions.target_sdk
-                    versionCode 198
-                    versionName "1.9.8"
+                    versionCode 1912
+                    versionName "1.9.12"
                 }
             }
         }
@@ -80,12 +80,12 @@ subprojects {
                             packaging project.pomPackaging
                             artifactId project.pomArtifactId
                             description project.pomDescription
-                            url 'https://github.com/dm77/barcodescanner'
+                            url 'https://github.com/hrupin/barcodescanner'
 
                             scm {
-                                url 'scm:git@github.com:dm77/barcodescanner.git'
-                                connection 'scm:git@github.com:dm77/barcodescanner.git'
-                                developerConnection 'scm:git@github.com:dm77/barcodescanner.git'
+                                url 'scm:git@github.com:hrupin/barcodescanner.git'
+                                connection 'scm:git@github.com:hrupin/barcodescanner.git'
+                                developerConnection 'scm:git@github.com:hrupin/barcodescanner.git'
                             }
 
                             licenses {
