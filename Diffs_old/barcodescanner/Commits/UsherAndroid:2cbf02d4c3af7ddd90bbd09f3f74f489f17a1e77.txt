diff --git a/build.gradle b/build.gradle
index d24758c..d5beda0 100644
--- a/build.gradle
+++ b/build.gradle
@@ -49,7 +49,8 @@ subprojects {
                 }
             }
         }
-        if(project.isLibrary && rootProject.hasProperty('sonatypeUsername') && rootProject.hasProperty('sonatypePassword')) {
+        def DISABLE_SONA_TYPE_CONFIG = true
+        if (!DISABLE_SONA_TYPE_CONFIG && project.isLibrary && rootProject.hasProperty('sonatypeUsername') && rootProject.hasProperty('sonatypePassword')) {
             configure(project) {
                 apply plugin: 'maven'
                 apply plugin: 'signing'
