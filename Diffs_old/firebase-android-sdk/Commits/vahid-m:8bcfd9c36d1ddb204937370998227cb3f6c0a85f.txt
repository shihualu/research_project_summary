diff --git a/README.md b/README.md
index ae860561f..c816bf051 100644
--- a/README.md
+++ b/README.md
@@ -1,3 +1,15 @@
+## This is the original Firebase android SDK with proxy handling in Firebase Remote Config
+
+Example: 
+```
+FirebaseRemoteConfigSettings configSettings = new FirebaseRemoteConfigSettings
+                .Builder()
+                .setDeveloperModeEnabled(BuildConfig.DEBUG)
+                .setProxyServer(/*PROXY TYPE*/, /*PROXY HOST*/, /*PROXY PORT*/)
+                .build();
+```                
+             
+
 # Firebase Android Open Source Development
 
 This repository contains a subset of the Firebase Android SDK source. It
