diff --git a/.travis.yml b/.travis.yml
index 3ed3b7a7..35047839 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -11,8 +11,4 @@ android:
     - extra-android-m2repository
 
 script:
-    - echo "twitterConsumerKey=$TWITTER_CONSUMER_KEY" >> samples/app/twitter.properties
-    - echo "twitterConsumerSecret=$TWITTER_CONSUMER_SECRET" >> samples/app/twitter.properties
-    - echo "apiKey=$API_KEY" >> samples/app/twitter.properties
-    - echo "apiSecret=$API_SECRET" >> samples/app/twitter.properties
     - ./gradlew assemble test
diff --git a/dependencies.gradle b/dependencies.gradle
index 3e97bf2a..0304573c 100644
--- a/dependencies.gradle
+++ b/dependencies.gradle
@@ -12,7 +12,7 @@ ext {
 
     dependency = [
             // Gradle plugins
-            spoonGradlePlugin          : 'com.stanfy.spoon:spoon-gradle-plugin:1.0.4',
+            //spoonGradlePlugin          : 'com.stanfy.spoon:spoon-gradle-plugin:1.0.4',
             androidGradlePlugin        : 'com.android.tools.build:gradle:3.1.1',
 
             // Mopub
diff --git a/settings.gradle b/settings.gradle
index 6f478b12..45b4b8fa 100644
--- a/settings.gradle
+++ b/settings.gradle
@@ -19,5 +19,4 @@ include ':twitter-core'
 include ':tweet-composer'
 include ':tweet-ui'
 include ':twitter-mopub'
-include ':twitter'
-include ':samples:app'
\ No newline at end of file
+include ':twitter'
\ No newline at end of file
diff --git a/tweet-composer/build.gradle b/tweet-composer/build.gradle
index 17f0f751..1bca2353 100644
--- a/tweet-composer/build.gradle
+++ b/tweet-composer/build.gradle
@@ -56,7 +56,7 @@ android {
 
     testOptions {
         unitTests.returnDefaultValues = true
-        //unitTests.includeAndroidResources true
+        unitTests.includeAndroidResources true
     }
 }
 
@@ -67,6 +67,14 @@ dependencies {
     implementation dependency.retrofit
     implementation dependency.tweetText
 
+    implementation dependency.mockito
+    implementation dependency.junit
+    implementation dependency.retrofitMock
+    implementation(dependency.robolectric) {
+        exclude group: 'commons-logging', module: 'commons-logging'
+        exclude group: 'org.apache.httpcomponents', module: 'httpclient'
+    }
+
     androidTestImplementation dependency.mockito
     androidTestImplementation dependency.dexmakerMockito
 
diff --git a/tweet-composer/internal-build.gradle b/tweet-composer/internal-build.gradle
index ba823cfd..0622a133 100644
--- a/tweet-composer/internal-build.gradle
+++ b/tweet-composer/internal-build.gradle
@@ -72,7 +72,7 @@ android {
 
     testOptions {
         unitTests.returnDefaultValues = true
-        //unitTests.includeAndroidResources true
+        unitTests.includeAndroidResources true
     }
 }
 
diff --git a/tweet-ui/build.gradle b/tweet-ui/build.gradle
index 6c3254cb..972f3876 100644
--- a/tweet-ui/build.gradle
+++ b/tweet-ui/build.gradle
@@ -62,7 +62,7 @@ android {
 
     testOptions {
         unitTests.returnDefaultValues = true
-        //unitTests.includeAndroidResources true
+        unitTests.includeAndroidResources true
     }
 }
 
@@ -73,11 +73,17 @@ dependencies {
     implementation dependency.supportCoreV4
     implementation dependency.supportCompatV4
     implementation dependency.recyclerview
-    implementation dependency.junit
     implementation dependency.okHttp
     implementation dependency.gson
     implementation dependency.retrofit
 
+    implementation dependency.mockito
+    implementation dependency.junit
+    implementation(dependency.robolectric) {
+        exclude group: 'commons-logging', module: 'commons-logging'
+        exclude group: 'org.apache.httpcomponents', module: 'httpclient'
+    }
+
     androidTestImplementation dependency.retrofitMock
     androidTestImplementation dependency.mockito
     androidTestImplementation dependency.dexmakerMockito
diff --git a/tweet-ui/internal-build.gradle b/tweet-ui/internal-build.gradle
index db3e7d3b..036738a6 100644
--- a/tweet-ui/internal-build.gradle
+++ b/tweet-ui/internal-build.gradle
@@ -79,7 +79,7 @@ android {
 
     testOptions {
         unitTests.returnDefaultValues = true
-        //unitTests.includeAndroidResources true
+        unitTests.includeAndroidResources true
     }
 }
 
diff --git a/twitter-core/build.gradle b/twitter-core/build.gradle
index 969e866f..28518edb 100644
--- a/twitter-core/build.gradle
+++ b/twitter-core/build.gradle
@@ -62,6 +62,15 @@ dependencies {
     implementation dependency.gson
     implementation dependency.okHttp
 
+    implementation dependency.mockito
+    implementation dependency.junit
+    implementation dependency.retrofitMock
+    implementation(dependency.robolectric) {
+        exclude group: 'commons-logging', module: 'commons-logging'
+        exclude group: 'org.apache.httpcomponents', module: 'httpclient'
+    }
+    implementation dependency.equalsVerifier
+
     androidTestImplementation dependency.mockito
     androidTestImplementation dependency.dexmakerMockito
 
diff --git a/twitter-core/internal-build.gradle b/twitter-core/internal-build.gradle
index d92f30b4..1a250e9a 100644
--- a/twitter-core/internal-build.gradle
+++ b/twitter-core/internal-build.gradle
@@ -79,6 +79,15 @@ dependencies {
     implementation dependency.gson
     implementation dependency.okHttp
 
+    implementation dependency.mockito
+    implementation dependency.junit
+    implementation dependency.retrofitMock
+    implementation(dependency.robolectric) {
+        exclude group: 'commons-logging', module: 'commons-logging'
+        exclude group: 'org.apache.httpcomponents', module: 'httpclient'
+    }
+    implementation dependency.equalsVerifier
+
     androidTestImplementation dependency.mockito
     androidTestImplementation dependency.dexmakerMockito
 
diff --git a/twitter-mopub/build.gradle b/twitter-mopub/build.gradle
index 7fdc2d88..4fa0bc8a 100644
--- a/twitter-mopub/build.gradle
+++ b/twitter-mopub/build.gradle
@@ -47,6 +47,7 @@ android {
 
     lintOptions {
         warningsAsErrors true
+        disable 'InvalidPackage'
     }
 
     compileOptions {
@@ -62,16 +63,22 @@ android {
 
     testOptions {
         unitTests.returnDefaultValues = true
-        //unitTests.includeAndroidResources true
+        unitTests.includeAndroidResources true
     }
 }
 
 dependencies {
-
     implementation (dependency.mopub) {
         transitive = true
     }
 
+    implementation dependency.mockito
+    implementation dependency.junit
+    implementation(dependency.robolectric) {
+        exclude group: 'commons-logging', module: 'commons-logging'
+        exclude group: 'org.apache.httpcomponents', module: 'httpclient'
+    }
+
     androidTestImplementation dependency.mockito
     androidTestImplementation dependency.junit
     androidTestImplementation(dependency.robolectric) {
diff --git a/twitter-mopub/internal-build.gradle b/twitter-mopub/internal-build.gradle
index 8b5a63ff..46b370a3 100644
--- a/twitter-mopub/internal-build.gradle
+++ b/twitter-mopub/internal-build.gradle
@@ -73,7 +73,7 @@ android {
 
     testOptions {
         unitTests.returnDefaultValues = true
-        //unitTests.includeAndroidResources true
+        unitTests.includeAndroidResources true
     }
 }
 
@@ -82,9 +82,16 @@ dependencies {
         transitive = true
     }
 
-    testCompile dependency.mockito
-    testCompile dependency.junit
-    testCompile(dependency.robolectric) {
+    implementation dependency.mockito
+    implementation dependency.junit
+    implementation(dependency.robolectric) {
+        exclude group: 'commons-logging', module: 'commons-logging'
+        exclude group: 'org.apache.httpcomponents', module: 'httpclient'
+    }
+
+    androidTestImplementation dependency.mockito
+    androidTestImplementation dependency.junit
+    androidTestImplementation(dependency.robolectric) {
         exclude group: 'commons-logging', module: 'commons-logging'
         exclude group: 'org.apache.httpcomponents', module: 'httpclient'
     }
diff --git a/twitter/build.gradle b/twitter/build.gradle
index e5e4ab6f..0d7aeffa 100644
--- a/twitter/build.gradle
+++ b/twitter/build.gradle
@@ -29,6 +29,7 @@ android {
 
     lintOptions {
         warningsAsErrors true
+        disable 'InvalidPackage'
     }
 
     buildTypes {
