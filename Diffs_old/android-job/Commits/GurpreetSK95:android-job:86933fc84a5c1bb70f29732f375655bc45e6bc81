diff --git a/library/src/main/java/com/evernote/android/job/JobRequest.java b/library/src/main/java/com/evernote/android/job/JobRequest.java
index 28763e4..6037ded 100644
--- a/library/src/main/java/com/evernote/android/job/JobRequest.java
+++ b/library/src/main/java/com/evernote/android/job/JobRequest.java
@@ -298,6 +298,10 @@ public boolean isExact() {
                 offset = mFailureCount * getBackoffMs();
                 break;
 
+            case STRICTLY_LINEAR:
+                offset = getBackoffMs();
+                break;
+
             case EXPONENTIAL:
                 if (mFailureCount == 0) {
                     offset = 0L;
@@ -1057,7 +1061,7 @@ public Builder setPeriodic(long intervalMs, long flexMs) {
          * Note that it's not allowed to change the back-off criteria for a periodic job.
          *
          * @param backoffMs The initial interval to wait when the job has been rescheduled.
-         * @param backoffPolicy Is either {@link BackoffPolicy#LINEAR} or {@link BackoffPolicy#EXPONENTIAL}.
+         * @param backoffPolicy Is either {@link BackoffPolicy#LINEAR}, {@link BackoffPolicy#STRICTLY_LINEAR} or {@link BackoffPolicy#EXPONENTIAL}.
          * @see Job.Result#RESCHEDULE
          * @see Job#onReschedule(int)
          */
@@ -1183,6 +1187,10 @@ public int hashCode() {
          * backoff = numFailures * initial_backoff.
          */
         LINEAR,
+        /**
+         * backoff = initial_backoff.
+         */
+        STRICTLY_LINEAR,
         /**
          * backoff = initial_backoff * 2 ^ (numFailures - 1).
          */
