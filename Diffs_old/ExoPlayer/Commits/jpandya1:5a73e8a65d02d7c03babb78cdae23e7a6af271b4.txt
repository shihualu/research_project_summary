diff --git a/build.gradle b/build.gradle
index 1d0b459bf5..54395fe0ce 100644
--- a/build.gradle
+++ b/build.gradle
@@ -11,6 +11,9 @@
 // WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 // See the License for the specific language governing permissions and
 // limitations under the License.
+
+apply plugin: 'java'
+
 buildscript {
     repositories {
         google()
@@ -21,22 +24,33 @@ buildscript {
         classpath 'com.novoda:bintray-release:0.9'
         classpath 'com.google.android.gms:strict-version-matcher-plugin:1.1.0'
     }
+
+	println "Hello World!"
+	println myProperty
+
 }
+
+test{
+	useJUnit()
+	include('**/testutils/src/test/java/com/google/android/exoplayer2/testutil/FakeDateSetTest.java')
+	println "Hello World!"
+}
+
 allprojects {
     repositories {
         google()
         jcenter()
     }
-    project.ext {
-        exoplayerPublishEnabled = false
-    }
-    if (it.hasProperty('externalBuildDir')) {
-        if (!new File(externalBuildDir).isAbsolute()) {
-            externalBuildDir = new File(rootDir, externalBuildDir)
-        }
-        buildDir = "${externalBuildDir}/${project.name}"
-    }
-    group = 'com.google.android.exoplayer'
+//    project.ext {
+//        exoplayerPublishEnabled = false
+//    }
+//    if (it.hasProperty('externalBuildDir')) {
+//        if (!new File(externalBuildDir).isAbsolute()) {
+//            externalBuildDir = new File(rootDir, externalBuildDir)
+//        }
+//        buildDir = "${externalBuildDir}/${project.name}"
+//    }
+//    group = 'com.google.android.exoplayer'
 }
 
 apply from: 'javadoc_combined.gradle'
diff --git a/gradle.properties b/gradle.properties
index 31ff0ad6b6..10746a22a2 100644
--- a/gradle.properties
+++ b/gradle.properties
@@ -4,3 +4,5 @@ android.enableJetifier=true
 android.enableUnitTestBinaryResources=true
 buildDir=buildout
 org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
+
+myProperty=This is my direct property
diff --git a/run-tests.sh b/run-tests.sh
index d609465d39..33c4f2e317 100644
--- a/run-tests.sh
+++ b/run-tests.sh
@@ -5,5 +5,6 @@
 # output those results into a execution test
 # Also in a README.txt put the number of passed or failed tests
 
-cd ../ExoPlayer/testutils/src/test/java/com/google/android/exoplayer2/testutil
+#cd ../ExoPlayer/testutils/src/test/java/com/google/android/exoplayer2/testutil
 
+gradle -q > test-execution.txt
diff --git a/test-execution.txt b/test-execution.txt
new file mode 100644
index 0000000000..b5ba318ea0
--- /dev/null
+++ b/test-execution.txt
@@ -0,0 +1,37 @@
+
+> Configure project :
+Hello World!
+This is my direct property
+Hello World!
+
+> Task :help
+
+Welcome to Gradle 5.6.2.
+
+To run a build, run gradle <task> ...
+
+To see a list of available tasks, run gradle tasks
+
+To see a list of command-line options, run gradle --help
+
+To see more detail about a task, run gradle help --task <task>
+
+For troubleshooting, visit https://help.gradle.org
+
+BUILD SUCCESSFUL in 1s
+1 actionable task: 1 executed
+Hello World!
+This is my direct property
+Hello World!
+
+Welcome to Gradle 5.6.2.
+
+To run a build, run gradle <task> ...
+
+To see a list of available tasks, run gradle tasks
+
+To see a list of command-line options, run gradle --help
+
+To see more detail about a task, run gradle help --task <task>
+
+For troubleshooting, visit https://help.gradle.org
