diff --git a/demos/main/src/main/java/com/google/android/exoplayer2/demo/PlayerActivity.java b/demos/main/src/main/java/com/google/android/exoplayer2/demo/PlayerActivity.java
index 0278a0eca3..70b7b6a2ed 100644
--- a/demos/main/src/main/java/com/google/android/exoplayer2/demo/PlayerActivity.java
+++ b/demos/main/src/main/java/com/google/android/exoplayer2/demo/PlayerActivity.java
@@ -130,6 +130,7 @@
   private static final CookieManager DEFAULT_COOKIE_MANAGER;
 
   private static final String TAG = "Harry___stall";
+  private int flag = 0;
 
   static {
     DEFAULT_COOKIE_MANAGER = new CookieManager();
@@ -698,12 +699,21 @@ public void onPlayerStateChanged(boolean playWhenReady, int playbackState) {
           Log.d(TAG, System.currentTimeMillis() + "\t" + "Stop");
         }
         if (playWhenReady && playbackState == Player.STATE_READY) {
+          if (flag == 1) {
+            Log.d(TAG, System.currentTimeMillis() + "\t" + "resume");
+            flag = 0;
+          }
           // media actually playing
         } else if (playWhenReady) {
           // might be idle (plays after prepare()),
           // buffering (plays when data available)
           // or ended (plays when seek away from end)
-          Log.d(TAG, System.currentTimeMillis() + "\t" + "might be idle, buffering, or ended");
+          if (playbackState == Player.STATE_BUFFERING) {
+            Log.d(TAG, System.currentTimeMillis() + "\t" + "rebuffer");
+            flag = 1;
+          }
+
+          //Log.d(TAG, System.currentTimeMillis() + "\t" + "might be idle, buffering, or ended");
         } else {
           // player paused in any state
           //PrintWriter printWriter = new PrintWriter(new FileWriter("/sdcard/"+System.currentTimeMillis()+"stall.log"));
