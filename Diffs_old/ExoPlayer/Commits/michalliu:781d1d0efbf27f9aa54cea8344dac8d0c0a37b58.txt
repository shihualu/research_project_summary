diff --git a/library/all/aio.gradle b/library/all/aio.gradle
new file mode 100644
index 0000000000..2b2dbdec6c
--- /dev/null
+++ b/library/all/aio.gradle
@@ -0,0 +1,16 @@
+apply plugin: 'com.android.library'
+
+android {
+}
+
+hello { task ->
+    println "$task.project.name " +  "-> okay!"
+}
+
+dependencies {
+    compile project(':exoplayer2-library-core')
+//    compile project(':exoplayer2-library-dash')
+    compile project(':exoplayer2-library-hls')
+//    compile project(':exoplayer2-library-smoothstreaming')
+//    compile project(':exoplayer2-library-ui')
+}
diff --git a/library/core/aio.gradle b/library/core/aio.gradle
new file mode 100644
index 0000000000..a88e235a12
--- /dev/null
+++ b/library/core/aio.gradle
@@ -0,0 +1,12 @@
+apply plugin: 'com.android.library'
+
+android {
+}
+
+hello { task ->
+    println "$task.project.name " +  "-> okay!"
+}
+
+dependencies {
+    compile 'com.android.support:support-annotations:' + supportLibraryVersion
+}
\ No newline at end of file
diff --git a/library/dash/aio.gradle b/library/dash/aio.gradle
new file mode 100644
index 0000000000..bbacf9ed90
--- /dev/null
+++ b/library/dash/aio.gradle
@@ -0,0 +1,13 @@
+apply plugin: 'com.android.library'
+
+android {
+}
+
+hello { task ->
+    println "$task.project.name " +  "-> okay!"
+}
+
+dependencies {
+    compile project(':exoplayer2-library-core')
+    compile 'com.android.support:support-annotations:' + supportLibraryVersion
+}
diff --git a/library/hls/aio.gradle b/library/hls/aio.gradle
new file mode 100644
index 0000000000..2fadc9d138
--- /dev/null
+++ b/library/hls/aio.gradle
@@ -0,0 +1,13 @@
+apply plugin: 'com.android.library'
+
+android {
+}
+
+hello { task ->
+    println "$task.project.name " +  "-> okay!"
+}
+
+dependencies {
+	compile project(':exoplayer2-library-core')
+    compile 'com.android.support:support-annotations:' + supportLibraryVersion
+}
diff --git a/library/smoothstreaming/aio.gradle b/library/smoothstreaming/aio.gradle
new file mode 100644
index 0000000000..bbacf9ed90
--- /dev/null
+++ b/library/smoothstreaming/aio.gradle
@@ -0,0 +1,13 @@
+apply plugin: 'com.android.library'
+
+android {
+}
+
+hello { task ->
+    println "$task.project.name " +  "-> okay!"
+}
+
+dependencies {
+    compile project(':exoplayer2-library-core')
+    compile 'com.android.support:support-annotations:' + supportLibraryVersion
+}
diff --git a/library/ui/aio.gradle b/library/ui/aio.gradle
new file mode 100644
index 0000000000..bbacf9ed90
--- /dev/null
+++ b/library/ui/aio.gradle
@@ -0,0 +1,13 @@
+apply plugin: 'com.android.library'
+
+android {
+}
+
+hello { task ->
+    println "$task.project.name " +  "-> okay!"
+}
+
+dependencies {
+    compile project(':exoplayer2-library-core')
+    compile 'com.android.support:support-annotations:' + supportLibraryVersion
+}
