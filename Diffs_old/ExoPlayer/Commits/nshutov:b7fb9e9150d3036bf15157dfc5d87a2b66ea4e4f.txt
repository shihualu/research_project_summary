diff --git a/.gitignore b/.gitignore
index 4731d5ba99..1b06c32235 100644
--- a/.gitignore
+++ b/.gitignore
@@ -75,3 +75,5 @@ extensions/cronet/libs/*
 # Cast receiver
 cast_receiver_app/external-js
 cast_receiver_app/bazel-cast_receiver_app
+demos/main/sumavision_signing.bat
+demos/main/sumavision.keystore
diff --git a/demos/main/build.gradle b/demos/main/build.gradle
index 7089d4d731..6c8ff66cba 100644
--- a/demos/main/build.gradle
+++ b/demos/main/build.gradle
@@ -80,3 +80,15 @@ dependencies {
 }
 
 apply plugin: 'com.google.android.gms.strict-version-matcher-plugin'
+
+task sumasignDebug(type: Exec) {
+    commandLine = [projectDir.toString() + '\\sumavision_signing.bat', projectDir.toString() + "\\buildout\\outputs\\apk\\noExtensions\\debug\\demo-noExtensions-debug.apk"]
+}
+
+android.applicationVariants.all { variant ->
+    if (variant.name == 'noExtensionsDebug') {
+        def taskSuffix = variant.name.capitalize()
+        def assembleTaskName = "assemble${taskSuffix}"
+        tasks[assembleTaskName].finalizedBy = [sumasignDebug]
+    }
+}
